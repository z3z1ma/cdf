[package]
name = "cdf-semantic"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Data-only semantic type registry and resolution authority for cdf."

[dependencies]
arrow-schema = "58.3.0"
cdf-kernel = { path = "../cdf-kernel" }
hex = "0.4.3"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
sha2 = "0.10.9"

[lints]
workspace = true
