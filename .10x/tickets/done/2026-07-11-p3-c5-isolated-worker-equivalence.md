Status: done
Created: 2026-07-11
Updated: 2026-07-20
Parent: .10x/tickets/done/2026-07-10-p3-ws-c-deterministic-parallelism.md
Depends-On: .10x/tickets/done/2026-07-11-p0-wx1-portable-partition-task-protocol.md, .10x/tickets/done/2026-07-11-p3-c2-parallel-frontier-execution.md, .10x/tickets/done/2026-07-11-p3-c4-jobs-invariance-scaling-matrix.md, .10x/tickets/done/2026-07-11-p3-a8-drain-epoch-executor.md

# P3 C5: isolated worker serialization equivalence law

## Scope

Build the local isolated-worker harness, route bounded and drain partition tasks through canonical serialize/drop/reconstruct/execute/result-verify/coordinator-assemble flow, and prove equivalence to direct local execution without remote transport.

## Acceptance criteria

- Harness reconstructs without borrowed resource/store/path/secret/runtime objects.
- Direct and capsule paths produce identical fixed-input segment/package/verdict/state semantics at jobs 1/N.
- Tampered/stale/missing-capability tasks/results fail before authority advances.
- High-cardinality task/result metadata remains bounded and data bypasses control messages.
- A mock source/format addition passes with registry changes only.

## Evidence expectations

Golden task/results, direct/capsule hashes, tamper/fence/generation/cancel/memory matrices, dependency scans, and adversarial distribution-seam review.

## Explicit exclusions

No RPC, remote state/artifact deployment, worker daemon, or Spark/Flink/Ballista integration.

## Blockers

None. The active canonicalization decision scopes the two-barrier protocol to compiler-proven
partition-separable tasks and requires package-global work to fail before artifacts or source I/O.
WX1, C2, C4, and A8 are done, and the isolated-worker equivalence law is closed below.

## References

- `.10x/specs/portable-partition-task-protocol.md`
- `.10x/decisions/partition-separable-isolated-segment-canonicalization.md`

## Journal

- 2026-07-19: Activated after WS-A and its A9 conformance tail closed. Workspace-wide `cargo fmt --all -- --check`, strict all-target workspace Clippy, and strict all-features workspace Clippy pass at commit `1201792e`. The concurrent Iceberg/dependency lane remains dirty and is explicitly outside this ticket's edit and staging boundary.
- 2026-07-19: Added the neutral local isolated-host round trip in `cdf-runtime`. The host accepts only bounded serialized task/attempt bytes, reconstructs one owned source/partition/execution authority through its own registry and verifier, invokes a worker-owned executor, and returns only serialized result bytes. The coordinator API independently decodes and admits that result against its current lease, registry, artifacts, and source facts before exposing the private `AdmittedPartitionWorkerResult`. The API executes one task at a time so callers can bound the concurrent frontier without retaining cardinality-sized metadata; bulk data remains referenced rather than entering control messages. The focused round-trip/stale-fence test, all 11 worker-protocol tests, formatting, and strict all-target runtime Clippy pass.
- 2026-07-19: Added the engine-owned capsule compiler and inverse authority decoder. One real `EnginePlan` partition now lowers through an injected artifact writer into the exact source, partition, project, schema, validation, normalization, compiled-expression, operator-graph, segmentation, extent, decode-unit, and segment-plan artifacts required by WX1; the task carries only typed references and semantic hashes. The inverse verifier reads each compiler artifact under the task's memory knob, checks bytes and provider generation, decodes the actual engine types, and rejects a set that does not form one coherent plan/source/partition graph. Store transport and paths remain outside `cdf-engine`; output data verification stays on hash-while-write/provider facts rather than forced rereads. The focused real-plan compile/reconstruct/tamper test and strict all-target engine Clippy pass.
- 2026-07-19: Closed a missing execution handoff at the neutral boundary. The verifier now attaches one opaque, owned executable program to the authority reconstructed from compiler bytes; `cdf-runtime` can validate and transport that value without knowing engine types, while the matching executor can downcast it to the exact `EnginePlan`. This prevents both reparsing/reoptimizing and a second artifact read, and it does not serialize the program or admit it from worker-authored bytes. The runtime protocol suite, focused engine compiler test, formatting, and strict all-target Clippy for both crates pass.
- 2026-07-19: Added the first actual-engine isolated execution checkpoint. The fixture sends only a dataset id and independently checked source-byte fact through the compiled source plan, reconstructs an owned engine program from immutable digest-keyed compiler artifacts, resolves the mock source through the worker registry, executes the real engine in a worker-owned host, publishes canonical Arrow segments only through fenced `WorkerArtifactWriteSession` writes, then independently verifies hashes, Arrow row counts, exact observations, and source facts before coordinator admission through a distinct verifier instance. Direct and capsule engine executions produce identical segment entries, profile, lineage, positions, and execution evidence with worker CPU budgets of one and four slots. This intentionally proves isolated engine execution and segment publication only: it does not claim jobs-N partition concurrency or coordinator package assembly, both of which remain open. The focused law, all worker-protocol tests, formatting, and strict all-target Clippy for `cdf-runtime` and `cdf-engine` pass. The full engine suite remains at its pre-existing committed-HEAD baseline of 23 unrelated failures (invalid historical hash/accounting fixtures, schema-manifest drift, and an ownership scan); this change adds one passing test and no new failure.
- 2026-07-20: Adversarial review exposed the unavoidable global-ordinal barrier hidden by the one-partition checkpoint. The original ratification, now historical at `.10x/decisions/superseded/two-barrier-isolated-segment-canonicalization.md`, established prepared rows followed by separately fenced ordinal finalization. The active decision narrows that mechanism to compiler-proven partition-separable tasks because a prefix barrier cannot make package-global limit/dedup/drain operators separable.
- 2026-07-20: Introduced the typed `PreparedSegment` artifact and receipt vocabulary at the neutral protocol boundary. Prepared rows now cross the same fenced cumulative write authority and independent content/hash/generation/row-count admission as canonical segments, but remain a distinct non-package artifact kind. Result accounting accepts either prepared or canonical segment rows without conflating their identities; compiler/output authorities were updated exhaustively. The 12 worker-protocol tests, the existing actual-engine isolated checkpoint, formatting, and strict all-target Clippy for runtime/engine pass. Finalization task authority and engine production remain open.
- 2026-07-20: Added the source-free second-barrier control protocol. `PortableSegmentTask` binds exactly one admitted prepared segment, its preparation-result digest, independently verified output-schema and segmentation-policy artifacts, a checked dense package-row prefix, and one canonical-segment output scope; it carries no source, secret, checkpoint, or coordinator commit authority. A dedicated serialized local host reconstructs those artifacts independently on the worker, invokes an opaque engine-owned finalizer, and returns one bounded result which the coordinator reconstructs and verifies again under the current fencing lease. The shared write session now operates over the minimal portable-task lease/budget/output contract instead of special-casing partition tasks. Fourteen protocol tests—including full worker/coordinator serialization, separate verifier instances over the shared content authority, and stale-fence rejection—pass; formatting and strict all-target runtime Clippy pass. Engine preparation/finalization and multi-partition coordinator assembly remain open.
- 2026-07-20: Implemented the engine-owned second barrier and removed a discovered duplicate-payload read from coordinator admission. The worker reconstructs verified prepared IPC, logical output schema, and segmentation policy under task byte/memory bounds; appends the coordinator-assigned dense `_cdf_package_row_ord`; and publishes exactly one fenced canonical segment. Both local package encoding and isolated finalization now call the single `cdf-package` canonical LZ4 IPC encoder. The coordinator compiles finalization only from the private `AdmittedPartitionWorkerResult`, verifies the returned output object, and never decodes the prepared payload again. The actual-engine fixture now publishes prepared rows, crosses the serialized finalization host, and reproduces direct canonical segment hashes byte-for-byte at one and four CPU slots. Focused package/runtime/engine tests, crate formatting, diff checks, and strict all-target Clippy for all three touched crates pass. True multi-partition preparation and coordinator package/state assembly remain open.
- 2026-07-20: Clean-worktree execution of the exact fast-CI surface at `f85e865b` passed: workspace formatting; core library Clippy and tests; CLI core and generated-artifact tests; CLI all-feature Clippy; and tracked-source Gitleaks. Added the coordinator-side canonical-segment import boundary in `cdf-package`: already encoded worker bytes are ordinal-validated, atomically persisted/hash-while-written, and registered through the ordinary durable receipt journal without a decode/re-encode cycle. Native encode and imported bytes now produce identical `SegmentEntry`, manifest, and package hash; forged ordinal ranges fail before publication. The focused package test and strict all-target package Clippy pass. Coordinator engine-draft assembly remains open.
- 2026-07-20: Replaced the single-partition execution approximation with real partition-scoped plan reconstruction. The verified full plan remains the sole identity/semantic authority; each capsule derives an operational one-partition scan and schedule only after proving the task's canonical ordinal/partition binding. Partition source-byte and observation admission are independently scoped rather than copied from whole-source totals. The fixture now executes two partition capsules sequentially at `jobs=1` and concurrently at `jobs=4`, admits each result independently, assigns dense package-row prefixes in canonical partition order, and reproduces the direct engine's canonical segment hashes byte-for-byte in both modes. Multi-partition plans with package-global selectors or drain semantics fail before source contact until their explicit global-operator/epoch topology exists; silently treating those operators as partition-local would be incorrect. Both focused actual-engine laws and strict all-target engine Clippy pass. Drain/global-operator topology and coordinator package/state assembly remain open.
- 2026-07-20: Commit `c82ca220` passed the exact fast-quality workflow from a detached clean worktree: locked metadata, workspace formatting, core library Clippy/tests, both CLI test modes, all-feature CLI Clippy, and tracked-source Gitleaks. Added the missing finite-drain capsule archetype without weakening the multi-partition frontier guard. A one-partition unbounded-capability source now reconstructs the real drain plan, executes through a fresh finite `DrainEpochController`, crosses both serialized worker barriers, and matches direct execution's segment/profile/lineage/position/processed-observation semantics plus deterministic epoch frontier/closure evidence; its finalized canonical segment hash is identical. The three actual-engine capsule laws and strict all-target engine Clippy pass. Multi-partition drain coordinator-frontier selection and package/state assembly remain open.
- 2026-07-20: Commit `f9a9661d` passed the same exact clean-worktree fast-quality and Gitleaks surface. Shifted the nonseparable-plan guard from worker execution to task compilation: multi-partition limit/dedup/drain plans now fail before any compiler artifact is written, much less source contact, and name the required canonical global-operator or epoch task instead of suggesting a fake whole-package partition worker. A focused falsifier proves zero control-artifact writes and zero source opens. The compiler tests, all three capsule laws, and strict all-target engine Clippy pass.
- 2026-07-20: The first consolidated adversarial review returned `fail` with three critical and four significant findings. Critical finding 1 is repaired: coordinator admission no longer treats content hash plus row count as sufficient canonical-segment evidence. The neutral runtime now requires typed independently observed logical-schema and dense package-row-ordinal facts; the engine verifier derives those facts from the exact stored Arrow IPC object under the task's encoded/decoded resource bounds and compares them with the task before constructing `AdmittedSegmentWorkerResult`. A shifted-ordinal falsifier fails at that boundary. The runtime library suite (140 passed, 2 ignored), all three actual-engine capsule laws, formatting, and strict all-target runtime/engine Clippy pass. The review's package-assembly side-channel, record-authority conflict, real lease, metadata-cardinality, source-registry, and failure-matrix findings remain open and are not represented as closed evidence.
- 2026-07-20: Critical review finding 2 is repaired. The worker now publishes one fenced, content-addressed `PartitionEvidence` artifact carrying typed mergeable profile, lineage, positions, stream-admission, retry/checkpoint, frontier, and finite-drain evidence. The coordinator independently reads and validates that object against the admitted result and full plan, then `assemble_isolated_worker_package` consumes only those admitted evidence values plus admitted canonical-segment references: it imports canonical bytes through `PackageBuilder`, rebuilds plan/schema/lineage/epoch identity, and alone finalizes/verifies the package. The `Arc<Mutex<Option<EngineRunOutputWithSegmentPositions>>>` acceptance side channel was deleted. Direct and coordinator-assembled manifests and package hashes are identical for bounded jobs 1/4, two partitions at jobs 1/4, and a finite drain epoch. Unsupported statistics, schema-quarantine, observed-at, multi-partition drain, and resident evidence fail explicitly rather than producing incomplete packages; their complete referenced forms remain open work. Focused clean-worktree capsule tests pass (3/3).
- 2026-07-20: Removed the remaining cardinality-sized payload from the worker control result. `PartitionWorkerResult` no longer contains an inline `Vec<WorkerProcessedObservation>`; the sole copy remains inside the fenced, content-addressed partition-evidence artifact already required for coordinator assembly. Engine source admission resolves that exact reference behind `EngineWorkerAdmissionVerifier` and compares its observations with independently observed source facts. The old inline JSON member is rejected rather than retained as a compatibility shim, and the canonical result fixture is current V1 because no deployed artifact compatibility exists. Runtime protocol tests prove the result stays below 2 KiB and contains no observation payload; all 14 protocol tests, all three actual-engine equivalence laws, formatting, and strict all-target runtime/engine Clippy pass.
- 2026-07-20: Removed complete-segment Arrow materialization from canonical isolated admission and coordinator package import. `PackageRowOrdinalValidator` now validates storage-schema equality and dense package ordinals incrementally; both the engine verifier and `PackageBuilder::import_canonical_segment` retain only the current decoded batch instead of collecting every batch. The verifier's decoded-memory check is consequently a per-live-batch working-set bound rather than a misleading sum over already-released batches. Native/imported package identity remains byte-for-byte equal and all three isolated-engine equivalence laws pass. Prepared-segment reconstruction and encoded artifact ownership still require real lease/streaming repair before the review's memory finding is closed.
- 2026-07-20: Repaired the isolated artifact memory boundary with actual `cdf-memory` ownership. Compiler, evidence, prepared, and canonical reads now acquire encoded leases before exposing bytes; prepared IPC retains a reconciled decoded lease for the complete lifetime of its reconstructed batches; canonical verification remains one-batch-at-a-time under a decoded working-set reservation; and coordinator admission receives the task budget when independently verifying referenced output. Canonical finalization reserves one cumulative working set for retained prepared rows, the generated dense ordinal, and a bounded adaptively grown IPC output buffer, then shrinks the retained output lease to the buffer's real allocation before publication. The former `Vec<u8>` sink bypass and lease-dropping `into_batches` escape are gone. A focused falsifier proves a retained artifact charges the ledger, drop releases it to zero, and an undersized decoded window fails cleanly with zero residual usage. In a detached worktree containing only this diff atop `761718c7`, workspace formatting, strict all-target runtime/engine Clippy, all 142 runtime library tests, the three direct/capsule equivalence laws, and the memory falsifier pass. The newly landed Iceberg baseline independently leaves the full engine suite at its 23 previously recorded fixture/ownership failures; that non-fast surface is not attributed to this batch.
- 2026-07-20: The exact fast-quality commit gate passed in the detached C5 worktree: locked metadata; workspace formatting; strict core library Clippy including `cdf-engine`; 364 kernel/contract/package/runtime tests passed with 8 performance tests ignored; 35 CLI-core tests; 37 CLI-artifact tests; strict all-target/all-feature CLI Clippy; and Gitleaks over the exact staged source tree with no findings. This supports a CI-clean memory-ownership commit while preserving the separately recorded limits of the non-fast engine fixture suite.
- 2026-07-20: Removed the parallel engine-owned source-authority extension seam. Coordinator source-result admission now resolves the exact compiled plan's ordinary `SourceDriver` and invokes that driver's fail-closed isolated verification hook; generic engine orchestration contains no source identity switch or second source registry. The protocol fixture and actual-engine mock are source additions implemented only by registering a driver, while the existing descriptor-only format fixture remains a registry-only format addition. The 14 worker-protocol tests and bounded, multi-partition jobs 1/4, and finite-drain engine capsule laws pass. Commit `6e55acd9` passed the exact detached-worktree fast-quality and tracked-source Gitleaks gate.
- 2026-07-20: Superseded the overbroad two-barrier decision with `.10x/decisions/partition-separable-isolated-segment-canonicalization.md`. The compiler-proven safe-partition topology is the C5 authority; package-global limit/dedup/multi-partition drain continues on the ordinary whole-transfer runtime and is rejected before isolated compiler artifacts or source contact. Future typed global-operator distribution remains owned by the existing distributed-execution and J5 plan-marshaling tickets rather than being smuggled into a partition capsule.
- 2026-07-20: Commit `c9937d90` added serialized-host failure coverage for missing worker services, an unregistered source driver, and a stale driver version. All fail during task/host admission, before execution or artifact authority can advance; the exact detached fast-quality and tracked-source Gitleaks gate passed.
- 2026-07-20: A final distribution-seam review found three remaining authority-boundary defects in one pass. The repair seals engine package assembly behind `AdmittedEnginePartitionEvidence`, which can only be constructed by joining a generically admitted result to independently observed source facts and the frozen checkpoint/drain program. Checkpoint eligibility now comes from the source driver; drain frontier, closure cause, terminality, and compiled cadence/rotation/termination are checked before engine evidence is admitted. Raw decoded `EnginePartitionEvidence` can no longer enter assembly through the public API.
- 2026-07-20: The artifact-provider seam now rechecks the current fencing lease and exact object-generation state inside the provider mutation transaction. The concrete shared C5 store holds lease/fence and object locks across validation and insertion. Protocol and concrete-provider falsifiers advance the fence after session preflight, then prove the stale mutation fails and no object appears. This closes the prior check-then-write race without adding destination or engine identity branches.
- 2026-07-20: Extended the actual-engine matrix to terminal schema drift. The isolated source driver now preserves the complete registered mock resource rather than reconstructing a weaker batch-only facsimile; its result attestation derives the exact physical-schema identity from admitted/quarantined observation evidence. Coordinator assembly merges and validates referenced schema-quarantine catalogs/observations and writes the same package artifacts as direct execution. Direct and capsule manifest/package hashes, terminal quarantine evidence, and execution evidence are identical; no canonical segment is invented for a fully quarantined partition.
- 2026-07-20: Final detached clean-worktree gate for this batch passed: workspace formatting; focused runtime protocol tests (16/16); focused actual-engine bounded jobs 1/4, multi-partition jobs invariance, finite drain, and terminal schema-quarantine laws (4/4); exact fast CI (366 core tests passed, 8 performance tests ignored, CLI 35/35 and artifact mode 37/37); strict workspace all-target/all-feature Clippy across every crate; and tracked-source Gitleaks with no findings. The concurrent Iceberg, benchmark, dependency-lock, and canonical-schema serialization edits were absent from the checked tree and remain outside C5.
- 2026-07-20: The final focused adversarial pass first found five remaining critical/significant authority defects: cross-plan replay could reuse matching ids, drain state was not fully joined, host result encoding could allocate beyond its ceiling, lease rollback/domain collision remained possible, and processed/quarantined outcomes were not exactly reconciled. One consolidated repair sealed exact plan/task/preparation identities, rejected unsupported drain state before admission, bounded serialization while writing, made lease authority domain-scoped and monotonic, and joined every processed/quarantined outcome to its evidence. Falsifiers cover all five defects. The same reviewer then returned `pass` with no unresolved critical or significant finding.
- 2026-07-20: The terminal detached clean-worktree gate passed over the exact closure diff: workspace formatting; 367 core tests passed with 8 performance tests ignored; CLI core 35/35 and artifact mode 37/37; strict workspace all-target/all-feature Clippy across every crate; and tracked-source Gitleaks with no findings. Focused runtime protocol coverage is 17/17 and the four direct/capsule engine equivalence laws pass. This is the closure evidence for C5.

## Evidence

- Harness isolation: serialized local hosts reconstruct source, partition, engine, and segment-finalization authority exclusively from bounded control bytes, registries, provider references, and worker-owned services. Protocol tests reject payloads, secrets, paths, borrowed runtime objects, missing capabilities/services, missing drivers, and version drift.
- Direct/capsule equivalence: the four `actual_engine_capsule_*` laws compare direct and isolated manifest/package identity, canonical segment hashes, profile, lineage, positions, execution evidence, finite epoch closure, and terminal schema-quarantine evidence at jobs 1/4 and one/two partitions.
- Authority failure matrix: protocol tests cover tampered compiler artifacts, semantic rehashes, stale attempt/fence, generation conflicts, row/count/ordinal mismatches, missing service/driver/version, and the post-preflight provider race. Both worker artifacts and engine evidence are opaque admitted wrappers before coordinator assembly.
- Bounded control/data separation: one task/result is processed at a time; results stay below the recorded 2 KiB fixture ceiling; observations and Arrow payloads are content-addressed references; encoded and decoded artifact memory holds real `cdf-memory` leases; canonical reads validate one batch at a time.
- Extension boundary: the engine has no source identity switch. The mock source implements ordinary `SourceDriver` registration and its isolated verification hook; the format fixture remains registry-only.
- Static/security gate: detached `cargo fmt --all -- --check`, strict workspace `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`, the exact fast-CI test surface, and Gitleaks all pass.

## Retrospective

- The first one-partition proof hid two global boundaries: package-row ordinals and coordinator-owned evidence. Making those values explicit as separate prepared/finalized artifacts and sealed admitted types was simpler and safer than letting orchestration infer trust from call order.
- Content hashes prove byte identity, not semantic authority. Durable admission must join stored bytes to independently observed source facts, compiled control policy, and current provider fencing at the last mutation boundary.
- A source fixture reconstructed from batches was not equivalent to the registered source: it silently discarded effective-schema and terminal-attestation behavior. Preserving the complete source object made the schema-drift law both more realistic and architecturally cleaner.
- Package-global operators remain a different task topology. Failing their partition-capsule compilation before artifact or source work preserves performance and correctness without contaminating the safe partition protocol with speculative scheduler machinery.

## Review

- Initial consolidated adversarial verdict: `fail`; three critical and four significant findings identified missing semantic admission, package assembly authority, provider fencing, bounded metadata/memory, and extension-boundary guarantees. Each finding was repaired and falsified rather than waived.
- Final focused adversarial verdict: `pass`. Exact plan/task/preparation sealing, drain-state rejection, bounded result serialization, domain-scoped monotonic lease authority, and exact outcome reconciliation close the last five prior findings. No critical or significant issue remains.
- Residual risk: statistics-profile distribution, observed-at contract context, package-global operators, multi-partition drain topology, and resident execution remain deliberately outside the partition-separable C5 contract. They fail closed here and retain their existing owners; C5 does not claim a remote scheduler or transport.
