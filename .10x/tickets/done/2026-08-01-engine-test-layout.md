Status: done
Created: 2026-08-01
Updated: 2026-08-01
Parent: .10x/tickets/done/2026-08-01-rust-crate-architecture-cleanup.md

# Split the engine white-box test monolith

## Scope

Replace the 11,399-line `cdf-engine/src/tests.rs` with focused white-box modules for planning, execution, schema admission, segmentation, workers, retry/drain, determinism, and package evidence.

## Non-goals

- No engine behavior, fixture semantics, assertion, golden, or ignored-test change.
- No overlap with production execution decomposition beyond import/module declaration reconciliation.

## Acceptance Criteria

- Test modules have focused names and ownership; shared fixtures live in one explicit test-support module.
- Test names, count, ignored status, and assertions are preserved.
- Engine tests and strict lint pass after reconciliation with the execution-decomposition child.

## References

- `.10x/knowledge/rust-crate-organization.md`
- `.10x/specs/architecture-layering-runtime.md`

## Assumptions

- Record-backed: the current test root is 11,399 lines and its scenarios are separable by engine responsibility.

## Journal

- 2026-08-01: Ticket opened from the white-box test layout finding.
- 2026-08-01: Execution assigned. Read the ticket and both governing references completely. Scope is test-file ownership only: production behavior and visibility, fixtures, assertions, test names, ignored states, and engine semantics remain authoritative.
- 2026-08-01: Attempted the repository-required architecture query before source work, but the `graphify` executable is not installed (`zsh: command not found: graphify`). Inspected the source and captured the executable test inventory directly instead.
- 2026-08-01: Captured the pre-split baseline: `cdf-engine/src/tests.rs` was 11,399 lines with 136 `#[test]` functions and zero `#[ignore]` attributes; `cargo test -p cdf-engine --lib --locked -- --list` reported 219 tests for the full crate.
- 2026-08-01: Mechanically extracted every test function as one complete item, leaving assertions and bodies byte-for-byte unchanged, and centralized all non-test fixtures/helpers in `tests/support.rs`. Added focused owners for planning, execution, schema admission, segmentation, workers, retry/drain, determinism, and package evidence.
- 2026-08-01: Reconciled white-box visibility only: support values and inherent fixture methods are `pub(super)` so sibling test modules can share them; no production item or API visibility changed. The 78-line `tests.rs` now contains shared imports and the explicit module map.
- 2026-08-01: The first full engine run exposed a layout assumption in `production_runtime_ownership_is_centralized`: it excluded files whose names contained `test`, but recursively treated files below conventional `src/tests/` directories as production. That produced false positives for test-only thread spawns in the new engine layout and the concurrently split CLI tests. Updated the scanner to skip directories named `tests`; it still recursively scans every production source directory and retains the exact forbidden-runtime assertion, so the production-only invariant is preserved rather than weakened.
- 2026-08-01: Re-ran the corrected guard directly with its fully qualified name (1/1 passed), then completed the full engine suite, strict lint, formatting, and patch-hygiene validation. `graphify update .` was attempted after edits and again failed because the executable is unavailable.
- 2026-08-01: Closure repair authorized after independent review. Scope is limited to dissolving the 3,912-line wildcard-imported support catch-all: move the reviewed single-owner fixture families beside their focused scenarios, retain genuinely cross-module fixtures in one support owner, replace every case-module support wildcard with explicit imports, and preserve all 136 test items byte-for-byte.
- 2026-08-01: Moved complete non-test item families for worker artifacts/equivalence, planning DataFusion resources, retry stalled/source-plan fixtures, determinism skew/drain fixtures, package collectors/tracing/schema batches, schema-specific rename/output helpers, and the segmentation positioned batch into their sole case owners. Their former `pub(super)` visibility is now module-private. An ownership-first pass temporarily reduced shared support to 1,063 lines; compile-guided dependency checking identified `mock_unbounded_source_plan`, `mock_compiled_source_plan_with_speculation`, and `RetainedEngineRun` as genuinely cross-module fixtures, so they returned to the shared owner. Final support is 1,149 lines, a 2,763-line (70.6%) reduction from the reviewed 3,912-line catch-all; test bodies and attributes were not edited by this repair.
- 2026-08-01: Replaced all eight `use super::support::*;` preludes with explicit named imports. No focused case module has a support or parent wildcard import, no scenario-local helper remains sibling-visible, and the shared support owner exposes 36 top-level items plus its parent re-export for named case imports. Three support-internal helpers were narrowed from `pub(super)` to private.
- 2026-08-01: Closure-repair validation completed. All-target compilation, the full non-guard engine suite, strict Clippy, and rustfmt passed. The unfiltered 219-test run reached 211 passed and seven ignored, with only the repository-wide production-runtime ownership guard failing because concurrently changed `cdf-project/src/runtime_tests/*.rs` files contain `futures_executor::block_on`; rerunning with exactly that unrelated guard filtered passed 211/211 with seven ignored. `graphify update .` was attempted and remains unavailable because the executable is not installed.
- 2026-08-01: Final support-ownership repair started from the fresh re-review's sole remainder. Confirmed `apply_mock_exact_filters` has exactly one consumer, the module-private `DataFusionMockResource` in `planning.rs`; captured its visibility-normalized source hash, moved the helper unchanged beside that fixture, narrowed it from `pub(super)` to private, and replaced the support import with the two array types its body requires. No test item or production source was edited. The required pre-edit `graphify query` was attempted and remains unavailable (`zsh: command not found: graphify`).
- 2026-08-01: Final support-ownership repair validation completed. The helper's normalized post-move hash exactly matched its pre-move hash, ownership search found only the private definition and sole planning call, focused all-target check and strict Clippy passed, rustfmt check passed, and scoped diff hygiene passed. The post-edit `graphify update .` attempt remains unavailable. The ticket stays active for one final fresh review.
- 2026-08-01: Aggregate workspace `nextest` exposed a remaining classification defect in `production_runtime_ownership_is_centralized`: the guard skipped literal `tests/` directories and filenames containing `test`, but scanned the externally declared `#[cfg(test)] mod runtime_tests;` tree in `cdf-project`, falsely classifying its test-only `futures_executor::block_on` calls as production. The authorized aggregate repair will replace name-based exclusions with paths derived from external `#[cfg(test)] mod ...;` declarations, retain recursive production traversal and the exact forbidden-pattern set, and add synthetic arbitrary-name/same-name coverage.
- 2026-08-01: Repaired the runtime-ownership guard with a two-pass, source-derived classifier. The first pass enumerates Rust sources, recognizes Cargo integration-test roots only at manifest-owning package roots, and resolves each external `#[cfg(test)] mod name;` to its exact sibling `name.rs` and child-module `name/` paths using Rust's crate-root/`mod.rs` versus file-module layout. The second pass recursively scans every remaining Rust file, strips inline `#[cfg(test)]` items, and applies the unchanged nine forbidden-runtime patterns. Removed both the literal-anywhere `tests/` directory exception and `name.contains("test")` file exception, so similarly named production paths remain covered.
- 2026-08-01: Added a synthetic temporary crate proving both external test-module forms: an arbitrary `quality_gate/` test tree and `fixture_owner.rs` test file may contain a forbidden executor, while a same-named `production/quality_gate/` directory is still scanned and produces the sole expected violation. The focused synthetic test and exact repository guard each passed 1/1.
- 2026-08-01: Aggregate guard-repair validation completed. All-target check and warnings-denied Clippy passed; the full engine library suite passed 213 tests with seven pre-existing ignores and no failure; rustfmt initially reported only mechanical wrapping in the new helper/test, `cargo fmt -p cdf-engine` applied it, and final formatting plus scoped diff hygiene passed. The ticket remains active for fresh independent review.
- 2026-08-01: Fresh aggregate review supplied valid Rust counterexamples to the text-based classifier: nested inline modules change the logical module directory, `#[path]` changes the external file authority, and comment/string text can mimic `#[cfg(test)]`. The authorized repair is limited to replacing that classifier with structural Rust parsing, propagating inherited test ownership through module contexts, and adding the review's negative synthetic cases without changing the forbidden-runtime set or production behavior.
- 2026-08-01: Replaced raw cfg/module text parsing with a test-only `syn` AST analysis using `proc-macro2` source locations. The resolver carries separate ordinary-module and `#[path]` bases through inline module nesting, resolves exact external files plus their physical child-module trees, propagates test ownership to declarations inside test-owned source files to a deterministic fixed point, and lets the nearest production ownership win ties so complementary test/production declarations cannot hide a shared path. Cargo package-root integration-test exclusions and the exact nine forbidden strings are unchanged.
- 2026-08-01: Expanded the synthetic guard proof into two adversarial tests. The module-resolution case covers a nested inline cfg-test module, external `#[path]` outside an inline module, `#[path]` inside an inline module, an inherited cfg-test inline parent with an ordinary child declaration, one physical file with complementary test/production ownership, and same-named conventional production files that must be reported. The syntax case places fake cfg-test declarations in both a comment and string while requiring the following production modules to be reported.
- 2026-08-01: The first focused command stopped before compilation because `--locked` correctly rejected the new direct test-dependency edges. Re-running that focused test with `--offline` updated only `cdf-engine`'s existing `proc-macro2`/`syn` lockfile dependency list and passed without network access; every subsequent command used `--locked`. The first exact workspace guard then found test-only thread-spawn helpers on associated methods inside a production `impl` in `cdf-memory/accounting.rs`. Extended structural range masking to cfg-test impl, trait, and foreign members; the exact guard then passed.
- 2026-08-01: Final AST guard-repair validation completed. Both focused regressions, the exact repository guard, all-target check, warnings-denied Clippy, the full engine library suite, rustfmt, and scoped diff hygiene passed. The suite now has 221 tests: 214 runnable passes, seven pre-existing ignored benchmark/stress tests, and zero failures. The ticket remains active for another fresh independent review.
- 2026-08-01: Final minor guard repair authorized from independent review. The exact-only `cfg(test)` classifier can falsely report valid conjunctive and `cfg_attr`-derived test-only code; structural range masking also needs to cover cfg-capable struct fields, enum variants, and variant fields. The bounded repair will conservatively determine whether effective `cfg`/recursive `cfg_attr` constraints can be true with `test=false`, preserve production-capable and unknown predicates in the scan, add adversarial shape and availability regressions, and leave module path/ownership resolution plus the exact forbidden-pattern set unchanged.
- 2026-08-01: Replaced exact cfg recognition with a conservative structural availability evaluator. With `test=false`, it handles `all`/`any`/`not`, empty combinators, multiple conjunctive `cfg` attributes, conditional `cfg_attr` effects, and recursively generated `cfg_attr`; every unknown non-test predicate may take either value, so uncertain source remains production-visible. Applied the same classifier and source-range masking to impl/trait/foreign members, struct fields, enum variants, and variant fields. The AST module/path ownership algorithm and nine forbidden strings are unchanged.
- 2026-08-01: Added adversarial regressions using the same forbidden executor string across test-only and production-capable cases. External modules prove conjunctive, multiple-cfg, empty-any, and recursive-cfg_attr test-only paths are excluded while `any(test, feature)`, `not(test)`, conditional cfg_attr, and empty-all paths are reported. Direct source analysis proves test-only struct fields, enum variants, and variant fields are masked while eight production-capable member occurrences remain.
- 2026-08-01: Final minor-repair validation completed. Four focused ownership regressions and the exact repository guard passed; the full 223-test engine library suite completed with 216 passes, seven pre-existing ignores, and zero failures. All-target check, warnings-denied Clippy, rustfmt check, and scoped diff hygiene passed. The ticket remains active for fresh independent review.

## Blockers

None.

## Evidence

- Initial split layout before closure repair: `wc -l` reports 78 lines for `tests.rs`; focused case files contain 373 determinism, 528 execution, 1,505 package-evidence, 769 planning, 1,857 retry/drain, 1,269 schema-admission, 680 segmentation, and 476 worker lines. The initial shared support owner was 3,912 lines. The test distribution is 18 planning, 16 execution, 22 schema-admission, 13 segmentation, 8 worker, 24 retry/drain, 9 determinism, and 26 package-evidence tests, totaling 136.
- Test/assertion parity: before extraction and again after formatting both the baseline and split sources, an item-level script keyed each `#[test] fn` by its leaf identifier and SHA-256 hashed the complete attribute-plus-function text. Result: `before 136 after 136 exact_after_rustfmt True`, with empty missing, added, and changed sets. This proves test attributes, fixture calls, bodies, and assertions were not edited. Module-qualified harness paths intentionally gained their focused ownership component; all 136 test function names are unchanged. Shared fixture implementations were mechanically moved as complete non-test items, with only sibling-test visibility qualifiers added, and are behaviorally covered by the full suite.
- Ignored-state parity: the baseline monolith and all split white-box modules each contain zero `#[ignore]` attributes. The full engine suite continues to report the same seven ignored tests, all owned by pre-existing non-white-box benchmark/stress modules.
- Enumeration parity: post-split `cargo test -p cdf-engine --lib --locked -- --list` still reported 219 tests and 0 benchmarks for the crate. The 136 moved leaf identifiers exactly match the baseline inventory.
- Compilation: `cargo check -p cdf-engine --all-targets --locked` passed after the split. The later all-target strict Clippy run also compiled the final guard reconciliation.
- Guard reconciliation: `cargo test -p cdf-engine standalone_host::tests::production_runtime_ownership_is_centralized --lib --locked -- --exact` passed 1/1. The earlier shorthand exact filter selected zero tests and is not evidence; it was immediately corrected with the fully qualified name.
- Engine behavior: final `cargo test -p cdf-engine --lib --locked -j 12` passed with 212 passed, 0 failed, and 7 ignored. This exercises all 136 moved white-box tests plus the crate's other unit tests; ignored benchmark/stress scenarios were not exercised.
- Strict lint: `cargo clippy -p cdf-engine --all-targets --locked --no-deps -- -D warnings` passed on the final source state.
- Formatting and patch hygiene: `cargo fmt -p cdf-engine -- --check` passed. Scoped `git diff --check` for the test layout, guard reconciliation, and ticket passed after the completed evidence update.
- Graph freshness limit: `graphify update .` could not run because the executable is absent. No claim is made that `graphify-out/` reflects the new test module paths.
- Closure-repair ownership: `tests/support.rs` fell from 3,912 to 1,149 lines (2,763 lines, or 70.6%). Final focused-file sizes are 827 determinism, 540 execution, 1,771 package-evidence, 963 planning, 2,002 retry/drain, 1,375 schema-admission, 718 segmentation, and 2,189 worker lines. Scenario-local fixture families now live beside their only consumers with module-private visibility; the cross-consumer `MockResource`, execution/package primitives, schema primitives, source-plan builders, and retained-run fixture remain in the sole shared support owner. A top-level inventory reports 36 sibling-visible support items, down from the reviewed 91 named exports, excluding the support module's parent re-export.
- Closure-repair dependency visibility: scoped `rg` found zero `use super::support::*;` or `use super::*;` declarations in the eight focused case modules. Each module has explicit named support imports, and scoped `rg` found zero top-level `pub(super)` scenario helpers in those case files.
- Closure-repair test parity: a brace-aware item comparison against `HEAD:crates/cdf-engine/src/tests.rs` found 136 tests before and 136 after, with empty missing and added sets. One item hash differs: `residual_multi_partition_decisions_share_verified_effective_schema_and_keep_identity`, whose assertion expansion is the separately authorized residual-lifecycle repair recorded by `.10x/tickets/done/2026-08-01-engine-execution-decomposition.md`. Its baseline and current item hashes are `3e884392f9776a5e1834f6d5e09a778571a40ce6caa5fabea2656b652dfcd01b` and `b265de7b9f7b61edb472eb32bb82f88b80a4ad3b2a6c158fac3bb652e2f116c0`; normalizing only that known external repair makes the aggregate test-item hash exactly equal to the baseline, `5d8cca4c25b9a02ad14716b954db3b53e8cbbe68dbe43799f9c06af1b6a95017`. This closure repair changed imports and moved non-test items only.
- Closure-repair compilation: `cargo check -p cdf-engine --all-targets --locked` passed after explicit-import reconciliation. Final `cargo clippy -p cdf-engine --all-targets --locked --no-deps -- -D warnings` also passed, proving the narrowed support visibility and imports compile across all engine targets.
- Closure-repair engine behavior: unfiltered `cargo test -p cdf-engine --lib --locked -j 12` enumerated all 219 tests and completed with 211 passed, seven ignored, and one failure: `standalone_host::tests::production_runtime_ownership_is_centralized` reported only concurrent out-of-scope `cdf-project/src/runtime_tests/*.rs` uses of `futures_executor::block_on`. `cargo test -p cdf-engine --lib --locked -j 12 -- --skip standalone_host::tests::production_runtime_ownership_is_centralized` then passed all 211 runnable tests with seven ignored and one filtered. No test exercising `cdf-engine` behavior failed.
- Closure-repair formatting and hygiene: final `cargo fmt -p cdf-engine -- --check` passed, and a scoped trailing-whitespace scan over all nine test files and this ticket returned no matches.
- Closure-repair graph freshness limit: final `graphify update .` again failed with `zsh: command not found: graphify`; graph freshness is therefore not claimed.
- Final support-ownership parity: before the move, `sed -n '742,775p' crates/cdf-engine/src/tests/support.rs | sed '1s/^pub(super) //' | shasum -a 256` produced `16234c374fddc8e6ab66285ecca614d010cbb1bfd98d3ccd63ae02d802560cbb`. After the move, hashing the private function's complete `planning.rs:797-830` text produced the same digest. The only source change within the item is the required removal of `pub(super)`; its parameters, branches, exact-filter expression, Arrow filtering, error mapping, payload counts, and returned batch are byte-identical after visibility normalization.
- Final support-ownership visibility: scoped `rg` reports only `planning.rs:797` for the private definition and `planning.rs:881` for its sole call. `support.rs` no longer defines or exports the helper, and the planning support prelude no longer imports it. `BooleanArray` and `Int32Array` are now named explicitly because the localized body uses them. Shared support is 1,111 lines after removing the 34-line single-owner item.
- Final support-ownership compilation/lint: `CARGO_BUILD_JOBS=12 cargo check -p cdf-engine --all-targets --locked -j 12` passed, followed by `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-engine --all-targets --locked --no-deps -j 12 -- -D warnings`, which also passed.
- Final support-ownership formatting/hygiene: `cargo fmt -p cdf-engine -- --check` and scoped `git diff --check` passed. No runtime test was needed for this source-identical, test-only ownership move; the prior complete engine-suite evidence remains authoritative for the unchanged helper behavior.
- Final support-ownership graph freshness limit: the required post-edit `graphify update .` failed with `zsh: command not found: graphify`; no refreshed graph artifact is claimed.
- Aggregate guard failure: workspace `nextest` reached `standalone_host::tests::production_runtime_ownership_is_centralized` and reported test-only `futures_executor::block_on` occurrences under `cdf-project/src/runtime_tests/`. `cdf-project/src/lib.rs` declares that tree as `#[cfg(test)] mod runtime_tests;`; the failure therefore challenged the guard's layout classification, not runtime ownership.
- Aggregate guard classification: the repaired scanner collects exact test-owned module file/directory paths from external `#[cfg(test)] mod ...;` declarations before scanning production. Cargo integration-test directories are excluded only at package roots discovered by `Cargo.toml`; the standalone-host authority and the four pre-existing non-production benchmark/conformance crate roots remain explicit scope boundaries. Searches confirm the former `name.contains("test")` and literal recursive `tests` name filters are gone. The nine forbidden strings are unchanged and centralized in `FORBIDDEN_RUNTIME_OWNERSHIP_PATTERNS`.
- Aggregate guard synthetic proof: `CARGO_BUILD_JOBS=12 cargo test -p cdf-engine standalone_host::tests::runtime_ownership_guard_excludes_only_source_declared_test_trees --lib --locked -j 12 -- --exact` passed 1/1. Its temporary tree places forbidden executor text in arbitrary cfg-test-owned `quality_gate/` and `fixture_owner.rs` paths and in a same-named production directory; the helper returns exactly the production-path violation.
- Aggregate exact guard: `CARGO_BUILD_JOBS=12 cargo test -p cdf-engine standalone_host::tests::production_runtime_ownership_is_centralized --lib --locked -j 12 -- --exact` passed 1/1 with 219 filtered tests. The later full suite reran the same guard after formatting and also passed it.
- Aggregate engine behavior: `CARGO_BUILD_JOBS=12 cargo test -p cdf-engine --lib --locked -j 12` enumerated 220 tests and passed all 213 runnable tests with zero failures and seven pre-existing ignored benchmark/stress tests. The one-test increase is the newly authorized synthetic guard-classification regression; the 136 split white-box test inventory remains unchanged.
- Aggregate compilation/lint: `CARGO_BUILD_JOBS=12 cargo check -p cdf-engine --all-targets --locked -j 12` passed, followed by `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-engine --all-targets --locked --no-deps -j 12 -- -D warnings`, which also passed.
- Aggregate formatting/hygiene: the first `cargo fmt -p cdf-engine -- --check` identified only rustfmt wrapping in the newly added helper and synthetic test. After `cargo fmt -p cdf-engine`, final `cargo fmt -p cdf-engine -- --check` and scoped `git diff --check` passed.
- AST dependency boundary: `cdf-engine` now declares test-only `syn = { version = "2.0.117", features = ["full"] }` and `proc-macro2 = { version = "1.0.107", features = ["span-locations"] }`. Both packages already existed in the workspace resolution; the offline lock update adds only those two names to `cdf-engine`'s dependency list and resolves the already-locked compatible `syn 2.0.119`. Production dependencies and compiled production code are unchanged.
- AST resolution proof: `CARGO_BUILD_JOBS=12 cargo test -p cdf-engine standalone_host::tests::runtime_ownership_guard_resolves_nested_and_explicit_test_modules --lib --offline -j 12 -- --exact` updated the lockfile offline and passed 1/1. After the associated-item masking correction, `CARGO_BUILD_JOBS=12 cargo test -p cdf-engine standalone_host::tests::runtime_ownership_guard_ --lib --locked -j 12` passed both focused regressions. The module fixture reports exactly the four production paths while excluding five exact test-owned file/tree paths; these cover nested inline context, both distinct `#[path]` bases, inherited test ownership, complementary same-file ownership, and same-name production detection. These two AST regressions supersede the earlier single `runtime_ownership_guard_excludes_only_source_declared_test_trees` test name retained above as historical pre-review evidence.
- AST syntax proof: `runtime_ownership_guard_ignores_cfg_test_text_in_comments_and_strings` reports both production modules following fake cfg-test text in a comment and a string. Because `syn::parse_file` supplies module declarations and cfg-test item spans, neither text fragment participates in ownership or masking.
- AST masking feedback: the first post-AST exact repository guard reported only `cdf-memory/src/accounting.rs`'s `#[cfg(test)]` associated method, proving that top-level-item spans alone were insufficient. After cfg-test impl/trait/foreign members were added to the structural range collector, `CARGO_BUILD_JOBS=12 cargo test -p cdf-engine standalone_host::tests::production_runtime_ownership_is_centralized --lib --locked -j 12 -- --exact` passed 1/1 with 220 filtered tests.
- Final AST engine behavior: `CARGO_BUILD_JOBS=12 cargo test -p cdf-engine --lib --locked -j 12` passed 214 tests with zero failures and seven pre-existing ignores across 221 total tests. Both focused AST regressions and the repository guard passed within this final run; the 136 split white-box tests remain unchanged.
- Final AST compilation/lint: `CARGO_BUILD_JOBS=12 cargo check -p cdf-engine --all-targets --locked -j 12` passed, followed by `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-engine --all-targets --locked --no-deps -j 12 -- -D warnings`, which also passed.
- Final AST formatting/hygiene: `cargo fmt -p cdf-engine` formatted the new test-only helpers, then final `cargo fmt -p cdf-engine -- --check` and `git diff --check -- Cargo.lock crates/cdf-engine/Cargo.toml crates/cdf-engine/src/standalone_host.rs .10x/tickets/done/2026-08-01-engine-test-layout.md` passed.
- Final cfg-availability proof: `cargo test -p cdf-engine --lib --locked runtime_ownership_guard_` passed all four focused guard regressions with zero failures and 219 filtered tests. `runtime_ownership_guard_models_effective_cfg_availability` expects violations only from the five production-capable module paths, while `runtime_ownership_guard_masks_only_test_only_fields_and_variants` retains exactly eight production-capable occurrences of the shared forbidden string and removes every named test-only member.
- Final cfg exact guard: `cargo test -p cdf-engine --lib --locked standalone_host::tests::production_runtime_ownership_is_centralized -- --exact` passed 1/1 with 222 filtered tests, proving the conservative classifier removes the workspace's valid test-only forms without weakening the current production ownership invariant.
- Final cfg engine behavior: `cargo test -p cdf-engine --lib --locked` passed all 216 runnable tests with zero failures and seven pre-existing ignores across 223 total tests.
- Final cfg compilation/lint/formatting: `cargo check -p cdf-engine --all-targets --locked` and `cargo clippy -p cdf-engine --all-targets --locked -- -D warnings` passed. Final `cargo fmt -p cdf-engine -- --check` and scoped `git diff --check` over the engine guard, dependency boundary, lockfile, and ticket also passed.

## Review

- 2026-08-01 independent red-team review used `open-code-review-delegate` to resolve the workspace file set and hostile engine rules, then inspected the test-layout and production-guard changes without repeating the executor's recorded test or lint runs.
- Findings:
  - **significant** — `crates/cdf-engine/src/tests/support.rs` is a 3,912-line catch-all rather than an owner only for genuinely shared support, and every focused case module imports its entire surface with `use super::support::*;` (`determinism.rs:1`, `execution.rs:1`, `package_evidence.rs:1`, `planning.rs:1`, `retry_drain.rs:1`, `schema_admission.rs:1`, `segmentation.rs:1`, and `workers.rs:1`). Of 91 named `pub(super)` support exports, 35 are referenced directly by exactly one case module. Concrete single-owner families include package-evidence collectors (`support.rs:16-50`), planning-only DataFusion services/resources (`:100`, `:2514`), worker-only artifact/equivalence fixtures (`:185-1887`), retry/drain source-plan and positioned-batch fixtures (`:1967-2075`, `:2449-2468`), determinism-only skew/drain fixtures (`:2084-2438`), and scenario-specific schema/package batch helpers (`:3556`, `:3762`, `:3787`). The eight case files themselves are coherently named and contain no `misc`/catch-all case module, but this support prelude relocates a large portion of the monolith behind broad sibling visibility and obscures which focused module owns scenario-local setup. To satisfy focused ownership, move single-module helpers and fixture families beside their cases, retain only truly cross-module primitives in `support`, and replace wildcard imports with explicit support imports.
- Falsification evidence:
  - An independent brace-aware comparison keyed all `#[test] fn` items from `HEAD:crates/cdf-engine/src/tests.rs` against the eight case files. It found 136 before and 136 after, with empty missing/added sets and zero byte differences in complete attribute-plus-function text. Test names, fixture calls, bodies, and assertions are therefore exactly preserved, not merely behaviorally similar.
  - Source inventories independently retain 219 `#[test]` attributes and seven `#[ignore]` attributes across `cdf-engine`, exactly matching `HEAD`; the split white-box files contain 136 tests and zero ignores with the recorded 18/16/22/13/8/24/9/26 distribution. The executor's post-split harness enumeration and 212-passed/7-ignored run corroborate this source parity.
  - A normalized item comparison found all 117 top-level items in `support.rs` in the former monolith after removing only `pub(super)`, whitespace/comments, and rustfmt's trailing commas for newly wrapped signatures. No test lives in `support.rs`. This supports fixture implementation parity while also exposing the ownership concern above.
  - The only production-scanner change is adding directory name `tests` to the existing recursive exclusion (`crates/cdf-engine/src/standalone_host.rs:2072-2088`); the forbidden-runtime list and production-file checks remain unchanged (`:2091-2117`). The affected engine and CLI `src/tests` modules are both declared behind `#[cfg(test)]`, so current production code is not hidden by the new exclusion. The ticket correctly rejects the zero-test shorthand command as evidence and records both a fully qualified 1/1 guard run and the full engine suite.
- Verdict: **concerns**. Test, attribute, ignore, fixture-implementation, scanner, and count parity hold, and no engine semantic regression was found. The new support catch-all is a significant ownership/layout defect in the ticket's central objective and should be resolved before closure.
- Residual risk: the seven pre-existing ignored benchmark/stress tests were not exercised; source/hash parity cannot by itself prove behavior outside the preserved assertions, though the full non-ignored suite and strict lint materially reduce that risk; `graphify-out/` freshness remains unverified because the executable was unavailable. The ticket remains active for closure judgment and separately authorized repair.

## Retrospective

- What worked: extracting complete top-level test items first, then comparing formatted per-function hashes, made assertion preservation mechanical rather than dependent on visual review of an 11,399-line move.
- What surprised: the runtime-ownership guard encoded test layout through filename substring matching. Once tests used the conventional directory module form, that approximation leaked test-only code into its production scan; the same issue was already visible in the concurrently split CLI test directory.
- Durable lesson: repository-wide architecture scanners should classify explicit test directories as well as test filenames and `#[cfg(test)]` modules. The corrected traversal preserves exhaustive production scanning while permitting standard Rust test layouts.
- Dead end: the first exact-filter guard command used only the leaf name and selected zero tests. The fully qualified filter was used immediately afterward and passed; only the latter is acceptance evidence.
- Follow-up ownership: no product-code defect or engine semantic follow-up was discovered. The CLI test-layout executor should be told that the shared runtime-ownership guard now recognizes `src/tests/` as test-only. The missing local `graphify` executable remains an environment limitation for the orchestrator.
- Closure-repair lesson: direct consumer counts alone missed indirect sharing through helper composition. Moving complete ownership families first and then compiling exposed the three fixtures with real cross-module consumers; returning only those fixtures preserved dependency direction without recreating a wildcard prelude.
- Closure-repair result: the significant review finding is addressed without changing a test item in this ticket. The ticket remains active for fresh independent re-review, as required by separation of duties.
- Final support-ownership lesson: even after a broad compiler-guided ownership repair, a final symbol-level consumer scan is valuable; it found one leaf helper whose body had no shared dependency despite living in support. Visibility-normalized item hashing made the last move mechanically auditable without repeating behavioral verification.
- Final support-ownership result: the re-review's sole remaining layout defect is repaired without changing any test item or production source. `planning.rs` now owns the full `DataFusionMockResource` fixture family, while shared support contains no `apply_mock_exact_filters` surface. The ticket remains active for the requested final fresh review.
- Aggregate guard-repair lesson: test ownership is a module-graph property, not a filename convention. Deriving exact external test-module paths from `#[cfg(test)] mod ...;` declarations and Cargo's package-root integration-test convention prevents both false positives on arbitrary test tree names and false negatives on production paths that merely contain `test`; the same-name synthetic counterexample keeps that distinction executable.
- Aggregate guard-repair result: the repository-wide guard now passes against the split `cdf-project/src/runtime_tests/` layout without weakening its recursive production coverage or forbidden-pattern set. One focused synthetic regression test was added; no production code or public API changed. The ticket remains active for independent review.
- AST repair lesson: Rust module ownership cannot be reconstructed from physical filenames plus identifiers. The logical inline-module directory, the separate root-versus-inline `#[path]` bases, and inherited conditional ownership are compiler semantics; using the existing Rust parser as a test-only dependency is smaller and safer than maintaining an incomplete lexer.
- AST repair validation lesson: structurally removing cfg-test code must include associated items inside production impl/trait/foreign owners, not only top-level items. The exact workspace guard caught that omission immediately in `cdf-memory/accounting.rs`, and retaining the failure plus correction prevents future reviews from mistaking a green synthetic suite for exhaustive repository coverage.
- AST repair result: test ownership now converges over exact AST-resolved file/tree roots with nearest-root precedence and production winning equal-depth ties. The review's nested-inline, custom-path, complementary-ownership, and fake-text counterexamples are executable regressions; the unchanged runtime-owner pattern set passes across the workspace. The ticket remains active for fresh review.
- Final cfg-repair lesson: conditional compilation is an availability question, not an exact-attribute spelling question. Evaluating effective cfg constraints with `test=false` removes conjunctive and cfg_attr-derived false positives while preserving fail-closed production coverage for disjunctions, negation, and unknown predicates; carrying that decision to every cfg-capable member shape prevents the same abstraction leak from recurring one AST level lower.
- Final cfg-repair conservative limit: unknown predicate occurrences are deliberately over-approximated independently. This may retain source that no correlated assignment could enable, but cannot mask production-capable source; unrecognized forms likewise remain visible. Custom Cargo `[[test]]` paths and macro-expanded items remain the previously recorded fail-closed residual limits and were not changed by this repair.
- Final cfg-repair result: the independent review's minor false-positive gap is repaired with no production code, public API, module/path ownership rule, or forbidden-pattern change. The ticket remains active for fresh independent review.

## Support-ownership Repair Re-review — 2026-08-01

### Findings

- **Minor — one planning-only helper remains sibling-visible in shared support.** `apply_mock_exact_filters` is still defined as `pub(super)` in `crates/cdf-engine/src/tests/support.rs:742` and imported only by `planning.rs:13`, where its sole call is part of the module-private `DataFusionMockResource` fixture at `planning.rs:846`. It has no support-internal or second case-module consumer. This leaves a small piece of the reviewed planning-only DataFusion fixture family in the shared owner and exposes it to every sibling test module. Move the complete helper unchanged beside `DataFusionMockResource` in `planning.rs`, make it module-private, and remove its support import. This is a layout/ownership defect only; no assertion or engine behavior currently changes.

No other repair defect was found. The support owner shrank from the reviewed 3,912 lines to 1,146 current lines, and the moved worker, planning, retry/drain, determinism, package-evidence, schema-admission, and segmentation helper families are module-private beside their scenario owners. All eight case modules use explicit named `support` imports; none uses a support/parent wildcard or exposes a top-level helper. The only case dependency edges are `focused case -> support`, with no case-to-case or support-to-case use, so the repair adds no module cycle. The root `use super::*` and support's parent re-export predate this closure repair; they do not grant an implicit case-module prelude because every case names each imported symbol explicitly.

Independent name inventory comparison found exactly 136 baseline and 136 current white-box tests with no missing or added leaf name and zero ignores in either set. The executor's brace-aware item hashes report one changed body only. Direct comparison of that body, `residual_multi_partition_decisions_share_verified_effective_schema_and_keep_identity`, found exactly the separately authorized residual-lifecycle additions: both finalization-hook observations, retained memory/spill/scratch assertions, post-run release/removal assertions, and hook-order assertion recorded by `.10x/tickets/done/2026-08-01-engine-execution-decomposition.md`. No repair code is reachable in production: the module tree remains behind `cdf-engine/src/lib.rs`'s `#[cfg(test)] mod tests`, and no production export or signature was changed.

### Verdict

**Concerns.** The significant catch-all finding is materially repaired: support is 70% smaller, scenario fixture families now have compiler-private owners, case dependencies are explicit, the graph is acyclic, and test/API parity holds. The single remaining planning-only helper is a minor but concrete exception to the repair's ownership invariant and should be localized before closure.

### Residual Risk

- Exact body parity beyond the authorized lifecycle assertion is supported by the executor's recorded brace-aware item hashes; this review independently checked the 136-name inventory and the full diff of the sole reported outlier rather than repeating that extraction or the journaled test/lint commands.
- The seven pre-existing ignored engine benchmark/stress tests remain outside the recorded ordinary suite. The unrelated repository-wide runtime-ownership guard failure from concurrent `cdf-project` test files also remains outside this repair's behavior.
- `graphify` was unavailable, so the test dependency graph was established through module/import/visibility searches rather than a refreshed graph artifact.

## Final Support-ownership Repair Re-review — 2026-08-01

### Findings

- None. `apply_mock_exact_filters` is now a module-private function at `planning.rs:797-830`, immediately before the module-private `DataFusionMockResource` family that solely consumes it at `planning.rs:881`. Repository-wide symbol search finds only that definition and call. Hashing the complete private function produces `16234c374fddc8e6ab66285ecca614d010cbb1bfd98d3ccd63ae02d802560cbb`, exactly matching the ticket's visibility-normalized pre-move digest; branches, Arrow filtering, error mapping, payload-count update, and returned batch are source-identical.
- Reapplying the prior symbol/consumer method to every top-level `pub(super)` item in `support.rs` found no remaining leaf export with one case-module consumer and no support-internal consumer. The two exports with one direct case owner are genuinely composed into shared support: `executable_mock_options` is used by two shared execution wrappers, and `mock_compiled_source_plan_with_speculation` implements the shared default source-plan builder. Every support export has at least one focused consumer; no orphaned sibling-visible helper remains.
- All eight focused case modules retain explicit named `super::support::{...}` imports. Searches find no `use super::support::*`, parent wildcard, case-to-case import, or top-level non-private case helper. The repaired graph remains one-way from focused cases into support and introduces no replacement catch-all or cycle.
- The move touches one non-test helper and its explicit planning imports only. Independent inventory still finds exactly 136 baseline and 136 current white-box test names with empty missing/added sets. The prior brace-aware parity evidence therefore remains applicable: the only test-body difference from `HEAD` is the separately authorized residual-lifecycle assertion owned by `.10x/tickets/done/2026-08-01-engine-execution-decomposition.md`. The entire tree remains behind `#[cfg(test)] mod tests`, so this final move changes no production path, public API, or engine semantics.

### Verdict

**Pass.** The final planning-only ownership remainder is closed source-identically. Shared support now contains no single-case leaf helper under the same method that found the defect; case dependencies remain explicit and acyclic; and the established 136-test parity is undisturbed. The ticket remains active for the orchestrator's closure judgment.

### Residual Risk

- `executable_mock_options` and `mock_compiled_source_plan_with_speculation` each have one direct case importer, but both also implement other shared support helpers; their shared ownership is compositional rather than visible from direct case counts alone. Future changes that remove those internal consumers should re-run the same ownership audit.
- This review relied on the ticket's brace-aware test-item hashes and prior full-suite evidence, corroborated by the unchanged 136-name inventory and the fact that the final repair edits no test item; it did not repeat the behavioral suite or strict lint.
- Seven pre-existing benchmark/stress tests remain ignored, and `graphify` remains unavailable, so neither those opt-in behaviors nor a refreshed graph artifact add evidence to this final pass.

## Aggregate Architecture-guard Repair Review — 2026-08-01

### Findings

- **Significant — `crates/cdf-engine/src/standalone_host.rs:1241-1256,1286-1309` can classify a production path as test-owned.** `classify_rust_source` reduces every external `#[cfg(test)] mod name;` it finds to only `name`, and `runtime_ownership_violations` resolves that name relative to the declaring file's physical directory. Rust resolves an external declaration inside an inline module relative to the inline module's logical path, which this classifier does not retain. For example, `mod outer { #[cfg(test)] mod quality_gate; }` resolves its test tree below `src/outer/quality_gate`, but this guard records `src/quality_gate` as test-owned; a real top-level production `mod quality_gate;` is then skipped wholesale and may contain any forbidden runtime owner without a violation. `strip_leading_attributes` also discards `#[path = "..."]` without using it, so a custom-path test module paired with a conventional same-named production module has the same false-exclusion failure. Finally, the raw `find("#[cfg(test)]")` plus brace/semicolon counting is not syntax-aware: that text in a comment immediately before a production item causes the item to be stripped as test-only. The synthetic regression at `standalone_host.rs:2232-2275` covers recursive descendants, file and directory forms, and a same leaf name at a different conventional path, but none of these false-exclusion cases. Repair the classifier with syntax-aware recursive module traversal that carries the logical module directory, honors `#[path]`, distinguishes test-only from production ownership of the same physical path, and add negative fixtures for nested inline modules, custom paths/complementary cfg declarations, and attribute text in comments.

No other aggregate-repair defect was found. Filesystem traversal and final diagnostics are deterministically sorted; package-root `tests/` exclusion is scoped by `Cargo.toml`; the former filename-substring and literal recursive-directory heuristics are absent; and the centralized nine-pattern forbidden set is exactly the pre-repair set. Current non-excluded workspace external test declarations are conventional file-level modules and no Rust `#[path]` attribute is present, so this review found no currently hidden runtime-owner violation. That current-tree result does not make the regression guard sound for the valid Rust layouts above.

### Verdict

**Fail.** The repair fixes the observed `cdf-project/src/runtime_tests/` false positive without weakening the forbidden-pattern list or determinism, but its central claim of exact source-derived test ownership is not met. A valid nested or custom-path test declaration can exclude a same-named production path, allowing the architecture invariant to regress silently. The ticket must remain active until those false exclusions are repaired and covered by synthetic negative cases.

### Residual Risk

- This review did not repeat the executor's test, lint, or formatting commands. Its verdict rests on direct control-flow inspection and concrete valid-Rust counterexamples to the classifier.
- The forbidden-runtime detector remains an exact textual pattern check after classification. The aggregate repair intentionally preserves that historical set; aliases or alternate spellings outside the nine strings remain outside this ticket's stated repair, but should be considered if the guard is later made fully syntax-aware.

## Final AST Architecture-guard Repair Re-review — 2026-08-01

Fresh independent review used OCR only for deterministic workspace selection and rule resolution, then manually inspected the final guard repair in `crates/cdf-engine/src/standalone_host.rs`, the test-only dependency additions in `crates/cdf-engine/Cargo.toml`, and their exact `Cargo.lock` effect. The reviewer did not author the repair and did not repeat its recorded validation commands.

### Findings

- **Minor — test-only cfg recognition remains deliberately narrower than Rust's full conditional-compilation language.** `has_cfg_test` at `standalone_host.rs:1218-1228` recognizes only an exact `#[cfg(test)]`; it does not prove that `#[cfg(all(test, unix))]` is test-only and does not evaluate `cfg_attr`. Conjunctive test-only forms already exist in the workspace. In addition, structural range masking covers cfg-test top-level items and impl/trait/foreign associated items, but not cfg-test struct fields or enum variants. A forbidden runtime type or call placed in one of those valid test-only forms would therefore be reported as a production violation. This is fail-closed: `cfg(any(test, ...))` can be production-enabled through another branch and `cfg(not(test))` is production-owned, so leaving non-exact predicates in the production scan cannot hide a real production runtime owner. The consequence is a future false positive and guard-maintenance burden, not a false exclusion or current architecture escape.

No significant or critical repair defect was found. Structural `syn` parsing removes the prior comment/string impersonation failure and supplies item/module spans; inherited ownership is carried through inline contexts and propagated through external sources to a deterministic fixed point. Ordinary inline-module bases and separate `#[path]` bases resolve the reviewed nested, outside-inline, inside-inline, inherited-parent, and redirected-file cases. Exact file and physical child-tree roots are both classified, and nearest-root precedence with production winning equal-depth ties preserves complementary production/test ownership of one path. Conventional Cargo integration-test trees are excluded only beneath manifest-owning package roots; same-named production trees elsewhere remain scanned. Source traversal, ownership collections, and final diagnostics are ordered, while the nine forbidden text patterns are byte-for-byte the pre-repair set.

The dependency change is appropriately narrow. `syn` and `proc-macro2` are dev-dependencies only; `syn` enables only `full`, which is required for complete file/item parsing, and `proc-macro2` enables only `span-locations`, which is required for source-range masking. Both packages already existed in the workspace resolution. The lockfile changes only `cdf-engine`'s dependency list, adding the already-resolved `proc-macro2 1.0.107` and `syn 2.0.119`; no package version, checksum, source, production edge, or ecosystem tuple changed.

### Verdict

**Concerns.** The significant false-exclusion cases from the prior review are repaired: comments and strings cannot forge cfg ownership, nested and explicit module paths resolve structurally, inherited and complementary ownership behave conservatively, production/test tree exclusions remain exact, and dependency scope is test-only. The remaining cfg-language/member-shape gap is minor because it can only create false positives, but it is a concrete limitation in a guard described as source-derived test ownership and should remain visible at closure.

### Residual Risk

- The guard intentionally preserves an exact textual forbidden-pattern detector after structural ownership classification. Aliases, macro expansion, token concatenation, and alternate spellings remain outside the nine-pattern invariant; comments or strings containing a forbidden pattern can still false-positive even though fake cfg text cannot change ownership.
- Custom Cargo `[[test]]` paths outside the conventional package-root `tests/` tree are conservatively scanned as production. This cannot hide a production owner but may require classifier extension if such targets contain a forbidden test executor.
- The ownership fixed point operates over parsed source files rather than rustc's fully expanded, cfg-evaluated module graph. The adversarial fixtures and direct source inspection cover the repaired layouts, but compiler changes or new module-producing macro patterns would require corresponding guard coverage.
- Seven pre-existing benchmark/stress tests remain ignored. This review relied on the executor's recorded focused regressions, exact repository guard, full ordinary suite, compilation, lint, and formatting evidence without repeating them.

## Final cfg-language/member-shape Repair Re-review — 2026-08-01

### Findings

None. The repair closes the prior minor finding without introducing a false-exclusion path.

The availability evaluator asks the precise conservative question needed by the guard: can this item exist when `test=false`? Exact `test` is false; unknown atoms retain both possibilities; `all`, `any`, and single-argument `not` compose those possibilities correctly, including `all()` as true and `any()` as false. Multiple cfg attributes are conjoined. Relevant `cfg_attr` effects are evaluated recursively, while non-cfg effects remain availability-neutral. Consequently `all(test, unknown)` and effective cfg-attr contradictions are test-only, whereas `any(test, feature)`, `not(test)`, and conditionally applied `cfg(test)` under an unknown feature remain production-capable and scanned.

Unknown-atom correlations are intentionally not solved. That approximation is safe: it may retain an actually impossible item in the production text, but it cannot prove an actually available item unavailable. The adversarial cases cover conjunctive test-only predicates, production-capable disjunction/negation, empty forms, multiple cfg attributes, nested cfg-attr effects, and both test-only and production-capable cfg-attr conditions.

The same `is_test_only` authority now masks top-level items; impl, trait, and foreign associated items; named and tuple struct fields; whole enum variants; and fields within production-capable enum variants. Test-only variants are removed as a whole, while `any(test, feature)` and `not(test)` members remain in the production scan. Inherited module ownership continues to use the same availability result, so the prior path/tree resolver and production-winning complementary ownership behavior remain intact.

### Verdict

**Pass.** Effective availability under `test=false` is modeled soundly for the requested cfg language, every reviewed member shape uses that authority, and production-capable disjunctions and negations remain scanned. The prior minor false-positive gap is closed. The ticket remains active for parent closure judgment.

### Residual Risk

- Correlated unknown cfg atoms are over-approximated rather than SAT-solved. This is deliberately fail-closed and can only produce a false-positive production scan.
- Conditional non-cfg effects such as a `cfg_attr`-selected `path` do not participate in availability and remain governed by the existing module-path resolver; future conditional-path requirements would need separate path-selection modeling.
- The forbidden owner check remains the established nine-string textual detector after structural masking, so aliasing and macro expansion remain outside this repair.
- This review inspected the implementation and adversarial assertions without repeating the executor's recorded tests, lint, or formatting runs.

## AST Guard Repair Re-review — 2026-08-01

### Findings

- None. The AST repair closes all four reviewed bypasses: nested inline modules carry their logical module directory; `#[path]` uses the correct root or inline base; comments and strings no longer participate in ownership classification; and equal-depth test/production ownership resolves to production, preventing same-path hiding. The adversarial tests cover each case, including inherited test ownership and recursive descendants.
- `syn` and `proc-macro2` are test-only dev dependencies, were already present in the workspace resolution, and are proportionate to replacing the unsafe handwritten parser. No new exclusion hole was found in this scoped review.

### Verdict

**Pass.** The significant aggregate architecture-guard finding is closed. The syntax-aware classifier and its negative fixtures preserve production coverage while excluding only structurally test-owned paths. Status remains active for orchestrator closure.

### Residual Risk

- This re-review did not repeat the executor's test, lint, or formatting commands; it relies on direct source inspection and the ticket's recorded validation evidence.
- The forbidden-runtime detector intentionally remains an exact nine-string check after structural ownership classification, so aliases or alternate spellings remain outside this repair's scope.
