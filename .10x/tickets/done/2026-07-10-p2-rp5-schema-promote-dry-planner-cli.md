Status: done
Created: 2026-07-10
Updated: 2026-07-10
Parent: .10x/tickets/done/2026-07-10-p2-residual-schema-promotion-program.md
Depends-On: .10x/tickets/done/2026-07-10-p2-rp1-residual-envelope-codec.md, .10x/tickets/done/2026-07-10-p2-rp3-correction-capabilities-row-provenance.md, .10x/tickets/done/2026-07-10-p2-rp4-schema-scope-lease-lock-cas.md, .10x/tickets/done/2026-07-10-p2-ws-a10g-explicit-sampled-binary-discovery.md

# P2 RP5 schema promote dry planner and CLI

## Scope

Implement `cdf schema promote RESOURCE [--type JSON_POINTER=ARROW_TYPE ...]` as a strictly no-write planner. Inventory verified residual evidence, fresh/pinned schema facts, target mappings/correction sheets, affected addresses, migrations, and recovery constraints; render P1 human/JSON output.

## Acceptance criteria

- Dry planning writes no snapshots, lockfiles, packages, destinations, checkpoints, leases, or ledger events.
- Plans list residual paths/type sets/counts, proposed types, affected packages/rows/targets, evidence availability, correction strategy, migrations, conflicts, and recovery command.
- Only unambiguous compatible fresh discovery may auto-propose a type; ambiguous/lossy cases require `--type`, and lossy also requires existing allowance.
- Unknown paths, invalid Arrow types, unsupported mappings/strategies, tombstone-only evidence, and stale pin authority produce precise errors.
- Plan artifacts carry old/new schema hashes, exact lock precondition, destination sheet hashes, and deterministic promotion identity.
- Command/help/JSON schema and command-specific diagnostics are covered.

## Evidence expectations

No-write filesystem/destination/ledger assertions, deterministic plan goldens, retained/tombstone/missing inventories, destination strategy matrix, CLI rendering, and adversarial review.

## Explicit exclusions

No `--execute`, correction writes, lease acquisition, lockfile mutation, or GC change.

## Progress and notes

- 2026-07-10: Opened after CLI and dry-plan semantics were confirmed.
- 2026-07-10: Activated after A10g closure and assigned to `/root/impl_i5`. Planning must consume verified residual/readback and destination protocol capabilities without concrete destination-name branches; the default command remains strictly no-write.
- 2026-07-10: Corrected an early architecture violation before integration: semantic inventory, Arrow/schema reasoning, normalization, mapping, strategy selection, deterministic identity, and conflict construction moved from `cdf-cli` into typed `cdf-project::plan_schema_promotion`; the CLI now only parses, calls, and renders, and its temporary Arrow dependencies were removed. Exact package ownership comes only from verified `StateDeltaPreimage.resource_id`; all receipt targets are inventoried rather than selecting one receipt. A locked readback capability is rendered but is not promoted to `destination_readback` availability without a verified probe. Because this dry planner has no source transport/secret or live destination protocol input, absent explicit types produce the typed `fresh_discovery_or_type_required` insufficiency conflict instead of laundering residual runtime types into fresh-discovery authority, and tombstones produce explicit unavailable/unassociated evidence rather than substring inference. `cargo check -p cdf-project` passes after this correction.
- 2026-07-10: Completed the typed dry-planner implementation and command boundary. `PromotionEvidenceInventory` is transport-neutral; its local adapter enumerates without silent I/O skips, attributes packages only through verified state preimages, streams one manifest segment at a time, and emits bounded examples plus canonical per-package/address/value digests rather than retaining every row. The planner canonicalizes arbitrary inventory order, records incomplete/unattributed/tombstone/missing evidence, treats recorded receipt structural coverage separately from pending live destination verification, and selects a correction strategy only when exactly one safe locked capability exists. Fresh discovery facts validate resource and manifest-reference authority, carry coverage/content identity, and auto-propose only compatible types; the planner verifies the strict compiled trust-policy hash, and its path helper serializes exact lossy-allowance reconciliation verdicts without granting runtime policy authority. The proposed promotion snapshot has a deterministic content-addressed preimage binding old pin, proposed schema, fresh discovery, compiler lineage, selected paths, and evidence digests, so later execution cannot invent a different hash. Shared destination mapping resolution now gives exact patterns precedence, rejects equal-specificity ambiguity, and covers current temporal/decimal/nested sheet patterns. CLI output lists paths, evidence, targets, migrations, conflicts, typed writes, recovery argv, and POSIX-safe recovery command; no `--execute` exists. Corrected two rejected approaches during verification: comparing a normalized snapshot hash directly to the manifest's pre-normalization effective hash, and expecting exhaustive evolve to create residual evidence; the final fixture uses the manifest reference as authority and a verified canonical residual package/receipt fixture. Evidence: `.10x/evidence/2026-07-10-p2-rp5-schema-promote-dry-planner.md`.
- 2026-07-10: Repaired the independent-review findings without expanding into execution. Promoted fields now preserve the verbatim terminal source identifier in `cdf:source_name` and the full residual pointer separately in `cdf:promoted_path`; nested paths remain reportable but cannot produce a snapshot or migration until a governed projection exists, and a future reconciliation test proves a promoted top-level field matches its observed source identity. Path facts now preserve exact package → receipt → destination/target associations, so migrations, evidence availability, strategies, and identity are target-specific rather than a path/target cross-product; mixed retained/tombstone and multi-target association tests cover this boundary. Proposed snapshots are real version-3 `SchemaSnapshotArtifact` values accepted by the existing store and validator, with promotion lineage in their deterministic hash input while version-1 declared and version-2 discovery authority remain unchanged. Human rendering now exposes source names, coercion verdicts, affected packages/address examples, fresh content identity, sheet hashes, receipt verification, and execution preconditions.
- 2026-07-10: Hardened the second review boundary. Version-3 snapshots now carry one typed, versioned `SchemaSnapshotPromotionAuthority` owned by the snapshot layer; `SchemaPromotionSnapshotPlan` carries only the artifact and cannot diverge through a second preimage. Constructor/store validation binds resource id, duplicated proposed schema, exact old-snapshot path, compiler authority, sorted selected-path evidence, field type/provenance metadata, coercion target types, and package/receipt/target associations; empty, arbitrary, wrong-resource, wrong-schema, and wrong-old-path artifacts are rejected. Target inventory is now the union of attributed receipt authority and path associations, while migrations and residual-value availability remain path-association-only. A real archived package with state-preimage and receipt authority is inventoried through the local adapter and produces a tombstone target report with no inherited paths/migrations, no strategy, and explicit conflicts.
- 2026-07-10: Removed the remaining duplicate promotion authority from generic snapshot metadata. The version-3 constructor now derives the sole compatibility projection, `cdf:normalizer`, from typed authority; store validation requires exact equality and rejects conflicting normalizer or legacy promotion metadata, while `SchemaSnapshotArtifact::normalizer_version` gives consumers one precedence-free accessor. The transport-neutral inventory canonicalizer now requires every path package/target association to carry nonempty unique receipt ids and match the attributed evidence map exactly on package, destination, target, receipt-id set, and availability before the association can reach target planning or identity. Generic adapter tests reject empty, duplicate, missing, extra, wrong-target, and wrong-availability authority; receipt-only tombstone target union remains intact.
- 2026-07-10: Parent verification and final adversarial re-review passed. Evidence is `.10x/evidence/2026-07-10-p2-rp5-schema-promote-dry-planner.md`; the initial fail review is retained at `.10x/reviews/2026-07-10-p2-rp5-schema-promote-dry-planner-review.md`, and the resolving pass review is `.10x/reviews/2026-07-10-p2-rp5-schema-promote-dry-planner-rereview.md`. Final gates included strict Clippy, formatting/diff checks, 157/157 `cdf-project` tests, 14/14 focused promotion tests, destination mapping tests, the CLI no-write integration, parser coverage, and generated CLI artifacts. RP5 is closed; RP9 is unblocked.

## Retrospective

- Schema promotion evidence must remain a graph, not flattened sets: package, receipt, destination/target, path, and availability are jointly authoritative.
- Content addressing is insufficient when the hashed payload is type-erased. Promotion snapshots now have one typed artifact-owned authority whose semantic invariants are validated at construction and hydration.
- Compatibility metadata is a projection, never competing authority. Version 3 derives `cdf:normalizer` from typed authority and exposes one accessor.
- Dry-plan tests must materialize advertised future artifacts and exercise real tombstone/package adapters; helper-only tests did not expose the original authority gaps.

## Blockers

None for the RP5 dry-planner scope. A full-planner positive `LossyAllowed` case remains separately owned by `.10x/tickets/2026-07-09-p2-ws-g2-type-mismatch-diagnostics.md`; `.10x/knowledge/type-policy-authority.md` requires that future allowance to be compiled into identity rather than injected at runtime. RP5 proves helper-level classification and full-planner fail-closed behavior without claiming the currently unreachable positive branch.
