Status: done
Created: 2026-07-13
Updated: 2026-07-14
Parent: .10x/tickets/2026-07-13-p0-fixed-schema-discovery-stream-admission.md

# P0 SA2: metadata inventory, two-axis coverage, and observation cache

## Scope

Make local/remote file inventory payload-free, remove local whole-file hashing from planning, encode independent file and within-file coverage, and add a versioned observation cache keyed by immutable generation plus codec/options/normalizer/contract identity.

## Non-goals

No fused decoder changes or dynamic producer lifecycle.

## Acceptance criteria

- Local/object-store/HTTP inventory reads no payload bytes.
- `sample_files` selection occurs before any probe for every registered format.
- Manifests encode `all_files|sampled_files` separately from `format_metadata|bounded_content|full_content`; unqualified exhaustive evidence is deleted.
- Local whole-file hashing occurs while extraction/spooling reads content, never during inventory.
- Cache exact hits avoid schema I/O; weak/mismatched/corrupt entries miss safely.
- Cache storage, bounds, cleanup, and telemetry are explicit and remain outside package identity.

## References

- `.10x/specs/schema-discovery-and-stream-admission.md`

## Assumptions

Cache keys and authority limits are fixed by the governing spec.

## Journal

- 2026-07-14: Execution began after SA0/SA1 closed. Initial source audit confirms two remaining inventory violations: local transport metadata computes `file_sha256`, and file-resource planning computes it again. Discovery selection already precedes registered-format probes, providing the seam for exact two-axis evidence. No observation cache exists yet; this ticket will add one keyed only by strong generation/checksum plus compiled format/options, normalizer, and admission identity, with weak local metadata forced to miss.
- 2026-07-14: Removed both inventory-time whole-file SHA-256 passes. Local plans now carry an explicit weak `source_generation` derived from bounded filesystem metadata and label `identity_strength = weak`; remote plans label strong/content-addressed identity without claiming the full object was transferred. `FilePosition` preserves the metadata generation separately from checksum/ETag/version so append incrementality remains possible without laundering weak evidence into a strong field. Fixed object-version-only manifest comparison, which previously always classified the object as changed. Parquet source validation now correctly uses the provider's `exact_ranges` capability for within-run random-access safety instead of conflating that capability with cross-command cache authority. Extraction-time content hashing remains the next part of this acceptance criterion.
- 2026-07-14: Replaced the ambiguous `exhaustive|sampled` discovery label with two independent, serialized axes: `all_files|sampled_files` and `format_metadata|bounded_content|full_content`. Format drivers now declare whether their schema observation comes from format metadata or bounded content, keeping the project compiler free of format-name branching. Candidate, manifest, kernel, source-identity, promotion, and CLI evidence records exact selected/unobserved counts plus observed bytes and records. The executor budget now carries independent byte and record ceilings; the prior hidden 1,000-record decoder limit is explicit evidence. `sample_files` selection remains upstream of every registered-format probe.
- 2026-07-14: Ratified the observation-cache mechanical policy within the active spec: `.cdf/cache/schema-observations/v1`, current-format-only entries, 4,096 entries, 64 MiB total, and 8 MiB per entry, with oldest-write eviction after successful writes. Exact keys bind canonical source location plus strong generation/checksum, format driver id/version, canonical format+transform interpretation, discovery byte/record contract, `namecase-v1`, and contract/baseline identity. Weak identity bypasses; absent, corrupt, unsupported, mismatched, oversized, or unavailable entries miss without affecting discovery correctness. Cache observations retain their original coverage counts for deterministic artifacts while actual source bytes and hit/miss telemetry remain non-identity report data.
- 2026-07-14: Implemented the versioned observation store and wired it through every CLI/project discovery entry point. Exact strong-generation hits reuse canonical schema observations without source payload I/O; local weak identities bypass without creating cache state. Entries are installed atomically, immutable under their content-addressed key, validated against their canonical schema hash, bounded by entry/count/byte limits, and evicted oldest-first after successful writes. Corrupt, unsupported, oversized, mismatched, unavailable, or conflicting entries safely fall back to discovery. Cache telemetry is attached only to non-identity source reporting; replay/package identity retains the original observation coverage rather than the cache access path.
- 2026-07-14: Completed extraction-time content attestation without restoring a planning pre-scan or forcing all files through a temp spool. The kernel now supports monotonic `FileManifest` evidence enrichment: a terminal SHA-256 may enrich an otherwise identical metadata generation, while any path/size/generation/ETag/version change or conflicting hash fails closed. Each `ResourceStream::open` returns an invocation-bound `OpenedPartitionStream`; terminal evidence belongs to that exact stream, is available only after EOF, and is single-use. Sequential local formats hash the chunks already flowing through decode. Seekable/adaptive local formats overlap one ledger-accounted sequential hash sweep with random-access decode. Weak remote streams use the same one-pass hash wrapper, while strong/checksummed remotes retain their existing authority without paying the sweep. The engine joins terminal evidence into segment positions and processed-observation/checkpoint evidence without any destination identity branch.
- 2026-07-14: Repaired live HTTP metadata regressions exposed by Hugging Face and GitHub. Logical file identity is now separate from ephemeral access location, so redirect targets (including signed URLs) can drive transfer without entering plan/package evidence. Metadata requests follow at most ten redirects, rechecking the source egress allowlist at every hop. A server that rejects `HEAD` or omits length falls back to a one-byte `GET` whose body is never buffered or drained; `Content-Range` supplies the full object size. Truly unversioned HTTP sources are admitted only as weak, sequential sources and receive terminal SHA-256 from the extraction stream. The prior unconditional ETag/version/checksum planning rejection is deleted.
- 2026-07-14: First adversarial review returned `fail`: it found the project manifest planner still rejected identity-poor candidates, completion evidence used a shared per-resource mailbox, weak HTTP ETags were treated as strong, signed redirect material could enter Debug/reqwest errors, HTTP performed redundant inner hashing, completion-to-segment enrichment was not total, and one project transport fixture had not implemented the header-only API. The review also correctly separated the still-open cold-discovery spool reuse from this ticket; that behavior remains SA3 scope.
- 2026-07-14: Repaired every in-scope review finding. Identity-poor candidates are always treated as changed during pre-extraction incrementality and can run only through weak sequential input plus terminal SHA. Completion evidence now belongs to one open invocation. Weak ETags are discarded as generation authority and cannot enable ranges or cache hits. Access locations are opaque in observation Debug, redirect `Location` is fully hidden, signed query-name coverage includes AWS/GCS/Azure conventions, and reqwest errors have URLs removed. The HTTP transport hashes only when it owns an expected checksum; weak-source terminal hashing remains in the source adapter. Segment enrichment requires one matching partition path whenever rows were produced. Redirect-aware generation ranges use the observed access location.
- 2026-07-14: The new unversioned-HTTP end-to-end regression reached a successful DuckDB commit and SHA-enriched checkpoint, then deliberately failed its initial request-count assertion because the count included both cold discovery's materialized adaptive spool and execution. This is direct evidence for SA3 rather than an SA2 regression: execution itself performs one payload GET, but cold discovery still discards a full spool. The corrected SA2 test scopes the one-transfer assertion to post-plan execution; SA3 must retain and hand the discovery spool through the plan barrier.
- 2026-07-14: A second independent adversarial review returned `fail`. It found that the access location still used a serializable transport-location type, the public generation-range default did not require request preconditions or validate response totals, exact weak-source SHA propagation had not been asserted, and duplicate terminal paths collapsed. The repair makes access location a private non-serializable opaque type; removes the generation-range default so every transport must implement the promise; binds HTTP ranges with `If-Match`, exact `Content-Range` totals, response ETag, and before/after logical reattestation; binds object-store ranges with provider preconditions; rejects duplicate/extra/missing terminal paths recursively; and applies terminal enrichment only after package dedup and the segment frontier finish.
- 2026-07-14: Added closure laws for missing HTTP validators, wrong range totals, redirect access plus `If-Match`, CloudFront/AWS/GCS/Azure signed-location opacity, recursive terminal-position merging, and post-dedup terminal enrichment. The unversioned project regression now requires the exact extraction payload digest in both checkpoint and per-segment positions. The all-target build initially exposed only a test helper still fixed to the superseded raw stream alias; the helper now accepts the actual stream contract generically, with no compatibility surface added.
- 2026-07-14: A third independent review of the accumulated diff returned `fail` and found the remaining closure set in one pass: dynamic schema quarantine stopped before EOF but still requested EOF-gated attestation; processed/checkpoint positions used union aggregation rather than exact terminal enrichment; local sources did not recheck the planned generation after opening; an access-resource method remained public; speculative partition-open construction could age signed redirect capabilities; `HEAD 403` did not enter bounded-GET fallback; and the obsolete facade range APIs remained as a second transport surface. The implementation was frozen against this complete finding set rather than sent through another incremental review loop.
- 2026-07-14: Repaired the full third-review set. Whole-partition schema quarantine now drains the exact invocation to EOF without validating or segmenting further batches, accounts all input rows/bytes, and exact-merges terminal content identity before processed evidence is committed. Processed positions use the same recursive exact terminal merge even when filtering or package dedup emits no segment. Local opens compare size and filesystem generation before any hash/decode work. Access-resource unwrapping is crate-private, and `ResourceStream::open` is constructed only when the scheduler polls the partition-open future; the `ResourceStream: Send + Sync` contract now states the concurrency requirement explicitly. HTTP metadata falls back on method-specific 403 as well as 400/405/501. The unused facade `ByteRange`, unbound/bound range methods, body-buffering file-response field, and full-body file-send method were deleted; production byte sources are the only payload interface.
- 2026-07-14: The affected-library sweep then caught one architectural regression that focused transport tests could not: production HTTP metadata had introduced its own `thread::spawn` solely to construct/drop Reqwest's blocking runtime. The workaround is deleted. File metadata requests now return futures and run exclusively through the facade's injected `ExecutionServices` I/O runtime; the synchronous REST client is constructed lazily only inside the existing host-confined blocking lane. This preserves one runtime owner and prevents a file transport from creating hidden scheduling infrastructure.
- 2026-07-14: Final review found no critical issues and one significant closure blocker: cache lookup allocated an entire externally enlarged entry before applying the per-entry bound. Lookup now opens the entry, rejects oversized metadata immediately, and still wraps the actual read in a hard `max_entry_bytes + 1` limiter to close the metadata/read race. The same pass found magic-confirmation reads additive to the driver budget; confirmation and schema observation now share one configured byte ceiling. The provider-wide blocking Reqwest client is explicitly not claimed closed here: file metadata has one runtime owner, while the remaining REST-only blocking surface is durably owned by P3 B5.

## Blockers

None. Generation-strength and neutral byte-source prerequisites are committed.

## Evidence

- `CARGO_BUILD_JOBS=12 cargo check --workspace --all-targets` passed after the metadata-identity change.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-files -p cdf-kernel --lib` initially exposed the strength/capability conflation in Parquet; after separating those concepts, `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-files --lib` passed all 32 tests and the kernel run passed all 33 tests.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-project --lib http_parquet_auto_pin_plan_preview_and_run_use_file_runtime` passed the HTTP Parquet plan/preview/run integration and proves bounded discovery no longer appears as a full-object `bytes_loaded` inventory claim.
- `CARGO_BUILD_JOBS=12 cargo check --workspace --all-targets` passed after the two-axis artifact/API change, including every downstream evidence constructor.
- The current `cdf-project` test binary passed all 8 `discovery_manifest` tests, `tests::local_ndjson_discovery_is_bounded_and_writes_nothing_until_pin`, and `tests::local_parquet_discover_autopin_persists_all_file_metadata_manifest`; these observations prove deterministic pre-probe file selection, bounded row-oriented evidence, and all-file metadata-only Parquet evidence respectively.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-kernel --lib` passed all 33 tests, including `two_axis_discovery_coverage_evidence_is_total_and_round_trips`.
- `CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=2 cargo check -p cdf-project --all-targets` passed after the observation-cache integration; disabling incremental debug state avoided host swap thrash while retaining parallel compilation.
- `CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=2 cargo test -p cdf-project --lib observation_cache -- --nocapture` passed 5 tests. The remote integration proves an exact hit performs no schema GET and records zero discovery source bytes while preserving schema/manifest hashes; an ETag change performs a new GET. Unit coverage proves corruption removal, immutable-key conflict rejection, generation/options/normalizer/admission mismatch misses, and bounded cleanup.
- `CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=2 cargo test -p cdf-project --lib local_ndjson_discovery_is_bounded_and_writes_nothing_until_pin -- --nocapture` passed and proves a weak local identity bypasses the cache without creating its directory.
- `CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=2 cargo check -p cdf-cli --all-targets` passed, covering the cache wiring in add, schema, scan, and deep-validation command paths.
- `CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=2 cargo check -p cdf-kernel -p cdf-engine -p cdf-source-files --all-targets` passed after terminal completion-attestation integration.
- `CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=2 cargo test -p cdf-kernel --lib position_aggregation::tests -- --nocapture` passed 2 tests proving checksum enrichment and fail-closed generation/hash conflicts.
- `CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=2 cargo test -p cdf-project --lib general_project_run_commits_multi_file_resource_manifest_checkpoint -- --nocapture` passed. The live two-file NDJSON run proves inventory stays metadata-only while extraction produces SHA-256 evidence in both resource checkpoint and per-segment manifests.
- `CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=2 cargo check -p cdf-source-files -p cdf-transport-http -p cdf-engine --all-targets` passed after the HTTP observation/access split and weak-remote attestation repair.
- `CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=2 cargo test -p cdf-source-files -p cdf-transport-http --lib --no-fail-fast` passed 34/34 and 4/4 tests. Focused laws cover `HEAD 400` fallback, full-size `Content-Range` parsing, redirect access remaining ephemeral, per-hop egress rejection, unversioned sequential-only capability, accounted streaming, and the existing local/object-store/file-runtime matrix.
- A fresh debug CLI binary ran the public GitHub five-file Parquet resource end to end in 8.51 seconds: five redirects resolved to `raw.githubusercontent.com`, 5,000 rows produced five segments, DuckDB receipt verification succeeded, and the checkpoint committed. Package inspection found only the configured logical GitHub URLs in plan, lineage, segment positions, and processed observations; no redirect access URL or signed query entered evidence.
- Independent `curl` and CDF observations separate two fixture problems from the repaired transport: FineWeb redirects to `cas-bridge.xethub.hf.co`, which its current test-project allowlist does not include and is therefore correctly denied before the next request; the configured IMDb API URL returns HTTP 400 for both `HEAD` and `GET` outside CDF, while the current dataset Parquet URL is `.../resolve/refs%2Fconvert%2Fparquet/plain_text/train/0000.parquet` and likewise requires the Xet host in its allowlist.
- With the Xet redirect host temporarily added to the disposable test project's allowlist and then restored, live `cdf schema discover fineweb.documents` completed in 0.97 seconds against the 2,147,509,487-byte object. It read 3,926,176 footer bytes, discovered 1,058,640 rows across 1,059 row groups and nine fields, retained the configured Hugging Face URL as `stable_id`/path, and wrote no snapshot, lockfile, package, destination, or checkpoint. This proves redirect access composes with bounded Parquet discovery rather than downloading the object.
- `CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=4 cargo check -p cdf-kernel -p cdf-http -p cdf-source-files -p cdf-transport-http -p cdf-engine -p cdf-project --all-targets` passed after replacing the shared completion mailbox and repairing weak HTTP identity.
- `CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=4 cargo test -p cdf-kernel -p cdf-http -p cdf-source-files -p cdf-transport-http --lib --no-fail-fast` passed 36/36, 6/6, 36/36, and 4/4 tests. New laws prove EOF-bound single-use completion evidence, weak ETags never authorize strong identity, strict one-byte Content-Range metadata, and opaque AWS/GCS/Azure signed redirect diagnostics.
- The first execution of `tests::unversioned_http_parquet_runs_and_commits_terminal_content_identity` completed the unversioned HTTP Parquet run, destination commit, and SHA-enriched checkpoint before failing only its over-broad cold+execution transfer count (2 versus 1). The assertion is now scoped to execution; a clean rerun remains required before closure. The observed cold transfer is durably owned by SA3.
- `CARGO_BUILD_JOBS=6 cargo check -p cdf-kernel -p cdf-source-files -p cdf-transport-http -p cdf-engine -p cdf-declarative -p cdf-project --all-targets` passed after the third-review repairs and deletion of the legacy range surface.
- `CARGO_BUILD_JOBS=6 cargo test -p cdf-engine --lib tests::dynamic_schema_quarantine_drains_to_eof_and_commits_terminal_content_identity -- --exact --nocapture` passed. Two incompatible batches were fully consumed, produced no segment, recorded all three input rows, and committed one quarantined observation with the terminal SHA-enriched manifest.
- `CARGO_BUILD_JOBS=6 cargo test -p cdf-source-files --lib runtime::tests::local_open_rejects_planned_generation_mismatch_before_hashing -- --exact --nocapture` passed, proving a local generation mismatch fails before extraction hashing.
- `CARGO_BUILD_JOBS=6 cargo test -p cdf-source-files --lib transport::tests::file_transport_http_metadata_falls_back_from_head_errors_and_keeps_access_ephemeral -- --exact --nocapture` passed for both `HEAD 400` and `HEAD 403`, with the redirected access location remaining outside public/debug evidence.
- `CARGO_BUILD_JOBS=6 cargo test -p cdf-project --lib tests::unversioned_http_parquet_runs_and_commits_terminal_content_identity -- --exact --nocapture` passed. The weak HTTP Parquet path completed extraction and destination commit, and both checkpoint and segment positions contain the exact terminal payload SHA-256.
- `CARGO_BUILD_JOBS=6 cargo check -p cdf-source-files -p cdf-transport-http -p cdf-project -p cdf-engine --all-targets` passed after making HTTP metadata asynchronous through the shared execution host.
- `CARGO_BUILD_JOBS=6 cargo test -p cdf-transport-http --lib` passed all 6 tests, including header-only completion without draining a 1 MiB response body and signed-URL error redaction on the async client.
- `CARGO_BUILD_JOBS=6 cargo test -p cdf-engine --lib standalone_host::tests::production_runtime_ownership_is_centralized -- --exact` passed after deleting production transport thread/runtime ownership.
- `CARGO_BUILD_JOBS=6 cargo test -p cdf-kernel -p cdf-source-files -p cdf-transport-http -p cdf-engine --lib --no-fail-fast` passed 38/38, 36/36, 6/6, and 125/125 non-ignored tests respectively. Six explicitly slow/release engine tests remained ignored by their declarations; no executed test failed.
- `CARGO_BUILD_JOBS=6 cargo test -p cdf-project --lib observation_cache` passed all 6 cache tests. `oversized_lookup_is_bounded_before_deserialization` proves the hard reader consumes exactly `max_entry_bytes + 1` before classifying a racing/unknown-length oversized stream, while an already oversized cache file is removed without deserialization.
- `CARGO_BUILD_JOBS=6 cargo test -p cdf-source-files --lib` passed all 36 then-existing tests after the budget repair; the additional focused `format_confirmation_shares_the_configured_discovery_byte_budget` test passed and proves confirmation bytes are subtracted from the schema-observation allowance and exhaustion fails explicitly.

## Review

- 2026-07-14: Initial adversarial verdict `fail`. Critical findings: identity-poor HTTP still failed in project manifest planning; signed access URLs were not comprehensively opaque; weak ETags authorized strong behavior; and the all-target project fixture missed `send_headers`. Significant findings: shared completion mailbox, redundant HTTP hash work, non-total segment enrichment, and the SA3 cold-spool lifecycle gap. Minor findings: redirect-aware generation range and strict Content-Range/socket coverage. All in-scope findings have implementation owners in the current diff; fresh adversarial review is required. SA3 remains the explicit owner of discovery-spool reuse.
- 2026-07-14: Second independent verdict `fail`. Critical findings: access locations were not a strictly opaque type and the public generation-range method was not generation-safe. Significant finding: weak/unversioned closure evidence was insufficiently exact. Minor finding: duplicate terminal file paths overwrote silently. All four findings are repaired in the current diff; focused verification and a new independent closure review remain required.
- 2026-07-14: Third independent verdict `fail`. Critical findings: dynamic quarantine violated the EOF-attestation contract, processed checkpoint evidence did not exact-merge terminal identity, and local open could bind changed content to a planned generation. Significant findings: access-resource visibility, signed-access capability lifetime, and incomplete `HEAD` fallback. Minor finding: duplicate facade range APIs. All findings are repaired and have focused regression evidence above; one final review of the frozen diff remains required.
- 2026-07-14: Final independent verdict `fail` with no critical findings and one significant bounded-cache-read finding. Two minor observations covered discovery-budget composition and the intentionally still-open REST blocking client. The significant defect and discovery-budget observation are repaired with focused assertions; P3 B5 now explicitly owns deleting the REST blocking client during its already-required async migration. Closure review inspected those exact repairs and accepts the residual lack of dedicated compressed-weak-object-store and filtered-to-zero-output end-to-end cases because the generic EOF/recursive-merge laws cover both and SA5 remains the program-level conformance owner. Verdict after repair: `pass`.

## Retrospective

The regression came from treating transport metadata as if it were already durable content identity. A redirect response is neither the file nor its generation, and requiring every server to publish an ETag before extraction made correctness authority prevent safe ingestion. Separating logical identity from runtime access location removed both errors without weakening evidence: strong providers remain cheap, weak providers are sequential and become content-attested only after the exact extraction stream completes. The first completion design repeated an architectural smell the program is meant to remove—a shared mailbox on a long-lived resource—because the API did not bind evidence to an open invocation. Making the opened stream own its terminal evidence is both smaller and concurrency-correct. Live public endpoints exposed behavior that synthetic direct-200 fixtures did not; redirecting, unversioned, weak-ETag, and `HEAD`-hostile providers now have focused laws. The corrected unversioned test also proved that SA3's retained-spool handoff is substantive, not a micro-optimization. Cache corruption is an untrusted-input boundary even when the cache is local: bounds must wrap reads, not merely inspect their resulting allocation. Four Cargo build jobs are configured, but this host's severe swap pressure can still stall a single rustc process independently of job count.
