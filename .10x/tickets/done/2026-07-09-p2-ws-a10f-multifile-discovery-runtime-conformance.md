Status: done
Created: 2026-07-09
Updated: 2026-07-10
Parent: .10x/tickets/done/2026-07-09-p2-ws-a10-multi-file-schema-discovery-pin.md
Depends-On: .10x/tickets/done/2026-07-09-p2-ws-a10e-file-quarantine-processed-positions.md, .10x/tickets/done/2026-07-10-p2-ws-a10g-explicit-sampled-binary-discovery.md

# P2 WS-A10f multi-file discovery/runtime conformance

## Scope

Gate A10 with deterministic multi-file discovery, pin, compatible execution, drift quarantine, replay, budget, and preview/run parity conformance. Use local fixtures plus an injected transport facade so the law is not tied to local filesystem behavior.

## Acceptance criteria

- Parquet and Arrow IPC conformance cover cardinality one and many through the same discovery artifacts.
- Compatible union/widening/missing-null, metadata variance, add/remove/change manifest identity, immutable baseline, effective schema, package replay, and no-source-contact ordinary runs are asserted.
- Incompatible month/file drift completes with named quarantine evidence and correct manifest advancement/retry.
- Preview uses the same exhaustive file resolution/discovery/reconciliation/normalization front end, deterministically selects at most the global batch budget of payload partitions, and keeps rendered rows under the global limit in deterministic order.
- Preview implements `preview-balanced-stratified-v1`: global defaults are 500 rendered rows, 64 MiB decoded input, and 64 batches; payload membership uses `stratified-hash-v1`; selected partitions receive deterministic fair-share batch quotas; and all selected, partially inspected, metadata-attested, and payload-uninspected partitions are reported truthfully.
- A green preview cannot later fail run for a file/decode/schema reason within the bytes/batches preview actually inspected.
- Default and overridden executor budgets are visible; exhaustion is explicit and never activates or changes sampling.
- Large-N deterministic fixtures prove bounded in-flight bytes/concurrency plus byte-identical `stratified-hash-v1` selection without requiring a production network.
- The friction registry and S2/S6/S8 rows are promoted only for the exact behavior proved; HTTP-template/cloud exclusions remain pending under WS-E/WS-I.

## Evidence expectations

Standalone conformance functions, registry owner validation, golden package hashes, recorded plan/package/manifest/quarantine artifacts, repeated determinism runs, full workspace/quality gates, and independent adversarial review.

## Explicit exclusions

No S3/GCS/Azure implementation, HTTP template enumeration, text inference, zip, distributed scheduler, or public-network CI dependency.

## Progress and notes

- 2026-07-09: Opened as the A10 closure gate; final public/cloud scenarios remain owned by WS-E/WS-I.
- 2026-07-10: Activated after A10e/A10g closure and assigned to `/root/impl_a10`. This child must close the known first-partition preview gap through the same resource front end and may promote only the exact local/fixture S2/S6/S8 cells it proves; remote HTTP template/cloud cells remain pending.
- 2026-07-10: Replaced the CLI-only first-partition direct-stream path with a source-neutral `cdf-engine::preview_resource` front end over the serialized `EnginePlan` and `ResourceStream`. Preview and package execution now share per-observation coercion validation, residual filters, projection/explicit row limiting, contract evaluation, residual-variant materialization, identifier normalization, effective-output canonicalization, and compiled-output-schema checks. CLI dispatch no longer has separate file/REST/SQL preview open methods.
- 2026-07-10: Preview traverses planned partitions deterministically and reports truthful coverage: planned partitions, payload-opened partitions, metadata-attested terminal-quarantine partitions, payload-inspected partitions/batches, decoded input bytes inspected, separately rendered output bytes, row/file quarantine counts, limits, and truncation. Terminal quarantines reuse the same exact attestation rule as run, cache repeated observation attestations, never claim a payload open, and remain no-write. Configured byte limits account compacted decoded input before contract work; normalization/variant expansion cannot erase inspected evidence.
- 2026-07-10: Exact local fixture cells were added without promoting the still-pending public/cloud rows. The S8 conformance law now calls the production engine preview front end for file/REST/Postgres instead of manually opening partition zero. A two-file NDJSON fixture proves deterministic partition identity, no-write preview, both payload partitions inspected, package verification, and preview/run row/batch parity. Multi-file Parquet CLI fixtures prove compatible widening/missing-null/empty-file traversal, explicit `--limit` as one global row bound while later partitions are still opened, sampled unseen drift as one attested terminal quarantine plus two admitted payload partitions, and byte-stable project state. The S2/S6/S8 registry rows remain `pending` while indexing these exact tests plus the existing 10,000-candidate selector and discovery-budget/no-substitution regressions.
- 2026-07-10: Focused evidence so far: `cdf-engine` all-feature tests passed 47/47 and strict engine `clippy -D warnings --no-deps` passed; `cdf-conformance` all-feature tests passed 83/83, including the new two-file law, existing file/REST/Postgres parity, S2/S6/S8 registry validation, 10,000-candidate/budget test indexing, live destinations, chaos, and golden repeats. The sampled multi-file CLI preview/quarantine/package/checkpoint regression, compatible three-file Parquet preview/run/replay regression, and shared-engine file-filter regression each passed directly. A combined all-feature CLI run passed 245/248: the generated-artifact failure belongs to the concurrent promotion command addition, while two historical preview tests still asserted the superseded first-file/fail-on-terminal-quarantine behavior. Both A10 expectations were updated to every-sorted-partition and successful attested terminal-quarantine preview respectively; their immediate rerun was then blocked in the concurrently edited untracked `cdf-project/src/promotion.rs` before reaching CLI tests. `git diff --check` passed. Final parent integration should rerun those two tests and the affected check/Clippy suite after promotion stabilizes.
- 2026-07-10: The user ratified the production preview policy in `.10x/decisions/preview-global-budget-and-payload-selection.md`: 500 rendered rows, 64 MiB decoded input, and 64 batches globally by default; deterministic `stratified-hash-v1` payload membership; fair-share batch quotas; metadata-only attestation and truthful uninspected evidence for the remainder. The interim one-batch-per-payload behavior is no longer authority.
- 2026-07-10: Implemented `preview-balanced-stratified-v1` in the shared engine front end. The executor fixes `K=min(N,B)` membership and fair-share quotas before payload I/O, admits decoded batches atomically, reports decoded and rendered bytes separately, and emits planned/eligible/selected/opened/attested/inspected/partial/uninspected counts plus selector membership, scores, identities, quotas, and inspected batches. Default limits are 500 rendered rows, 64 MiB decoded input, and 64 admitted batches; CLI `--limit` changes only the row bound. The source-neutral kernel selector preserves discovery's canonical `stratified-hash-v1` bytes and project discovery adapts to it rather than owning a competing implementation. Focused selector, quota, row exhaustion, byte admission, terminal attestation, and 10,000-partition bounded-open tests are recorded in `.10x/evidence/2026-07-10-p2-a10f-preview-balanced-stratified-policy.md`.
- 2026-07-10: Post-integration verification passed all CLI (253), conformance (83), engine (50), kernel (22), and project (159) unit tests; the standalone conformance doctest harness; discovery selector golden compatibility; strict affected-crate Clippy; and formatting/diff checks. An initial combined-command rustdoc missing-rlib artifact race cleared on standalone rerun. Exact commands and limits are preserved in the evidence record; A10f remains active for parent review and closure reconciliation.
- 2026-07-10: Closure review clarified graph direction: WS-I is the downstream owner of the still-pending HTTP-template/cloud S8 cells, not an upstream dependency of this bounded local/fixture A10f gate. The stale dependency edge was removed without promoting those remote cells.
- 2026-07-10: Repaired the significant canonical-identity finding from `.10x/reviews/2026-07-10-p2-a10f-independent-adversarial-review.md`. `StratifiedHashBoundedIdentity` and its recursively key-sorted compact JSON encoder now live with the single kernel selector authority; discovery retains its public names as aliases to that source-neutral type, and preview no longer owns a private field-order identity dialect. Both adapters construct kernel candidates through `from_bounded_identity`. A cross-adapter golden uses the review's exact S3/ETag candidate and proves identical bytes, the unchanged `1caaf43016737e252f12a8b0568d67951ecfd702010906cc8a7eaddb7b1caa27` score, and identical sampled membership. Final post-repair gates passed CLI 253/253, conformance 83/83, engine 50/50, kernel 22/22, project 160/160, strict affected-crate Clippy, formatting, and diff checks. A10f remains active for independent re-review and parent closure.
- 2026-07-10: Closed after `.10x/reviews/2026-07-10-p2-a10f-canonical-identity-repair-review.md` and `.10x/reviews/2026-07-10-p2-a10-a10f-closure-repair-review.md` passed. Acceptance is mapped by `.10x/evidence/2026-07-10-p2-a10f-preview-balanced-stratified-policy.md`. Retrospective: a versioned selector must own both its typed identity and canonical bytes at the kernel boundary; that invariant is now durable in the preview policy decision, shared kernel API, golden, and superseding review. HTTP-template/cloud completion remains owned by WS-I rather than residual A10f scope.

## Blockers

No execution blocker remains for the local/fixture A10f scope. HTTP-template/cloud/public-network cells remain owned by WS-E/WS-I and are not claimed here.
