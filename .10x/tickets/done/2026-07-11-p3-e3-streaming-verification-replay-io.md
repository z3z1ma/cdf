Status: done
Created: 2026-07-11
Updated: 2026-07-19
Parent: .10x/tickets/done/2026-07-10-p3-ws-e-hashing-package-io.md
Depends-On: .10x/tickets/done/2026-07-11-p3-e2-streaming-manifest-durability.md, .10x/tickets/done/2026-07-11-p3-a5-streaming-operator-graph.md

# P3 E3: bounded verification and replay I/O

## Scope

Anchor reopened-package verification to filesystem capabilities, keep explicit verification bounded at high cardinality, retain opened segment objects through staged destination consumption, and preserve the fresh-run hash-while-write path without redundant rereads. Evaluate consumer-read fusion and unsafe mmap only far enough to retain or reject them honestly; package roofline work remains E4 and the 1 TB process law remains F4.

## Acceptance criteria

- Fresh package finalization issues consumption authority from hash-while-write receipts with zero content rereads; reopened packages perform one explicit verification pass.
- Reopened segment access remains beneath the anchored package capability, and staged destinations retain already-opened length-bound file objects instead of reopening pathnames.
- Explicit verify is bounded, canonically reported, and detects current missing/extra/tamper/non-file/symlink/path-alias cases; parallelism is absent unless admitted and proven beneficial.
- Million-entry verification stays comfortably within a 2 GiB process limit. Unsafe mmap is absent; the 1 TB process law is owned by F4.

## Evidence expectations

Tamper/golden parity, million-entry/RSS, cancellation/error cleanup, destination-reader integration, and same-host fresh-run non-regression.

## Explicit exclusions

No unsafe/mmap without a new decision/fuzz target.

## Blockers

None. `.10x/decisions/capability-rooted-package-verification.md` ratifies the pinned cross-platform dependency and exact path/mutation authority; E2 and A5 are complete.

## References

- `.10x/specs/package-io-hashing-durability.md`

## Progress and notes

- 2026-07-11: Introduced a package-owned `VerifiedPackageReader` consumption authority and threaded it through generic replay, recovery, prepared commit, staged ingress, final binding, and destination planning. A commit operation now performs one full verification and reuses that unforgeable-in-API authority instead of calling whole-package verification at replay-input reconstruction, replay validation, final binding, and binding reconstruction. The fresh 104 MiB/2,964,624-row TLC staged run fell from the recent 1.89-second median to 1.64 seconds; three redundant 104 MiB scans were removed. Replay consumer-read hash fusion, bounded/parallel explicit verification, and high-cardinality closure remain open. Evidence: `.10x/evidence/2026-07-11-p3-e3-single-pass-consumption-verification.md`.
- 2026-07-11: Fresh package finalization now issues the same typed authority directly from its reconciled hash-while-write receipts after manifest publication. Ordinary run therefore performs zero package-content rereads between build and staged final binding; reopened replay still verifies independently. Three fresh TLC controls measured 1.55, 1.58, and 2.13 seconds (median 1.58), a further 3.7% versus the preceding 1.64-second observation and 16.4% versus the recent 1.89-second control median. Replay read/hash fusion and explicit verifier boundedness remain open.

## Journal

- 2026-07-19: Implemented the ratified cross-platform filesystem boundary. `PackageRoot` opens the package directory once as a `cap-std` capability, binds verification authority to the opened object identity, opens descendants one component at a time with symlink following disabled, and now owns manifest, identity-artifact, segment, and archive-sidecar verification reads. Package readers retain and reuse this root instead of canonicalizing and reopening ambient paths. CDF adds no unsafe code.
- 2026-07-19: Removed the verifier's remaining cardinality-sized success output and working collections. `VerificationReport` now reports checked file/archive counts instead of cloning every manifest entry; `cdf package verify` renders the compact counts rather than a million-row file table. Identity verification streams one exact file hash at a time, fails on the first canonical error, and uses binary search over the already-loaded canonical manifest. Segment-to-file reconciliation changed from quadratic linear searches to binary searches. Archive verification likewise removed its `BTreeSet`, cloned segment report, and accumulated failure vector.
- 2026-07-19: Defined manifest identity order with the same allocation-free portable case-fold comparator used for collision validation. This makes case aliases adjacent and permits exact linear collision detection without a Bloom filter, fixed memory ceiling, hash map, or quadratic valid-path fallback. Writers reject backslashes, colon/ADS spellings, controls, trailing dot/space, DOS device names, aliases, traversal, and noncanonical component syntax before publication. Manifest JSON parsing now streams directly from the capability-opened file instead of retaining a second whole-manifest byte buffer.
- 2026-07-19: Capability-rooted archive verification now verifies expected sidecars and fidelity metadata directly, counts the visited archive entries on the valid path, and performs an exact but intentionally slower orphan search only after a count mismatch. The old ambient, sorted `BTreeSet` archive crawler and platform-string missing-file classifier were deleted. The unused public ambient `read_segment_file` shim was also deleted; all live package segment decoding uses the retained package capability.
- 2026-07-19 pre-benchmark hot-path audit: Removed millions of avoidable transient allocations from high-cardinality verification. Canonical manifest path validation now validates the existing UTF-8 string in place instead of rebuilding every path, required-directory membership uses `split_once` instead of allocating eight formatted prefixes per entry, manifest JSON is parsed through a `BufReader`, and manifest identity hashing writes known scalar/string fields directly to its digest sink instead of materializing a `serde_json::Value` and `Vec<u8>` for every field. The fixed package-hash fixture remains the byte-identity guard.
- 2026-07-19: Added exact `cap-std = 4.0.2` and `cap-fs-ext = 4.0.2`, imported the Bytecode Alliance cargo-vet audits, reviewed and certified the remaining `maybe-owned 0.3.4` transitive source, and recorded publisher trust for the pinned capability stack. `cargo vet check` passed with 32 fully audited, one partially audited, and 470 exempted packages; `cargo deny check advisories licenses bans sources` passed with only the pre-existing duplicate/yanked warnings.
- 2026-07-19 local verification: `cargo test -p cdf-package --lib --locked -j 12` passed 59 tests with zero failures and three pre-existing performance-only ignores. Strict all-target/all-feature package Clippy passed. Affected engine, CLI, DuckDB, Postgres, and Parquet destination test targets all compiled with `CARGO_BUILD_JOBS=12`. The implementation remains active pending dedicated-host high-cardinality/performance evidence and fresh-run regression proof.
- 2026-07-19 adversarial closure review found and repaired one authority downgrade: generic staged ingress exposed a verified segment as an ambient pathname, and DuckDB reopened that spelling inside its table-function workers. `StagedSegmentRequest` now extracts and length-validates an already-opened `DurableLocalFile`; fresh segments open once at staging admission, reopened packages open beneath the retained package capability, and DuckDB transfers each handle exactly once into its parallel scanner. Pathnames remain diagnostic only. The superseded pathname method and destination-side reopen path were deleted rather than retained as compatibility shims.
- 2026-07-19 opened-object local gates: the runtime regression replaces the pathname after request construction and proves the retained handle still reads the original bytes. The affected four-crate test-target check passed; package/runtime/DuckDB library suites passed (59 + 87 + 29 tests, with only performance ignores); the focused retained-handle and nested/decimal DuckDB scanner tests passed; strict all-target/all-feature Clippy passed for `cdf-runtime`, `cdf-package`, `cdf-project`, and `cdf-dest-duckdb`.
- 2026-07-19 dedicated-host closure: revision `d0bfe70417d4496fe37558fd0c5ce01cc0aded9f` ran the full-year 41,169,720-row local TLC-to-DuckDB cell three times under a 6 GiB cgroup at 9.914, 10.296, and 10.387 seconds (10.296-second median, 3,998,687 rows/s, zero spill). This is 1.7% faster than the 10.477-second established floor and 2.1% faster than the immediately preceding 10.517-second E3 control, so the opened-object authority introduced no regression. Raw report: `.10x/evidence/.storage/2026-07-19-p3-e3-opened-authority-local-tlc-run.json`.
- 2026-07-19 dedicated-host high-cardinality closure: a 131,000,575-byte manifest naming 1,000,001 empty identity files across 1,000 shards verified in 7.755 seconds median across three warm samples. Peak process RSS was 207,511,552 bytes and peak cgroup memory was 210,214,912 bytes under `MemoryMax=2G`, with zero `high`, `max`, OOM, or swap events. Raw report and cgroup log: `.10x/evidence/.storage/2026-07-19-p3-e3-opened-authority-million-verify.json` and `.10x/evidence/.storage/2026-07-19-p3-e3-opened-authority-million-verify.systemd.log`.
- 2026-07-19 scope reconciliation: raw-byte hash fusion was not retained for Arrow IPC because its seek-based decoder does not observe every identity byte; wrapping `Read + Seek` would make a false integrity claim, while an extra post-verification hash would add a third segment pass and regress the measured hot path. Explicit reopened verification plus retained no-follow file capabilities is the smallest honest path. Mmap remains excluded by the unsafe gate and lacks a measured need. E4 owns package roofline/hash-share profiling; F4 owns the 1 TB process-tree law. No duplicate follow-up was opened.
- 2026-07-19: Reactivated as the highest-fanout package/performance blocker after WS-V closure. The program's autonomous ratification authority resolves the 2026-07-12 dependency questions through `.10x/decisions/capability-rooted-package-verification.md`: pin and audit `cap-std`/`cap-fs-ext` 4.0.2, enforce one portable path grammar, anchor manifest and descendant opens to a capability, claim exact opened-handle bytes rather than an atomic tree snapshot, and keep ordinary fresh-run hash-while-write free of redundant verification.

- 2026-07-12: Resumed E3 as the sole executor. Read the owning ticket, governing package-I/O spec, E1/E2/A5 history, and the existing E3 evidence. The shared worktree was clean at inspection. Source inspection found explicit verification collecting every discovered identity path and hash into a `Vec<FileEntry>`, then constructing a package-sized `BTreeMap`, before a sequential expected-entry pass. The public `VerificationReport.checked_files` vector is a v1 return-contract cost; the discovery vector and map are redundant working-set costs owned by E3.
- 2026-07-12: Selected bounded explicit verification as the next smallest complete outcome. Exact replay read/hash fusion cannot safely reuse the current `VerifiedPackage` authority by merely skipping segment hashes: that authority is consumed by replay reconstruction and final binding as proof of full verification. Arrow IPC file reading also seeks to the footer before record blocks and skips non-block bytes, so a correct fused reader requires a distinct pending-consumption authority plus sequential low-level IPC decode; weakening the current authority or wrapping `Read + Seek` would silently misstate exact-byte coverage.
- 2026-07-12: Replaced the explicit verifier's discovered-entry vector and `BTreeMap` with streaming path reconciliation against the already-owned canonical manifest and scoped hashing workers capped at four. Contiguous manifest chunks and ordered joins preserve canonical report/failure order; symlinks/non-files retain the existing missing-file semantics. The only cardinality-proportional success allocation added by verification is now the v1 `VerificationReport.checked_files` result itself.
- 2026-07-12: The first `--locked` gate stopped before compilation because the concurrently owned CLI dependency change had not yet updated `Cargo.lock`. Verification therefore ran from an isolated `/tmp` source snapshot with its own offline lock resolution and target, leaving the shared CLI lane untouched. `cargo test -p cdf-package --lib bounded_verification_reports_parallel_results_in_canonical_order --offline` passed. The package-only suite reported 52 passed, 2 failed, and 3 ignored; both failures (`persisted_archive_default_fails_on_tamper_and_force_replaces` and `force_archive_reports_replaced_when_manifest_metadata_survives_missing_tree`) reproduce with `ops.rs` reverted to `HEAD` and are already owned by `.10x/tickets/done/2026-07-11-package-archive-force-replacement-regression.md`. `cargo clippy -p cdf-package --all-targets --all-features --offline -- -D warnings`, package formatting, and touched-file diff checks passed.
- 2026-07-12: Review rejected that first attempt. The four workers retained cardinality-proportional chunk results without admitted I/O authority; unexpected-path sorting was unbounded; traversal skipped unexpected non-files; panic conversion and canonical-order handling were incomplete. The review findings below supersede the earlier boundedness claim.
- 2026-07-12: Repaired the slice with a smaller single-threaded design. Explicit verification hashes one expected file at a time, retains no per-file success results, and moves the already-loaded canonical manifest file vector into the public report only after every check passes. Failures append directly into the one required public error string.
- 2026-07-12: Canonical unexpected reporting repeatedly streams identity entries and retains only the next lexicographic unexpected path. This intentionally pays O(n²) traversal only for invalid packages with unexpected entries. Traversal now surfaces all non-directory entries, including symlinks/FIFOs/sockets, and refuses to follow required-directory symlinks. Strict increasing manifest file order is validated before binary-search membership.
- 2026-07-12: Focused locked tests passed for canonical mixed unexpected/missing/tampered ordering, unexpected symlink rejection without following it, noncanonical manifest rejection, exact regular-file tamper, and tampered/missing state/commit preimages. Strict all-target/all-feature `cdf-package` Clippy passed after the repair. The final no-copy report move recompiled and the two bounded-verification tests passed; a chained repeat of the already-passing noncanonical test and Clippy was cancelled while waiting on another executor's shared build lock, with no observed failure. Formatting and touched-file diff checks pass. Broad verification remains with the orchestrator.
- 2026-07-12: Critical re-review found that strict sorting did not make manifest paths safe: `../outside`, absolute paths, aliases, and expected files below symlinked intermediate directories could reach stat/open/hash and disclose outside byte counts/SHA values. Repaired only that trust boundary. Every manifest file path is now normalized through the package artifact policy before discovery or expected-file I/O, must equal its canonical normalized spelling, and must be relative/component-safe under a required identity directory (or the trace file). Invalid paths suppress all expected-path filesystem work.
- 2026-07-12: Expected-file hashing now walks each contained path component with `symlink_metadata`, rejects any symlinked intermediate directory before opening the leaf, and opens only the already-validated contained leaf path. Focused locked tests passed for parent traversal, absolute path, `./` alias, intermediate-directory symlink, unexpected leaf symlink, and canonical mixed diagnostics. Escape tests assert the returned error contains neither the outside file's actual SHA-256 nor its byte-count/SHA diagnostic. Strict all-target/all-feature `cdf-package` Clippy and formatting pass. Filesystem replacement between component inspection and open remains the already-recorded TOCTOU limit; no outside path is followed in the tested stable filesystem state.
- 2026-07-12: Final review correctly rejected check-then-open because a concurrent component swap can escape after metadata inspection. Inspected the locked and supply-chain-audited graph for a safe handle-bound repair. `rustix` 1.1.4 is exempted `safe-to-deploy` and exposes safe `openat` plus `OFlags::NOFOLLOW` on non-Windows targets, but `rustix::fs` is compiled out on Windows. `windows-sys` 0.61.2 is also exempted but exposes raw `CreateFileW`/`NtCreateFile` FFI; using it requires CDF-authored unsafe, explicitly forbidden. `nix` is Unix-only. No `cap-std`, `cap-fs-ext`, or equivalent audited safe cross-platform capability is locked.
- 2026-07-12: Per the ticket fallback, reverted the entire bounded explicit-verifier source/test slice to `HEAD` rather than shipping TOCTOU-vulnerable containment. All review findings and focused observations remain in this ticket as historical research. No focused implementation checks are claimed after the revert; the source is the previously integrated baseline. Retry requires explicit dependency/platform-authority shaping, including Windows alias/ADS/device/reserved-name/trailing-dot-space/case rules and handle-relative no-follow conformance.
- 2026-07-12 shaping: `.10x/research/2026-07-12-e3-cross-platform-safe-package-open.md` found no adequate locked or standard-library capability. It recommends ratifying and auditing pinned `cap-std` 4.0.2 + `cap-fs-ext` 4.0.2, then anchoring the package before manifest I/O and opening one validated component at a time with follow disabled. Exact portable Windows-alias grammar and the boundary between race-safe contained opens versus an impossible portable whole-tree snapshot remain explicit ratification questions. `fs_at` is a lower-maturity fallback; Windows-wide verification unavailability conflicts with active package/release authority.

## Evidence

- Fresh-run non-regression: `.10x/evidence/.storage/2026-07-19-p3-e3-opened-authority-local-tlc-run.json` records a 10.296-second median for 41,169,720 TLC rows at revision `d0bfe704`, faster than both the 10.477-second established floor and the 10.517-second immediate control.
- High-cardinality boundedness: `.10x/evidence/.storage/2026-07-19-p3-e3-opened-authority-million-verify.json` records a 7.755-second median and 207,511,552-byte peak RSS for 1,000,001 identity files under a 2 GiB cgroup; the companion systemd log records successful scope completion.
- Opened-object continuity: `staged_segment_request_exposes_only_length_bound_durable_local_files` constructs the request, replaces the pathname, and reads the original bytes from the transferred handle. Static integration leaves DuckDB's scan context with `DurableLocalFile` objects and no `File::open(path)` path.
- Capability containment and tamper parity: the package suite exercises final-root and descendant symlink refusal, unexpected symlink reporting without following, portable path/alias rejection before open, exact file tamper/missing detection, archive tamper/missing/orphan/fidelity behavior, verified-artifact post-verification mutation, and authority rejection across distinct package directory objects.
- Bounded valid-path working set: explicit verification owns the streamed manifest model required by the artifact contract, one directory entry/path candidate, one file/hash state, and scalar counts. It allocates no package-sized discovered-file vector, path map/set, worker result, success clone, archive set, or error collection. Invalid packages retain only the first canonical failure.
- Local gates: `CARGO_BUILD_JOBS=12 cargo test -p cdf-package --lib --locked -j 12` — 59 passed, 0 failed, 3 ignored; `cargo clippy -p cdf-package -p cdf-package-contract --all-targets --all-features --locked -j 12 -- -D warnings` — pass; affected engine/CLI/destination test-target check — pass.
- Canonical explicit-verification output is fail-fast and deterministic: the verifier reports the first portable-order unexpected entry, then checks expected files in canonical manifest order.
- Current tamper parity: the package-only run passed the tampered identity, missing state/commit preimage, symlink/unregistered-writer, archive verification, unknown-version, and verified-segment tamper tests outside the two separately owned force-replacement failures.
- Boundedness supported: beyond the required loaded v2 manifest and compact scalar success/error value, verification retains one file hash state and at most one unexpected path. It owns no discovery vector/map, worker/chunk result, success clone, archive set, or failure collection.
- Non-file coverage: a focused Unix regression installs an unexpected broken symlink below `stats/`; verification reports it canonically and does not follow it. Static traversal routes every non-directory entry through the same check and treats expected non-regular entries as missing.
- Admission and cleanup: hashing is single-threaded because this layer has no neutral admitted I/O-concurrency authority. No worker joins, detached tasks, or panic-conversion branches remain.
- Limits: verification does not claim an atomic snapshot against a principal concurrently mutating already-opened regular-file contents. Consumer-read raw-byte hash fusion and mmap were rejected as above. Package roofline/cold-warm strategy remains E4; the 1 TB process-tree result remains F4.

## Retrospective

- Capability authority must survive the extension boundary. Verifying beneath a retained directory and then handing a destination only a pathname silently discards the proof; a generic opened-object carrier made every destination integration honest and removed a pathname lookup from DuckDB's hot path.
- The performance gate caught the right invariant: moving authority into the generic request improved the full-year median rather than charging a correctness tax. Static syscall reasoning predicted fewer namespace lookups, but the dedicated-host three-sample result—not intuition—authorized closure.
- A seek-observing hash wrapper is not proof of exact Arrow IPC file coverage: the reader fetches the footer first and does not consume every identity byte. Fusion must be designed around sequential low-level decode and an authority that cannot reach final binding until every segment has acknowledged its exact digest.
- Shared-worktree dependency edits can invalidate `--locked` without implicating the scoped crate. An isolated offline source snapshot gave reproducible package evidence without mutating or waiting on another executor's lockfile ownership.
- Existing terminal evidence had already given the archive-force failures a durable owner. Searching before opening a follow-up prevented duplicate backlog.
- A fixed concurrency cap is not admission. Without governing resource authority, single-worker hashing is the bounded choice; parallelism belongs at a caller that can supply an admitted permit.
- Canonical reporting and bounded working memory can coexist without an in-memory sort by trading time on invalid inputs: repeated minimum selection is quadratic but retains one path. Valid packages require one discovery scan because no unexpected successor exists.
- A success report needs only scalar checked counts. Reusing or cloning the loaded manifest vector as public output still makes presentation cardinality part of verification memory, so the superseded per-file report was deleted.

## Review

### Findings

- **significant — bounded-memory claim is falsified by cardinality-proportional working results.** `verify_expected_file_chunk` reserves and retains one `checked_files` vector for every manifest chunk; `verify_expected_files_bounded` first collects all of those chunks and then allocates a second `checked_files` vector with capacity equal to the complete manifest before moving the entries into it. At merge time, successful verification therefore owns the public report allocation plus worker allocations whose total capacity is package-cardinality proportional. Invalid packages additionally retain per-worker failure vectors, a merged failure vector, and the final joined error string. Discovery also retains and sorts every unexpected path in `unexpected_paths`, so an adversarial package with many extras remains unbounded even if the manifest is small. This contradicts the journal statement that the v1 report is the only cardinality-proportional success allocation and does not meet the spec's bounded verification/no package-sized working vector requirement. The focused ordering test uses a small fixture and cannot expose this; the ticket correctly records that million-file verification RSS remains absent.
- **significant — unexpected symlinks and other non-files are not detected.** `visit_identity_file_paths` descends directories and visits only entries whose `file_type().is_file()` is true; symlinks, FIFOs, sockets, and other non-files are silently skipped. The worker correctly treats an expected path replaced by a symlink/directory as missing and never follows that symlink, but an extra symlink or non-file under an identity directory produces neither an `unexpected identity file` failure nor another verification failure. Regular missing, regular extra, and exact-byte tamper behavior remain preserved. This leaves the requested symlink/non-file/extra tamper surface incomplete and is not covered by the focused test.
- **significant — concurrency is capped but is not under memory/I/O admission.** `available_parallelism().min(4)` limits thread count, but the workers acquire no memory-ledger or I/O-admission lease before simultaneously opening and hashing files. A fixed thread cap is not the governing spec's required memory/I/O admission, and no high-cardinality or constrained-budget evidence establishes that four concurrent readers are beneficial or safe for the active budget.
- **minor — scoped-worker panic conversion is incomplete on short-circuit paths.** The join iterator stops at the first worker `Err` or converted panic. Remaining scoped handles are then dropped and automatically joined by `thread::scope`; if any unconsumed worker panics, `thread::scope` itself panics instead of returning `CdfError::internal("package verification worker panicked")`. All workers are still awaited, so cleanup is structurally scoped, but the advertised panic-to-error boundary is not guaranteed. No focused panic/error-cleanup assertion exercises this branch.
- **minor — canonical ordering is conditional on an unvalidated manifest invariant.** Valid builder output is path-sorted, and ordered handle joins preserve that order. However, the verifier explicitly accepts a self-consistent manifest whose `identity.files` vector is not sorted, falls back to linear membership checks, and emits checked-file and file-failure results in that noncanonical order. The focused test proves sorted extras precede two failures from an already canonical builder manifest; it does not prove or enforce canonical report/error order for a parseable noncanonical manifest.

### Verdict

**fail** for the bounded explicit-verification slice. Exact regular-file bytes are hashed with canonical ordering for normal builder-produced manifests, and regular missing/extra/tamper semantics are preserved, but the implementation still has package-cardinality-proportional working collections, lacks memory/I/O admission, and misses unexpected symlink/non-file entries. Those are direct governing-spec requirements rather than evidence-only residuals. Replay consumer-read fusion, million-file/RSS, 1 TB replay, and buffered/pread profiling remain openly unimplemented and were not treated as regressions in this review.

### Residual Risk

No broad or package-suite verification was repeated; executor-journaled results remain authoritative within their stated limits. Review was limited to the E3 `ops.rs` diff, focused package assertions, the E1/E2/E3 evidence, and the governing spec. Exact-byte hashing was established statically through `file_entry_for_path` using `std::io::copy` over the opened regular file, but concurrent filesystem mutation between `symlink_metadata` and `File::open` remains a TOCTOU limit. Worker throughput, filesystem contention, peak RSS, cancellation behavior under large inputs, and 1 TB replay behavior remain unmeasured.

### Repair response and re-review request

The failed slice has been corrected against every finding:

- no cardinality-proportional working result or unexpected-path sort remains;
- unexpected symlinks and non-directory/non-regular entries are surfaced without traversal;
- hashing is single-threaded in the absence of admitted neutral I/O authority;
- all worker/join/panic machinery was deleted;
- strict canonical manifest file order is validated before membership lookup;
- canonical existing tamper diagnostics remain ordered and focused tests pass.

Fresh adversarial review is requested for corrected `crates/cdf-package/src/ops.rs`, `storage.rs`, and focused tests. The prior verdict remains historical and does not claim the repair passed review.

### Critical repair response and re-review request

The critical path-containment finding is repaired: normalized canonical manifest paths are validated before filesystem use, parent/absolute/root/prefix/empty/alias paths fail closed, and symlinked intermediate directories are rejected before leaf open/hash. Focused escape tests prove no outside byte-count or SHA disclosure. Fresh re-review is requested for this critical correction; all other remaining E3 scope and evidence limits stay open.

### 2026-07-12 Re-review

#### Findings

- **critical — expected manifest paths can escape the package and intermediate directory symlinks are followed.** Strict lexical ordering is now enforced, but each `FileEntry.path` is passed directly through `package_path` without `normalize_artifact_path` or an equivalent containment check. A self-consistent manifest can therefore contain a strictly sorted path such as `../outside`; `symlink_metadata` and `file_entry_for_path` will stat/open/hash that external file, and a mismatch diagnostic discloses its byte count and SHA-256. The same issue remains for a normal-looking expected path below a required or nested directory that has been replaced by a symlink: `visit_identity_entries` correctly surfaces the directory symlink as unexpected and does not traverse it, but the subsequent expected-file loop follows the intermediate symlink while resolving `stats/expected-file`, hashes the outside target, and only later returns the already-accumulated unexpected-entry error. Thus no verification authority is issued in the directory-symlink case, but the claimed refusal to follow required-directory symlinks is false and out-of-package I/O still occurs. The new focused symlink test covers only an unexpected leaf symlink and cannot falsify either path. This violates the governing path-escape and symlink safety rail at the package trust boundary.

No other prior finding reproduced:

- **resolved — cardinality-proportional verifier intermediates.** Success retains the already-required loaded manifest vector and moves it into `VerificationReport`; each expected-file hash is temporary. Unexpected discovery retains only the previous and next path, while the one final error string is the externally returned failure value. There is no discovery map/vector, worker result collection, or second success vector.
- **resolved — bounded invalid-extra ordering.** Repeated minimum selection is quadratic on invalid packages but uses bounded working state and emits unexpected entries in lexical order. Expected missing/tampered diagnostics then follow strict manifest path order, followed by segment diagnostics in manifest order.
- **resolved — unexpected leaf symlink/non-file detection.** Traversal now surfaces every non-directory entry, including symlinks, FIFOs, and sockets; an expected non-regular leaf is reported missing without being opened. Required/intermediate directory symlink traversal remains blocked only in discovery, subject to the critical expected-path issue above.
- **resolved — admission and authority honesty.** Hashing is single-threaded; the unadmitted fixed worker pool and all worker/panic machinery are gone. The implementation no longer claims parallel authority it does not possess.
- **resolved — deterministic cleanup and panic handling.** There are no scoped workers, detached tasks, or join short-circuits. Files and directory iterators are synchronous RAII values, so an early I/O error releases them deterministically.
- **resolved — canonical file ordering.** File paths must be strictly increasing, so duplicates and reordered file vectors fail before filesystem hashing. This establishes deterministic regular missing/tamper and success-report order for safe builder-produced paths, but it is not path normalization or containment validation.

#### Verdict

**fail**. The original boundedness, non-file discovery, unadmitted concurrency, worker cleanup, and lexical-order findings are repaired. However, path traversal in a self-consistent manifest and following symlinked intermediate directories permit out-of-package reads/hashing at an untrusted package boundary. This critical safety failure blocks acceptance of the repaired explicit-verification slice.

#### Residual Risk

No broad verification was rerun and no implementation was modified. This re-review inspected the updated `ops.rs`, `storage.rs`, focused tests, and journal only. Static inspection supports exact-byte hashing for contained regular files through `std::io::copy`, bounded verifier working allocations apart from required public success/error values, deterministic semantic error ordering for strict safe manifest paths, and synchronous early-error cleanup. Million-entry RSS, 1 TB replay, consumer-read hash fusion, cold/warm local-I/O selection, filesystem mutation races between metadata inspection and open, and segment-vector canonicality remain outside this repaired slice or unmeasured.

### 2026-07-12 Final focused re-review

#### Findings

- **critical — containment is still vulnerable to a component-check/open race.** `contained_expected_file` validates each pathname component with separate `symlink_metadata` calls, returns a `PathBuf`, and `file_entry_for_verified_path` subsequently re-resolves that pathname with ordinary `File::open`. A concurrently writable package can rename a checked intermediate directory and replace it with a symlink after its metadata check but before the leaf open. `File::open` then follows the replacement and hashes the outside target. A mismatch again includes the outside byte count and SHA-256; if the manifest already names the outside target's digest, verification can accept bytes reached outside the package. The same pathname-based design cannot bind the opened leaf to the directory objects that were inspected. The new intermediate-symlink test proves only a stable pre-existing symlink and cannot exercise this race. This is the prior critical trust-boundary failure narrowed to concurrent mutation, not eliminated. Closing it requires handle-relative component traversal/open with no-follow/beneath semantics appropriate to each supported platform, or an explicitly ratified immutable-package/exclusive-lock authority covering verification.
- **significant — lexical normalization does not establish canonical spelling on Windows-style filesystems.** `normalize_artifact_path(Path::new(path))` rejects parent/root/prefix components, backslash aliases on Windows, repeated separators, and `./` aliases. It does not reject filesystem aliases that remain `Component::Normal`, including NTFS alternate-data-stream spellings (`stats/file:stream`), case aliases on case-insensitive volumes, trailing-dot/space aliases, or reserved DOS names. A strictly sorted manifest can therefore name two strings that resolve to the same physical file or name an alternate stream; component checks and `File::open` use the platform interpretation. The macOS-focused tests cover parent, absolute, and dot aliases but provide no Windows/path-alias conformance. If those spellings are intentionally unsupported, the package path policy needs a platform-independent component grammar; if they are supported, canonical identity semantics require explicit ratification and cross-platform evidence.

No additional regression was found in the repaired stable-filesystem path:

- Parent traversal, absolute/root/prefix paths, empty/out-of-layout paths, and noncanonical dot/separator aliases fail before discovery or expected-file filesystem work. Because one invalid path sets `paths_are_safe = false`, the verifier does not hash any expected path and does not disclose an outside digest/byte count in these stable cases.
- A stable symlinked required or nested directory is detected component-by-component before leaf open, while leaf symlinks/non-files are treated as missing and unexpected non-files remain visible to discovery.
- Strict file-path ordering, lexical unexpected ordering, manifest-order missing/tamper diagnostics, exact regular-file hashing, the no-copy success-report move, single-threaded execution, and bounded verifier intermediates remain intact.

#### Verdict

**fail**. The repair closes stable parent/absolute/alias escapes and stable intermediate-symlink traversal, preserves bounded canonical verification, and prevents outside SHA/byte-count leakage in the focused stable cases. It does not provide race-safe containment between component validation and open, and its canonical spelling policy is incomplete on Windows-style filesystems. The critical package trust-boundary requirement therefore remains unmet.

#### Residual Risk

No broad checks were rerun and no implementation was modified. Inspection was limited to the updated validation/resolution code, focused escape/symlink/order tests, and ticket journal. Beyond the findings above, the verifier still has the general post-hash mutation window already inherent in issuing reusable verification authority for pathname-addressed files; even an atomically contained open would not by itself prove later consumers reopen the same inode/bytes. Million-entry RSS, 1 TB replay, consumer-read fusion, and local-I/O strategy evidence remain open ticket scope rather than claims of this repair.

### 2026-07-19 final closure review

#### Findings

None. The 2026-07-12 attempts and fail verdicts above are historical findings against code that was reverted. The current implementation replaces check-then-open with one-component no-follow capability traversal on every supported platform; enforces the portable component/case-alias grammar before I/O; retains no cardinality-sized verifier intermediates beyond the artifact's required manifest; and transfers an opened, length-bound file object through generic staged ingress instead of reopening a destination-specific pathname. The DuckDB scanner's per-segment mutex is used once to transfer ownership to one worker, not on the batch hot path. No fixed concurrency cap, CDF-authored unsafe code, compatibility shim, or destination identity branch was introduced.

#### Verdict

**pass**. Every reshaped E3 acceptance criterion maps to local tamper/authority tests or dedicated-host evidence. The exact revision improved the full-year TLC median and held the million-entry verifier below 211 MB cgroup peak, so closure does not trade performance for authority.

#### Residual risk

CDF does not claim an atomic snapshot against a principal concurrently mutating regular-file contents through an already-open handle; this is the explicit limit of `.10x/decisions/capability-rooted-package-verification.md`. E4 remains responsible for package write/hash/verify rooflines and local I/O profiling. F4 remains responsible for the 1 TB process-tree memory law. Neither is silently claimed by E3, and both already have active owners.
