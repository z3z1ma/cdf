Status: active
Created: 2026-07-11
Updated: 2026-07-19
Parent: .10x/tickets/2026-07-10-p3-ws-g-remote-io-overlap.md
Depends-On: .10x/tickets/done/2026-07-11-p3-g3-codec-download-decode-overlap.md, .10x/tickets/done/2026-07-11-p3-d2-duckdb-arrow-bulk.md, .10x/tickets/done/2026-07-11-p3-c4-jobs-invariance-scaling-matrix.md

# P3 G4: remote/local I/O envelope and TLC closeout

## Scope

Run recorded and live public TLC plus S3/GCS/Azure/local roofline scenarios, publish network/device overlap evidence, tune safe defaults, and make I/O-bound acceptance permanent without making ordinary CI network-dependent.

## Acceptance criteria

- Full-year TLC HTTPS-to-DuckDB meets the 1.5x composite target where environment permits and profile is I/O/destination-bound.
- S3/GCS/Azure/live cells are labeled and recorded; deterministic fixtures gate CI.
- Local sequential/range strategy reaches its measured roofline without unratified unsafe paths.
- Remote controller overhead, retries, waste, cache/spool, memory, and identity are within budgets.

## Evidence expectations

Host/network/provider reports, raw profiles/timelines, live/recorded comparison, jobs/memory/identity conformance, and adversarial weak-validator/throttle review.

## Explicit exclusions

No guarantee about third-party public endpoint uptime/bandwidth.

## Blockers

Depends on G1-G3; DuckDB bulk and deterministic scaling closeout are complete.

## Assumptions

- Record-backed: HTTP transfers may run for hours, so the transport MUST NOT impose a total request deadline. File byte-source progress deadlines may bound response establishment and body-frame idle time because healthy long transfers reset the body deadline every time bytes arrive.
- User-ratified: performance and correctness are the joint first priority; a production change in this ticket must preserve or improve the measured hot path unless it repairs incorrect behavior.

## References

- `.10x/specs/remote-local-io-overlap.md`
- `.10x/decisions/terabyte-scale-performance-envelope.md`

## Progress and notes

- 2026-07-14: The live single-object FineWeb proxy is I/O-bound after prepared decode metadata and growing-spool overlap: 16.21 seconds CDF end to end versus 14.70 seconds immediate curl (1.10x), with a 2.205 GB governed package, DuckDB receipt, and committed checkpoint. This is strong proxy evidence but does not close G4's full-year TLC, provider matrix, memory, retry/waste, or recorded-fixture gates. Evidence: `.10x/evidence/2026-07-14-p3-g2-fineweb-growing-spool-overlap.md`.
- 2026-07-16: Activated after G3 closure. The first action is to turn the catalog's currently unavailable full-year TLC cells into an opt-in, host-labeled runner and run the current release path against the public project fixture. Provider-live cells stay opt-in; deterministic controller/backpressure/identity fixtures remain the ordinary-CI authority. Tuning is permitted only after the same-host composite floor identifies a measured deficit.
- 2026-07-16: The first twelve-month live attempt produced two partitions and then made no durable progress for roughly four minutes while consuming almost no CPU (`real 246.65s`, `user 4.63s`). The same release completed a fresh three-month run over the same origin in `4.71s` for 9.6 million rows, 153 MiB of source data, a 334 MiB governed package, and a DuckDB receipt. This falsifies a general decode/segment/DuckDB collapse and isolates an unbounded stalled-HTTP-response liveness risk. The HTTP client had no connect or read-idle deadline, so a silent socket could never enter the existing typed transient retry path.
- 2026-07-17: Corrected the HTTP liveness boundary after adversarial review. The ratified default is now private to file byte-source transfer phases: 10 seconds to receive the file response, 10 seconds between delivered body frames, and no total transfer deadline. The shared reqwest client no longer carries a global read timeout, so REST and file metadata keep their prior semantics. Timeout failures retain the transport's existing sanitized `Transient` taxonomy; the deterministic slice proves bounded classification/cleanup and preserves slow-progressing transfers, while scheduler retry success remains governed by the compiled source retry path.
- 2026-07-17: Release remeasurement after the liveness correction: fresh three-month TLC completed in `real 5.40s` (`user 5.98s`, `sys 3.61s`, 9.6M rows, 51 segments), preserving the established fast path within public-endpoint noise but not improving it. Fresh full-year TLC was interrupted after `real 114.73s` (`user 3.02s`, `sys 9.42s`, max RSS about 1.0 GB) with only first-partition segment files present and only startup events in the state ledger. This fails the same-host 10.31-second G4 ceiling and keeps G4 open.
- 2026-07-16: Same-host raw controls established the current G4 floor: two-way parallel curl downloaded the 12 public TLC objects (660 MiB) in `4.34s`; DuckDB natively created the 41,169,720-row table in `2.53s`. The ratified 1.5x composite ceiling is therefore `10.31s` on this observation. The native cell omits CDF evidence/package work and is labeled as a favorable roofline rather than semantic equivalence.
- 2026-07-17: Tested a file-source scheduler hard cap as a source-owned experiment, not as an accepted product default. The experiment preserved generic engine orchestration but violated the P3 performance rule: it could degrade healthy multi-file scans while trying to protect one public-origin workload. The hidden hard cap is rejected; the file source now advertises one high-parallelism capability value and any future pressure control must be adaptive and/or explicitly configured rather than hard-set globally.
- 2026-07-17: Live remeasurement after the four-way source lane cap falsified the cap as a sufficient G4 fix. A disposable 12-file TLC project under `/private/tmp/cdf-g4-full-after-cap` deep-validated 12 partitions, then the release run was interrupted at `real 102.19s` (`user 8.56s`, `sys 11.21s`, peak RSS about 1.6 GB) after missing the 10.31s same-host ceiling. The interrupted package contained 69 data segments across only partitions `p00000000` through `p00000003` (about 478 MiB package data), while no package segment events had reached the run ledger before finalization. This proves the cap prevented all-12 over-admission but introduced wave/head-of-line behavior; the next fix must preserve bounded public-origin pressure without serializing the month set into slow waves.
- 2026-07-17: A retry of the explicit TLC numeric template (`yellow_tripdata_2024-{01..12}.parquet`) initially failed with an HTTP GET 403, but rebuilding the release CLI made the same disposable project deep-validate 12 partitions successfully. The 403 was stale-binary measurement noise, not a source-layer enumeration finding.
- 2026-07-17: Rebuilt-release full-year template run still failed the envelope: `/private/tmp/cdf-g4-template-rebuilt.ZD3czp` deep-validated 12 partitions, then `cdf run tlc.yellow` was interrupted at `real 103.09s` (`user 3.74s`, `sys 3.65s`, peak memory footprint `927466888`). The partial package contained only partitions `p00000000` and `p00000001` (32 segment files, about 220 MiB). This strengthens the conclusion that the four-way source blocking lane is the wrong default: it bounds pressure but lets slow first-wave partitions hold the frontier while later months do no work.
- 2026-07-17: Reframed the current G4 miss as a P0 regression hunt after local/remote isolation. A single local April TLC Parquet file to the Parquet destination completed in `real 0.94s`, while the same local file to DuckDB timed out at `real 45.02s` after producing only 10 segment files and no finalized package events. That falsifies HTTP/range/discovery/source-frontier as the immediate owner and isolates DuckDB staged ingress. The stall aligned with the first hidden mid-stream DuckDB Arrow appender flush threshold (~64 MiB), so the fix removes that arbitrary mid-stream flush and keeps only the final appender flush. Rebuilt release remeasurement of the same local April file to DuckDB then completed in `real 1.93s` for 3.5M rows and 18 segments with zero swaps. This repairs a throughput regression without adding a hard cap or destination-specific orchestration branch.
- 2026-07-17: The rebuilt full-year remote TLC run after the DuckDB flush repair still timed out at the 45-second guard, with only partitions `p00000000` through `p00000003` materialized (69 Arrow segments, about 478 MiB). This keeps G4 open and narrows the remaining owner to remote multi-partition frontier/admission/head-of-line behavior rather than the repaired local DuckDB staged-ingress stall.
- 2026-07-17: Removed the network from the remaining failure. A complete local 12-month TLC control set under `/private/tmp/cdf-g4-tlc-control` loaded to the Parquet destination in `real 5.90s` for 41.2M rows and 215 segments, while the same local files to DuckDB timed out at `real 45.04s` after 106 segments / about 745 MiB and no final target table. This falsifies source decode, package build, local file I/O, schema admission, and destination-independent finalization as the current P0 owner. The remaining regression is DuckDB destination bulk ingestion at full-year scale. A destination-internal temp-staging-table experiment was rejected: it still timed out at 45s, reduced progress to 69 segments, and forced about 1.2 GiB of DuckDB temp spill, so it is not retained.
- 2026-07-17: Replaced knob-poking with a deterministic git bisect using the local 12-file TLC-to-DuckDB predicate (`timeout 30s cdf run tlc.yellow`). `b1ca3879` completed in `real 21.50s`, `703e5293` completed in `real 16.52s`, and current `main` timed out before the fix. `git bisect` identified `3a5e1802 feat(runtime): execute canonical source frontier` as the first bad commit. The static root cause was the new canonical source frontier's partition-retirement path: `finish_current()` called `fill_head()`, collapsing the configured open frontier back to one active partition whenever the current partition retired. The predecessor replenished the open frontier on every partition completion. The fix changes retirement to `fill_active()` so the frontier keeps its declared `maximum_active` warm while preserving the one-prefetched-batch memory gate.
- 2026-07-17: Rebuilt release after the frontier retirement fix and reran the deterministic full-year local TLC-to-DuckDB control. `/private/tmp/cdf-g4-frontier-fix.0DMgcq` completed in `real 16.25s` (`user 24.40`, `sys 3.30`) for 41.2M rows, 215 segments, and a committed DuckDB receipt. This restores and slightly improves the good `703e5293` baseline under the same predicate. The measured remaining bottleneck is destination ingress (`15.52s`) and segment encode/persist (`5.79s`/`2.12s`), not a low-CPU scheduler stall.
- 2026-07-17: Added the user-supplied Hugging Face TLC mirror as a live E2E lane because the original CloudFront object now returns HTTP 403 to direct curl/HEAD from this host. Mirror config: `[source.tlc] root = "https://huggingface.co/datasets/Lucky239/taxi-data/resolve/main"` with allowlist `huggingface.co` and `us.aws.cdn.hf.co`. The mirror pins all 12 files successfully and removes the provider-unavailability excuse: raw parallel curl downloaded all 12 objects in `real 7.65s`, while CDF over the same mirror scales close to serial file waves (one file `3.93s`, two files `7.67s`, four files `12.62s`, twelve files timed out at `45.02s` after only January materialized). G4 therefore remains open on remote multi-partition transfer overlap/source scheduling, not raw provider bandwidth.
- 2026-07-17: Rejected an immediate-active-frontier opening experiment. Changing source-frontier initialization from `fill_head()` to `fill_active()` was tempting because large remote Parquet row groups delay first-batch frontier warmup, but measured HF four-file TLC performance regressed from `real 12.62s` to a `25s` timeout. The source was reverted. Any retained fix must overlap remote full-scan transfers without forcing an eager decode/open pattern that slows healthy paths.
- 2026-07-17: Added the Hugging Face TLC mirror to the P3 benchmark dataset catalog as an optional `remote_files.mirrors[]` entry, with its egress allowlist and notes. This does not claim envelope closure; it makes the live lab resilient to the CloudFront 403 and keeps the benchmark catalog as the authoritative place to discover alternate public TLC endpoints.
- 2026-07-17: Rejected a broader active-frontier plus lazy prepared-stream prestart experiment. The design was architecturally cleaner than the first `fill_active()` patch — a generic `SourceFrontierOpenPolicy` plus file-source-owned growing-spool prestart — but the measured local 12-file TLC-to-DuckDB control timed out at `real 30.02s` versus the `16.25s` repaired baseline. That is a direct performance regression on the established local floor, so the entire uncommitted experiment was reverted. Future G4 attempts must either be explicit knobs/off-by-default or prove local single/full-year controls before any default change.
- 2026-07-17: Made the typed Parquet statistics profile an explicit `cdf run --stats-profile` opt-in instead of default hot-path work. The evidence-bearing in-memory `ExecutionProfile` and existing manifest/stat artifacts remain default; only the extra `stats/profile.parquet` side artifact is gated. This preserves the local 12-file TLC-to-DuckDB floor after rebuild: `/private/tmp/cdf-g4-statsonly-local.p2QVSr3r` completed in `real 16.56s` (`user 21.29`, `sys 3.28`) with all 12 partitions, 215 Arrow segments, and no `stats/profile.parquet`.
- 2026-07-17: Split HTTP file transfers onto a separate reqwest client configured for file-source semantics (`http1_only`, no redirect following, larger idle pool) while REST continues to use the existing async client. Current HF 12-file mirror run still fails G4, but it no longer reproduces the "only January materialized" symptom: `/private/tmp/cdf-g4-hf-current-12.JkDjAGS8` reached partitions `p00000000` through `p00000003` (56 Arrow segments) before the `45s` guard (`real 45.03`, `user 6.26`, `sys 9.43`). This is not closure; next work must remove the remaining remote full-scan transfer/frontier serialization.
- 2026-07-17: Rejected a destination-bulk-path segmentation experiment. Wiring `PreparedBulkPath` directly into canonical package segmentation looked architecturally tempting, but DuckDB's declared 64k/16MiB appender grain is smaller than the engine's default 1M/32MiB segment grain; the patch made the local floor worse (`/private/tmp/cdf-g4-bulkpolicy-local2.o3XlqDBG` timed out at `real 30.04s`, only five partitions, 258 segments). The experiment was reverted before commit. Future segment/batch tuning must distinguish destination append batch size from canonical segment size and prove it with the local TLC floor first.
- 2026-07-17: Rejected two more generic throughput experiments before retaining any code. Disabling IPC LZ4 in the package writer did not produce a useful win and made the copied local control stall under the guard; a generic staged-ingress channel/window experiment using DuckDB's two in-flight segment capability made the local 12-file TLC-to-DuckDB floor worse (`/private/tmp/cdf-g4-staged-window-local.WUsd6l` timed out at `real 35.06`). Both changes were reverted. A clean rebuild immediately restored the local floor (`/private/tmp/cdf-g4-clean-local.3BIoQr`, `real 16.61`).
- 2026-07-17: Clean HF sampling showed the scheduler itself was not reducing top-level work (`cdf plan` reported 12 effective jobs and 2.7 GiB managed memory available). The interrupted run had only partitions `p00000000` and `p00000001` fully persisted after roughly 15 seconds, and stack sampling found workers parked rather than CPU-bound. Static inspection identified the real shape: Parquet creates row-group decode units under each file, and a growing sequential spool was advertising the upstream HTTP byte source's high useful range concurrency even though it has one sequential transfer. That let a few opened files monopolize the shared run-work permits with intra-file row-group work and starve cross-file overlap.
- 2026-07-17: Corrected the growing-spool capability truth: a sequential growing spool remains exact-range/seekable but now advertises one useful range reader because parallel row-group reads over one sequential download do not create additional network bandwidth. This preserved the established local 12-file TLC-to-DuckDB floor (`/private/tmp/cdf-g4-spoolcap-local.dNO61F`, `real 16.54`, 41.2M rows, 215 segments) and converted the HF 12-file mirror from a 45-second timeout after partial materialization into a successful run (`/private/tmp/cdf-g4-spoolcap-hf.Znl8kA`, `real 22.22`, 41.2M rows, 215 segments, committed DuckDB receipt). G4 remains open because the same-host composite target is still about `10.31s`; the next owner is package/destination hot-path cost and/or true remote transfer overlap beyond sequential-spool fairness.
- 2026-07-17: Rejected a DuckDB Arrow C-stream bridge amortization experiment. Importing all persisted batches for one segment through one C stream preserved correctness checks (`cargo test -p cdf-dest-duckdb arrow`, `cargo clippy -p cdf-dest-duckdb --all-targets`) and local performance was neutral (`/private/tmp/cdf-g4-duckbridge-local.FnBgat`, `real 16.49`), but HF live performance regressed within the only external hot-path gate (`/private/tmp/cdf-g4-duckbridge-hf.LvzT6K`, `real 22.57` versus the retained `22.22`). The patch was reverted before commit; do not reintroduce bridge batching without a clearer win and a retained external/live gate.
- 2026-07-17: Re-tested global immediate source-frontier opening after the growing-spool range-concurrency repair. The source-frontier law `preaccounted_head_materializes_before_speculative_producers_open` failed exactly as designed, proving the change would open preaccounted speculative producers before the canonical head's first retained batch proves memory admission. The patch was reverted without running live timing; any future remote predownload fix must be source-owned and must not weaken this generic memory-safety law.
- 2026-07-17: Rejected promoting DuckDB native thread or memory knobs into new defaults. A one-file April local control was neutral (`CDF_DUCKDB_THREADS=1`, `real 1.68`; `CDF_DUCKDB_THREADS=4`, `real 1.73`), but the full local 12-file floor regressed badly with `CDF_DUCKDB_THREADS=4` (`/private/tmp/cdf-duck-threads-4full.LHfFuM`, timed out at `real 35.05`, 56 segments). Raising DuckDB native memory to 3 GiB under an 8 GiB CDF memory budget also regressed (`/private/tmp/cdf-duck-mem-3gfull.3bPafY`, timed out at `real 35.04`, 16 segments). The knobs remain useful for operator experiments; G4 must not change defaults without a measured win on the retained local floor.
- 2026-07-17: Rejected a DuckDB persistence-schema precomputation experiment. The current local profile after CG1 remained healthy at `/private/tmp/cdf-profile-local.oYUVXb2W` (`real 16.40`, 41.2M rows, 215 segments) and phase telemetry isolated the hot path to destination ingress (`15.18s`) overlapped with segment encode (`5.95s`) and persist/hash (`2.47s`). An apparently harmless destination-owned patch that cached the append/merge persistence `Schema` objects instead of rebuilding them per batch passed `cargo test -p cdf-dest-duckdb arrow` and `cargo clippy -p cdf-dest-duckdb --all-targets`, but the retained local floor regressed to a timeout (`/private/tmp/cdf-duck-layout-local.N5HGgJxK`, `exit_code=124`, `real 40.03`, no JSON output). The patch was fully reverted. This strengthens the working rule: DuckDB bulk-path changes require full local TLC timing, not micro-structural intuition.
- 2026-07-19: D14/D15 closed the local package/destination wall without reviving a duplicate handoff or custom runtime. The sole stock-libduckdb canonical-segment scanner completed the controlled local full-year workload in a three-sample `10.255642670s` median at `4,014,348` rows/s, versus the prior `33.955522533s` measured-command baseline. Peak process RSS was `3,992,252,416` bytes, cgroup peak was `6,358,822,912` bytes, and pressure/OOM/spill counters remained zero. This is approximately `1.21x` the favorable `8.47s` same-host composite ceiling, so it is major progress but not G4 closure: the current stock release now needs the retained Hugging Face remote mirror and provider/controller cells before the remote I/O acceptance can be judged.
- 2026-07-17: Rejected a canonical durable-segment size experiment. Raising `CanonicalSegmentationPolicy::p3_v2` from a 32 MiB target / 64 MiB max to a 128 MiB target / 128 MiB max looked like a safe way to amortize IPC file and segment-ack overhead while leaving 64k execution microbatches unchanged. It was not safe as a default: `/private/tmp/cdf-g4-seg128-local.l2AumX` timed out under the 40-second local 12-file TLC-to-DuckDB guard (`real 40.02`, `user 0.93`, `sys 0.58`, zero JSON output, only 4 segment files). The patch was fully reverted. Any future segment-size change must be an explicit, versioned policy/knob with same-host local TLC proof; larger durable segments are not a free performance win.
- 2026-07-17: Rechecked the raw DuckDB Arrow appender path before attempting another destination edit. `arrow_appender_tlc_envelope_benchmark` in release appended 1,048,576 TLC-shaped rows at 9.84M rows/s, with a 12.06x speedup over scalar rows. This falsifies "the DuckDB Arrow appender itself is the 16s full-run floor" and reframes the remaining local G4 gap as pipeline overlap/backpressure/segment-production interaction around staged ingress, not a reason to add DuckDB-specific orchestration or default native-resource knobs.
- 2026-07-17: Reran the full local 12-file TLC-to-DuckDB control after a stale/mismatched release binary timed out at 40s. Rebuilding `target/release/cdf` from current HEAD restored the retained floor: `/private/tmp/cdf-g4-current-local-rebuilt.ISofCH` completed in `real 16.96`, with 41.2M rows, 215 segments, zero swaps, and `run_succeeded` elapsed 16.224s. Phase telemetry stayed consistent with prior observations: `segment_encode=6.10s`, `persist_hash=2.23s`, `destination_ingress=15.59s`, `package_execution=15.86s`.
- 2026-07-17: Rejected a DuckDB checkpoint-threshold default experiment. The settings probe showed DuckDB defaults `checkpoint_threshold`/`wal_autocheckpoint` to 16 MiB, so a temporary adapter patch raised `checkpoint_threshold` to 1 GiB. The local 12-file TLC gate completed, but only at `/private/tmp/cdf-g4-duck-checkpoint1g.jA8xM3` `real 16.47`, `segment_encode=5.90s`, `persist_hash=2.27s`, `destination_ingress=15.24s`, and `package_execution=15.53s`. This is at best noise against the retained 16.4–17.0s floor and is not enough to justify a hidden default with WAL/disk tradeoffs. The patch was reverted; any future DuckDB checkpoint tuning must be an explicit knob or show a repeatable same-host win.
- 2026-07-17: Investigated a suspected post-D9 local regression after one current-main release binary timed out under the local 12-file TLC-to-DuckDB guard. The suspicion was invalidated by same-host history remeasurement after clearing build/disk pressure: pre-D9 `7b20dd07` completed `/private/tmp/cdf-g4-pre-d9-local.3XN3Th` in `real 16.12`, and rebuilt current `main` `e3edbe7a` completed `/private/tmp/cdf-g4-main-local.vPnWWl` in `real 16.44`. D9's reachability records are therefore not the G4 hot-path owner. Treat stale or pressure-contended release binaries as measurement contamination and require rebuild-confirmed local TLC evidence before reverting architectural cold-path work.
- 2026-07-18: Added and measured a native DuckDB Parquet ingest plus SQL-generated row-key diagnostic on the dedicated EC2 host. The no-row-key native DuckDB reference is `4.17s`, but adding `CAST(row_number() OVER () AS UBIGINT) AS _cdf_row_key` over the same 12 local TLC files produced median `32.381451900s` and 1,271,398 rows/s, essentially the same class as the persistent Arrow appender and current CDF local baseline. This rejects the tempting "just use native DuckDB CTAS and generate provenance in SQL" shortcut: it preserves no useful performance. The next retained architecture must carry CDF row identity/provenance in a streaming/vectorized form or use another adapter-owned bulk protocol; a global window stage is not acceptable.
- 2026-07-18: Reused the persistent DuckDB Arrow-appender diagnostic with `_cdf_row_key` omitted. Median wall remained `31.294180181s`, versus `31.831857687s` with row-key enabled. This rejects Rust-side row-key-array construction as the major owner. The remaining hot path is DuckDB persistent Arrow appender/materialization itself, so production work should focus on a different adapter-owned bulk protocol or a lower-level measured DuckDB import path, not provenance-column micro-optimizations.

## Evidence

- HTTP liveness slice: `.10x/evidence/2026-07-16-p3-g4-http-liveness.md`.
  - `CARGO_BUILD_JOBS=12 cargo test -p cdf-transport-http --lib --locked -j 12`: 15 passed.
  - `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-transport-http --all-targets --locked -j 12 -- -D warnings`: passed.
  - `CARGO_BUILD_JOBS=12 cargo build -p cdf-cli --release --locked -j 12`: passed.
  - Fresh 3-month live TLC, `/private/tmp/cdf-g4-tlc3-after.nBhv2i`: `real 5.40s`, success.
  - Fresh 12-month live TLC, `/private/tmp/cdf-g4-tlc-after.PvMGvm`: interrupted at `real 114.73s`; failed G4 envelope.
- File-source hard-cap rejection:
  - `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-files execution_capabilities_share_advertised_parallelism_with_blocking_lane --locked -j 12` — passed, 1 passed. Proves the driver capability and blocking lane share the same advertised source parallelism, not a separate hidden hard cap.
  - `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-files file_source_blocking_lane_matches_advertised_parallelism --locked -j 12` — passed, 1 passed. Replaces the rejected cap-specific test.
  - `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-source-files --all-targets --locked -j 12 -- -D warnings` — passed.
  - G4 remains open for an adaptive or operator-configured pressure-control design with measured non-regression evidence; no hard-cap experiment is retained as closure evidence.
- Growing-spool useful-concurrency repair:
  - `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-files growing_spool --locked -j 12` — passed, 3 passed.
  - `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-files growing_spool_serves_prefix_and_bounded_tail_while_body_transfer_continues --locked -j 12` — passed, 1 passed, including the law that the growing spool advertises one useful range reader.
  - `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-source-files --all-targets --locked -j 12 -- -D warnings` — passed.
  - `CARGO_BUILD_JOBS=12 cargo build -p cdf-cli --release --locked -j 12` — passed.
  - Local non-regression gate: `/private/tmp/cdf-g4-spoolcap-local.dNO61F`, `target/release/cdf --project ... run tlc.yellow --progress never --color never`, `real 16.54`, `user 21.26`, `sys 3.13`, 41.2M rows, 215 segments, committed DuckDB receipt.
  - HF live improvement gate: `/private/tmp/cdf-g4-spoolcap-hf.Znl8kA`, `timeout 45s target/release/cdf --project ... run tlc.yellow --progress never --color never`, completed in `real 22.22`, `user 22.08`, `sys 11.29`, 41.2M rows, 215 segments, committed DuckDB receipt. This proves progress but not G4 closure.
- Post-cap live falsification:
  - Disposable project: `/private/tmp/cdf-g4-full-after-cap`, `glob = "yellow_tripdata_2024-*.parquet"`.
  - `target/release/cdf --project /private/tmp/cdf-g4-full-after-cap validate --deep --color never` — passed with 12 partitions, no writes.
  - `/usr/bin/time -lp target/release/cdf --project /private/tmp/cdf-g4-full-after-cap run tlc.yellow --progress never --color never` — interrupted at `real 102.19`, `user 8.56`, `sys 11.21`, peak memory footprint `1604274024`; failed the 10.31s G4 ceiling.
  - Partial package inspection found 69 segment files across first-wave partitions only: `p00000000` 16 segments / 109,465,200 bytes, `p00000001` 16 / 110,295,472, `p00000002` 19 / 130,376,014, `p00000003` 18 / 128,165,620. The SQLite run ledger contained only `run_started`, `plan_recorded`, and `package_started` because segment events are not durable until later package progress/finalization in this interrupted path.
  - Rebuilt-release template rerun: `/private/tmp/cdf-g4-template-rebuilt.ZD3czp`, `glob = "yellow_tripdata_2024-{01..12}.parquet"`, deep validate passed with 12 partitions; run interrupted at `real 103.09`, `user 3.74`, `sys 3.65`, peak memory footprint `927466888`; partial package held 32 segment files for `p00000000` and `p00000001` only.
- DuckDB staged-ingress regression isolation and repair:
  - Local April source file: `/private/tmp/cdf-g4-local-files/yellow_tripdata_2024-04.parquet`.
  - Parquet destination control: `/private/tmp/cdf-g4-local-april-parquetdest.8AeKAw`, `cdf run tlc.yellow` completed in `real 0.94`, `user 1.04`, `sys 0.35`; proves local source decode/package production is not the P0 stall owner.
  - DuckDB destination before repair: `/private/tmp/cdf-g4-local-april.LYOM0D`, `timeout 45s target/release/cdf --project ... run tlc.yellow --progress never --color never` timed out at `real 45.02`, `user 0.94`, `sys 0.21`, max RSS `534 MiB`; partial package had 10 Arrow segments and manifest status `extracting`.
  - `CARGO_BUILD_JOBS=12 cargo build -p cdf-cli --release --locked -j 12` — passed after target cleanup, rebuilding the edited DuckDB destination into the release CLI.
  - DuckDB destination after repair: `/private/tmp/cdf-g4-local-april-duckdb-rerun.UVZs8R`, `cdf run tlc.yellow` completed in `real 1.93`, `user 2.11`, `sys 0.32`, max RSS `585678848`, 3.5M rows, 18 segments, package `sha256:82444782deb65487edf3354bc6351dbfe55ef42effaa48dd2c2a5dc6e4cce49e`.
  - Full-year remote after repair: `/private/tmp/cdf-g4-full-duckdb-flushfix.mlljXv`, `timeout 45s cdf run tlc.yellow` exited `124` at `real 45.05`, `user 8.62`, `sys 10.20`, max RSS about `1.65 GiB`; partial package contained 69 Arrow segments / 478,302,306 bytes for partitions `p00000000` through `p00000003`. This fails the G4 envelope and remains the next active performance target.
  - Local full-year Parquet destination control: `/private/tmp/cdf-g4-local-full-parquetdest.Dh2IUU`, `cdf run tlc.yellow` completed in `real 5.90`, `user 12.20`, `sys 3.87`, 41.2M rows, 215 segments, package `sha256:ebff72047da350c5ddd04b5095595c62e005fc6a8dadfa9a94fa5964916b8919`.
  - Local full-year DuckDB remaining failure: `/private/tmp/cdf-g4-local-full-duckdb.KABzRa`, `timeout 45s cdf run tlc.yellow` exited `124` at `real 45.04`, `user 11.94`, `sys 2.31`; partial package contained 106 Arrow segments / 744,719,724 bytes across partitions `p00000000` through `p00000005`, and DuckDB had no final `yellow` table after rollback.
  - Rejected temp-staging experiment: `/private/tmp/cdf-g4-local-full-duckdb-stage.gC8GED`, `timeout 45s cdf run tlc.yellow` exited `124` at `real 45.05`, `user 5.46`, `sys 1.57`; progress regressed to 69 segments / 478,302,306 bytes and DuckDB created about 1.2 GiB of temp spill under `.cdf/dev.duckdb.tmp`, so the uncommitted experiment was reverted.
- Canonical source-frontier regression bisect and repair:
  - `git bisect start main b1ca3879` with predicate `CARGO_BUILD_JOBS=12 cargo build -p cdf-cli --release --locked -j 12 && timeout 30s cdf run tlc.yellow` over a fresh project copied from the 12-file local TLC fixture.
  - Known-good anchors: `b1ca3879` completed in `real 21.50s`; `703e5293` completed in `real 16.52s`.
  - Bad/current symptom: low-user-CPU timeout, proving a wait/backpressure regression rather than codec or validation compute.
  - First bad: `3a5e1802 feat(runtime): execute canonical source frontier`.
  - `CARGO_BUILD_JOBS=12 cargo test -p cdf-runtime partition_finish_replenishes_the_configured_open_frontier --locked -j 12` — passed, 1 passed. The new test fails the old retirement behavior because partition 2 is not opened after partition 0 finishes while partition 1 is already prefetched.
  - `CARGO_BUILD_JOBS=12 cargo test -p cdf-runtime --lib --locked -j 12` — passed, 84 passed, 1 ignored.
  - `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-runtime --all-targets --locked -j 12 -- -D warnings` — passed.
  - `CARGO_BUILD_JOBS=12 cargo build -p cdf-cli --release --locked -j 12` — passed.
  - Repaired local full-year TLC-to-DuckDB: `/private/tmp/cdf-g4-frontier-fix.0DMgcq`, `timeout 30s cdf run tlc.yellow` completed in `real 16.25`, `user 24.40`, `sys 3.30`, max RSS `2.46 GiB`, 41.2M rows, 215 segments, package `sha256:07c3ef5fc12f3876e9bbaca7b17056c62b0c464cf554036443e4caf586d78639`.
- Hugging Face TLC mirror live lane:
  - Original CloudFront TLC control object `https://d37ci6vzurychx.cloudfront.net/trip-data/yellow_tripdata_2024-01.parquet` returned HTTP/2 403 to direct curl/HEAD from this host on 2026-07-17; this is treated as public-endpoint availability and not as a CDF regression.
  - Mirror root: `https://huggingface.co/datasets/Lucky239/taxi-data/resolve/main`; allowlist: `huggingface.co`, `us.aws.cdn.hf.co`.
  - `/private/tmp/cdf-g4-hf-tlc.GlDqZL`, `cdf schema pin tlc.yellow` over `yellow_tripdata_2024-{01..12}.parquet` completed in `12.823s`, read `151340` discovery bytes, and produced schema hash `sha256:693685f9b70f3b1b1779aa6afae0a75e51d91af51ea760b32040299e673c0063` / discovery manifest `sha256:959be6613e89bb0497d577558f858fa4a08451f8f4d99966b93f2012addcc438`.
  - Direct raw January mirror fetch: `curl -L --fail --silent --show-error --output /dev/null --max-time 45 .../yellow_tripdata_2024-01.parquet` completed in `real 2.12`.
  - Direct raw twelve-file mirror fetch with twelve parallel curl processes completed in `real 7.65`.
  - CDF one-file mirror run `/private/tmp/cdf-g4-hf-jan.CPHZVO` completed in `real 3.93`, `user 2.11`, `sys 1.97`, max RSS `607223808`, 3.0M rows, 16 segments.
  - CDF two-file mirror run `/private/tmp/cdf-g4-hf-{01..02}.ToVsaW` completed in `real 7.67`, `user 4.20`, `sys 4.91`, max RSS `684261376`.
  - CDF four-file mirror run `/private/tmp/cdf-g4-hf-{01..04}.aBwHe0` completed in `real 12.62`, `user 8.93`, `sys 6.89`, max RSS `975667200`.
  - CDF twelve-file mirror run `/private/tmp/cdf-g4-hf-tlc.GlDqZL` was interrupted at `real 45.02`, `user 3.46`, `sys 17.51`, max RSS `1180696576`, after only partition `p00000000` materialized. This proves current CDF remote full-scan orchestration is not saturating the available provider/network lane.
  - Rejected frontier experiment: immediate active opening rebuilt into the release binary made the four-file HF run `/private/tmp/cdf-g4-hf-4-after.3IBZvl` time out at `25.02s` against the prior `12.62s` control. The experiment was reverted and is not closure evidence.
  - `CARGO_BUILD_JOBS=12 cargo test -p cdf-benchmarks embedded_p3_dataset_catalog_validates --lib --locked -j 12` — passed. Proves the committed benchmark catalog deserializes and validates with the optional HF mirror entry.
- Rejected active-frontier/lazy-prestart experiment:
  - `CARGO_BUILD_JOBS=12 cargo check -p cdf-runtime -p cdf-source-files -p cdf-engine --locked -j 12` — passed for the draft.
  - `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-files --lib --locked -j 12` — passed for the draft after fixing payload-cache behavior.
  - `CARGO_BUILD_JOBS=12 cargo test -p cdf-runtime --lib --locked -j 12` — passed for the draft.
  - `CARGO_BUILD_JOBS=12 cargo test -p cdf-engine source --lib --locked -j 12` — passed for the draft after updating the expected package hash for the new recorded execution-policy identity.
  - `CARGO_BUILD_JOBS=12 cargo build -p cdf-cli --release --locked -j 12` — passed for the draft.
  - Local non-regression control `/private/tmp/cdf-g4-policy-local.kTMa02`, after explicit `schema pin`: `timeout 30s target/release/cdf --project ... run tlc.yellow --progress never --color never` exited `124` at `real 30.02`, `user 3.76`, `sys 0.92`, max RSS `1708621824`. This failed the `16.25s` repaired baseline and caused immediate revert before any commit.
- Stats-profile opt-in and HTTP file-client split:
  - `CARGO_BUILD_JOBS=12 cargo test -p cdf-engine tier_a_resource_runs_engine_projection_filter_limit_into_package --locked -j 12` — passed, 1 passed. This runs with `EngineExecutionOptions::with_statistics_profile(true)` and proves the typed profile path still writes when explicitly enabled.
  - `CARGO_BUILD_JOBS=12 cargo test -p cdf-cli args::run_jobs_tests::run_stats_profile_is_explicit_opt_in --locked -j 12` — passed, 1 passed.
  - `CARGO_BUILD_JOBS=12 cargo test -p cdf-transport-http --lib --locked -j 12` — passed, 15 passed.
  - `CARGO_BUILD_JOBS=12 cargo check -p cdf-cli -p cdf-conformance --locked -j 12` — passed.
  - `CARGO_BUILD_JOBS=12 cargo build -p cdf-cli --bin cdf --release --locked -j 12` — passed.
  - Local non-regression floor: `/private/tmp/cdf-g4-statsonly-local.p2QVSr3r`, `timeout 30s target/release/cdf --project ... run tlc.yellow --progress never --color never` completed at `real 16.56`, `user 21.29`, `sys 3.28`, 215 Arrow segments, all 12 partitions.
  - HF mirror remaining failure: `/private/tmp/cdf-g4-hf-current-12.JkDjAGS8`, `timeout 45s target/release/cdf --project ... run tlc.yellow --progress never --color never` exited `124` at `real 45.03`, `user 6.26`, `sys 9.43`, 56 Arrow segments across partitions `p00000000` through `p00000003`.
  - Rejected bulk-path segmentation experiment: `/private/tmp/cdf-g4-bulkpolicy-local2.o3XlqDBG`, `timeout 30s target/release/cdf --project ... run tlc.yellow --progress never --color never` exited `124` at `real 30.04`, `user 8.95`, `sys 2.27`, only five partitions, 258 Arrow segments. Patch reverted.
- Rejected DuckDB native-resource default changes:
  - One-file April controls: `CDF_DUCKDB_THREADS=1` completed at `real 1.68`, `user 1.85`, `sys 0.31`, max RSS `657457152`; `CDF_DUCKDB_THREADS=4` completed at `real 1.73`, `user 1.82`, `sys 0.32`, max RSS `637009920`.
  - Full 12-file local control with `CDF_DUCKDB_THREADS=4`: `/private/tmp/cdf-duck-threads-4full.LHfFuM`, `timeout 35s cdf run tlc.yellow`, exited `124` at `real 35.05`, `user 5.24`, `sys 1.29`, max RSS `2317615104`, 56 segment files. This fails the retained `~16.5s` local floor.
  - Full 12-file local control with `--memory-budget 8GiB CDF_DUCKDB_MEMORY_LIMIT=3GiB`: `/private/tmp/cdf-duck-mem-3gfull.3bPafY`, `timeout 35s cdf run tlc.yellow`, exited `124` at `real 35.04`, `user 1.27`, `sys 0.63`, max RSS `1597882368`, 16 segment files. This fails the retained local floor and is not a default candidate.
- Rejected DuckDB persistence-schema precomputation:
  - Baseline profile after CG1: `/private/tmp/cdf-profile-local.oYUVXb2W`, `/usr/bin/time -lp timeout 40s target/release/cdf --project ... run tlc.yellow --progress never --color never --json`, completed at `real 16.40`, `user 21.05`, `sys 3.23`, zero swaps, 41.2M rows, 215 segments.
  - Phase telemetry from `cdf inspect run`: `source_read=2.98s`, `decode=0.09s`, `validation_normalization=0.15s`, `segment_encode=5.95s`, `persist_hash=2.47s`, `destination_ingress=15.18s`, `package_execution=15.47s`.
  - Draft correctness checks before timing: `CARGO_BUILD_JOBS=12 cargo test -p cdf-dest-duckdb arrow --locked -j 12` passed; `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-dest-duckdb --all-targets --locked -j 12 -- -D warnings` passed; `CARGO_BUILD_JOBS=12 cargo build -p cdf-cli --release --locked -j 12` passed.
  - Timing gate rejected the patch: `/private/tmp/cdf-duck-layout-local.N5HGgJxK`, `timeout 40s target/release/cdf --project ... run tlc.yellow --progress never --color never --json`, exited `124` at `real 40.03`, `user 4.75`, `sys 1.22`, zero JSON output. No code retained.
- Rejected canonical segment-size default change:
  - Draft changed only the v2 canonical segment logical byte target/max from 32/64 MiB to 128/128 MiB; execution microbatch bounds remained unchanged.
  - `CARGO_BUILD_JOBS=12 cargo build -p cdf-cli --bin cdf --release --locked -j 12` passed in `4m08s`.
  - Local timing gate rejected the patch: `/private/tmp/cdf-g4-seg128-local.l2AumX`, `timeout 40s target/release/cdf --project ... run tlc.yellow --progress never --color never --json`, exited via timeout at `real 40.02`, `user 0.93`, `sys 0.58`, max RSS `1489207296`, zero swaps, zero JSON output, and only four `.arrow` segment files. Patch reverted; no code retained.
- DuckDB Arrow appender sanity check:
  - `CARGO_BUILD_JOBS=12 cargo test -p cdf-dest-duckdb arrow_appender_tlc_envelope_benchmark --release --locked -j 12 -- --ignored --nocapture` — passed. It reported `rows_per_second=9835744`, `direct_rows_per_second=9816297`, `scalar_rows_per_second=815313`, and `speedup=12.06x` for 1,048,576 TLC-shaped rows. This is diagnostic evidence only; it does not close G4.
- Current local floor and rejected checkpoint-threshold tuning:
  - `CARGO_BUILD_JOBS=12 cargo build -p cdf-cli --bin cdf --release --locked -j 12` — passed before the fresh control, after a stale/mismatched release binary produced a false 40s timeout.
  - Fresh rebuilt control: `/private/tmp/cdf-g4-current-local-rebuilt.ISofCH`, `timeout 40s target/release/cdf --project ... run tlc.yellow --progress never --color never --json`, completed at `real 16.96`, `user 21.47`, `sys 3.35`, zero swaps, 41.2M rows, 215 segments, `package_execution=15.864s`, `destination_ingress=15.586s`.
  - Temporary settings probe: `CARGO_BUILD_JOBS=12 cargo test -p cdf-dest-duckdb print_duckdb_write_settings --locked -j 12 -- --ignored --nocapture` passed and printed DuckDB's default `checkpoint_threshold = 16.0 MiB` / `wal_autocheckpoint = 16.0 MiB`. The temporary integration-test probe was deleted before commit because it dragged in the broad dev graph.
  - Rejected 1 GiB checkpoint-threshold patch: `/private/tmp/cdf-g4-duck-checkpoint1g.jA8xM3`, completed at `real 16.47`, `user 21.13`, `sys 3.37`, zero swaps, 41.2M rows, 215 segments, `package_execution=15.529s`, `destination_ingress=15.239s`. Patch reverted; no code retained.
- Post-D9 false-regression check:
  - `git switch --detach 7b20dd07` then `CARGO_BUILD_JOBS=12 cargo build -p cdf-cli --bin cdf --release --locked -j 12` — passed in `4m08s`.
  - Pre-D9 timing: `/private/tmp/cdf-g4-pre-d9-local.3XN3Th`, `timeout 45s target/release/cdf run tlc.yellow --progress never --color never --unicode never`, completed at `real 16.12`, `user 21.47`, `sys 3.38`, 41.2M rows, 215 segments.
  - `git switch main` then `CARGO_BUILD_JOBS=12 cargo build -p cdf-cli --bin cdf --release --locked -j 12` — passed in `4m08s`.
  - Current-main timing: `/private/tmp/cdf-g4-main-local.vPnWWl`, `timeout 45s target/release/cdf run tlc.yellow --progress never --color never --unicode never`, completed at `real 16.44`, `user 21.75`, `sys 3.34`, 41.2M rows, 215 segments.
- Current HF mirror remeasurement and rejected DuckDB row-key/batch-shape experiments:
  - `CARGO_BUILD_JOBS=12 cargo build -p cdf-cli --bin cdf --release --locked -j 12` — passed in `4m08s` after the Arrow IPC stream-driver commit, producing the current release binary used for the live run.
  - Fresh HF mirror project `/private/tmp/cdf-g4-hf-current.8fyfuP`, `glob = "yellow_tripdata_2024-{01..12}.parquet"`, `cdf schema pin tlc.yellow` succeeded, and `timeout 45s target/release/cdf --project ... run tlc.yellow --progress never --color never --unicode never` completed at `real 21.67`, `user 22.44`, `sys 12.57`, zero swaps, 41.2M rows, 215 segments. This confirms the retained growing-spool/source-frontier work fixed the old timeout but G4 remains above the HF mirror composite target.
  - Phase telemetry from `cdf inspect run run-afbbbbbc09be8eff9ef19fe0db1886e3 --json`: `source_read=49.78s` aggregate over 36 growing-spool operations / 693 MB source bytes, `decode=31.64s` aggregate over concurrent partitions, `segment_encode=5.85s`, `persist_hash=2.10s`, `destination_ingress=15.67s`, and `package_execution=18.69s`. Because phase durations overlap and current wall is 21.67s, the remaining miss is dominated by DuckDB/package hot-path cost rather than the former serialized remote range-read pathology.
  - DuckDB sequence/default row-key experiment was rejected before code: DuckDB can fill `_cdf_row_key` from `DEFAULT nextval(...)`, but persistent sequences burn values across transaction rollback, and replacing defaults leaves stale sequence dependencies that cannot be dropped cleanly. This is not a safe default replacement for CDF's exact compact provenance allocator.
  - DuckDB appender chunk-shape knob draft was rejected and fully reverted. The draft added off-by-default `CDF_DUCKDB_APPEND_BATCH_ROWS`; local 12-file TLC with `CDF_DUCKDB_APPEND_BATCH_ROWS=65536` completed `/private/tmp/cdf-g4-duck-chunk-local.e1za16` at `real 17.12`, `user 22.27`, `sys 3.72`, which does not beat the rebuilt local floor. No code or knob was retained.
- Dedicated benchmark-host promotion boundary:
  - Future G4 promotion evidence must move to the dedicated FQ12 EC2 benchmark-host protocol recorded in `.10x/specs/performance-lab-and-envelope.md` and proven by `.10x/tickets/done/2026-07-18-p3-l6-ec2-benchmark-host.md`.
  - The laptop HF/local timings in this ticket remain valid triage and rejection evidence: they identified real timeout regressions, restored the local floor, and rejected risky defaults. They are no longer sufficient by themselves to close the TLC envelope or promote a default-changing performance patch.
- EC2 benchmark-host remeasurement:
  - Evidence record: `.10x/evidence/2026-07-18-p3-g4-ec2-tlc-envelope.md`; machine storage: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-tlc-summary.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-hf-tlc-duckdb-run.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-local-tlc-duckdb-run.json`.
  - Host class `host-class-95da083e15eebd1c`; current release revision label came from the L6 synchronized clean host path.
  - Raw HF mirror floor: 12 parallel `curl -L` downloads of the TLC mirror files completed in `real 1.22s` for 661 MiB.
  - Cold 12-file schema pin over the HF mirror completed in `real 8.68s`, read only `151340` discovery bytes, matched/selected all 12 files, and used `format_metadata` within-file coverage. The one-month scratch-workspace pin correctly failed after the glob was changed, proving source authority is not being bypassed.
  - CDF HF mirror to DuckDB completed in `real 43.39s` (`user 46.94`, `sys 5.07`, max RSS about 2.86 GiB) for 41,169,720 rows and 215 segments.
  - CDF local files to DuckDB, using the already-downloaded 12 mirror files on the same EC2 host, completed in `real 40.75s` (`user 46.56`, `sys 3.55`, max RSS about 3.02 GiB) for the same 41,169,720 rows and 215 segments.
  - Phase telemetry makes the current owner concrete: remote CDF aggregate phases include `source_read=13.77s`, `decode=10.17s`, `segment_encode=9.86s`, `persist_hash=1.64s`, `destination_ingress=33.11s`, and `package_execution=33.91s`; local CDF keeps the same destination/package cost while local decode drops to `0.23s`. Remote transfer adds only about 2.6 seconds to wall on this host, so G4 is no longer blocked by the old remote range/read overlap pathology.
  - CDF local files to filesystem Parquet destination timed out at `real 90.08s` (`user 3.67`, `sys 1.75`) after only 32 Arrow segments/two partitions were present in the partial package and about 408 MiB had been written to the destination root. This creates a separate high-priority P3 destination-staging performance blocker; it must not be conflated with the DuckDB G4 lane, but it does prove the Parquet destination path is not a clean fast package/source split on this host.
  - Current conclusion: G4 remains active. The next retained performance change must attack package/destination execution with EC2 same-host proof, not remote transport knobs. Benchmark-lab coverage also needs a native DuckDB Parquet ingest reference cell on the EC2 host so the composite ceiling is no longer approximated from ad hoc tooling.
- EC2 benchmark-host storage correction and tuned rerun:
  - L6 found the initial benchmark host was provisioned with default gp3 storage (`3,000` IOPS / `125 MiB/s`), making the first EC2 storage-sensitive numbers diagnostic rather than promotion authority. The host was tuned to gp3 `16,000` IOPS / `1,000 MiB/s`; direct durable 1 GiB write/read probes improved to `0.43s` / `0.61s`.
  - Tuned-host evidence record: `.10x/evidence/2026-07-18-p3-g4-ec2-tlc-envelope.md`; machine storage: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-tuned-tlc-summary.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-tuned-hf-tlc-duckdb-run.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-tuned-hf-tlc-pin.json`.
  - Raw HF mirror floor after tuning: 12 parallel `curl -L` downloads of the TLC mirror files completed in `real 1.48s` for 661 MiB, followed by `sync`.
  - Cold 12-file schema pin over the HF mirror completed in `real 8.63s`.
  - CDF HF mirror to DuckDB completed in `real 36.51s` (`user 46.33`, `sys 4.94`, max RSS about 2.71 GiB) for 41,169,720 rows and 215 segments. Phase telemetry reports `source_read=16.14s` with growing spool, `decode=10.97s`, `segment_encode=9.62s`, `persist_hash=1.62s`, `destination_ingress=32.35s`, and `package_execution=33.51s`.
  - The tuned local-files-to-DuckDB control did not complete and must not be counted as a throughput cell. After local schema pin completed in `0.04s`, the run was terminated at `304.55s`; the CDF process was waiting on `futex_wait_queue`, consuming about `2.7%` CPU, with about `2.0 GiB` RSS and about `345 MiB` written. This points to a fast-source/backpressure/DuckDB-ingress blocking bug that remote latency can mask.
  - Current conclusion: G4 remains active and higher-priority than lifecycle migration. The immediate owner is not more remote range-read tuning; it is the local fast-source/DuckDB blocking path plus the package/destination hot path that still dominates the successful HF run.
- EC2 scheduler-default admission repair:
  - Code under test: `5e2f0e0a2b3fb4602a5e9368939982e2bc49b2f4+dirty`, where the dirty diff joins `StagedDurableSegments` destination in-flight pressure into default job admission and leaves explicit `--jobs` as the operator overdrive knob.
  - Machine storage: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-scheduler-default-summary.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-scheduler-default-local-run.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-scheduler-default-hf-run.json`.
  - Local full-year TLC files to DuckDB now completes by default in `33.83s` (`user 46.27`, `sys 2.90`, max RSS about 2.16 GiB), 41,169,720 rows and 215 segments, replacing the previous `304.55s` low-CPU termination.
  - HF full-year TLC mirror to DuckDB completes by default in `36.98s` (`user 46.96`, `sys 4.90`, max RSS about 2.27 GiB), preserving the tuned remote envelope class.
  - Both plans report `effective_jobs.jobs = 2`, limiting factor `staged_destination_in_flight`, while source capability remains 16-way. This is a generic staged-ingress pressure join, not a destination-identity branch or a hidden hard cap; explicit job configuration remains the overdrive knob.
  - Current conclusion: the local fast-source/DuckDB blocking regression is repaired enough to retain the default scheduler change. G4 remains active because the successful local and remote walls are still dominated by destination/package execution (`destination_ingress` about 33 seconds), far beyond the TLC envelope.
- EC2 native DuckDB reference floor:
  - Added a first-class `DuckDbParquetIngest` reference workload to `cdf-p3-lab`: persistent `CREATE TABLE native_ingest AS SELECT * FROM read_parquet([...])`, fresh output database per sample, host-labeled through `run-cell`.
  - Machine storage: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-native-duckdb-ingest-observation.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-native-duckdb-ingest-run-cell.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-native-duckdb-ingest-reference.json`.
  - Native DuckDB persistent ingest median over three warm samples: `4.174575150s`, 41,169,720 rows, 693,001,713 physical input bytes, about 9.86M rows/s, peak RSS about 3.26 GiB.
  - With the tuned raw parallel HF download floor of `1.48s`, the measured raw+native composite floor is about `5.65s` and the 1.5x G4 ceiling is about `8.47s`. The current CDF HF default wall of `36.98s` is about `4.37x` that ceiling.
  - Local validation: `cargo fmt --check && CARGO_BUILD_JOBS=12 cargo test -p cdf-benchmarks --locked -j 12` — passed, including the new idempotent DuckDB ingest reference unit test, baseline coverage, and staged-destination auto-jobs invariant update.
  - Current conclusion: G4 has enough benchmark authority to stop treating the gap as measurement ambiguity. The next retained code change must materially reduce destination/package execution rather than touching remote-read constants.
- L6 measured-command baseline:
  - After `.10x/tickets/done/2026-07-18-p3-l6-ec2-benchmark-host.md` added `measure-cdf`, the prepared full-year local TLC workspace was remeasured as a standard host-labeled lab observation instead of an ad-hoc stopwatch run.
  - Machine storage: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-local-default-measured.json`.
  - Command: `CDF_BENCH_REMOTE_WORKSPACE=/home/ec2-user/cdf-bench/repo/target/cdf-benchmarks/g4-scheduler-default-20260718085558/local-workspace CDF_BENCH_SAMPLES=1 CDF_BENCH_IO_MODE=uncontrolled CDF_BENCH_TIMEOUT_MS=120000 tools/p3-ec2-benchmark-host.sh measure-cdf target/cdf-benchmarks/g4-local-default-measured.json nyc_tlc_yellow_2024 tlc_local_duckdb_default -- run tlc.yellow --json --progress never`.
  - Result: observed one-sample local full-year TLC-to-DuckDB cell, `33.955522533s`, 41,169,720 rows, peak RSS about 2.15 GiB. Extracted phase telemetry: `source_read=2.052s`, `decode=0.156s`, `validation_normalization=0.210s`, `segment_encode=9.868s`, `persist_hash=1.617s`, `destination_ingress=32.916s`, `package_execution=33.136s`, `destination_write_receipt=0.677s`.
  - Current conclusion is unchanged but now cleaner: the measured local baseline proves G4's next code owner is destination/package execution. Any default-changing patch must beat this host-labeled cell, not a laptop or manual `/usr/bin/time` transcript.
- Rejected EC2 source-admission overdrive:
  - Machine storage: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-local-jobs3-measured.json`.
  - Command: `CDF_BENCH_REMOTE_WORKSPACE=/home/ec2-user/cdf-bench/repo/target/cdf-benchmarks/g4-scheduler-default-20260718085558/local-workspace CDF_BENCH_SAMPLES=1 CDF_BENCH_IO_MODE=uncontrolled CDF_BENCH_TIMEOUT_MS=120000 tools/p3-ec2-benchmark-host.sh measure-cdf target/cdf-benchmarks/g4-local-jobs3-measured.json nyc_tlc_yellow_2024 tlc_local_duckdb_jobs3 -- run tlc.yellow --jobs 3 --json --progress never`.
  - Result: failed supervised observation with `CDF command exceeded worker timeout of 119000ms`; immediate process-tree inspection found no remaining `cdf-p3-lab` worker or release `cdf` workload process.
  - Current conclusion: raising default source jobs above the staged destination pressure join is rejected. It does not beat the 33.96-second default cell; it reintroduces the old overdrive/non-completion shape. Future work must change staged-ingress/backpressure or destination bulk strategy, not simply lift admission.
- EC2 package/DuckDB isolation and rejected append coalescing:
  - Added a lab-only package shape probe, `cdf-p3-lab package-shape <package>`, after the package replay path showed the need to separate batch-shape facts from intuition. The first debug payload-decoding draft was killed because it spent minutes CPU-bound reading every IPC payload; the retained implementation reads Arrow IPC file footers only and produced the shape observation in under one second on the EC2 host.
  - Machine storage: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-local-package-replay-duckdb-measured.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-local-package-shape.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-local-duckdb-coalesce256-measured.json`.
  - Package replay command: `CDF_BENCH_REMOTE_WORKSPACE=/home/ec2-user/cdf-bench/repo/target/cdf-benchmarks/g4-scheduler-default-20260718085558/local-workspace CDF_BENCH_SAMPLES=1 CDF_BENCH_IO_MODE=uncontrolled CDF_BENCH_TIMEOUT_MS=120000 tools/p3-ec2-benchmark-host.sh measure-cdf target/cdf-benchmarks/g4-local-package-replay-duckdb-measured.json nyc_tlc_yellow_2024 tlc_local_package_replay_duckdb -- replay package /home/ec2-user/cdf-bench/repo/target/cdf-benchmarks/g4-scheduler-default-20260718085558/local-workspace/.cdf/packages/pkg-tlc-yellow-56794-1784364958724043936 --to duckdb://.cdf/replay-package.duckdb --json --progress never --color never --unicode never`.
  - Package replay result: `45.971040657s`, peak RSS about `0.85 GiB`. This is slower than the live `33.955522533s` local run, so the current G4 owner is not only live staged-ingress coupling; artifact replay through the DuckDB staged appender is also materially too slow.
  - Package shape result: 41,169,720 rows, 215 segments, 633 Arrow IPC batches, average `65,039` rows per batch, max estimated batch rows `65,536`, and 211 multi-batch segments. This disproves the current hypothesis that the retained EC2 baseline is dominated by 1,024-row Arrow appends.
  - Rejected DuckDB append coalescing experiment: a draft destination-local `CDF_DUCKDB_APPEND_TARGET_ROWS`/256k default coalesced batches before the DuckDB Arrow appender without changing package identity or crossing source/destination boundaries. EC2 measured it at `34.879734023s`, peak RSS about `2.32 GiB`, with `destination_ingress=33.831s` and `package_execution=34.052s`; this loses to the retained `33.955522533s` / `destination_ingress=32.916s` default. The code was reverted and the EC2 release binary rebuilt back to the non-regressing state.
  - Added a lab-only package payload read probe, `cdf-p3-lab package-read <package>`, to measure IPC decode cost without DuckDB commit. Machine storage: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-local-package-read.json`.
  - Package read result: the retained 1.51 GB full-year TLC package decoded all 215 segments / 633 Arrow IPC batches / 41,169,720 rows in `16.733112449s`. This explains a large part of package replay's `45.971040657s` wall, but it does not explain live staged ingress by itself because live `LiveStagedSegmentReader` transfers retained `RecordBatch`es rather than reopening IPC files.
  - Current conclusion: do not revisit simple append-batch coalescing or source-job overdrive without new evidence. The next G4 candidate must change the DuckDB bulk strategy itself, the provenance/receipt write shape, or an independently measured appender/persistent-table bottleneck; it must beat the host-labeled `33.955522533s` local default cell before retention.
- EC2 persistent DuckDB Arrow-appender isolation:
  - Added a `DuckDbArrowAppend` reference workload to `cdf-p3-lab` rather than exposing destination-private bridge internals or creating a production benchmark API. The workload writes a persistent DuckDB database from TLC-shaped synthetic Arrow batches, with parameterized row count, batch rows, `_cdf_row_key` inclusion, and checkpointing.
  - Local validation: `cargo fmt && CARGO_BUILD_JOBS=12 cargo test -p cdf-benchmarks duckdb_arrow_append_reference_materializes_persistent_table --locked -j 12` — passed.
  - EC2 validation: `tools/p3-ec2-benchmark-host.sh sync-repo && tools/p3-ec2-benchmark-host.sh build` rebuilt `cdf-p3-lab` for the dirty diagnostic revision; release lab relink took `8m37s`, with one `rustc` doing fat-LTO/link work at 100% CPU and healthy disk. `tools/p3-ec2-benchmark-host.sh preflight` passed before measurement, proving tuned gp3 storage, synced dirty revision, matched build marker, workspace presence, release binaries, and about 217 GB free.
  - Machine storage: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-append-observation.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-append-run-cell.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-append-reference.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-append-no-checkpoint-observation.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-append-no-checkpoint-run-cell.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-append-no-checkpoint-reference.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-append-no-row-key-observation.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-append-no-row-key-run-cell.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-append-no-row-key-reference.json`.
  - Result: three warm samples over 41,169,720 synthetic TLC-shaped rows, batch rows `65,536`, `_cdf_row_key` enabled, and `CHECKPOINT` enabled produced median wall `31.831857687s`, median rows/s `1,293,349`, median logical bytes/s `187,575,470`, median physical bytes/s `35,897,935`, and peak RSS about `1.41 GiB`.
  - No-checkpoint control: the same workload without an explicit `CHECKPOINT` produced median wall `31.986515768s`, median rows/s `1,287,096`, and peak RSS about `1.41 GiB`. This falsifies explicit checkpointing as the dominant owner.
  - No-row-key control: the same workload with `_cdf_row_key` omitted and explicit `CHECKPOINT` produced median wall `31.294180181s`, median rows/s `1,315,571`, and peak RSS about `1.45 GiB`. This falsifies Rust-side row-key array construction and a single extra UInt64 column as the dominant owner.
  - Current conclusion: this accounts for almost all of the retained `33.955522533s` CDF local default cell. The older in-memory 1M-row Arrow-appender microbench remains true within its limits but is not a representative full-scale persistent DuckDB proxy. The next G4 production candidate must change the DuckDB destination bulk strategy itself or prove an adapter-owned persistent materialization path that beats both the `31.831857687s`/`31.294180181s` appender diagnostics and the `33.955522533s` CDF local baseline; more source/read/concurrency knob work is rejected by current evidence.
- EC2 DuckDB Parquet-staged ingest diagnostic:
  - Added a `DuckDbParquetStagedIngest` reference workload to `cdf-p3-lab`: synthetic TLC-shaped Arrow batches are written to a Parquet handoff with `_cdf_row_key`, then DuckDB materializes a persistent table with native `read_parquet`. This tests a possible destination-owned bulk handoff shape without touching production.
  - Local validation: `cargo fmt && CARGO_BUILD_JOBS=12 cargo test -p cdf-benchmarks duckdb_parquet_staged_ingest_reference_materializes_persistent_table --locked -j 12` — passed.
  - EC2 validation: `tools/p3-ec2-benchmark-host.sh sync-repo && tools/p3-ec2-benchmark-host.sh build` rebuilt `cdf-p3-lab` for the dirty diagnostic revision; release lab relink took `8m35s`. `tools/p3-ec2-benchmark-host.sh preflight` passed before measurement, proving tuned gp3 storage, matched revision/build marker, workspace presence, release binaries, and about 215 GB free.
  - Machine storage: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-parquet-staged-ingest-observation.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-parquet-staged-ingest-run-cell.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-parquet-staged-ingest-reference.json`.
  - Result: three warm samples over 41,169,720 synthetic TLC-shaped rows, batch rows `65,536`, Parquet row-group target `65,536` rows / `64 MiB`, `_cdf_row_key` enabled, and no explicit DuckDB checkpoint produced median wall `20.615132934s`, median rows/s `1,997,063`, median logical bytes/s `289,635,564`, median physical bytes/s `342,794,357`, and peak RSS about `7.22 GiB`.
  - Current conclusion: the shape is materially better than persistent Arrow append (`31.83s`) but still far above the `8.47s` G4 ceiling and too memory-heavy for a default. It is a credible direction only if production can make the handoff ledger-accounted, bounded, and substantially faster; the measured result does not authorize retaining a naive Parquet-handoff default.
- EC2 DuckDB Parquet-staged ingest tuning:
  - After D10 cleanup and the rejected `CDF_DUCKDB_THREADS=16` appender probe, reran the lab-only Parquet handoff diagnostic at clean revision `48dd7d6d3ef265064b382624e18543046346ac95` and current host class `host-class-649c6f28be3544c8`.
  - Machine storage: observations, run-cells, and reference requests under `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-parquet-staged-rg{32m,128m,256m,256m-median3,512m,1g}-*.json`.
  - Single-sample results: `32MiB/32k` row groups measured `14.553145394s` and `5.25 GiB` RSS; `128MiB/131k` measured `10.484517844s` and `2.77 GiB`; `256MiB/262k` measured `10.502437148s` and `2.74 GiB`; `512MiB/524k` measured `10.761288241s` and `3.61 GiB`; `1GiB/1,048k` measured `11.218852136s` and `3.55 GiB`.
  - Confirmation: `256MiB/262k`, median-of-three uncontrolled samples, measured `10.472399505s`, MAD `12.644792ms`, median `3,931,259` rows/s, and peak RSS `2.757 GiB`.
  - Current conclusion: the earlier 64 MiB handoff rejection was a bad policy point, not a bad architecture. A DuckDB-owned Parquet handoff staged-ingress path is the fallback production candidate, provided it is ledger-accounted, knobbed, schema/disposition eligible, and promoted only after full CDF EC2 local/HF TLC evidence beats the current `~34s` appender baseline. D11 first falsifies the newer Arrow IPC/nanoarrow `read_arrow(...)` direction, because a working IPC handoff could avoid both appender and Parquet encode/decode overhead.
- EC2 DuckDB Arrow IPC handoff falsification:
  - Added lab-only DuckDB `read_arrow(...)` workloads to test generated Arrow IPC handoff files and an existing CDF package segment before changing any product default or artifact format.
  - Machine storage: `.10x/evidence/.storage/2026-07-18-p3-d11-arrow-ipc-small-none-observation.json`, `.10x/evidence/.storage/2026-07-18-p3-d11-arrow-ipc-small-lz4-observation.json`, `.10x/evidence/.storage/2026-07-18-p3-d11-arrow-ipc-current-segment-observation.json`, `.10x/evidence/.storage/2026-07-18-p3-d11-arrow-ipc-full-lz4-observation.json`, and `.10x/evidence/.storage/2026-07-18-p3-d11-arrow-ipc-full-none-median3-observation.json`.
  - Result: the pinned DuckDB crate can install and load community `nanoarrow`, and generated uncompressed Arrow IPC works through `read_arrow(...)`. Generated LZ4 IPC and a current CDF package segment both fail with `Compression type with value 1 not supported by this build of nanoarrow`, so direct package-segment ingestion and LZ4 IPC handoff are killed for this build.
  - Full-year generated uncompressed Arrow IPC handoff with `_cdf_row_key`, `65,536` batch rows, and `1,048,576` rows per handoff file materialized 41,169,720 rows in median `9.308415940s`, MAD `65.124961ms`, `4,422,849` rows/s, peak RSS `3.707338752 GiB`, and child-cgroup peak `9.272602624 GiB`.
  - D11 conclusion at closure: retain uncompressed Arrow IPC only as a product candidate for D12, with Parquet handoff as fallback; appender/source/scheduler knob work remained rejected by the then-current evidence.
- D12 Arrow IPC handoff product no-go:
  - D12 implemented an opt-in DuckDB-owned prototype and then removed it before commit after EC2 full-CDF evidence failed the ticket's retention threshold.
  - Machine storage: `.10x/evidence/.storage/2026-07-18-p3-d12-ec2-local-arrow-ipc-handoff-measured.json`, `.10x/evidence/.storage/2026-07-18-p3-d12-ec2-local-arrow-ipc-handoff-optin-measured.json`, `.10x/evidence/.storage/2026-07-18-p3-d12-ec2-local-arrow-ipc-handoff-ctas-optin-measured.json`, `.10x/evidence/.storage/2026-07-18-p3-d12-ec2-local-arrow-ipc-handoff-ctas-rowkey-only-optin-measured.json`, and `.10x/evidence/.storage/2026-07-18-p3-d12-ec2-local-arrow-ipc-handoff-ctas-rowkey-only-threads16-measured.json`.
  - Results: accidental default-control appender run measured `34.014931097s`; opt-in `INSERT INTO ... SELECT read_arrow(...)` measured `38.624126722s`; opt-in CTAS with full not-null restoration measured `37.274033174s`; opt-in CTAS with row-key-only not-null restoration measured `37.204744317s`; opt-in CTAS with `CDF_DUCKDB_THREADS=16` and `CDF_DUCKDB_MEMORY_LIMIT=12GiB` failed at the `179s` timeout.
  - Current conclusion: DuckDB Arrow IPC handoff is cancelled for the current pinned DuckDB/nanoarrow product path despite the generated-lab win. D13 subsequently cancelled the tuned Parquet handoff fallback too: full-CDF EC2 local TLC measured `37.546s` for the opt-in CTAS variant and `38.828s` for the planned-table + `INSERT ... read_parquet(...)` variant, both worse than the `~34s` appender baseline. G4 should stop retaining handoff variants that duplicate package bytes unless a full product benchmark proves they beat the appender; the next materialization strategy needs to attack the duplicate package→destination persistence architecture or use a direct DuckDB primitive with product semantics measured before implementation.
- D14 nanoarrow `0.8.0` LZ4 correction and direct canonical-segment result:
  - D11 measured the installed community extension correctly but generalized from an obsolete embedded nanoarrow revision. Apache nanoarrow `0.8.0` includes LZ4 IPC decompression; D14 built the same DuckDB extension revision against exact nanoarrow commit `a579fbf5d192e85b6249935e117de7d02a6dc4e9`, archive `sha256:ed186f0b8151c323fd41a1b7cfa830abad0ac84e1657cd597da12d98fa9a4be1`, with `NANOARROW_IPC_WITH_LZ4=ON`.
  - The corrected extension reports nanoarrow `0.8.0`, reads generated LZ4 IPC, and directly reads current canonical CDF LZ4 segments. This is not D12's duplicate uncompressed handoff and does not mutate package identity.
  - Three warm EC2 samples materializing all 215 canonical full-year TLC segments / 41,169,720 rows measured median `4.558788174s`, MAD `113.712491ms`, `9,030,847` rows/s, `2.289 GiB` peak process RSS, and `4.828 GiB` peak cgroup memory without pressure, OOM, or spill events. Raw observation: `.10x/evidence/.storage/2026-07-18-p3-d14-ec2-current-package-lz4-nanoarrow-080-observation.json`.
  - Current conclusion: D14's direct canonical-segment path is the highest-leverage retained DuckDB branch. It must now preserve that floor in a destination-owned full-CDF implementation with provenance, receipts, and checkpoint semantics; no generic runtime branch or duplicate handoff is authorized.
- EC2 native DuckDB row-key diagnostic:
  - Added a `DuckDbParquetIngestWithRowKey` reference workload to `cdf-p3-lab`: DuckDB materializes a persistent table with native `read_parquet` and SQL-generated `_cdf_row_key` through `CAST(row_number() OVER () AS UBIGINT)`.
  - Local validation: `cargo fmt && CARGO_BUILD_JOBS=12 cargo test -p cdf-benchmarks duckdb_parquet_ingest_with_row_key_reference_materializes_table --locked -j 12` — passed.
  - Local broader validation: `cargo fmt --check && CARGO_BUILD_JOBS=12 cargo test -p cdf-benchmarks --locked -j 12` — passed, 17 unit tests and 24 integration/policy tests passed, 1 live PostgreSQL test ignored.
  - Source-only secret validation: copied `git ls-files --cached --others --exclude-standard` into a temporary source tree and ran `gitleaks detect --no-git --source <temp> --no-banner --redact` — passed with no leaks. A raw whole-working-tree scan was intentionally not used as authority because it traverses ignored `target/` and reports generated build/key artifacts outside the tracked-source CI shape.
  - EC2 validation: `tools/p3-ec2-benchmark-host.sh sync-repo && tools/p3-ec2-benchmark-host.sh build` rebuilt `cdf-p3-lab` for dirty revision `05ae2b87011d799708f1324dad198363cc2d43e1+dirty`; release lab relink took `8m38s`. `tools/p3-ec2-benchmark-host.sh preflight` passed before measurement with tuned gp3 `16000` IOPS / `1000` MiB/s, matched dirty revision/build marker, host class `host-class-95da083e15eebd1c`, workspace presence, and `207942602752` free bytes.
  - Machine storage: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-native-duckdb-row-key-observation.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-native-duckdb-row-key-run-cell.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-native-duckdb-row-key-reference.json`.
  - Result: three warm samples over the same 12 local TLC Parquet files and 41,169,720 rows produced median wall `32.381451900s`, median rows/s `1,271,398`, median logical/physical bytes/s `21,401,193`, and peak RSS about `1.56 GiB`.
  - Current conclusion: SQL-generated row-key provenance collapses DuckDB's native no-row-key ingest floor (`4.17s`) back into the Arrow-appender/CDF baseline class (`31.83s`/`33.96s`). This is not a production accelerator. Any retained DuckDB native path must preserve CDF identity without a global window operation.
- DuckDB raw Arrow data-chunk diagnostic:
  - Added a lab-only `DuckDbArrowDataChunkAppend` reference workload to `cdf-p3-lab`. The workload bypasses the Rust binding's `append_record_batch` helper and calls DuckDB's C Arrow conversion APIs directly: `duckdb_schema_from_arrow`, `duckdb_data_chunk_from_arrow`, and `duckdb_append_data_chunk`. This is not a production path; it tests whether the persistent Arrow-appender floor is caused by the binding wrapper's per-vector slicing or by DuckDB persistence itself.
  - Local validation: `cargo fmt && CARGO_BUILD_JOBS=12 cargo test -p cdf-benchmarks duckdb_arrow_data_chunk_append_reference_materializes_persistent_table --locked -j 12` — passed. The test materialized 2,048 rows into a persistent DuckDB database and verified `_cdf_row_key` min/max continuity.
  - Local broader validation before commit: `cargo fmt --check && CARGO_BUILD_JOBS=12 cargo test -p cdf-benchmarks --locked -j 12 && git diff --check` — passed: 18 unit tests, 24 integration/policy tests, and one intentionally ignored live PostgreSQL cell. Source-shaped secret validation copied tracked plus unignored source files into ignored `target/` storage and ran `gitleaks detect --no-git --source <copy> --no-banner --redact` — passed with no leaks.
  - EC2 validation: `tools/p3-ec2-benchmark-host.sh sync-repo && tools/p3-ec2-benchmark-host.sh build && tools/p3-ec2-benchmark-host.sh preflight` passed at clean revision/build `3ca88ed8be07a49bbf980dbcd3569de8be87d124`; `cdf` was a cache hit and `cdf-p3-lab` rebuilt in `8m35s`. Preflight proved tuned gp3 `16000` IOPS / `1000` MiB/s, host class `host-class-95da083e15eebd1c`, workspace presence, and `205647618048` free bytes.
  - Machine storage: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-data-chunk-observation.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-data-chunk-run-cell.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-data-chunk-reference.json`.
  - Result: three uncontrolled samples over 41,169,720 synthetic TLC-shaped rows, batch rows `65,536`, `_cdf_row_key` enabled, and `CHECKPOINT` enabled produced median wall `31.998755593s`, median rows/s `1,286,603`, median logical bytes/s `186,597,121`, median physical bytes/s `35,710,700`, and peak RSS about `1.45 GiB`.
  - Current conclusion: bypassing the Rust binding's `append_record_batch` helper does not improve the persistent DuckDB floor; it is effectively tied with the existing persistent Arrow-appender diagnostic (`31.831857687s`) and does not beat the retained CDF local baseline (`33.955522533s`) enough to justify any production destination change. Do not productionize raw DuckDB data-chunk append without new evidence. The next G4 owner is either a different DuckDB materialization strategy that avoids global row-number/provenance cost, or reducing CDF package/destination work outside DuckDB's persistent append floor.
- DuckDB raw Arrow stream-scan diagnostic:
  - Added a lab-only `DuckDbArrowStreamScanIngest` reference workload to `cdf-p3-lab`. The workload exposes synthetic TLC-shaped Arrow batches as an Arrow C stream and uses DuckDB's deprecated `duckdb_arrow_scan` C API to materialize a persistent table through `CREATE TABLE AS SELECT * FROM cdf_arrow_stream`. This is explicitly benchmark-lab-only; it tests whether DuckDB's scan/materialize path and hidden `rowid` can beat appender-shaped persistence before any production provenance decision is considered.
  - Local validation: `cargo fmt && CARGO_BUILD_JOBS=12 cargo test -p cdf-benchmarks duckdb_arrow_stream_scan_reference_materializes_persistent_table_with_rowids --locked -j 12` — passed. The test materialized 2,048 rows into a persistent DuckDB database without `_cdf_row_key` and verified `rowid` count/distinct/min/max as `(2048, 2048, 0, 2047)`.
  - Local broader validation before commit: `cargo fmt --check && CARGO_BUILD_JOBS=12 cargo test -p cdf-benchmarks --locked -j 12 && git diff --check` — passed: 19 unit tests, 24 integration/policy tests, and one intentionally ignored live PostgreSQL cell. Source-shaped secret validation copied tracked plus unignored source files into ignored `target/` storage and ran `gitleaks detect --no-git --source <copy> --no-banner --redact` — passed with no leaks.
  - EC2 validation: `tools/p3-ec2-benchmark-host.sh sync-repo && tools/p3-ec2-benchmark-host.sh build && tools/p3-ec2-benchmark-host.sh preflight` passed at clean revision/build `11061e087fabb6dbf73248fc1ba6540ffecd8b4d`; `cdf` was a cache hit and `cdf-p3-lab` rebuilt in `8m38s`. Preflight proved tuned gp3 `16000` IOPS / `1000` MiB/s, host class `host-class-95da083e15eebd1c`, workspace presence, and `204504100864` free bytes.
  - Machine storage: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-stream-scan-observation.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-stream-scan-run-cell.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-stream-scan-reference.json`.
  - Result: three uncontrolled samples over 41,169,720 synthetic TLC-shaped rows, batch rows `65,536`, no `_cdf_row_key`, `CHECKPOINT` enabled, and hidden `rowid` verification enabled produced median wall `7.900311369s`, median rows/s `5,211,151`, median logical bytes/s `714,080,403`, median physical bytes/s `133,225,185`, and peak RSS about `5.88 GiB`.
  - Machine storage for the current-provenance control: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-stream-scan-row-key-observation.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-stream-scan-row-key-run-cell.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-stream-scan-row-key-reference.json`.
  - Current-provenance control: three uncontrolled samples over 41,169,720 synthetic TLC-shaped rows, batch rows `65,536`, existing `_cdf_row_key` payload column enabled, and `CHECKPOINT` enabled produced median wall `6.756290925s`, median rows/s `6,093,538`, median logical bytes/s `883,750,527`, median physical bytes/s `154,348,167`, and peak RSS about `6.25 GiB`.
  - Local validation after adding optional resource-control fields to the stream-scan reference: `cargo fmt && cargo fmt --check && CARGO_BUILD_JOBS=12 cargo test -p cdf-benchmarks duckdb_arrow_stream_scan_reference_materializes_persistent_table_with_rowids --locked -j 12 && CARGO_BUILD_JOBS=12 cargo test -p cdf-benchmarks --locked -j 12 && git diff --check` — passed: targeted stream-scan test, 19 unit tests, 24 integration/policy tests, and one intentionally ignored live PostgreSQL cell.
  - EC2 validation for the resource-control fields: `tools/p3-ec2-benchmark-host.sh sync-repo && tools/p3-ec2-benchmark-host.sh build && tools/p3-ec2-benchmark-host.sh preflight` passed at clean revision/build `1525e5baf22c40b51cccdeeba0346f699b21e22d`; `cdf` was a cache hit and `cdf-p3-lab` rebuilt in `8m39s`. Preflight proved tuned gp3 `16000` IOPS / `1000` MiB/s, host class `host-class-95da083e15eebd1c`, workspace presence, and `202419892224` free bytes.
  - Machine storage for bounded-resource controls: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-stream-scan-row-key-bounded-observation.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-stream-scan-row-key-bounded-run-cell.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-stream-scan-row-key-bounded-reference.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-stream-scan-row-key-threads8-observation.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-stream-scan-row-key-threads8-run-cell.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-stream-scan-row-key-threads8-reference.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-stream-scan-row-key-threads16-observation.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-stream-scan-row-key-threads16-run-cell.json`, `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-arrow-stream-scan-row-key-threads16-reference.json`.
  - Bounded single-thread result: with `_cdf_row_key`, `threads=1`, DuckDB `memory_limit=1GiB`, and `max_temp_directory_size=1GiB`, three samples produced median wall `29.027263185s`, median rows/s `1,418,312`, median logical bytes/s `205,698,885`, and peak RSS about `1.17 GiB`. This proves the scan API alone is not the speedup; DuckDB internal parallelism is decisive.
  - Bounded parallel result: with `_cdf_row_key`, `threads=8`, and the same `1GiB` memory/temp limits, three samples produced median wall `5.866582939s`, median rows/s `7,017,666`, median logical bytes/s `1,017,777,424`, and peak RSS about `1.48 GiB`.
  - Host-saturation result: with `_cdf_row_key`, `threads=16`, and the same `1GiB` memory/temp limits, three samples produced median wall `5.111650191s`, median rows/s `8,054,095`, median logical bytes/s `1,168,091,604`, and peak RSS about `1.60 GiB`.
  - Current conclusion: the production-shaped signal is now strong. CDF can preserve the current `_cdf_row_key` provenance column and still beat the appender baseline by about `6.64x` if DuckDB materializes via a parallel scanner-style path under bounded memory. The next retained production work should be a DuckDB-crate-only staged-ingress implementation that exposes the existing durable segment stream as a bounded Arrow C stream to DuckDB, keeps the runtime generic, records a destination bulk-path version bump, and measures the full CDF local/HF TLC cells on EC2 before replacing the appender default.
- D10 product stream-scan no-go and current baseline:
  - D10 attempted the DuckDB-crate-only staged stream-scan implementation suggested by the synthetic reference, then cancelled it as a measured no-go. The first product candidate timed out at the EC2 worker's `119000ms` full-year guard, the CTAS variant also timed out at `119000ms`, and a one-partition smoke timed out at `59000ms`. Runtime capabilities now advertise only the measured appender path again; `.10x/tickets/cancelled/2026-07-18-p3-d10-duckdb-stream-scan-staged-ingress.md` owns that failed attempt's review and retrospective.
  - Current-code EC2 baseline after disabling stream-scan exposure: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-current-local-default.json`. Command: `CDF_BENCH_REMOTE_WORKSPACE=/home/ec2-user/cdf-bench/repo/target/cdf-benchmarks/g4-scheduler-default-20260718085558/local-workspace CDF_BENCH_SAMPLES=1 CDF_BENCH_IO_MODE=uncontrolled CDF_BENCH_TIMEOUT_MS=120000 tools/p3-ec2-benchmark-host.sh measure-cdf target/cdf-benchmarks/g4-current-default-20260718172732.json nyc_tlc_yellow_2024 tlc_local_duckdb_current -- run tlc.yellow --json --progress never --color never --unicode never`.
  - Result: observed one-sample local full-year TLC-to-DuckDB cell, `34.144247813s`, 41,169,720 rows, peak RSS `2.404089856 GiB`, revision/build authority `6b9c9c7ad49996fb1d0c407fcab9a45a813f51aa` on host class `host-class-95da083e15eebd1c`. Extracted phase telemetry remains stable: `source_read=2.164s`, `decode=0.152s`, `validation_normalization=0.211s`, `segment_encode=9.802s`, `persist_hash=1.609s`, `destination_ingress=33.149s`, `package_execution=33.344s`, and `destination_write_receipt=0.655s`.
  - Current conclusion: the stream-scan synthetic signal was real but the attempted product adapter failed the actual staged-ingress contract. G4's next retained code change must not revisit D10's shape or source/remote knobs. The active owner remains DuckDB/package materialization itself: either a different destination-owned bulk materialization strategy that beats the `34.14s` current floor on the EC2 host, or a measured reduction in CDF package/segment encode and destination handoff cost that does not weaken package identity, row provenance, or generic runtime boundaries.
- D10 no-legacy cleanup:
  - Removed the disabled DuckDB product stream-scan branch and raw FFI shim from `cdf-dest-duckdb` after D10's cancellation. This deleted the unreachable `arrow_stream_scan` path id branch, `StagedArrowStream` adapter, deprecated `duckdb_arrow_scan` registration wrapper, and raw mirror helper duplicates; runtime capabilities remain appender-only and the benchmark-lab stream-scan references remain lab-only.
  - Validation: `cargo fmt --all && cargo fmt --check && CARGO_BUILD_JOBS=12 cargo test -p cdf-dest-duckdb --locked -j 12` passed after the deletion.
  - Current conclusion: this is architecture cleanup, not a claimed speedup. It lowers product-path surface area and prevents future work from accidentally reviving the measured-regressing D10 shape, while G4 still owns the unresolved `~34s` DuckDB/package materialization wall.
- Rejected DuckDB row-key default/sequence candidate:
  - Candidate: for append/replace staged ingress only, install a transaction-local persistent DuckDB sequence/default for `_cdf_row_key`, append only user Arrow columns through the existing appender, then drop the default and sequence before final commit. This was destination-owned and preserved the existing segment row-key range evidence, but it changed the hot path default and therefore required EC2 proof before retention.
  - Machine storage: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-rowkey-default-local.json`.
  - Validation before measurement: `cargo fmt --all && CARGO_BUILD_JOBS=12 cargo test -p cdf-dest-duckdb --locked -j 12` passed locally; `tools/p3-ec2-benchmark-host.sh sync-repo && tools/p3-ec2-benchmark-host.sh build-measure && tools/p3-ec2-benchmark-host.sh preflight-measure` passed on the tuned EC2 host at dirty revision `a1564d411d3a76e903b3a6f8e5e1df0799bc5071+dirty`.
  - Command: `CDF_BENCH_REMOTE_WORKSPACE=/home/ec2-user/cdf-bench/repo/target/cdf-benchmarks/g4-scheduler-default-20260718085558/local-workspace CDF_BENCH_SAMPLES=1 CDF_BENCH_IO_MODE=uncontrolled CDF_BENCH_TIMEOUT_MS=120000 CDF_BENCH_EXPECTED_ROWS=41169720 tools/p3-ec2-benchmark-host.sh measure-cdf target/cdf-benchmarks/g4-duckdb-rowkey-default-local-20260718111231.json nyc_tlc_yellow_2024 tlc_local_duckdb_rowkey_default_candidate -- run tlc.yellow --json --progress never --color never --unicode never`.
  - Result: one-sample EC2 local full-year TLC-to-DuckDB cell regressed to `35.402097853s`, 41,169,720 rows, peak RSS `2.325630976 GiB`, `destination_ingress=34.403782258s`, and `package_execution=34.605728502s`. This loses to the retained `34.14s` current floor and the earlier `33.96s` measured-command baseline.
  - Current conclusion: DuckDB default/sequence row-key generation is rejected as a production hot-path change. The code was reverted; the evidence remains to prevent retreading this candidate without materially new DuckDB/version evidence.
- Rejected DuckDB internal-thread default increase:
  - Candidate: keep the existing appender product path but run the same full-year local TLC cell with `CDF_DUCKDB_THREADS=16` through the already-supported env knob, testing whether the default `1` internal DuckDB thread is the performance limiter.
  - Machine storage: `.10x/evidence/.storage/2026-07-18-p3-g4-ec2-duckdb-threads16-local.json`.
  - Command: `CDF_BENCH_REMOTE_WORKSPACE=/home/ec2-user/cdf-bench/repo/target/cdf-benchmarks/g4-scheduler-default-20260718085558/local-workspace CDF_BENCH_SAMPLES=1 CDF_BENCH_IO_MODE=uncontrolled CDF_BENCH_TIMEOUT_MS=120000 CDF_BENCH_EXPECTED_ROWS=41169720 CDF_BENCH_MEASURE_ENV_JSON='{"CDF_DUCKDB_THREADS":"16"}' tools/p3-ec2-benchmark-host.sh measure-cdf target/cdf-benchmarks/g4-duckdb-threads16-local-20260718200436.json nyc_tlc_yellow_2024 tlc_local_duckdb_threads16 -- run tlc.yellow --json --progress never --color never --unicode never`.
  - Result: failed; the isolated benchmark child exceeded the `119000ms` worker timeout before producing a sample at clean revision `1c1cb8bb6e7bc475ccc0ecaa9bc65831277a1439`.
  - Current conclusion: raising DuckDB appender internal threads is rejected as a default change and should remain an explicit operator knob only. The materialization owner is not the one-thread setting; it is the current appender-shaped product path itself.
- Cgroup-enforced current local baseline:
  - Machine storage: `.10x/evidence/.storage/2026-07-18-p3-f1-ec2-cgroup-local-tlc-run.json`, `.10x/evidence/.storage/2026-07-18-p3-f1-ec2-cgroup-local-tlc-run.systemd.log`.
  - Command: `CDF_BENCH_SYSTEMD_MEMORY_MAX=6G CDF_BENCH_REMOTE_WORKSPACE=/home/ec2-user/cdf-bench/repo/target/cdf-benchmarks/g4-scheduler-default-20260718085558/local-workspace CDF_BENCH_SAMPLES=1 CDF_BENCH_TIMEOUT_MS=180000 CDF_BENCH_IO_MODE=uncontrolled CDF_BENCH_EXPECTED_ROWS=41169720 tools/p3-ec2-benchmark-host.sh measure-cdf target/cdf-benchmarks/f1-cgroup-local-tlc-run.json nyc_tlc_yellow_2024 cgroup_local_tlc_duckdb_run -- run tlc.yellow --json --progress never --color never --unicode never`.
  - Result: one-sample EC2 local full-year TLC-to-DuckDB cell at clean revision `3d9b106cd39c010f6d1fc7870dc6dd50ec3e47ac` completed in `34.017965399s`, 41,169,720 rows, systemd-scoped host class `host-class-349948f96d0a7b90`, child peak RSS `2.321 GiB`, cgroup peak `4.608 GiB` under `MemoryMax=6G`, zero cgroup OOM counters, `destination_ingress=33.211241285s`, and `package_execution=33.433597715s`.
  - Current conclusion: enforced memory does not change the G4 owner. The current code still fits the 6 GiB envelope on this host, and the remaining performance wall remains DuckDB/package materialization rather than source, decode, or remote I/O. Future retained changes must beat the current `~34s` local cell and preserve the cgroup-backed measurement path.
- Stock-libduckdb table-function falsification (closed into D14):
  - Question: can a non-deprecated CDF-owned C table function over finite finalized canonical LZ4 IPC segments reproduce nanoarrow's effective parallel scan/materialize mechanism through stock `libduckdb`? The lab path will use arrow-rs IPC decode, `duckdb_data_chunk_from_arrow`, and `duckdb_vector_reference_vector`; it will not use the deprecated `duckdb_arrow_scan`, the D10 live staged stream, a custom DuckDB build, or any product-path branch.
  - Provenance: DuckDB's public C table-function API supports parallel source callbacks through `duckdb_init_set_max_threads`; its Arrow C API transfers Arrow-buffer ownership into a DuckDB `DataChunk` and permits vector ownership references into the table-function output. Literal nanoarrow internals remain unavailable through the public ABI, so this spike tests equivalent mechanics rather than copying extension code.
  - Superseded retention gate: the initial 10% raw-parity threshold was too rigid because it ignored the substantial product value of deleting the custom DuckDB build, embedded extension, ABI-coupled release matrix, and unsigned development path. The user ratified accepting a modest raw delta for that simplification. Product integration is retained only if the full-CDF EC2 path remains exceptional, bounded, and materially faster than the appender; raw evidence still selects and tunes the mechanism rather than acting as a hard cutoff.
  - Local implementation evidence: `cargo fmt --all && CARGO_BUILD_JOBS=12 cargo test -p cdf-benchmarks duckdb_arrow_ipc_table_function_materializes_canonical_segments --locked -j 12` passed. The test registered the supported C table function over two LZ4 canonical segments, exercised two requested workers, materialized 2,048 rows, derived `_cdf_row_key` `1..=2048`, and proved the internal ordinal was excluded from the destination table. The full `cdf-benchmarks` suite passed all 12 unit tests and fixture tests but remains red in the pre-existing `first_party_destination_catalog_matches_runtime_inspection` policy cell because the committed catalog still expects `duckdb.connection` while live inspection now reports split `duckdb.staged_ingress`/`duckdb.final_binding` lanes; this spike did not alter either authority.
  - Current-package EC2 evidence: generated one fresh full-year package at clean revision `2bede24d2e656de83aa28cd8a048df1e9917ff37`, containing 215 current canonical LZ4 IPC segments, `1,678,253,294` segment bytes, `_cdf_package_row_ord`, and `41,169,720` rows. The stock-library table function materialized the exact package and derived `_cdf_row_key=1..=41,169,720` in `5.630615786s`, `5.645601216s`, and `6.297421074s`; median was `5.645601216s`, `7,292,353` rows/s, peak process RSS `3,822,854,144` bytes, peak observed cgroup memory `4,557,549,568` bytes, and zero cgroup memory events. Six-, eight-, and twelve-thread screens were slower; the automatic 16-thread host allocation is the retained raw policy on this host.
  - Exact-package nanoarrow comparator: nanoarrow `0.8.0` over those same 215 ordinal-bearing segments, with the same 16 threads, 4 GiB DuckDB memory, 32 GiB temp budget, CTAS projection, and row-key derivation measured `4.96s`, `4.89s`, and `4.97s`; median was `4.96s`. The stock public-ABI path is about 13.8% slower on identical semantic work but eliminates the custom runtime and extension lifecycle. This is sufficiently close to authorize destination-owned product integration; the full-CDF cell, not the raw delta alone, decides replacement.
  - Final product result: `.10x/evidence/.storage/2026-07-19-p3-d14-stock-default-full-year-three-sample.json` records `10.255642670s` median, `30.030883ms` MAD, `4,014,348` rows/s, exact `41,169,720` rows in every sample, bounded RSS/cgroup memory, and zero pressure/OOM/spill events. D14 deleted the custom runtime, nanoarrow lifecycle, appender product path, and unused crate edges. The local materialization branch is closed; G4 continues only for the live remote/provider envelope.
- Final remote-spool falsification and retained policy:
  - The stock release at `8408bac1be95190e8cc37e3e7944f2e19ff6d281` completed the 12-file Hugging Face TLC mirror in `19.580053641s`, exact `41,169,720` rows, peak process RSS `3,910,922,240` bytes, cgroup peak `6,254,161,920` bytes, and zero pressure/OOM/spill events. The same host fetched all 12 objects with parallel curl in `2.26s`; local CDF was `10.255642670s` median. Phase comparison showed growing-spool waits inflated package execution to `10.810050368s` versus `3.538189036s` locally. Evidence: `.10x/evidence/.storage/2026-07-19-p3-g4-hf-stock-full-year-smoke.json`.
  - A one-variable full-spool falsification completed in `16.229050834s`, exact rows, peak RSS `3,949,334,528` bytes, cgroup peak `6,307,774,464` bytes, and zero pressure/OOM/spill events. This is a measured `17.1%` improvement over the stock remote cell. Evidence: `.10x/evidence/.storage/2026-07-19-p3-g4-hf-full-spool-candidate.json`.
  - The first retained draft in `010b7ec1` exposed `auto|overlap|complete` and made inventory match count choose complete spooling automatically. Fresh review rejected that default before closure: inventory cardinality is not the incremental run's active-partition cardinality, so a multi-file resource with one changed large object could silently lose overlap. The clean draft measured `16.511129901s` remote and `10.315428900s` local with exact rows and zero pressure/OOM/spill events, but its heuristic is evidence only and is not product authority. Evidence: `.10x/evidence/.storage/2026-07-19-p3-g4-hf-auto-final-clean.json` and `.10x/evidence/.storage/2026-07-19-p3-g4-local-auto-final-clean.json`.
  - Commit `4686d5c6` deletes the speculative heuristic, its optional-reservation fallback helper, and all match-count plumbing from execution. The source-level surface is now only `spool_mode = "overlap|complete"`: `overlap` is the unchanged default, while `complete` is an explicit operator-selected knob that fails cleanly when the shared spill authority cannot admit the finite object. The option is serialized into the compiled source plan and therefore requires an intentional schema repin when changed. Final clean explicit-complete EC2 remeasurement is the only remaining closure action.
  - Verification: after the narrowing, all 82 ordinary `cdf-source-files` tests passed, one slow million-entry test remained intentionally ignored, strict all-target Clippy passed, and `git diff --check` passed. The broader pre-narrowing `cdf-project` run passed 193/198 tests; its five failures were outside the source spool branches, so they are not claimed as closure evidence for this focused change.

## Review

Adversarial review of the first draft found that a global reqwest read timeout leaked file-transfer policy into REST/metadata behavior and consumed the default retry budget. The implementation was revised to private file byte-source phase deadlines and expanded with REST non-inheritance and slow-progress tests. Fresh review of the final spool draft found that inventory match count was not valid authority for the current incremental execution set; `4686d5c6` removes that heuristic and preserves overlap as the default. Final verdict remains pending only on the explicit-complete live cell.

## Retrospective

Pending ticket completion.
