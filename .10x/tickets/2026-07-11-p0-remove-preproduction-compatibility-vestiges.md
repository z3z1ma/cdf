Status: active
Created: 2026-07-11
Updated: 2026-07-13
Parent: None
Depends-On: None

# P0: Remove pre-production compatibility vestiges

## Scope

Apply `.10x/decisions/pre-production-current-format-only.md` repository-wide. Enumerate and delete old artifact readers/migrations, deprecated CLI aliases and parser prepasses, compatibility-only API defaults/helpers, dormant destination/source fallbacks, and tests/fixtures whose only purpose is preserving a superseded CDF snapshot. Preserve current external protocol interoperability and benchmark-only before/after controls.

## Acceptance criteria

- Current package, schema snapshot, discovery manifest, dedup, event, sheet, and checkpoint writers/readers expose one canonical version each; older CDF artifact versions fail as unsupported without migration readers.
- CLI grammar and Rust APIs contain no deprecated aliases or compatibility shims.
- Destination/source capability sheets advertise only live current implementations.
- `rg`-based inventory classifies every compatibility/legacy occurrence as removed, current external-protocol interoperability, benchmark control, or non-compatibility English usage.
- Workspace current-format tests, strict lint, artifact goldens, and gitleaks pass with a leaner build graph.

## Evidence expectations

Before/after inventory, deleted-line/build-target counts, current-format golden tests, focused protocol tests, workspace check/lint, and adversarial review for accidentally removed current external interoperability.

## Explicit exclusions

No compatibility promise for the first production release is introduced here. No external protocol dialect is removed merely because that protocol names it legacy.

## Blockers

None.

## References

- `.10x/decisions/pre-production-current-format-only.md`
- `.10x/decisions/destination-ingress-protocol-capability-split.md`
- `.10x/specs/destination-receipts-guarantees.md`
- `.10x/specs/streaming-destination-ingress.md`

## Journal

- 2026-07-12: Began the destination/runtime deletion slice after P3 D5 review identified DuckDB's unadvertised finalized-package commit session as a second production path beside its advertised staged durable-segment ingress. Deleted the pending-session cache, finalized commit session/outcome, direct `commit_package` API, hidden execution-service constructor, duplicate receipt writer, and the tests that exercised the superseded path. Migrated surviving DuckDB and CLI fixtures through the canonical verified-package -> staged-ingress -> verified-final-binding runtime instead of introducing a test compatibility helper.
- 2026-07-12: Current-path migration exposed two legacy assumptions in CLI fixtures: the removed `_cdf_load`/`_cdf_segment`/`_cdf_row` DuckDB provenance columns and incomplete packages that omitted the current compiled-expression, scan, Arrow-schema, state, and commit-plan artifacts. Fixtures now build current packages and assert compact `_cdf_row_key` provenance.
- 2026-07-12: The current path also exposed a live normalization regression: the prebound vector evaluator treated the compiler-approved source identifier, normalized identifier, and destination-truncated identifier as unrelated schemas. Binding now records the exact compiler-sanctioned names for each ordinal and accepts only those names with identical cardinality, type, nullability, and position. Same-typed ordinal substitution remains rejected; mismatch diagnostics name both schemas.
- 2026-07-12: Audited the expression-plan naming requested during this slice. The implementation already exposes only `CompiledExpressionPlan`, `compiled_expression_plan`, and `validate_compiled_expression_plan`; no `Expression*Authority` Rust symbol remains. This was previously completed and evidenced in `.10x/tickets/done/2026-07-12-p3-j3-expression-ir-contract-linter.md`, so no alias or redundant rename was added.
- 2026-07-12: Fresh adversarial review rejected the first deletion snapshot on three significant findings: DuckDB planned through `DestinationProtocol` but its required `begin` always errored; test-local merge/replace inputs could disagree with package commit-plan evidence; and vector aliases did not reject conflicting `cdf:source_name` provenance or cross-ordinal alias ownership. No finding was waived.
- 2026-07-12: Ratified `.10x/decisions/destination-ingress-protocol-capability-split.md` to reconcile the older universal finalized-session contract with the newer staged-ingress contract. The common protocol now owns sheet/plan/verify/corrections; `DestinationRuntime::ingress` exposes exactly one capability-typed `DestinationIngress` variant backed by `FinalizedPackageIngress` or `StagedSegmentIngress`. Runtime selection is capability-driven, DuckDB exposes only staged segments, Parquet/Postgres expose finalized packages, and a fourth-destination conformance fixture must enroll the matching protocol explicitly. No destination-id branch or error-returning compatibility method remains.
- 2026-07-12: Rebuilt DuckDB test package authority so disposition and merge keys are identity-participating inputs at package construction and staged execution derives its commit/schema/keys from verified replay inputs. Removed the test-local duplicate pre-query/outcome shim. Vector binding now rejects conflicting source provenance and any alias owned by multiple ordinals.
- 2026-07-12: A second fresh adversarial review rejected the intermediate repair on three significant boundaries: finalized and staged methods still cohabited one runtime trait, Parquet/Postgres carried hidden prepared-session state, staged final binding did not derive exact Arrow-schema and merge-key authority from the verified package, and vector binding could accept a physical name whose `cdf:source_name` selected another column in partial schemas. Replaced the mixed runtime surface with capability-typed ingress traits, moved finalized preparation context into the prepared commit, bound staged execution to verified output-schema/merge-key evidence, and made vector name/provenance resolution conflict-strict at bind time.
- 2026-07-12: Adopted the precise ingress vocabulary `DestinationIngress::{FinalizedPackage, StagedSegments}`, `FinalizedPackageIngress`, and `StagedSegmentIngress`. This keeps package lifecycle adjectives on the ingress category they constrain and leaves no destination-adjective compatibility names.
- 2026-07-13: Targeted project replay initially exposed that its finalized and staged mock runtimes had not enrolled a selected bulk path after the earlier bulk-path authority work. Added measured mock descriptors matching each advertised ingress category; the exact generic finalized replay, staged replay/final binding, and pre-write stage-hook scenarios then passed. The unrelated `generic_` filter also reproduced the already-recorded process-global discovery registry races, so it is not claimed as a broad green run.
- 2026-07-13: Deleted the remaining DuckDB package-directory planning wrapper, empty-package effect enum, and dead target-existence projection after the current staged tests were migrated to schema-driven planning. Runtime registry fixtures now implement a real finalized ingress session instead of error-returning "not exercised" methods, keeping even test adapters honest to the typed category contract.
- 2026-07-13: A third fresh adversarial review failed the slice on one critical, four significant, and two minor findings. The critical finding proved that the public prepared replay requests let callers supply a second `PackageReplayInputs` authority over a verified package. The significant findings covered duplicate staged schema/key authority, unchecked Parquet pending context, projection-local rather than program-global vector aliases, and receipt-gate control flow leaked into destination planning. The minor findings identified one unused pending-context helper and a tautological DuckDB wrapper/session test. No finding was waived.
- 2026-07-13: Deleted the caller-supplied prepared replay/recovery requests and functions. Artifact replay and recovery now derive the complete checkpoint, target, disposition, keys, schema, segments, and positions from `PackageReader::replay_inputs_verified`; callers provide only a verified package location, resolved destination, checkpoint store, and—on recovery—the durable receipt. Exact crash-left proposed checkpoints are reusable, while a same-id proposal with any different status, receipt, head bit, or delta fails closed.
- 2026-07-13: Made staged ingress carry one schema/key authority: `StagingAttemptBinding` owns merge keys and both schema hashes, `StagedIngressRequest` validates the full Arrow schema payload against that hash, and all request fields are private behind accessors. Parquet now consumes pending context only after exact comparison with the generic prepared commit and plan. Vector binding validates alias ownership across the entire validation program before projection. Removed the destination-planning receipt hook, unused pending-context helper, and obsolete DuckDB finalization wrappers.
- 2026-07-13: Chaos verification exposed that a staged adapter could return a durable receipt without reporting its duplicate semantics. Added the destination-neutral `DestinationCommitOutcome`, returned by every staged final binding, so generic orchestration classifies receipt evidence without destination identity. DuckDB reports exact duplicate/non-duplicate outcomes; receipt-only staged adapters remain representable. The same matrix exposed and repaired standard `postgresql://` destination URI enrollment alongside `postgres://`.
- 2026-07-13: A fourth fresh adversarial review failed the repaired slice on two significant findings and one minor finding. Staged ingress had not distinguished the recorded scan/execution plan from the destination commit plan; finalized prepared commits still exposed mutable public authority without a generic pre-mutation comparison to verified package inputs; and Postgres registered `postgresql://` without advertising it in its runtime description. No finding was waived.
- 2026-07-13: Made `plan/scan.json` a typed package artifact read through `PackageReader::recorded_scan_plan`. Staging now names and binds `execution_plan_id` separately from the destination `CommitPlan`, and verified final binding rejects any execution-plan ID that differs from the recorded package. `PreparedDestinationCommit` fields are private, construction derives commit/schema from verified replay inputs, and generic orchestration revalidates commit, schema, target, and disposition immediately before destination mutation. Parquet additionally keeps its adapter-specific pending context exact. Postgres now advertises both registered URI schemes.
- 2026-07-13: Focused package replay found two conformance assertions that still encoded DuckDB's deleted receipt-only reporting shim. Replaced them with the destination-neutral `DestinationCommit { duplicate }` outcome; all ten package replay cases then passed. The separately selected live NDJSON registry-isolation failure remains unrelated and is not hidden by this repair.
- 2026-07-13: A fifth fresh adversarial review failed the slice on one critical, two significant, and one nit finding. Parquet and Postgres still exposed direct `commit_package` facades that bypassed the typed ingress/gate; scan and validation artifacts were reread after verification without rechecking their bytes against manifest identity; finalized destinations still owned package receipt persistence while staged ingress did so generically; and one conformance guard had a duplicated test attribute. No finding was waived.
- 2026-07-13: Repaired the fifth-review findings by deleting the Parquet/Postgres direct package commit facades and public outcomes, moving receipt persistence behind one generic verified-receipt gate for both ingress categories and recovery, consuming identity artifacts through exact hash/size checks at use time, and removing the duplicated conformance attribute. Package-to-destination preparation now reuses the supplied verified package token instead of performing another full package verification; Parquet and Postgres derive manifest segments and runtime schema through token-bound reader APIs.
- 2026-07-13: The sixth fresh review failed on three significant and two minor findings: same-id exact-body receipt comparison broke replay to a second physical destination instance; Postgres live coverage reconstructed the deleted direct session facade; Postgres schema discovery repeated full package verification; the fourth-destination matrix omitted package receipt assertions; and `ParquetCommitPlan` remained publicly orphaned. Ratified the spec clarification that `ReceiptId` names a logical commit while per-run checkpoint receipts retain truthful physical transaction metadata. Same-id dedup now requires exact agreement across every logical commit field and fails closed otherwise. Migrated ordinary Postgres live commits through `PostgresRuntime` + `FinalizedPackageIngress` + verified segment streaming and bulk preflight, retained direct session driving only for abort/rollback unit boundaries, bound schema/segments to supplied verification, asserted fourth-destination first/duplicate/crash/recovery receipt inheritance, and made the Parquet plan crate-private.
- 2026-07-13: Re-ran the requested naming audit after the ingress repair. The live vocabulary is exclusively `DestinationIngress::{FinalizedPackage, StagedSegments}`, `FinalizedPackageIngress`, `StagedSegmentIngress`, and `DestinationRuntime::ingress`; there is no runtime-suffixed compatibility alias or adjective attached to the destination itself. Expression execution likewise remains exclusively `CompiledExpressionPlan`.
- 2026-07-13: Completed the sixth-review Postgres fixture repair rather than preserving a test-only reconstruction of replay authority. Ordinary live commits now build current runtime-schema, state-delta-preimage, and destination-commit-preimage artifacts, reconstruct `PackageReplayInputs` through `PackageReader::replay_inputs_verified`, and enter through `PostgresRuntime` plus `FinalizedPackageIngress`. The only direct Postgres sessions left are abort/rollback transaction-boundary tests. Deleted the contradictory different-idempotency-token duplicate case because current package-token commits admit exactly one token: the finalized package hash. Append/replace fixtures no longer invent merge keys; merge/CDC fixtures retain their evidence-backed key.
- 2026-07-13: The final affected-library run exposed two test-only current-path defects and no product-path regression. DuckDB's staged helper still inferred a schema from segment zero when the mandatory runtime-schema artifact was absent; that fallback is deleted and the helper consumes the verified schema and recorded scan plan. Parallel Parquet tests shared a deliberately small logical memory ledger and could nondeterministically exhaust it before testing missing-segment finalization; the shared test host now has a contention-free logical budget while production memory limits remain unchanged.
- 2026-07-13: Deleted the deprecated CLI grammar instead of retaining parser aliases: `--no-color`, resource-option duplicates, caller-selected ordinary-run pipeline/target/package/checkpoint identities, `--projection`, singular `inspect destination`, contract option duplicates, resume run options, and rewind target/marker aliases are gone. Ordinary runs now mint all artifact identities and derive the canonical target from the compiled resource. Removed the generic string-output compatibility shim; help and version construct renderer documents directly. Regenerated the complete help/man/completion/command-doc surface and updated scaffolds and operator guidance to teach only current syntax.
- 2026-07-13: Migrating current-path CLI tests exposed two real execution defects. The planner had recorded the inspection package id while ordinary run later minted a different id; run now supplies its minted identity explicitly to plan construction. A multi-file Parquet observation with zero rows produced no batch-local coercion evidence and failed final evidence cardinality; execution now attests the empty observation's exact physical schema and records the already-planned coercion verdict without weakening drift checks. Caller-owned artifact-id assertions are being replaced by assertions against the identities minted into the verified package.
- 2026-07-13: The apparent missing-`ExecutionServices` conformance regression was a more specific compatibility leak: file conformance still treated declarative `CompiledResource` as an executable resource, bypassing the source registry and its injected transport/format/runtime dependencies. Replaced those direct conversions in the matrix, multi-file parity law, and drift fixture with `SourceRegistry` resolution through the existing conformance composition seam. The seam now explicitly registers the NDJSON driver it exercises; generic conformance code contains no format branch. S8 multi-file preview/run parity passes again.

## Evidence

- `CARGO_BUILD_JOBS=12 cargo test -p cdf-contract -p cdf-runtime -p cdf-dest-duckdb -p cdf-dest-parquet -p cdf-dest-postgres --lib --locked` passed the affected libraries: contract `90 passed, 2 ignored`; DuckDB `23 passed, 1 ignored`; Parquet `27 passed, 1 ignored`; Postgres `30 passed, 2 ignored`; runtime `35 passed, 1 ignored` (runtime was re-run after its finalized mock became executable). This covers exact vector binding and bind-time provenance conflicts, staged append/merge/replace/idempotency/correction paths, both ingress categories, live Postgres transaction/rollback behavior, and typed zero-segment evidence. It does not claim whole-workspace closure.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-kernel --lib --locked` passed `29 passed`; the common destination protocol remains independently testable after ingress execution moved to the runtime capability traits.
- `CARGO_BUILD_JOBS=12 cargo check -p cdf-cli --tests --locked` passed after migrating the CLI fixtures to current replay.
- Focused CLI tests passed: `doctor_` (`27 passed` plus the environment integration test), `schema_promote_plans_fresh_residual_correction_without_writes`, `schema_promote_execute_commits_correction_checkpoint_lock_and_idempotent_publication`, and `duckdb_destination_policy_normalizes_plan_preview_package_and_commit`.
- Source inventory finds no `DuckDbCommitOutcome`, DuckDB `pending_sessions`, `new_with_execution`, direct `.commit_package(DuckDbCommitRequest`, or `Expression*Authority` Rust symbol. Parquet's separately advertised finalized-package session remains and is not evidence about DuckDB.
- `CARGO_BUILD_JOBS=12 cargo check -p cdf-runtime -p cdf-dest-duckdb -p cdf-dest-parquet -p cdf-dest-postgres -p cdf-project -p cdf-conformance --tests --locked` passed with no warnings after the typed ingress split and mock-path repair.
- Exact project runtime tests `generic_lock_plan_replay_and_recovery_drive_mock_runtime_without_destination_branch`, `generic_replay_streams_verified_segments_through_staged_final_binding`, and `generic_stage_hook_stops_mock_replay_before_destination_write` passed serially. They prove finalized and staged selection remains destination-identity-neutral, staged authority binds through the verified package, and failure before destination mutation is preserved.
- `destination_catalog::fourth_registered_destination_inherits_the_generic_bulk_matrix_contract` passed after the fixture explicitly enrolled `FinalizedPackageIngress`, proving a new destination receives the generic bulk/replay path without destination-specific runtime wiring.
- A broad parallel `cdf-conformance` run was not closure evidence: 74 tests passed and 16 failed, predominantly because process-global source/format/destination registries were concurrently cleared or unregistered; one staged receipt-source expectation and several already-stale fixture inventories also failed. These failures are not claimed resolved by this slice and remain part of the open ticket/CI inventory.
- After third-review repairs, `CARGO_BUILD_JOBS=12 cargo check -p cdf-runtime -p cdf-dest-duckdb -p cdf-dest-parquet -p cdf-dest-postgres -p cdf-project -p cdf-conformance --tests --locked` passed without warnings.
- The affected library suite passed after the authority repair: contract `91 passed, 2 ignored`; DuckDB `23 passed, 1 ignored`; Parquet `28 passed, 1 ignored`; Postgres `30 passed, 2 ignored`; runtime `36 passed, 1 ignored`. The later staged-outcome repair was rechecked with DuckDB `23 passed, 1 ignored` and runtime `36 passed, 1 ignored`.
- Project recovery tests passed `12 passed` and artifact replay tests passed `7 passed`, including source-free DuckDB/Parquet/Postgres paths, exact artifact reconstruction, corrupted preimage rejection, stale compiled-expression rejection, stable duplicate receipts, and exact committed-checkpoint reuse.
- The dedicated package-replay conformance cases passed `10 passed`; the broader name filter also selected one unrelated live NDJSON fixture that failed because its process-global `ndjson` format registration was absent. That registry failure is not claimed as package-replay evidence.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-conformance 'runtime_chaos::' --locked -- --test-threads=1` passed `2 passed`, including the persisted 12-case matrix across DuckDB, filesystem Parquet, Postgres, and four crash windows. It proves source-free recovery, exact proposal reuse, stable duplicate receipts, no second destination write, and checkpoint-not-ahead-of-durable-data behavior.
- Inventory finds no old finalized/staged runtime names, caller-authoritative prepared replay/recovery request or function, exported pending-context rejection helper, or `Expression*Authority` Rust symbol. `git diff --check` passes.
- After fourth-review repair, `cargo check --locked -j 12 -p cdf-project -p cdf-conformance` passed the complete runtime/destination integration graph. A separately scoped `cdf-dest-parquet` check also passed with the finalized prepared-commit privacy and pending-context validation compiled together.
- The updated affected library suite passed: DuckDB `24 passed, 1 ignored`; Parquet `28 passed, 1 ignored`; Postgres `30 passed, 2 ignored`; runtime `36 passed, 1 ignored`. New negative cases prove that staged final binding rejects execution-plan drift and finalized prepared ingress rejects verified commit, schema, and destination-plan drift before mutation.
- `cargo test --locked -j 12 -p cdf-conformance runtime_chaos` passed `2 passed`, including the persisted cross-destination 12-case crash matrix. The exact `package_replay::tests::` module passed `10 passed`; project `recovery_` passed `12 passed`; project `artifact_replay` passed `7 passed`.
- Current-source inventory finds no old ingress type names, `staging_plan_id`, caller-authoritative prepared replay API, public `PreparedDestinationCommit::new`, or `Expression*Authority` Rust symbol. `git diff --check` passes and `gitleaks dir crates --no-banner --redact --exit-code 1` reports no leaks.
- `CARGO_BUILD_JOBS=12 cargo test --locked -j 12 -p cdf-dest-postgres --lib` passed `29 passed; 2 ignored`, including current-artifact append/duplicate, replace, merge, decimal, quarantine, correction, and direct abort/rollback boundaries.
- After deleting the DuckDB missing-schema fallback and isolating the shared Parquet test ledger, the affected libraries passed DuckDB `24 passed; 1 ignored`, Parquet `28 passed; 1 ignored`, and package `56 passed; 3 ignored`. The combined command was intentionally stopped while the already-compiled runtime test executable was stalled in macOS `_dyld_start`; no runtime result is claimed from that invocation, while the earlier post-change runtime result remains `36 passed; 1 ignored`.
- `CARGO_BUILD_JOBS=12 cargo check --locked -j 12 --tests -p cdf-kernel -p cdf-contract -p cdf-package -p cdf-runtime -p cdf-dest-duckdb -p cdf-dest-parquet -p cdf-dest-postgres -p cdf-project -p cdf-conformance -p cdf-cli` passed the complete affected compile graph. Removing the one reported stale CLI import made a second CLI test-graph check warning-free. `cargo fmt --all -- --check` and `git diff --check` passed.
- A full current-working-tree scan copied without `.git` or `target` and checked by `gitleaks detect --no-git --redact --exit-code 1` completed with `no leaks found`.
- The canonical-CLI slice compiled across `cdf-engine`, `cdf-project`, `cdf-cli`, and `cdf-conformance` with `CARGO_BUILD_JOBS=12 cargo check --locked -j 12 --tests ...`. Both generated-surface checks passed (`cdf-generate-cli-artifacts --check` and `--docs-only --check`), as did `cargo fmt --all -- --check`, `git diff --check`, and a fresh gitleaks scan of the working tree excluding only `.git` and `target` (`no leaks found`).
- Focused ordinary-run coverage passed `30 passed; 0 failed`, including local/remote Parquet, multi-file empty-Parquet evidence, REST, SQL, Postgres, ad-hoc retry, destination policy, and progress routing. Canonical replay passed 16 of 17 selected cases; the only failure is an older exit-class assertion for an unregistered destination URI (`Contract`/3 observed versus `NotSupported`/78 expected), independent of removed aliases or caller-owned identities. Resume reached 14 of 15 before the final stale human-field assertion was removed; the other fourteen cover positional resume, DuckDB/Postgres source-free recovery, stale-state failures, and human/rich rendering. State coverage reached 8 of 10 before the two remaining table/pipeline-format assertions were updated; product grammar, minted rewind markers, recovery, and state-store behavior passed.
- P2 S5 and S7 conformance passed on the canonical run grammar and package/checkpoint identities read from the verified run report. The broader `p2_s` filter also selected S8, which failed because its lower direct execution path did not inject file runtime services. The MVP acceptance demo similarly advanced through canonical plan/replay before its drift-quarantine helper hit the same error: `file resources require a resolved file runtime with transport, format, and transform registries`. This is not hidden as CLI evidence; it exposes remaining compatibility execution APIs that violate `.10x/specs/execution-host-structured-runtime.md` and is the next P0 deletion/repair slice.
- After replacing compiled-resource execution with registry resolution, `p2_s8_multifile_preview_traverses_the_same_planned_partitions_as_run` passed (`1 passed`). The MVP demo then advanced beyond source resolution and exposed B5's already-open row-local parity gap: the streamed NDJSON driver aborts the batch on `event_type: 42` against a UTF-8 physical authority instead of forwarding residual/quarantine evidence. That failure is now journaled in `.10x/tickets/2026-07-11-p3-b5-json-codecs.md`; it is not misclassified as an execution-host failure.

## Review

- First fresh adversarial review verdict: **fail**, with three significant findings (protocol category mismatch, package-authority bypass in tests, and provenance alias substitution). All three were repaired.
- Second fresh adversarial review verdict: **fail**, with three significant findings (mixed ingress methods and hidden adapter state, incomplete staged final authority, and bind-time name/provenance conflict). All three were repaired.
- Third fresh adversarial review verdict: **fail**, with one critical, four significant, and two minor findings (caller-controlled replay inputs; duplicate staged authority; unchecked Parquet context; partial-schema alias substitution; receipt-hook leakage; dead helper; tautological wrapper test). All seven were repaired. A fourth fresh review of the complete repaired slice is required before this slice is committed; ticket-wide closure is not claimed.
- Fourth fresh adversarial review verdict: **fail**, with two significant findings and one minor finding (execution-plan versus destination-plan ambiguity; mutable finalized prepared authority without generic pre-mutation validation; incomplete Postgres URI advertisement). All three were repaired. A fifth fresh review of the complete repaired slice is required before this slice is committed; ticket-wide closure is not claimed.
- Fifth fresh adversarial review verdict: **fail**, with one critical, two significant, and one nit finding (Parquet/Postgres direct commit bypasses; post-verification artifact TOCTOU; adapter-owned package receipt persistence; duplicate test attribute). Repair and a sixth fresh review are required before this slice is committed; ticket-wide closure is not claimed.
- Sixth fresh adversarial review verdict: **fail**, with three significant and two minor findings (physical-instance receipt metadata treated as logical conflict; Postgres live fixtures reconstructing the deleted direct facade and replay authority; repeated Postgres package verification; missing fourth-destination receipt assertions; public orphaned Parquet plan). All five were repaired. Root's post-repair adversarial pass additionally deleted the remaining DuckDB missing-schema test fallback and noncanonical Postgres token case, and found no old ingress or expression-authority names. Ticket-wide closure is not claimed by this batch commit.
- Root's fresh adversarial pass over the canonical-CLI batch found and repaired three integration leaks before commit: conformance still invoked deleted caller-owned run identities; the MVP transcript planned a caller-selected target different from its direct execution target; and human transcript capture assumed progress could not use stderr. All conformance callers now consume minted identities, the demo uses the resource-derived `issues` target end to end, and transcript capture preserves stderr progress plus stdout results. The remaining file-runtime-service failures are significant but pre-existing execution-host compatibility paths, explicitly owned by this still-active ticket rather than waived. Verdict for the CLI/empty-observation batch: **pass with the execution-host repair as the next slice**; ticket-wide closure is not claimed.

## Retrospective

Pending ticket completion.
