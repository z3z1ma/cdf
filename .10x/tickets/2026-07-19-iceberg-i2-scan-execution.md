Status: active
Created: 2026-07-19
Updated: 2026-07-19
Parent: .10x/tickets/2026-07-19-iceberg-glue-source-program.md
Depends-On: .10x/tickets/done/2026-07-19-iceberg-f4-externalized-scan-tasks.md, .10x/tickets/done/2026-07-19-iceberg-i1-catalog-discovery.md

# Iceberg I2: bounded scan execution and delete semantics

## Scope

Execute canonical Iceberg tasks through the neutral object-access-backed Iceberg storage bridge and aligned reader, emitting preaccounted Arrow batches with exact projection, evolution, partition constants/defaults, position/equality deletes, pruning fidelity, retries, cancellation, and deterministic parallelism.

## Non-goals

No append ancestry/changelog/tailing, ORC/Avro/v3/encryption enablement, destination or generic runtime branch, or independent object/runtime/memory pool.

## Acceptance Criteria

- Iceberg v1/v2 Parquet scans and both delete forms match reference results across schema/partition evolution.
- Every retained payload is ledger-owned; no whole-table/file/task collection; too-small memory/disk fails cleanly.
- Jobs 1/N, retries, skew, cancellation, and generation drift preserve deterministic package/position/verdict authority.
- Local filesystem/REST performance scales to the actual CDF Parquet/CPU/device roofline and records phase evidence.

## References

- `.10x/specs/iceberg-source.md`
- `.10x/specs/constant-memory-proof.md`
- `.10x/specs/deterministic-parallel-scheduler.md`

## Assumptions

- User-ratified 2026-07-19: official aligned Iceberg reader is primary until evidence justifies replacing a hot stage.

## Journal

- 2026-07-19: Activated after I1/F4 closure. Execution will consume the existing external canonical task artifact and injected neutral object-access authority; no upstream Iceberg runtime, private pool, or generic source branch will be introduced. The first tranche is one immutable Parquet task to a preaccounted ordinary `ResourceStream`, then evolution/defaults/partition constants and delete semantics extend that single path.
- 2026-07-19: Commit `855be499` installed a source-neutral external partition reader and cardinality-independent schedule/frontier authority. Iceberg now decodes one canonical task at a time while retaining its task-store bytes and parse lease; generic orchestration sees only `ExecutablePartition` plus opaque adapter retention.
- 2026-07-19: Implemented the first complete scan path through the aligned Iceberg Arrow 58 reader. Per-task blocking preparation resolves exact object generations through injected CDF object access, then a CPU stream performs field-ID projection, schema evolution/default filling, and Parquet decode. Ranged `AccountedBytes` cross the foreign reader zero-copy through `Bytes::from_owner`, retaining their CDF lease until the final foreign slice drops. Output batches reserve/reconcile the shared ledger and carry exact table-snapshot positions. Batch rows, retained bytes, metadata prefetch, range coalescing/concurrency, and internal stream depth are explicit source knobs.
- 2026-07-19: Added exact v2 position/equality-delete planning without adopting upstream's private, table-resident delete index. Delete manifests are loaded once in canonical bounded worker order into an invocation-local SQLite B-tree under a task-store-owned workspace. The cache plus maximum task assembly are memory-leased; database and rollback-journal growth is bounded by the injected shared spill reservation, whose page ceiling grows in an operator-configured quantum until the shared disk authority refuses it. Data entries query global equality and exact `(partition spec, typed partition tuple)` delete applicability with Iceberg sequence and referenced-file rules, then serialize canonical descriptors into the ordinary portable task. No SQLite path or implementation detail enters plan/task identity.
- 2026-07-19: Extended the real local v2 fixture across schema and partition evolution: the old file and its referenced position delete use unpartitioned spec 0, while the current file and a partition-scoped equality delete use bucket spec 1; a second unpartitioned equality delete remains global. Canonical task construction and the official aligned reader prove the semantics end to end: each delete reaches only its legal files by sequence/spec/typed tuple/reference, five of eight rows survive, schema evolution/default filling remains correct, and jobs 1/16 produce identical scan/task identities.
- 2026-07-19: The execution-boundary audit found and removed a latent scheduler mismatch: Iceberg task partitions declared snapshot-safe reopen semantics while the source advertised unit-only retry granularity. External task records become ordinary scheduled partitions at the generic boundary, so the source now correctly advertises partition retry. The real external-task fixture compiles its `CompiledSourceExecutionPlan`, materializes each bounded task binding, and proves the generic schedule accepts snapshot reattestation with partition-granularity retry.
- 2026-07-19: Installed the matching executable-partition reattestation hook. A retry revalidates the retained task against its content-addressed task-set authority and returns the exact pinned table snapshot plus compiled Arrow schema hash; it neither resolves catalog `current` nor opens payload. This closes the prior failure mode where partition retry compiled successfully but the generic retry loop received `None` from the source before reopen.
- 2026-07-19: Closed the external-task preview/run parity gap generically. Preview previously enumerated only inline `ScanPlan::partitions`, so an Iceberg plan truthfully reported two external tasks but preview inspected zero. The kernel now exposes a cardinality-bounded ordered form of the existing stratified-hash selector; the ordinary selector delegates to the same implementation, preserving v1 membership exactly. Engine preview makes a metadata-only counting pass over external tasks, then retains at most the operator's `max_batches` selected executable task payloads on a second pass, opens them through `open_executable`, and uses executable reattestation. No source identity branch entered the engine. The real evolved/deleted Iceberg fixture now compiles through `Planner::plan_tier_b`, previews five rows from two tasks, executes the same plan into a verified package, and records table-snapshot segment positions and checkpoint-eligible completion evidence.
- 2026-07-19: Proved the retry lifecycle against real Iceberg I/O rather than only schedule shape. A test-only neutral `FileTransport` wrapper injects one transient metadata failure into canonical data task zero after discovery and preview. Generic engine execution reattests the retained external task, reopens only that partition, completes the same five-row package, and records one transient retry history entry for ordinal zero. The source owns no retry loop and never re-resolves catalog `current`. The test execution host's delay implementation was also corrected to schedule timers on its owned Tokio runtime while remaining safe to poll from the executor-agnostic engine test, matching the `ExecutionHost` contract.
- 2026-07-19: Closed retry generation drift without a run-wide identity map. Each ledger-owned retained task now carries one fixed 256-byte attempt-attestation slot shared by its bounded retry clones. A complete prepare hashes the exact transport identities of that task's data/delete objects in canonical path order. Later attempts must match before decode begins; range reads still enforce the same identity within an attempt. The real fixture prepares task zero, replaces its Parquet object with a same-sized generation, and proves the next prepare fails as `Data`. This state is invocation-only and never enters task/package identity.
- 2026-07-19: Added cooperative cancellation fault injection at the neutral transport boundary. Cancellation during canonical data-task metadata preparation terminates generic engine execution, consumes the configured signal, and leaves the package lifecycle at `extracting`; it never reaches validation, packaging, or checkpoint eligibility. No Iceberg-specific cancellation loop or package behavior was introduced.
- 2026-07-19: Removed resident manifest-list cardinality from scan planning. A minimal v1/v2 Avro decoder now streams each manifest-list entry into the same invocation-local, spill-backed planning index that owns delete applicability; the upstream whole-list `Vec` and separate delete-only index are gone. Manifest identities remain canonically ordered through a `(content, path)` B-tree, counted in constant control memory, and fed to bounded parallel manifest workers without collecting the snapshot. The planning-index cache, reader cache, maximum task assembly, database, and rollback journal remain subordinate to the injected memory/spill authorities through explicit source knobs.
- 2026-07-19: Adversarial review rejected a naive statement-level `SQLITE_FULL` retry inside one transaction: SQLite may roll back the entire transaction, making that retry silently incomplete. The retained implementation treats a full insert/commit as a failed attempt, verifies that rollback retained zero manifest rows, grows only its shared spill reservation, and deterministically reparses the already-accounted manifest-list payload. A 10,000-manifest test forces 64 KiB growth quanta, proves canonical traversal and fixed ledger residency, and completes in 1.03 seconds in the debug test profile. Initial worker assignment was also moved inside scope cleanup so iterator/queue failures cannot strand blocking workers.
- 2026-07-19: Extended REST beyond empty discovery. A negotiated nonempty REST response now drives the real evolved v2 fixture through manifest/delete planning, two canonical external tasks, neutral local object access, aligned Parquet decode, and both delete forms to the exact five-row result. Discovery and resolution still make only the two catalog protocol requests (config and load-table); payload execution never recontacts the catalog or takes a REST-specific execution path.
- 2026-07-19: Closed the v1 execution gap with a real v1 table rather than treating manifest-list decoding as scan proof. The fixture writes v1 metadata, a v1 data manifest, v1 manifest list, field-ID-bearing Snappy Parquet, and one fixed snapshot. Ordinary filesystem discovery freezes its Arrow schema; planning externalizes one canonical task whose shared authority records table format v1; aligned execution emits the exact three rows.
- 2026-07-19: Proved projection and predicate fidelity through the complete engine boundary. Iceberg truthfully classifies `id > 4` as unsupported rather than claiming source pruning; the engine retains `id` only long enough to evaluate the residual after delete application, then packages only projected `label` plus the governed residual/provenance columns. The evolved/delete fixture yields exactly `[null, "eight"]`, demonstrating that deleted IDs 5/7 stay deleted, residual filtering removes the old/current nonmatching rows, and an internal filter dependency does not leak into the projected data schema.
- 2026-07-19: Exercised the first real AWS path against the existing FQ12 Glue/S3 table `gold.dim_date` in account `617739438897`, region `us-west-2`, using profile `PowerUser-617739438897` and secret-ref object credentials. This integration found four abstraction defects that unit fixtures had not exposed: the registry option-schema dialect omitted numeric maxima and array minima, the Glue adapter re-entered the shared I/O runtime from within an I/O future, generic orchestration counted only inline partitions rather than external task authority, and the registry wrapper failed to forward executable-task hooks. All were corrected at their generic owner boundary; no Glue/Iceberg branch entered CLI, project orchestration, scheduler, or engine execution.
- 2026-07-19: The same live table exposed two format/runtime fidelity defects. Its retained v2 manifest omits the Avro `schema-id` header while carrying embedded schema id 1, and the upstream reader returned projected fields in field-id order rather than the compiled table-schema order. Planning now preserves optional raw Avro header authority: absence falls back to the independently encoded schema or manifest-list spec, while presence must agree. Execution now aligns columns zero-copy by Iceberg field ID and rejects missing, duplicate, or surplus IDs. The broad real fixture now resolves through `SourceRegistry`, permanently covering the wrapper seam before preview, retry, cancellation, and package execution.
- 2026-07-19: Added a first-class `cdf-iceberg-package-worker` to the P3 lab instead of timing an ad-hoc command. A generated local PyIceberg table with 1,000,000 rows across 10 Parquet data files completed source-to-package in 0.370 seconds in a warm debug worker process with 8 effective jobs; decode was 0.204 seconds, validation/normalization 0.045 seconds, segment encoding 0.497 seconds, persist/hash 0.130 seconds, and finalize 0.018 seconds (parallel phase durations intentionally overlap). This is a functional and phase-telemetry baseline, not yet the release-profile roofline comparison required to close the performance criterion.
- 2026-07-19: A second live run against the unchanged FQ12 snapshot appended the same 1,097 rows again, increasing the DuckDB target to 2,194 rows. The committed checkpoints prove identical input/output `TableSnapshotPosition` values. This is not an I2 scan-result defect, but it falsifies I3's required unchanged-snapshot fast no-op and is recorded on that owning ticket before source-program closure.
- 2026-07-19: The live `bronze.transactions` table exposed a metadata-amplification failure: 122 historical schemas with roughly 2,050 fields each produced a 17 MiB task-set authority, and that authority was decoded, validated, and rehashed repeatedly for every data task. Release planning took 65.07 seconds wall / 60.12 seconds CPU with 1.92 GiB maximum RSS. Task records now bind the content-addressed task-set artifact rather than redundantly hashing its shared authority; one validated authority lazily caches typed schemas for all task readers and retries.
- 2026-07-19: Scan-task authority is now finalized only after canonical manifest traversal identifies the exact schema, partition-spec, and sort-order IDs required by emitted tasks. The source-neutral external task store moved to a customer-zero v2 one-pass layout (canonical task body, compact authority trailer, fixed footer), so late authority neither copies the task body nor temporarily doubles spill. An evolved fixture includes unused schema 99 and proves only task-required schema IDs 0/1 enter authority.
- 2026-07-19: On the same FQ12 `bronze.transactions` plan, compact authority reduced the task artifact from 17 MiB to 491 KiB and release planning to 3.77 seconds wall / 0.75 seconds CPU. Maximum RSS fell to 843 MiB (macOS peak footprint 551 MiB), still identifying the simultaneously retained JSON DOM plus upstream `TableMetadata` as the next memory owner. The remaining roughly three-second wall floor is metadata/network I/O and will be measured independently rather than hidden under prior CPU churn.
- 2026-07-19: Catalog loading now parses table metadata directly into the aligned Iceberg model rather than materializing and retaining a second `serde_json::Value` tree. Metadata generation is the SHA-256 of the logical raw JSON bytes (decompressed for gzip), computed while gzip parsing; raw transport bodies and temporary Glue/REST control allocations are released as soon as the typed table owns the observation. Branch/tag selection parses only a small reference-kind projection because upstream exposes ref snapshots but not ref kind. No catalog-specific representation crosses the Iceberg crate boundary.
- 2026-07-19: Three release FQ12 plans after direct parsing measured 2.64/3.45/4.08 seconds wall (0.59/0.63/0.62 seconds user CPU), with 440/443/521 MiB maximum RSS. The median is 3.45 seconds and 443 MiB, versus 3.77 seconds / 843 MiB immediately before removing the DOM and 65.07 seconds / 1.92 GiB before authority repair. A presigned direct GET of the exact 28,150,038-byte metadata object measured 0.934 seconds at 30.15 MB/s; remaining wall includes Glue, S3 HEAD, manifest-list, bounded parallel manifest reads, parse, and generic planning—not one serialized metadata download.
- 2026-07-19: Extended the P3 prepared-Iceberg worker to compile both filesystem and Glue catalogs through the real source registry, accept the existing concurrency/batch knobs, and report discovery/plan/execute wall time alongside generic source-I/O request, byte, queue, duration, and peak-concurrency deltas. The generic exact-range controller was admitting single reads without recording their bytes or request duration; single and coalesced reads now share one observation boundary without double counting, restoring both telemetry and adaptive feedback for the Iceberg Parquet hot path.
- 2026-07-19: Live `bronze.transactions` execution exposed two schema-evolution gaps hidden by compact fixtures. Projection had included nested element IDs even though the Iceberg Arrow reader accepts top-level projection IDs; it now emits only exact top-level IDs. Apache Iceberg Rust also rejected an optional list/map/struct absent from a physical Parquet file. CDF now pins the immutable merge of upstream [PR 2649](https://github.com/apache/iceberg-rust/pull/2649), which materializes typed all-null nested arrays; the temporary CDF-side synthesis experiment was deleted. Nested child-field metadata is rebound through Arrow `ArrayData` without copying buffers so the physical reader output exactly matches the pinned CDF snapshot metadata.
- 2026-07-19: Closed a pure-projection execution defect: the source aligned a projected reader batch against the full 2,052-column resource schema. Execution now derives its exact Arrow schema from the compiled top-level field-ID projection; full scans remain unchanged and projected scans do not materialize or package unrequested columns. A 30-column FQ12 scan over all 84 files and 3,513,266 rows completed source-to-package in 11.415 seconds internal / 13.11 seconds process wall in the debug profile, with 12 effective jobs, 137 segments, 264,394,090 logical output bytes, and roughly 1.04 GiB maximum RSS. Discovery was 3.160 seconds, planning 1.203 seconds, and execution 6.958 seconds. Execution issued 168 exact-range requests for 45,156,952 physical bytes with 12 concurrent requests at peak and 14.457 seconds of summed request duration, proving network I/O overlapped rather than serialized. This is functional debug evidence; release/EC2 roofline comparison remains open.
- 2026-07-19: An intentionally unprojected run over the same extremely sparse 2,052-column schema was stopped after 190.16 seconds and 1.6 GiB of package output. It was not source-byte-bound: Arrow must materialize offsets/validity for thousands of logically present, mostly absent strings. This is honest workload-shape evidence, not permission to hide the cost. Projection is the correct immediate path; compact logical-null representation and adaptive byte-targeted batching remain performance owners beyond this tranche.
- 2026-07-19: Established separate CPU, network, and full-CDF rooflines over the same immutable FQ12 snapshot and a 17-column projection physically present in all 84 files. Five warm local Arrow-rs projected decodes took 0.0658–0.0687 seconds for 3,513,266 rows and 868,395,056 bytes of decoded arrays, proving decoder CPU was not the limiter. A raw 12-way whole-object fetch of the 42,415,285-byte S3 object set took 2.01 seconds. The exact-range CDF baseline had a 5.954-second median full wall and 2.979-second median execution wall, with 172 execution requests, 32,697,563 transferred bytes, and 13.50 seconds summed request duration.
- 2026-07-19: Added one explicit remote-small-object policy knob, `parquet_whole_object_prefetch_bytes`; zero disables it and local or larger objects retain exact-range reads. Eligible immutable objects make one generation-bound full read into the existing ledger-owned `MemoryByteSource`, so Arrow-rs footer/column requests become zero-copy slices without a second cache or memory authority. The declared per-partition working set includes the retained object through one checked computation. A 1/2/8/16 MiB FQ12 sweep found the useful knee at 2 MiB: two runs took 4.883/4.932 seconds full wall and 2.209/2.224 seconds execution, with 92 requests and 30,787,995 bytes. Fetching every file at 16 MiB reduced requests to 84 but raised transfer to 42,415,285 bytes and was not faster, so 2 MiB is the measured default rather than a hidden hard cap.
- 2026-07-19: The repeatability probe found package hashes changing across processes while all segment bytes remained exact. Retained artifacts isolated drift to the compiled source plan hash inside schema-admission evidence; Arrow `Schema` metadata was serializing directly from randomized hash maps. `CompiledSourcePlan.schema` now uses the kernel's complete `CanonicalArrowSchema` serde form. This is a precise field-level repair—not global JSON key sorting, which would corrupt exact-byte task/worker artifact hashes. Three repeated runs per configuration now have identical complete package hashes and identity-artifact digests; exact-range and prefetch runs also have byte-identical data segments.
- 2026-07-19: Final release evidence places the optimized execution path at the observed remote roofline: approximately 2.22 seconds versus 2.01 seconds for raw parallel download, while CDF additionally decodes, validates, encodes, hashes, and persists 3.51 million rows. One paired `/usr/bin/time -lp` run improved from 5.65 to 4.95 seconds and from 1,005,780,992 to 885,080,064 bytes maximum RSS. I2's bounded execution contract is complete; snapshot ancestry/no-op product behavior and the remaining catalog-control-plane latency belong to I3.

## Blockers

None.

## Evidence

- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg --lib`: 25 passed. The real local v2 fixture plans two files written under schema IDs 0/1, executes both canonical tasks, projects the current two-column schema, fills the evolved optional field for the old file, emits eight rows, and attests the fixed snapshot.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-memory --lib`: 22 passed, including pointer-identity and lease-lifetime proof for zero-copy foreign `Bytes` ownership.
- `CARGO_BUILD_JOBS=12 cargo check -p cdf-engine -p cdf-project`: passed, proving the external-task and prepared-CPU seams compose through generic engine/project consumers.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg -p cdf-task-store --lib --locked -j 12`: 26 Iceberg tests and 4 task-store tests passed; the slow million-task task-store law remains explicitly ignored in the fast tier. The local Iceberg fixture physically applies both delete forms across schema/spec evolution and proves five exact surviving rows plus jobs-invariant task/scan hashes.
- `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-source-iceberg -p cdf-task-store --all-targets --no-deps --locked -j 12 -- -D warnings`: passed. A dependency-inclusive invocation separately found an unrelated `needless_borrow` in the concurrently modified `cdf-runtime/src/watermark.rs`; I2 did not edit that worker-owned file.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg nonempty_snapshot_plans_canonical_tasks_independent_of_source_jobs --lib --locked -j 12`: passed. Beyond reader/delete results, the external task now traverses `CanonicalPartitionSchedule::scheduled_partition` and proves its compiled retry authority is `Partition`, preventing the prior unit/partition mismatch from reaching a real run.
- `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-source-iceberg --all-targets --no-deps --locked -j 12 -- -D warnings`: passed after the retry-boundary repair.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg nonempty_snapshot_plans_canonical_tasks_independent_of_source_jobs --lib --locked -j 12`: passed with both tasks reattested through `ResourceStream::attest_executable`; each yields its immutable table-snapshot position and the exact compiled schema hash before ordinary execution.
- `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-source-iceberg --all-targets --no-deps --locked -j 12 -- -D warnings`: passed after installing executable reattestation.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg nonempty_snapshot_plans_canonical_tasks_independent_of_source_jobs --lib --locked -j 12`: passed. The real fixture now additionally proves generic engine preview/run parity: two planned/eligible/opened external partitions, five preview rows, five packaged rows, two lineage partitions, only table-snapshot segment positions, and checkpoint-eligible execution evidence.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-kernel stratified_selection --lib --locked -j 12`: 4 passed. Ten thousand ordered candidates retain at most 64 task identities throughout selection and produce the exact ordinary v1 plan; permutation, edge, and single-member laws remain green.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-engine preview_large_plan_selects_and_opens_at_most_the_global_batch_budget --lib --locked -j 12` and `... preview_traverses_every_planned_partition_through_the_engine_front_end ...`: passed. The existing inline bounded-selection and full traversal behavior remains unchanged. A broader `preview_` filter had seven passes and one pre-existing planning failure in `preview_terminal_quarantine_uses_run_attestation_without_opening_payloads` (`effective schema discovery manifest does not match its pinned schema snapshot`) before preview execution; that unrelated fixture/authority drift is outside I2.
- `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-kernel -p cdf-engine -p cdf-source-iceberg --all-targets --no-deps --locked -j 12 -- -D warnings`: passed.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg nonempty_snapshot_plans_canonical_tasks_independent_of_source_jobs --lib --locked -j 12`: passed after one injected transient failure. The run consumed the failure, emitted five rows across the same two snapshot-positioned partitions, remained checkpoint-eligible, and exposed exactly one retry evidence record for partition ordinal zero with a selected delay and no exhaustion.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg --lib --locked -j 12`: 26 passed after the transport fault-injection tranche.
- `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-source-iceberg --all-targets --no-deps --locked -j 12 -- -D warnings`: passed after the transport fault-injection tranche.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg nonempty_snapshot_plans_canonical_tasks_independent_of_source_jobs --lib --locked -j 12`: passed with cancellation during task preparation and same-sized object replacement between prepared attempts. The cancelled package remained exactly `extracting`; the replaced generation failed before decode.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg --lib --locked -j 12`: 26 passed after cancellation and generation attestation.
- `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-source-iceberg --all-targets --no-deps --locked -j 12 -- -D warnings`: passed after cancellation and generation attestation.
- `/usr/bin/time -p env CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg manifest_cardinality_does_not_change_ledger_residency --lib --locked -j 12`: passed in 1.03 seconds of test execution. Ten thousand reverse-ordered manifests forced repeated 64 KiB spill-reservation growth and deterministic transaction replay, then traversed in canonical order while CDF ledger residency stayed exactly fixed; dropping the index returned both memory and spill usage to zero.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg --lib --locked -j 12`: 28 passed. This includes real v1 manifest-list decoding, the evolved v2 data/delete execution fixture, canonical external tasks, retry/cancellation/generation fault injection, spill-backed delete applicability, and the new high-cardinality manifest law.
- `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-source-iceberg --all-targets --no-deps --locked -j 12 -- -D warnings`: passed after the spill-backed manifest planner and transaction-replay correction.
- `git diff --check -- Cargo.lock crates/cdf-source-iceberg`: passed.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg rest_catalog_nonempty_snapshot_executes_canonical_local_objects --lib --locked -j 12`: passed. The REST binding negotiated embedded metadata exactly once, planned two canonical tasks from local manifest objects, read local Parquet/delete objects through the neutral transport, and produced five surviving rows without a third catalog request.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg --lib --locked -j 12`: 29 passed after nonempty REST execution coverage.
- `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-source-iceberg --all-targets --no-deps --locked -j 12 -- -D warnings`: passed after the REST execution tranche.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg v1_parquet_snapshot_discovers_plans_and_executes --lib --locked -j 12`: passed. The generated v1 metadata/manifest-list/manifest/Parquet table discovered one field, planned one external task with `table_format_version=1`, and executed three rows.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg --lib --locked -j 12`: 30 passed after adding real v1 execution.
- `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-source-iceberg --all-targets --no-deps --locked -j 12 -- -D warnings`: passed after the v1 execution tranche.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg nonempty_snapshot_plans_canonical_tasks_independent_of_source_jobs --lib --locked -j 12`: passed with a projected `label` package and unsupported `id > 4` residual. Two rows survive with values `[null, "eight"]`; package columns are exactly `label`, `_cdf_variant`, and `_cdf_package_row_ord`.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg --lib --locked -j 12`: 30 passed after the projection/residual integration law.
- `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-source-iceberg --all-targets --no-deps --locked -j 12 -- -D warnings`: passed after the projection/residual tranche.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg --lib --locked -j 12`: 32/32 passed after the FQ12-derived fixes. The main evolved/delete fixture now traverses the real `SourceRegistry` wrapper, and focused tests prove omitted manifest-header handling plus zero-copy field-ID column reordering.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-conformance resource:: --lib --locked -j 12`: 10/10 focused resource-capability and execution-conformance tests passed with the new typed table-snapshot capability.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-kernel table_snapshot --lib --locked -j 12`: 3/3 typed position and aggregation laws passed.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-benchmarks --lib --locked -j 12`: 12/12 benchmark-lab tests passed.
- `/usr/bin/time -p target/debug/cdf-p3-lab cdf-iceberg-package-worker <request>` against a generated local PyIceberg v2 table: the worker reported 1,000,000 rows, 10 partitions, 8 effective jobs, 0.370 seconds measured wall time, no spill, and a verified canonical package hash `sha256:16a0cf2195800d0d6868e4ac877260925495f6e29109c03011523dcc310a8fee`. Limit: debug-profile warm functional baseline, not a roofline claim.
- FQ12 live Glue/S3 → DuckDB: run `run-1e6d1d7d5096c0d4d4f7ea7c5984d5e2` committed package hash `sha256:c1c16fb3a7403b0a47a4fdb1cb47c00d1bc29e685e20c6cb3d47765be6073616`, 1,097 rows, four segments, zero quarantine, verified receipt, and exact DuckDB range `2023-07-06` through `2026-07-06` with 1,097 distinct provenance row keys. Wall time was 3.28 seconds; decode 0.939 seconds, validation 0.010 seconds, encode 0.004 seconds, persist/hash 0.030 seconds, and destination receipt 0.058 seconds. Credentials were supplied through `secret://env/CDF_FQ12_S3_OPTIONS`; no secret value entered a record or artifact.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-task-store million_tasks_hold_the_configured_metadata_budget --lib --locked -- --ignored`: passed in 2.89 seconds after the v2 one-pass trailer layout. One million task records retain the configured 64 KiB control-memory ceiling and fit the 256 MiB spill ceiling without a full-artifact finalize copy.
- Clean current-HEAD overlay: `CARGO_BUILD_JOBS=12 cargo test -p cdf-task-store -p cdf-source-iceberg --lib --locked -j 12`: 32 Iceberg and 4 fast task-store tests passed; the real evolved fixture carries unused historical schema 99 and asserts authority contains only task-required schema IDs 0/1.
- Clean current-HEAD overlay: `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-task-store -p cdf-source-iceberg --all-targets --no-deps --locked -j 12 -- -D warnings`: passed.
- `/usr/bin/time -lp target/release/cdf plan lake.transactions --no-pin` against read-only live FQ12 Glue/S3 `bronze.transactions`: 3.77 seconds wall, 0.75 seconds user CPU, 0.17 seconds system, 842,579,968-byte maximum RSS, 550,962,472-byte peak footprint, zero swaps. The canonical task artifact is 503,162 bytes versus the prior 17,625,448 bytes. The command reaches the unrelated DuckDB ICU timestamp contract after source planning, so this proves planning improvement and limits but not destination execution.
- Clean current-HEAD overlay after direct metadata parsing: `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg --lib --locked -j 12`: 34/34 passed, including filesystem/Glue identity equivalence, REST response reuse, bounded gzip decoding with complete logical-stream generation hashing, and branch/tag reference-kind projection across plain, gzip, and embedded REST JSON.
- Clean current-HEAD overlay: `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-source-iceberg --all-targets --no-deps --locked -j 12 -- -D warnings`: passed after direct metadata parsing.
- Three `/usr/bin/time -lp target/release/cdf plan lake.transactions --no-pin` runs against the same read-only FQ12 table after direct parsing: wall 2.64/3.45/4.08 seconds, user CPU 0.59/0.63/0.62 seconds, max RSS 440,352,768/442,957,824/521,486,336 bytes. All reach the unrelated DuckDB ICU contract after planning. A presigned `curl` GET of the same 28,150,038-byte S3 metadata generation measured 0.933590 seconds, 30,152,463 B/s, 0.086105-second connect, and 0.232526-second time-to-first-byte. Limit: the direct GET isolates only that object; CDF also contacts Glue, HEADs metadata, and reads the manifest graph.
- `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-kernel -p cdf-runtime -p cdf-conformance -p cdf-project -p cdf-source-iceberg -p cdf-cli -p cdf-benchmarks --all-targets --no-deps --locked -j 12 -- -D warnings`: passed.
- Clean current-HEAD overlay with Apache Iceberg PR 2649 pinned: `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg --lib --locked -j 12`: 37/37 passed. Focused execution coverage proves compiled field-ID projection, matching projected retry attestation, zero-copy top-level alignment, and nested child-metadata rebinding.
- Clean current-HEAD overlay: `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-source-iceberg -p cdf-runtime -p cdf-benchmarks --all-targets --locked -j 12 -- -D warnings`: passed with dependency lints enabled.
- `/usr/bin/time -lp target/debug/cdf-p3-lab cdf-iceberg-package-worker <FQ12 bronze.transactions request>` with 30 projected columns, 12 jobs, and `parquet_batch_rows=32768`: 13.11 seconds process wall, 11.415 seconds internally measured, 3,513,266 rows, 84 partitions, 137 canonical segments, and 1,042,939,904-byte maximum RSS. Generic source-I/O evidence recorded 201 total requests / 78,003,462 bytes across catalog discovery, planning, and execution; execution peaked at 12 concurrent requests and had zero retries or throttles. Limit: debug-profile local-host measurement; the separately governed EC2 release roofline remains required for the acceptance criterion.
- Broad-suite limit: a full `cdf-conformance` unit run reached an existing committed-golden mismatch and then aborted on a stack overflow in `duplicate_live_package_replay_is_noop_for_destination_and_mirrors`; the focused resource suite passed and no golden or unrelated test was weakened.
- Clean current-HEAD overlay: `CARGO_BUILD_JOBS=12 cargo test -p cdf-runtime --lib --locked -j 12` passed 142 tests with two explicit performance ignores, including the new metadata-order-independent canonical source-schema serialization law and all portable-worker protocol tests.
- Clean current-HEAD overlay: `CARGO_BUILD_JOBS=12 cargo test -p cdf-source-iceberg -p cdf-benchmarks --lib --locked -j 12` passed 38 Iceberg and 15 benchmark-lab tests. The Iceberg suite includes exact v1/v2 data/delete/evolution execution plus the remote/bounded/disableable whole-object policy; the lab includes a parallel projected multi-file Arrow-rs reference.
- Clean current-HEAD overlay: `cargo fmt --all -- --check` and dependency-inclusive `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-runtime -p cdf-source-iceberg -p cdf-benchmarks --all-targets --locked -j 12 -- -D warnings` passed.
- Release FQ12 A/B, exact same table snapshot/projection/jobs: exact ranges had medians of 5.954 seconds full wall and 2.979 seconds execute, 172 requests, 32,697,563 bytes, and 13.50 seconds summed request duration. The retained 2 MiB policy measured 4.883/4.932 seconds full wall and 2.209/2.224 seconds execute, 92 requests, 30,787,995 bytes, and 7.41/7.62 seconds summed request duration. All runs emitted 3,513,266 rows and 108 byte-identical segments with zero spill.
- Raw rooflines: five release Arrow-rs projected decodes over the local copies measured 0.0658–0.0687 seconds; the separately recorded 12-way S3 whole-object fetch measured 2.01 seconds. Limit: local Arrow is a warm CPU/codec roofline and the S3 fetch is a transport roofline; neither performs CDF evidence work. Optimized CDF execution is approximately 1.10x the remote transport roofline while performing the additional governed stages.
- `/usr/bin/time -lp` paired FQ12 runs: exact-range baseline 5.65 seconds / 1,005,780,992-byte maximum RSS / 849,757,720-byte peak footprint; 2 MiB prefetch 4.95 seconds / 885,080,064-byte maximum RSS / 728,974,752-byte peak footprint. Both had zero CDF spill. Lake Formation-specific authorization and snapshot-product conformance remain owned by G2/I3.

## Review

Fresh-hat adversarial review: pass. The review traced the complete diff from compiled option schema through scheduler memory admission, generation-bound object resolution, ledger-owned payload lifetime, upstream reader slicing, benchmark bias, and package identity. It corrected two pre-closure weaknesses: non-segment diagnostic artifacts are now selected against exact manifest segment paths rather than a directory-name convention, and the maximum execution working set has one checked computation shared by validation and capability compilation. It confirmed that the optimization is remote-only, size-bounded, explicitly disableable, generation-preserving, and byte-identical at the data-segment boundary; no generic runtime or engine branch names Iceberg/Glue. Residual risk is bounded and durably owned: I3 owns pre-no-op task planning, ancestry/time travel, and full product conformance; G2 owns Lake Formation; the 2,052-column unprojected sparse-schema cost is a distinct physical-representation workload rather than hidden here.

## Retrospective

The decisive technique was separating rooflines before optimizing: a 67 ms local Arrow decode falsified CPU/codec work, while summed request duration and a 2.01-second raw transfer exposed remote small-object latency. One bounded full fetch per small immutable object improved both wall time and RSS because it removed duplicated range-response lifetimes without inventing a cache. A threshold sweep mattered: fewer requests alone was not the objective, and 16 MiB transferred 38% more bytes without beating 2 MiB. The determinism failure likewise became tractable only after the lab reported non-segment identity digests; retained package comparison reduced a package-wide symptom to one randomized schema field. Future source performance work should preserve this order: phase telemetry, matching raw reference, mechanism-specific knob, measured sweep, exact artifact comparison, then default selection.
