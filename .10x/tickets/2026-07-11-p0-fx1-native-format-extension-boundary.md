Status: active
Created: 2026-07-11
Updated: 2026-07-13
Parent: .10x/tickets/2026-07-05-implement-cdf-system.md
Depends-On: .10x/tickets/done/2026-07-11-p0-dx1-neutral-runtime-crate.md, .10x/tickets/done/2026-07-11-p3-a2-unified-memory-ledger.md, .10x/tickets/done/2026-07-11-p3-a4-injected-execution-host.md, .10x/specs/native-format-codec-runtime.md

# P0 FX1: native format extension and byte-source boundary

## Scope

Add neutral byte-source/transform/format registry contracts, migrate declarative format configuration from closed enums to registry-validated ids/options, split first-party codec build domains, and prove one mock external codec can discover/plan/preview/run through local and remote byte sources without generic orchestration edits. Preserve required behavior while deleting superseded dispatch and compatibility surfaces; decoder optimizations and new formats remain WS-B children.

## Acceptance criteria

- Generic declarative/file runtime contains no first-party format match tree; format selection/discovery/decode route through a registry.
- Byte-source/codec contracts expose no Tokio/object-store/HTTP/filesystem/parser types and use accounted envelopes.
- Physical decode and shared schema reconciliation are separate; discovery/preview/run use one driver interpretation.
- Logical-file decode units preserve whole-file `FileManifest` completion and deterministic order.
- Existing Parquet/CSV/JSON/NDJSON/Arrow IPC behavior passes compatibility conformance.
- Parser dependencies are codec-local; changing/building one codec does not force unrelated codec implementation crates.
- A mock codec and transform register with only composition/catalog/fixture edits and pass shared laws.

## Evidence expectations

Cargo graph/rebuild evidence, architecture static tests, registry/config schema migration, mock codec/transform, local/remote/spool conformance, artifact/golden compatibility, malformed/cancellation/memory tests, and adversarial extension review.

## Explicit exclusions

No new parser dependency, new native format, optimized decoder, dynamic plugin ABI, archive container, or distributed decode.

## Blockers

None external. The 2026-07-13 aggregate adversarial review failed closure. The pinned-`Hints` governance defect is fixed. Remaining in-scope blockers are: replace first-party format branches with descriptor capabilities; bind codec version/options/detection/unit policy into the executable plan; delete the surviving monolithic `cdf-formats` parser/dispatch surface; and prove a project-level external codec over a remote provider. These are FX1 acceptance work, not deferred follow-ups.

## References

- `.10x/decisions/native-format-driver-and-byte-source-boundary.md`
- `.10x/research/2026-07-11-format-extension-streaming-audit.md`
- `.10x/specs/native-format-codec-runtime.md`
- `.10x/knowledge/source-destination-extension-invariant.md`
- `.10x/decisions/datafusion-analysis-scheduling-identity-boundary.md`
- `.10x/specs/datafusion-currency-bridges.md`
- `.10x/specs/single-crossing-schema-admission.md`

## Progress and notes

- 2026-07-11: Landed the neutral runtime foundation: immutable `ByteSource` identity/capabilities with accounted sequential and exact-range reads; `FormatDriver` discovery/unit-planning/physical-decode contracts; streaming `ByteTransformDriver`; accounted physical Arrow outcomes; and deterministic format/transform registries with pre-mutation id/alias/extension/strong-magic collision checks. Descriptors pin semantic version, options, detection, pushdown fidelity, unit policy, memory, random-access, checksum, member, and expansion claims. No concrete transport, executor, parser, project, CLI, source, or destination type crosses the boundary. Production first-party/declarative migration remains open; the old dispatch is not being retained as a final compatibility surface. Evidence: `.10x/evidence/2026-07-11-p0-fx1-neutral-format-contracts.md`.
- 2026-07-11: Extracted the first parser-local implementation into `cdf-format-parquet`. It uses only the neutral runtime contracts; performs footer discovery, row-group unit planning, projection, bounded-concurrency async range reads, and incremental Arrow decode; and retains source ledger leases through Parquet's owner-backed `Bytes` lifetime. A real Parquet fixture passes detect/discover/plan/decode through a mock external byte source with zero residual memory. Production composition and deletion of the monolithic path remain open. Evidence: `.10x/evidence/2026-07-11-p0-fx1-parquet-driver-extraction.md`.
- 2026-07-11: `cdf-source-files::LocalByteSource` now supplies the first real neutral provider with accounted zero-copy chunks/ranges, cancellation, and strong Unix generation reattestation. Format drivers can consume local bytes without receiving paths or file handles. Production registry selection remains open. Evidence: `.10x/evidence/2026-07-11-p3-g1-local-accounted-byte-source.md`.
- 2026-07-11: Moved physical-to-effective Arrow materialization into `cdf-contract` and made engine preview/run execute the typed per-observation plan when a codec emits an unadorned physical batch. Projection, widening, missing nullable fields, and provenance now have one format-neutral implementation; source metadata injection remains rejected. Accounted ownership for cast allocations and production registry routing remain open. Evidence: `.10x/evidence/2026-07-11-p0-fx1-shared-schema-materialization.md`.
- 2026-07-12: The DataFusion currency boundary preserves native primary codecs and permits a future DataFusion `FileFormat` only as an optional FX1 driver for exotic formats. `.10x/tickets/2026-07-12-p3-j6-datafusion-selective-adoption-audit.md` owns measurement and semantics parity; FX1 must not expose DataFusion types or add a generic runtime branch for it.
- 2026-07-12: Remote/local binary discovery now resolves the same registered driver and optional transform as execution. Deleted the project `format == parquet` branch, declarative Parquet probe APIs, `cdf-formats` discovery engine/`RangeChunkReader`, and old-behavior tests. Format-specific evidence travels through a collision-checked neutral observation map. Evidence/review: `.10x/evidence/2026-07-12-fx1-registry-driven-remote-discovery.md`, `.10x/reviews/2026-07-12-fx1-registry-driven-remote-discovery-review.md`.
- 2026-07-11: Added one kernel-neutral `PayloadRetention` primitive and moved native physical-batch leases into it when entering the kernel stream. The same primitive replaced the destination-only retention type, so source and commit boundaries share one lifetime mechanism with no `cdf-memory` dependency in the kernel and no compatibility alias. Evidence: `.10x/evidence/2026-07-11-p0-fx1-accounted-batch-retention.md`.
- 2026-07-11: Production local and remote-spooled Parquet now resolve through the CLI-composed `FormatRegistry` and stream from the injected I/O scope through the generic registered-format path. Dependency-free file runtime constructors and compiler-owned file execution functions were deleted. CLI local/HTTP runs pass. TLC medians regressed from about 1.53/1.62 wall/CPU seconds to 1.63/1.80, so B2 remains explicitly responsible for eliminating local neutral-range overhead; unregistered format fallback also remains before FX1 closure. Evidence: `.10x/evidence/2026-07-11-p0-fx1-production-parquet-registry-stream.md`.
- 2026-07-11: A second production format, Arrow IPC file framing, now uses the same registry-selected local/remote-spool execution path without a format-specific execution branch. Its parser dependency is isolated in `cdf-format-arrow-ipc`; the former local IPC reader and remote hard rejection were deleted from `cdf-source-files`. This proves the generic driver seam across two binary formats, but FX1 remains open because schema attestation and CSV/JSON/NDJSON still contain closed-enum/fallback dispatch. Evidence: `.10x/evidence/2026-07-11-p3-b3-native-arrow-ipc-file-driver.md`.
- 2026-07-12: Hardened the neutral byte-transform boundary so implementations can satisfy the ledger rather than manufacture unaccounted output. `ByteTransformRequest` binds allocation owner, output chunk, expanded bytes, ratio, planned input size, and cancellation; the former signature was deleted. First-party transform implementations and registry-driven file composition remain open. Evidence: `.10x/evidence/2026-07-12-p3-b1-transform-allocation-authority.md`.
- 2026-07-12: Proved the byte-transform extension seam with `cdf-transform-gzip`, a parser-dependency-isolated leaf crate implementing the neutral driver contract without importing project, source, transport, destination, or sibling codec crates. Product composition remains open and must occur only in the standard registry. Evidence: `.10x/evidence/2026-07-12-p3-b1-native-gzip-driver.md`.
- 2026-07-12: A second independent transform (`cdf-transform-zstd`) now uses the same neutral cursor, expansion guard, driver request, and registry descriptor without edits to source/project orchestration. This validates the seam beyond a single implementation; standard product composition and static add-a-codec checks remain open. Evidence: `.10x/evidence/2026-07-12-p3-b1-native-zstd-driver.md`.
- 2026-07-12: Added runtime-neutral transform/source composition. `TransformedByteSource` turns any registered transform plus any `ByteSource` into a sequential byte source with correctly reduced capabilities and generation identity; codec/source crates need no transform-specific adapter. The standard product registry is still the only remaining composition edit. Evidence: `.10x/evidence/2026-07-12-p3-b1-transformed-byte-source.md`.
- 2026-07-12: Removed the hash-only physical decode request. `PhysicalDecodeRequest` now carries the exact planned physical `SchemaRef`; drivers derive its canonical hash for attestation and batch headers. The file source resolves that schema from `EffectiveSchemaRuntime`'s catalog and groups hash/schema as one authority value, falling back to bounded discovery only when no catalog schema exists. This unblocks row codecs from re-inferring types at execution. Evidence/review: `.10x/evidence/2026-07-12-fx1-physical-decode-schema-authority.md`, `.10x/reviews/2026-07-12-fx1-physical-decode-schema-authority-review.md`.
- 2026-07-12: A third independent transform (`cdf-transform-snappy`) uses the same driver, request, cursor, expansion, and accounting contracts while adding only Snappy framing/parser/checksum logic. The shared cursor gained bulk exact-read/skip operations after the release benchmark rejected byte-at-a-time polling; gzip/zstd can reuse these operations for future framing work. Evidence: `.10x/evidence/2026-07-12-p3-b1-native-snappy-framed-driver.md`.
- 2026-07-12: A fourth independent transform (`cdf-transform-lz4`) adds linked-block dictionary and multi-checksum behavior entirely inside its leaf crate. Runtime/source contracts required no new codec-specific method or branch, and LZ4 Flex/xxhash remain local dependencies. Evidence: `.10x/evidence/2026-07-12-p3-b1-native-lz4-frame-driver.md`.
- 2026-07-12: A fifth independent transform (`cdf-transform-brotli`) adds a magic-less strict-window state machine entirely inside its leaf crate, again without changing runtime/source orchestration. This exercises descriptor-based explicit/extension detection and native state admission on the same seam. Evidence: `.10x/evidence/2026-07-12-p3-b1-native-brotli-driver.md`.
- 2026-07-12: A sixth independent transform (`cdf-transform-bzip2`) adds a new low-level decoder dependency and concatenated-member semantics entirely inside its leaf crate. No runtime/source API or codec branch changed. Evidence: `.10x/evidence/2026-07-12-p3-b1-native-bzip2-driver.md`.
- 2026-07-12: A seventh independent transform (`cdf-transform-xz`) adds an FFI/native static dependency, hard decoder memlimit, and concatenated terminal semantics entirely inside its leaf crate. No runtime/source API or codec branch changed; dependency review removed ambient system linking. Evidence: `.10x/evidence/2026-07-12-p3-b1-native-xz-driver.md`.
- 2026-07-12: The complete character family (`text_auto`, UTF-8, UTF-16LE/BE, Windows-1252, ISO-8859-1) now shares one dependency-free leaf implementation and produces normalized UTF-8 through the same transform contract. Generic runtime/source code required no encoding enum or branch. Evidence: `.10x/evidence/2026-07-12-p3-b1-character-transforms.md`.
- 2026-07-12: Established the single product composition point for all first-party transforms. `cdf-source-files` now receives only `ByteTransformRegistry`; implementation crates terminate at `cdf-cli`, and neutral id/extension/magic lookup requires no source match tree. Execution selection and old dispatch deletion remain open. Evidence: `.10x/evidence/2026-07-12-p3-b1-standard-transform-registry.md`.
- 2026-07-12: Removed the closed compression enum and all monolithic gzip/zstd decoder fallback. Registry descriptors now own transform detection; source execution composes `ByteSource -> ByteTransformDriver -> verified spool -> FormatDriver`, and row discovery moved out of the declarative compiler. One mock/add-a-codec law and compressed binary probe parity remain before FX1 closure. Evidence: `.10x/evidence/2026-07-12-p3-b1-registry-transform-execution.md`.
- 2026-07-12: Compressed Parquet/Arrow discovery, attestation, and execution now share a source-owned composed probe, including exhaustive multi-file pinning. Remote Arrow IPC discovery no longer hard-rejects. Review removed a nested runtime call and made the blocking-transport/async-data-plane boundary explicit. The mock external codec/transform law and remaining monolithic format fallbacks still block FX1 closure. Evidence: `.10x/evidence/2026-07-12-p3-b1-compressed-binary-parity.md`.
- 2026-07-12: Replaced the closed five-variant file-format declaration with a validated transparent registry id. Existing TOML strings and standard constructors remain source-compatible internally, but arbitrary external ids now parse, serialize, compile into physical plans, and fail only if absent from the injected product registry. Generic diagnostics render the dynamic id. No legacy enum or unknown-value shim remains. Project discovery still has first-party adapter dispatch; the next slice must route discovery through `FormatDriver` before the external end-to-end law can close. Evidence/review: `.10x/evidence/2026-07-12-p0-fx1-open-format-declaration.md`, `.10x/reviews/2026-07-12-p0-fx1-open-format-declaration-review.md`.
- 2026-07-12: Local registered-format discovery now resolves and invokes the injected `FormatDriver::discover` contract for every non-row format id, records driver version/content identity, and no longer calls Parquet/Arrow helpers from project dispatch. The private adapter collapsed from five concrete binary variants to one registry-backed variant plus the three still-legacy row formats. Remote registered formats share the same path after a verified spool, except uncompressed Parquet retains its bounded-range probe until G1 supplies neutral remote `ByteSource`. Evidence/review: `.10x/evidence/2026-07-12-p0-fx1-registered-format-discovery.md`, `.10x/reviews/2026-07-12-p0-fx1-registered-format-discovery-review.md`.
- 2026-07-12: Added an executable external-extension law with test-only `external_mock` format and `external_passthrough` transform implementations. They register only through neutral registries, resolve an explicit `.mock.mt` resource, compose transform-to-spool, discover through the driver, plan/decode through the generic registered stream, preserve `FileManifest`, and release the ledger to zero. Production runtime contains no knowledge of either id. Remote provider and project-level add/pin coverage remain before full FX1 closure. Evidence/review: `.10x/evidence/2026-07-12-p0-fx1-external-codec-transform-law.md`, `.10x/reviews/2026-07-12-p0-fx1-external-codec-transform-law-review.md`.
- 2026-07-12: Extracted NDJSON into dependency-isolated `cdf-format-json` and registered it at composition roots. Source execution and project discovery now use the neutral driver; the source fallback rejects NDJSON instead of retaining the old full-file path. Bounded discovery retains accounted input chunks without duplicating the sample, while execution feeds Arrow JSON's tape decoder incrementally and reserves output authority before decode. Local and gzip object-store discover/pin/run fixtures pass. CSV and JSON-document fallbacks still block FX1. Evidence/review: `.10x/evidence/2026-07-12-p3-b5-streaming-ndjson-driver.md`, `.10x/reviews/2026-07-12-p3-b5-streaming-ndjson-driver-review.md`.
- 2026-07-12: Extracted CSV into parser-local `cdf-format-delimited`, added a neutral lease-owning `AccountedChunksReader` shared by row-format discovery, and routed CSV discovery/execution through the injected registry. The source fallback rejects CSV, the old project CSV adapter and legacy test expectation are deleted, and local discovery/run preserves manifest position and returns the ledger to zero. Only JSON-document source fallback remains in FX1's original five-format live path. Evidence/review: `.10x/evidence/2026-07-12-p3-b4-streaming-csv-driver.md`, `.10x/reviews/2026-07-12-p3-b4-streaming-csv-driver-review.md`.
- 2026-07-12: Extracted top-level JSON documents into `cdf-format-json::JsonDocumentFormatDriver` with streamed, memory-authorized object framing into Arrow's tape decoder. Project discovery and source execution now use that driver, and the final source-owned `compile_format`/declared-schema decoder fallback was deleted: every original first-party format must resolve through `FormatRegistry`. One-byte rechunking, bounded complete-record sampling, malformed trailing-comma, live source, project discovery, strict Clippy, and static no-fallback checks pass. FX1 remains open for remote external-provider/add-pin coverage and aggregate closure evidence. Evidence/review: `.10x/evidence/2026-07-12-p3-b5-streaming-json-document-driver.md`, `.10x/reviews/2026-07-12-p3-b5-streaming-json-document-driver-review.md`.
- 2026-07-12: `FileTransport::open_byte_source` now supplies a provider-neutral remote extension point, and the first implementation streams/ranges object stores directly into the same registered format drivers used locally. Runtime does not branch by format or cloud provider; unavailable providers fall back to the verified spool. Direct Arrow IPC and ranged Parquet fixtures prove the remote seam. FX1 still needs an external mock provider through project add/pin and the aggregate closure audit. Evidence/review: `.10x/evidence/2026-07-12-p3-g1-object-store-byte-source.md`, `.10x/reviews/2026-07-12-p3-g1-object-store-byte-source-review.md`.
- 2026-07-13: Repaired the registry migration's missing physical-to-declared reconciliation stage at the engine boundary, not in the file adapter. A source that emits a physical batch without precompiled observation evidence is now constrained once by the engine using the resource's trust/type policy; width widening and opt-in parse coercion produce the same serialized verdict plan for files, SQL, REST, and future drivers. `cdf-source-files` retains no contract dependency or coercion implementation. Arrow IPC now truthfully declares seekable access, so transformed inputs take the generic verified-spool policy instead of failing through an impossible sequential contract. Local and remote discovery identities again include the canonical schema hash, and compressed/multi-file/HTTP Arrow discovery parity is executable. Adversarial review rejected this execution-derived plan as the final architecture: the active P2 decision requires declared observations/verdicts in the compiler artifact. The fallback remains a fail-closed regression repair only until FX1 binds declared observations at plan time and deletes it. Evidence: `.10x/evidence/2026-07-13-fx1-registered-schema-reconciliation.md`.
- 2026-07-13: Generalized effective-schema evidence over explicit declared and pinned baselines, made runtime file observation exhaustive, propagated resource coercion allowances into planning, and deleted execution-derived reconciliation. Declared Arrow IPC widening, opt-in NDJSON parse coercion, and two-file Parquet widening now succeed solely through partition-bound compiler evidence; missing evidence fails closed. The project auto-pin helper now rehydrates its published snapshot and returns a plan-ready resource, proven through an injected HTTP provider from discovery through DuckDB commit and manifest checkpoint. Evidence: `.10x/evidence/2026-07-13-fx1-compiler-bound-schema-observations.md`.
- 2026-07-13: Reconciled the aggregate review's critical pinned-hints finding. Schema-source pin/unpin transitions now preserve hints provenance; project and ad-hoc lock hydration, `--no-pin`, ordinary pinned preparation, and explicit repinning consume that shared transition. The CLI regression plans unchanged and drifted Parquet observations after the first pin and asserts the original lock/snapshot bytes remain unchanged. Evidence: `.10x/evidence/2026-07-13-fx1-pinned-hints-governance.md`.
- 2026-07-13: Deleted the dependency-free local file discovery/autopin entry points and migrated the project test corpus onto resolved file runtimes with explicit codec/transport dependencies. New files now trigger fresh compiler observations in the incremental fixture; validation-rule mutations rebind `CompiledExpressionPlan`; destination tests bind the selected sheet; obsolete DuckDB receipt and long-name assumptions were removed. The project suite moved from 136/178 before this work to coverage of all 177 current tests, with strict Clippy green. Evidence: `.10x/evidence/2026-07-13-fx1-resolved-file-runtime-regression-sweep.md`.
- 2026-07-13: The user established the single-crossing expensive-source invariant while FX1 was replacing descriptor branches. FX1 now pins driver version, canonical options, detection probe policy, access/unit policy, and working-set claims so SA1 can compile deferred admission. Generic file inventory must consume descriptor extensions without pre-reading payload; FX1 must not close by replacing concrete pre-probes with a generic pre-probe loop. Runtime fused admission/cache work is owned by `.10x/tickets/2026-07-13-p0-single-crossing-schema-admission.md` rather than hidden here.
- 2026-07-13: Moved source compilation out of the declarative grammar crate: it now emits a neutral request and product composition owns driver resolution. Added runtime-neutral `CompiledFormatBinding` so file source artifacts pin the complete driver descriptor plus canonical options and fail if execution registers different semantics. Project discovery collapsed to one registered-format adapter with dynamic option-hash evidence; remote inventory stopped reading format/compression magic and a transport counter proves zero payload ranges. Old tests that encoded pre-read confirmation now assert deferred fail-closed admission. Local content hashing, fused first-window admission, the remaining declarative inference branch, monolithic `cdf-formats`, and remote external-codec coverage remain open under their recorded owners. Evidence: `.10x/evidence/2026-07-13-fx1-compiled-format-binding-metadata-inventory.md`.
- 2026-07-13: Began deleting the monolithic parser aggregation boundary by moving shared `ReadOptions` into neutral `cdf-runtime`. File/REST sources, Python/subprocess adapters, conformance, and benchmarks now depend on the runtime contract directly; `cdf-formats` is only a temporary consumer while its remaining eager/closed/parser surfaces are removed. Focused build and library suites remain green. Evidence: `.10x/evidence/2026-07-13-fx1-runtime-owned-read-options.md`.
- 2026-07-13: Deleted the format crate's duplicate schema-hash and validation-program wrappers and the declarative crate's unused local Arrow IPC discovery compatibility APIs. Canonical Arrow hashing is kernel-owned, validation compilation is contract-owned, and live discovery remains driver-backed. `cdf-declarative` and `cdf-source-files` dropped their `cdf-formats` dependency edges; the remaining consumers are explicitly inventoried for the final parser-facade deletion. Evidence: `.10x/evidence/2026-07-13-fx1-schema-wrapper-and-declarative-shim-deletion.md`.
- 2026-07-13: Removed `cdf-python` from the monolithic format facade. Its dict fallback now flushes fixed row windows during iteration, rejects windows above the configured byte boundary, and decodes through its owned Arrow JSON boundary before joining the common Python Arrow batch path. The old implementation accumulated the full iterable and the IPC test kept a compatibility reader live; both are gone. P3 H2 still owns fully incremental, ledger-accounted Arrow C/row production and is not misreported as complete. Evidence: `.10x/evidence/2026-07-13-fx1-python-row-window-decoupling.md`.

## Review

- 2026-07-13 aggregate adversarial review — **Verdict: fail**.
  - **Critical:** a `Hints` resource pinned on its first command recompiles as unpinned hints on the next command because CLI lock hydration recognized only `Discover`; this could republish the governed baseline instead of treating drift as a contract event.
  - **Significant:** project/declarative/source generic layers still contain first-party format allowlists, metadata cases, sampling restrictions, and Parquet/Arrow-only attestation paths rather than consuming registry descriptors.
  - **Significant:** executable file plans do not pin driver semantic version, canonical options, detection evidence, and decode-unit policy; execution repeatedly constructs empty options and can observe a registry implementation different from the one planned.
  - **Significant:** `cdf-formats` still links the original parser family and retains closed `FileFormat`/eager `FormatRead` dispatch while production crates depend on it, so parser build domains are not actually isolated.
  - **Residual risk:** the external codec law is local-only; the existing HTTP provider law uses first-party Parquet and does not prove external codec composition through the project remote path.
  - 2026-07-13 reconciliation: the critical pinned-hints finding is fixed and covered by `.10x/evidence/2026-07-13-fx1-pinned-hints-governance.md`; the aggregate verdict remains fail while significant findings and residual risk remain.

## Retrospective

Pending implementation completion and passing aggregate review.
