Status: active
Created: 2026-07-11
Updated: 2026-07-19
Parent: .10x/tickets/2026-07-10-p3-ws-h-interop-boundaries.md
Depends-On: .10x/tickets/done/2026-07-11-p3-h1-interop-measurement-copy-proof.md, .10x/tickets/done/2026-07-11-p3-a4-injected-execution-host.md, .10x/tickets/done/2026-07-11-p3-a2-unified-memory-ledger.md

# P3 H2: incremental Python Arrow/row boundary

## Scope

Replace `PythonBatchRead`/C-stream materialization in production with the neutral incremental producer, live memory/backpressure/cancellation/lane integration, real Arrow C lifetime/copy evidence, adaptive direct row conversion, shared reconciliation, and GIL/free-threaded measurement.

## Acceptance criteria

- Python resources can emit data exceeding the configured memory budget by orders of magnitude while every CDF-owned/imported payload and conversion window remains globally leased and bounded. Producer-native allocations made before an in-process callable yields are not an enforceable H2 boundary and MUST remain explicitly unclaimed.
- Real PyArrow C Array/Stream paths cover type/lifetime/error/cancellation cases and verified copy labels.
- Dict rows retain only one accounted conversion window and are never whole-resource JSON collections.
- Schema variance uses the shared contract policy; no Python-local competing schema truth remains.
- GIL/free-threaded concurrency uses declared execution-host lanes and produces deterministic fixed-input packages.

## Evidence expectations

Real interpreter/PyArrow matrices, memory traces, copy probes, backpressure/cancellation/error tests, package hashes, before/after throughput, and adversarial FFI/lifetime review.

## Explicit exclusions

No arbitrary Python execution inside engine operators, untrusted sandbox claim, or enforceable process-memory bound for producer-native allocations made before an in-process callable yields. H3 owns force-authorized process isolation; H5 owns calibrated native-scratch/RSS envelope evidence and descriptor headroom.

## Blockers

None. H1, A4, and A2 are done; this ticket is executable.

## References

- `.10x/specs/foreign-stream-interop.md`

## Journal

- 2026-07-18 — Activated after H1 closeout. First slice migrated the internal Python iterator emission boundary onto IX1 `ForeignBatchOutcome` values. Existing materializing collectors now wrap the neutral outcome stream instead of owning a second Python-local semantics, and production `PythonResource::execute_stream` consumes neutral outcomes before sending ordinary runtime batches.
- 2026-07-18 — Removed the now-dead `visit_python_iterable` internal wrapper instead of suppressing dead-code warnings. The remaining internal boundary is `visit_python_foreign_iterable`; compatibility materialization still exists only through public collector APIs and remains to be burned down in later H2 slices.
- 2026-07-18 — Migrated direct JSON/dict-row helpers to the same neutral outcome visitor. `batches_from_json_dict_rows` is now a collector wrapper over `visit_json_dict_rows`; dict-row conversion has one batch-to-foreign-outcome conversion path shared with Python iterator execution.
- 2026-07-18 — Bounded audit of `pyo3-arrow 0.19.0` found import/export C Data surfaces and buffer-protocol comments, but no ready-made allocation/copy-proof signal. H2 must keep Arrow C outcomes `copy_unknown` until a real pointer/lifetime/allocation probe is implemented; do not infer zero-copy from use of the crate alone.
- 2026-07-19 — Removed the production `PythonBatchRead`, `BoundaryChannel`, DLT batch collectors, and deterministic batch-vector hash helpers. The bridge now emits one neutral outcome at a time and returns metadata-only `PythonStreamSummary` values; capped collection exists only in tests. Python compiled plans now retain the shared effective-schema runtime and policy allowances, and differing physical schemas cross as separate observations for ordinary engine reconciliation instead of failing in a Python-local one-schema rule.
- 2026-07-19 — Made dict compatibility conversion directly incremental over one NDJSON byte window, including the transient serialized row and decoded Arrow allocation in the boundary peak. The byte knob can flush a window before the row-count target, while exponential bounded capacity growth avoids a reallocation per row. `PythonStreamSummary.peak_boundary_bytes` exposes the observed retained peak. Objects implementing both Arrow protocols now take bounded C Array import first; tables/readers remain incremental C Stream imports.
- 2026-07-19 — Added a dedicated ignored PyArrow 25 matrix so fast checks do not acquire a heavyweight Python dependency. Against real PyArrow 25.0.0 on CPython 3.12, C Array and C Stream imports retained aliasing producer/imported buffer ranges across primitives, strings/slices, lists, structs, dictionary arrays, decimal128, and timezone timestamps; imported values survived producer deletion/GC; mixed C Array schemas remained distinct observations; stream callback cancellation stopped after one producer pull; and an exception between stream batches propagated after exactly one emitted outcome. Production labels remain conservatively `copy_unknown`; the matrix records the verified PyArrow cells without adding pointer-probe overhead to the hot path.
- 2026-07-19 — Made the two boundary controls compiled source inputs: `dict_batch_rows` and `max_boundary_bytes`. Both reject zero, serialize into the physical plan and redacted compiled options, inform execution capabilities and memory admission, and name both remedies when a producer batch exceeds the configured window. There is no hard product ceiling; the 64 MiB byte value is a configurable default. Direct project references use the same defaults, while source declarations can tune both values.
- 2026-07-19 — Measured the dict compatibility curve in release mode over one million two-field rows: 1,024 rows/window took 472 ms (2.117 M rows/s, 977 outcomes, 68,894-byte peak); 8,192 took 466 ms (2.142 M rows/s, 123 outcomes, 549,150-byte peak); 65,536 took 464 ms (2.154 M rows/s, 16 outcomes, 4,391,198-byte peak). Ratified 8,192 as the default because it removes 87.4% of downstream outcomes for a measured 1.2% throughput gain without paying the 8x retained-window increase of 65,536 for only another 0.6%. The one-million-row constant-memory case retained less than 8 MiB while total emitted bytes exceeded the peak by more than 100x.
- 2026-07-19 — Repaired a registry-boundary regression exposed by the Python product tests: lazily compiled direct project references disappeared from `cdf doctor` resource counts. `SourceRegistry::health_checks` now receives generic configured references, resolves their owning driver, deduplicates them with compiled plan ids, and gives every driver the same configured-resource inventory. The CLI contains no Python-specific counting branch. Compiler-time Python reference handling no longer validates execution-host interpreter options; bounded health and resolution remain the authorities for execution-environment compatibility.
- 2026-07-19 — Completed the interpreter matrix. CPython 3.12/GIL used `GilSerialized` with effective parallelism 1 and observed peak 1; CPython 3.14.6 free-threaded used `FreeThreadedParallel` with effective parallelism 2 and observed peak 2. Both produced fixture hash `sha256:a8882bfa14934b40fbdc4ac36b8eefe0139bc78a3666d40a15c23a65ccb46f65`. Full CLI plan→preview→run→replay passed under both runtimes and both produced canonical package-segment data hash `sha256:6203b3b40a1af3c6f6220032a1f79dfecce2040644321569ef3f1a92c6d34e06`. Full manifest hashes are not the comparison authority because receipt/lifecycle artifacts vary by run.
- 2026-07-19 — The first consolidated adversarial review correctly failed closure: the interpreter-aware lane test bypassed production registry resolution; dict inference/decode still ran while attached to the GIL; the watchdog surface was inert; direct references could not tune boundary controls; the memory stress bypassed the global coordinator; Python/Arrow exception details were unredacted; and the real FFI matrix omitted large cross-thread lifetime coverage. The review also identified that actual transfer/copy reporting remains H5 work.
- 2026-07-19 — Repaired execution admission at the generic source boundary rather than adding Python orchestration. `SourceDriver::bind_blocking_lane` may only tighten a compiled lane, `SourceRegistry` validates and installs that binding, and scheduler resolution consumes the installed host-bound ceiling. Python plans retain interpreter-independent identity: the source ceiling is unbounded unless the resource disables parallel partitions, while the shared interpreter lane binds to one worker on GIL builds or all admitted host CPU slots on free-threaded builds. The production registry→host→scheduler test now proves GIL jobs 1 and free-threaded jobs greater than 1; both retain the identical fixture hash.
- 2026-07-19 — Moved all expensive Rust-side work (Arrow batch wrapping/accounting, dict schema inference/Arrow JSON decode, downstream memory admission/send) outside the Python lock while retaining Python iteration and `json.dumps` under the interpreter. Removed the full `serde_json::Value` reparse of a value already proven to be a Python dict. The post-repair release curve improved the default 8K window from the previous 466 ms / 2.142 M rows/s to 454 ms / 2.201 M rows/s; the 1K and 64K cells were 473 ms / 2.111 M rows/s and 461 ms / 2.165 M rows/s.
- 2026-07-19 — Removed the unused watchdog type, option, module, and deleted boundary-channel vocabulary. In-process Python is honestly `CooperativeOnly`: cancellation is observed at yielded outcomes and backpressure boundaries, while a callable blocked before yielding cannot be safely killed in-process. H3 owns the isolated subprocess path for force-authorized termination; no fake timeout claim remains in H2.
- 2026-07-19 — Direct `python://` mappings now take `dict_batch_rows` and `max_boundary_bytes` from governed `[python]` project options, compile both into hash-bearing physical/redacted plan inputs, and reject zero. Python exceptions now publish only a sanitized exception class plus local-inspection guidance; Arrow C stream errors also discard raw producer exception text. The PyArrow matrix adds a >2 MiB binary payload whose buffers alias after import, survive producer deletion/GC, and are destroyed on a downstream Rust thread.
- 2026-07-19 — Added a one-million-row production-spine stress using the resolved Python resource, host blocking lane, bounded stream, and shared DataFusion memory coordinator under an exact 128 KiB budget. It exposed and repaired a coordinator release race: pool capacity became visible before the CDF snapshot released the old lease, allowing an impossible telemetry peak above budget. Pool release and snapshot release now share the coordinator lock; the production stress completes with zero retained bytes, a peak no greater than budget, and observed reservation waits proving backpressure.
- 2026-07-19 — The final two-slice adversarial review failed closure rather than accepting the outcome-only bridge as the neutral contract. Architecture review found that production did not implement `ForeignProducer`/events/terminal authority, scheduler admission could fall back to the portable Python ceiling before runtime resolution, and the concurrency test invented 64 partitions although a Python resource plans one. Safety review found uncancellable memory admission, post-import oversized Arrow evidence gaps, payload-bearing Arrow JSON diagnostics, double-counted poll/decode working-set declarations, and synthetic rather than production-stream concurrency evidence.
- 2026-07-19 — Repaired the architecture at the shared seams. `PythonResource` now implements `ForeignProducer`; production `ResourceStream::open` invokes that trait, consumes the neutral event stream, preserves outcomes through the foreign boundary, and owns invocation termination independently. Every opened producer publishes exactly one typed terminal status on normal/error/cancel paths. `ForeignCancellation` gained wakeable, unregistering cancellation futures so memory admission can select between both run and foreign cancellation without polling. Python's runtime event producer attaches cursor/foreign-state authority before emission and keeps the shared schema/reconciliation path unchanged.
- 2026-07-19 — Made portable lane resolution explicit and fail-closed with `BlockingLaneBinding::{Static, RuntimeResolvedRequired, RuntimeResolved}`. The naming records what actually happens: PyO3 embeds one interpreter in-process; execution resolves the portable plan against that interpreter's GIL mode and the machine/cgroup CPU authority. Scheduler admission now rejects a required-but-unresolved lane. The superseded `bind_blocking_lane` name and host-bound vocabulary were deleted rather than retained as aliases.
- 2026-07-19 — Removed the false per-resource partition-parallel claim and the unused `__cdf_parallel__` custom-resource metadata. One Python resource invocation plans one partition and advertises source concurrency one; the shared runtime-resolved lane governs concurrency across independent invocations. The interpreter matrix now opens and fully drains two real resolved production streams. Free-threaded CPython uses a shared Python barrier that cannot complete unless both foreign producers are admitted concurrently; GIL CPython runs the same streams serially. Both modes emit identical fixture hash `sha256:df11c0ba710bc76029917a59bc920c67a52c6df16adf8eb53d93221a1f062a61`.
- 2026-07-19 — Corrected Python's scheduler memory declaration to one exact boundary window: a one-byte poll authority plus the remaining decode authority, rather than two copies of `max_boundary_bytes`. The production 128 KiB stress now passes scheduler admission under an exact 128 KiB host budget before running. Memory reservation is cancellation-aware; a test holds the whole budget, observes the producer waiting, then terminates and joins it without releasing the blocker first. Arrow batches above the configured boundary are rejected before any outcome is emitted, and Arrow JSON inference/decode errors no longer interpolate offending values.
- 2026-07-19 — Deleted the superseded `ParallelDisabled` concurrency mode and `resource_parallel` argument. The removed `__cdf_parallel__` resource metadata no longer leaves a dormant policy branch behind: interpreter mode and admitted runtime CPU slots are the only inputs to the shared lane ceiling, while each resource invocation still plans one partition.
- 2026-07-19 — Final consolidated adversarial review found four significant issues: hostile Python exception type names could bypass message redaction; the public SDK/dlt compatibility surface still exposed ignored parallel knobs; production batch projection returned at the first terminal without proving upstream EOF; and the acceptance wording could be read as an enforceable bound over arbitrary Python allocations made before yielding. The repair validates exception names against a bounded ASCII identifier grammar, deletes the unused Python and dlt parallel parameters/metadata/example plus Rust parsing, and drains the neutral event stream through exactly one terminal before publishing EOF/error. The fourth finding is an architectural boundary rather than a hidden implementation claim: in-process code cannot have its pre-yield allocations forcibly bounded. H2 now states only the enforceable CDF-owned/imported window guarantee; H3 owns process isolation and H5 owns calibrated native scratch/RSS headroom.

## Evidence

- `CARGO_BUILD_JOBS=12 cargo test -p cdf-python bridge --lib --locked -j 12` — passed. Covers neutral foreign outcome emission for dict rows and verifies downstream stop still halts the Python generator before exhaustive materialization.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-python direct_dict_rows_emit_neutral_outcomes_without_collecting_all_batches --lib --locked -j 12` — passed. Confirms direct dict-row compatibility exits through the neutral outcome visitor and can stop after the first bounded row window.
- `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-python --lib --tests --locked -j 12 -- -D warnings` — passed. This caught and forced removal of the superseded internal batch wrapper.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-python --all-targets --locked -j 12` — passed after the consolidated repair: 25 fast tests, 7 deliberate performance/PyArrow evidence tests ignored.
- `PYTHONPATH=$PWD/target/cdf-python/pyarrow-cp312-site CARGO_BUILD_JOBS=12 cargo test -p cdf-python real_pyarrow --lib --locked -j 12 -- --ignored` — passed all 3 real PyArrow 25.0.0 C Array/Stream lifetime, alias, cancellation, error, type, and schema-variance cases.
- `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-python --all-targets --locked -j 12 -- -D warnings` — passed after replacing over-wide constructors with the existing `CompiledSourcePlanInput`/`CompiledSourcePlan` parameter authorities rather than adding an H2-specific argument bag.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-python million_row_dict_stream_keeps_boundary_memory_constant --release --lib --locked -j 12 -- --ignored --nocapture` — passed for one million rows with more than 900 incremental outcomes and peak boundary retention below the configured 8 MiB window; cumulative output exceeded peak retention by more than 100x.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-python dict_row_batch_curve_reports_throughput_without_changing_defaults --release --lib --locked -j 12 -- --ignored --nocapture` — passed and produced the 1K/8K/64K throughput/outcome/retention curve recorded in the Journal.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-cli python --lib --locked -j 12` — passed all 11 Python front-door, doctor, plan, preview, run, and replay tests. In particular, a configured-but-lazily-compiled Python reference again causes a precise doctor failure when the interpreter is absent.
- `PYO3_PYTHON=.../python3.14t CARGO_TARGET_DIR=target/h2-cp314t CARGO_BUILD_JOBS=12 cargo test -p cdf-python admitted_python_work_is_mode_correct_and_fixture_hash_stable --lib --locked -j 12 -- --nocapture` — passed under CPython 3.14.6 free-threaded with effective parallelism and observed overlap 2; the fixture hash exactly matched the CPython 3.12/GIL run.
- `PYO3_PYTHON=.../python3.14t PYTHONPATH=$PWD/target/cdf-python/pyarrow-site CARGO_TARGET_DIR=target/h2-cp314t CARGO_BUILD_JOBS=12 cargo test -p cdf-python real_pyarrow --lib --locked -j 12 -- --ignored` — passed all 3 PyArrow 25.0.0 lifetime, alias, cancellation, error, type, and schema-variance cells under the free-threaded interpreter.
- `CDF_PYTHON_PACKAGE_DATA_HASH_OUTPUT=... cargo test -p cdf-cli python_resource_plan_preview_run_and_replay_use_the_product_spine --lib --locked -j 12` under the ordinary CPython 3.12 build and the isolated CPython 3.14.6 free-threaded build — both passed full plan→preview→run→replay and emitted identical canonical segment-data hash `sha256:6203b3b40a1af3c6f6220032a1f79dfecce2040644321569ef3f1a92c6d34e06`.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-python million_row_python_resource_uses_the_global_memory_coordinator --release --lib --locked -j 12 -- --ignored --nocapture` — passed after reproducing and repairing the DataFusion coordinator release/snapshot race. One million rows crossed the resolved production source under an exact 128 KiB managed budget; the coordinator reported `current_bytes=0`, `peak_bytes=131072`, and 634 producer waits, proving exact-budget retention and live backpressure.
- `PYTHONPATH=$PWD/target/cdf-python/pyarrow-cp312-site CARGO_BUILD_JOBS=12 cargo test -p cdf-python real_pyarrow --lib --locked -j 12 -- --ignored --nocapture` — passed all 4 PyArrow 25.0.0 tests, including >2 MiB alias/lifetime/cross-thread destruction and redacted error propagation.
- `PYO3_PYTHON=.../python3.14t PYTHONPATH=$PWD/target/cdf-python/pyarrow-site CARGO_TARGET_DIR=target/h2-cp314t CARGO_BUILD_JOBS=12 cargo test -p cdf-python real_pyarrow --lib --locked -j 12 -- --ignored --nocapture` — passed the same 4-test matrix on CPython 3.14.6 free-threaded.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-python dict_row_batch_curve_reports_throughput_without_changing_defaults --release --lib --locked -j 12 -- --ignored --nocapture` — passed post-repair at 2.111/2.201/2.165 M rows/s for 1K/8K/64K windows; the selected 8K default is 2.8% faster than the pre-repair measurement while retaining the same knob and memory tradeoff.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-runtime scheduler --lib --locked -j 12` — passed all 9 scheduler tests after runtime-bound lane ceilings became a generic scheduler input.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-runtime --lib --locked -j 12` — passed 87 tests with the one deliberate performance-lab benchmark ignored, covering the generic registry, execution-host lane, scheduler, memory, graph, format, and cancellation boundaries together.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-engine memory --lib --locked -j 12` — passed all 4 fast memory tests (1 slow stress ignored) after atomic pool/snapshot release ordering.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-foreign-stream -p cdf-python --lib --locked -j 12` — passed 7 neutral-contract tests and 28 Python tests, with 7 deliberate performance/PyArrow cells ignored. New coverage proves wakeable foreign cancellation, exact terminal status, real `ForeignProducer` events, scheduler fail-closed runtime resolution, cancellation during exhausted-memory admission, and payload-redacted dict decoder failures.
- `PYO3_PYTHON=.../python3.14t CARGO_TARGET_DIR=target/h2-cp314t CARGO_BUILD_JOBS=12 cargo test -p cdf-python admitted_python_work_is_mode_correct_and_fixture_hash_stable --lib --locked -j 12 -- --nocapture` — passed with `FreeThreadedParallel`, runtime-resolved concurrency 18, and two real production foreign streams crossing a shared Python barrier. The ordinary GIL run passed the same two production streams at concurrency 1; both emitted fixture hash `sha256:df11c0ba710bc76029917a59bc920c67a52c6df16adf8eb53d93221a1f062a61`.
- `PYTHONPATH=.../pyarrow-cp312-site cargo test -p cdf-python real_pyarrow --lib --locked -j 12 -- --ignored --nocapture` and the CPython 3.14t/PyArrow environment — each passed all 4 cells after adding the over-limit large Arrow case. A >2 MiB imported batch under a 1 MiB boundary emits zero outcomes and names the tuning remedies; the admitted case retains the existing alias/GC/downstream-thread evidence.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-python million_row_python_resource_uses_the_global_memory_coordinator --release --lib --locked -j 12 -- --ignored --nocapture` — passed in 2.36 s after neutral event integration versus 2.47 s before it. The exact 128 KiB scheduler working set is admitted, one million rows complete, peak equals 131,072 bytes, current returns to zero, and 4 producer waits prove live backpressure.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-cli python --lib --locked -j 12` — passed all 11 CLI Python plan/doctor/preview/run/replay tests through the neutral producer-backed source.
- `CARGO_BUILD_JOBS=12 cargo check --workspace --all-targets --locked -j 12` — passed after the runtime-resolved lane state became part of every compiled/runtime blocking-lane declaration.
- `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-foreign-stream -p cdf-runtime -p cdf-engine -p cdf-python -p cdf-cli --all-targets --locked -j 12 -- -D warnings` — passed.
- `python3 -m py_compile python/cdf_sdk/resource.py python/cdf_sdk/dlt.py python/examples/github_issues.py` plus a source-tree `rg` for `parallelized|__cdf_parallel__|parallel=True|parallel: bool|ParallelDisabled|resource_parallel` — passed with no retained dormant parallel surface.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-foreign-stream -p cdf-python --lib --locked -j 12` — passed 8 neutral-contract and 28 Python tests after the closure-review repair. The new production projection tests reject outcome/control/duplicate-terminal events after terminal authority; the hostile Python exception-class test proves both class name and message remain redacted.
- `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-foreign-stream -p cdf-python -p cdf-cli --all-targets --locked -j 12 -- -D warnings` — passed after deleting the SDK/dlt/Rust parallel metadata surface.
- `CARGO_BUILD_JOBS=12 cargo test -p cdf-cli python --lib --locked -j 12` — passed all 11 Python CLI product-spine and doctor tests after the SDK and event-terminal cleanup.

## Review

Both pre-closure review rounds returned **fail**; their consolidated findings and repairs are recorded in the Journal. The second repair tranche is implemented and reverified; final independent review is pending. Transfer/copy telemetry survival and the shared exactly-once release/conformance matrix remain explicitly owned by H5, while force-authorized termination of an uncooperative foreign callable is owned by H3's isolated process boundary. H2 makes no claim for either unfinished program-level behavior.

## Retrospective

The production resource path was already closer to the target than the ticket text suggested: it streamed batches through the injected blocking lane. The architecture gap was that the internal bridge still spoke Python-local `Batch`/`PythonYieldKind` rather than the neutral foreign contract. Migrating the internal emission point first kept FFI/copy proof source-agnostic and made deletion of the old collectors straightforward, but review correctly showed that adopting one neutral value type was not the same as implementing the neutral producer lifecycle. Two review-oriented checks paid for themselves: real producer buffer ranges prevented an unearned zero-copy product claim, and compiling the same fixture against an installed free-threaded interpreter found the true lane behavior without mocks. The batch curve also reinforced the performance rule: expose byte/row knobs, choose the default from measured marginal gain, and do not turn a default into a hard cap. The first review found that a correct helper is worthless if production does not call it; the second found the same failure one abstraction higher. The durable result is now a real `ForeignProducer` path, an explicit portable→runtime-resolved lane state transition, and tests that run actual resolved producer streams. The production memory stress likewise demonstrated why evidence must traverse the real coordinator and scheduler: it found both a pool/snapshot race and a doubled working-set declaration that bridge-local arithmetic could never see.
