Status: active
Created: 2026-07-18
Updated: 2026-07-19
Parent: .10x/tickets/2026-07-10-p3-ws-d-destination-bulk-paths.md
Depends-On: .10x/tickets/done/2026-07-18-p3-d11-duckdb-arrow-ipc-handoff-falsification.md, .10x/tickets/2026-07-18-p3-d15-canonical-package-row-ordinal.md

# P3 D14: DuckDB nanoarrow 0.8.0 LZ4 revalidation

## Scope

Correct D11's overbroad LZ4 conclusion by building the DuckDB nanoarrow extension against the signed Apache nanoarrow `0.8.0` release with `NANOARROW_IPC_WITH_LZ4=ON`, then re-run the direct current-CDF-segment and full-year TLC Arrow IPC ingestion probes on the controlled EC2 benchmark host. Determine whether direct consumption of CDF's canonical LZ4 Arrow IPC segments removes the redundant destination handoff and materially closes the DuckDB G4 envelope gap.

## Non-goals

- No destination-local row enumeration that duplicates canonical package authority.
- No uncompressed duplicate handoff.
- No generic runtime branch for DuckDB, nanoarrow, Arrow IPC paths, or extension installation.
- No product default change before full-CDF EC2 evidence meets the retained performance and memory thresholds.
- No unpinned extension source, nanoarrow source, build flag, or binary artifact.
- No hard-coded performance cap without a configuration authority.

## Acceptance Criteria

- The investigation records the exact DuckDB extension revision, nanoarrow `0.8.0` tag/commit, archive digest, and `NANOARROW_IPC_WITH_LZ4=ON` build configuration.
- The pinned extension loads under CDF's bundled DuckDB `v1.5.4`, reports its version, and successfully reads a generated LZ4 Arrow IPC fixture.
- The pinned extension successfully reads at least one current CDF package segment directly, or records the precise remaining incompatibility with evidence.
- EC2 evidence measures full-year TLC-shaped direct LZ4 Arrow IPC ingestion through `read_arrow(list_of_files)` with 41,169,720 rows, wall time, throughput, peak RSS, and cgroup peak.
- If the direct LZ4 path clears the generated retention threshold, a destination-owned product implementation is retained only after a full-CDF EC2 run proves it faster than the current appender baseline without duplicating package bytes or leaking destination identity into orchestration.
- D11, D12, G4, and WS-D references are corrected so the backlog reflects the new evidence and no stale claim says nanoarrow lacks LZ4 support.

## References

- `.10x/tickets/done/2026-07-18-p3-d11-duckdb-arrow-ipc-handoff-falsification.md`
- `.10x/tickets/cancelled/2026-07-18-p3-d12-duckdb-arrow-ipc-handoff-ingress.md`
- `.10x/tickets/2026-07-11-p3-g4-tlc-remote-io-envelope.md`
- `.10x/specs/destination-bulk-path-runtime.md`
- `.10x/specs/constant-memory-proof.md`
- `.10x/decisions/destination-runtime-composition-boundary.md`
- `https://github.com/apache/arrow-nanoarrow/pull/819`
- `https://github.com/apache/arrow-nanoarrow/releases/tag/apache-arrow-nanoarrow-0.8.0`

## Assumptions

- Record-backed: Apache nanoarrow `0.8.0` includes LZ4 IPC decompression from PR 819, gated by the build option `NANOARROW_IPC_WITH_LZ4`.
- Record-backed: the DuckDB community extension artifact tested by D11 was extension revision `42e4199a67c4cd0789087562a025e87e7130fdc3`, which vendors pre-LZ4 nanoarrow revision `4bf5a9322626e95e3717e43de7616c0a256179eb` and does not enable the LZ4 build option.
- Record-backed: D11's observed failure is valid only for that old extension artifact; it does not establish that nanoarrow or `read_arrow(...)` cannot consume CDF LZ4 IPC.
- User-ratified: use nanoarrow `0.8.0`, investigate thoroughly, and prioritize this correction before unrelated architecture work.
- User-ratified: performance and correctness are joint first priority; retain no potentially slower default without controlled EC2 evidence.

## Journal

- 2026-07-18: Opened after upstream verification invalidated D11's general LZ4 conclusion. Apache PR 819 merged LZ4 IPC decompression on 2025-10-27 and nanoarrow `0.8.0` released it behind `NANOARROW_IPC_WITH_LZ4`. The installed DuckDB community extension's `.info` file identifies extension revision `42e4199`; that revision fetches nanoarrow `4bf5a932` from 2025-01-09 and therefore produced the observed `Compression type with value 1 not supported by this build of nanoarrow` error.
- 2026-07-18: Built the DuckDB nanoarrow extension on the controlled EC2 host from extension revision `42e4199a67c4cd0789087562a025e87e7130fdc3`, DuckDB `v1.5.4` commit `08e34c447bae34eaee3723cac61f2878b6bdf787`, and Apache nanoarrow `0.8.0` commit `a579fbf5d192e85b6249935e117de7d02a6dc4e9`. The exact nanoarrow commit archive digest is `sha256:ed186f0b8151c323fd41a1b7cfa830abad0ac84e1657cd597da12d98fa9a4be1`; the extension was compiled with `NANOARROW_IPC_WITH_LZ4=ON` against system `lz4-devel 1.9.4`. The loaded extension reported `nanoarrow_version() = 0.8.0` under DuckDB `v1.5.4`.
- 2026-07-18: The corrected extension counted `1,048,576` rows from a generated LZ4 Arrow IPC file and `196,608` rows from an exact current canonical CDF LZ4 package segment. The previous incompatibility was entirely the pre-0.8.0 extension artifact, not CDF's segment framing or compression.
- 2026-07-18: Extended the performance lab's existing Arrow IPC reference workload with an explicit absolute-path loadable-extension variant and required nanoarrow-version assertion. Unsigned-extension enablement is confined to this lab-only variant; the community-extension probes retain their existing signed install/load path.
- 2026-07-18: Ran three warm full-year samples in an isolated `MemoryMax=24G` user cgroup against the exact 215 canonical CDF LZ4 segment files from the full-year TLC package. All samples materialized `41,169,720` rows. Wall times were `4.672500665s`, `4.558788174s`, and `4.210656148s`; median was `4.558788174s` with `113.712491ms` MAD, `9,030,847` rows/s, `2,289,385,472` bytes peak process RSS, and `4,828,008,448` bytes peak cgroup memory. No memory-high, max, OOM, or spill event occurred.
- 2026-07-18: The result clears the generated retention threshold by a wide margin: direct canonical-segment materialization is approximately `7.5x` faster than the current approximately `34s` full-CDF appender path and approximately `2.0x` faster than D11's generated uncompressed duplicate-handoff median. Its stated limit is equally important: the probe omits CDF receipt/checkpoint work and destination provenance-column construction, so a destination-owned product path must still clear the full-CDF EC2 gate before replacing the default.
- 2026-07-18: Falsified three provenance strategies on the same EC2 package before changing product code. Per-segment `row_number() OVER ()` union materialized exact ranges but took `36.76s`; per-segment positional joins took `36.51s`; both destroy the direct-read advantage and are rejected. A transaction-local sequence preserved a contiguous key range in `11.95s`, but its approximately four million voluntary context switches expose per-row sequence contention and make it a fallback, not the preferred design.
- 2026-07-18: A vectorized post-materialization strategy retained the raw floor: order-preserving `CREATE TABLE AS read_arrow(...)`, one `_cdf_row_key` column addition, and one in-transaction `UPDATE ... SET _cdf_row_key = rowid + first_row_key` completed the full 41,169,720-row materialization plus count/min/max verification in a single uncontrolled `4.50s` sample at approximately 3.23 GiB peak RSS. Setting the stored key `NOT NULL` required a separate `0.37s` uncontrolled probe. DuckDB documents materialized `rowid` as the non-window row enumeration mechanism and documents order preservation for single-table `FROM`/`SELECT`; product conformance must additionally prove that nanoarrow's ordered path list maps each segment to its expected contiguous key range at `threads=1` and `threads=N` before this can become authority.
- 2026-07-18: Added the vectorized row-key strategy to the existing controlled reference workload with explicit `row_key_start`, explicit `preserve_insertion_order=true`, exact min/max verification, and optional `NOT NULL` restoration. This keeps the next EC2 median comparable and prevents an unmeasured product prototype from becoming the benchmark.
- 2026-07-18: Landed the destination-neutral prerequisite locally. `DurableSegmentReader` now optionally exposes the exact length-bound local file represented by its staged identity; live execution supplies the hash-while-write canonical segment, and replay supplies a package-verification-bound segment object. Replay decoding is lazy: a destination consuming the canonical file performs no redundant Arrow IPC decode, while batch-oriented destinations invoke the same accounted decoder on first `next_batch()`. No generic layer names DuckDB, nanoarrow, or Arrow IPC strategy ids.
- 2026-07-18: The user ratified shifting row enumeration into the canonical post-verdict Arrow stream. D15 now owns a dense `_cdf_package_row_ord`, explicit per-segment manifest starts, verification, memory/performance evidence, and cross-destination adoption. D14 no longer retains the destination-side `rowid` prototype: after D15, nanoarrow computes the transaction-owned `_cdf_row_key` directly from the persisted package ordinal with no file-order premise.
- 2026-07-18: Implemented the first destination-owned product path behind explicit digest-pinned configuration. `cdf-dest-duckdb` alone declares/selects `nanoarrow_0_8_0_lz4`; generic orchestration still sees staged durable segments and never names DuckDB, Arrow IPC, or an extension. The adapter validates an absolute extension path and exact SHA-256 before opening DuckDB, enables unsigned loading only for that explicitly pinned artifact, verifies `nanoarrow_version() = 0.8.0`, retains only length/hash-authorized canonical file paths during staging, and performs one transactional `INSERT ... SELECT` over `read_arrow([...])` at final binding. `_cdf_row_key` is vectorized as allocated package start plus `_cdf_package_row_ord`; merge order consumes the same ordinal. There is no file-order premise, duplicate handoff, runtime fallback, destination-local enumeration, or changed generic ingress interface. The receipt records bulk path, nanoarrow version, and extension digest. The appender remains selected when the extension is not configured until controlled full-CDF evidence earns a packaged default.
- 2026-07-18: The first full-CDF product smoke completed correctly but falsified the one-thread product envelope: `41,169,720` rows took `40.932s`, including `37.242s` in final destination write/receipt, at `2.973GB` process RSS and `5.647GB` cgroup peak. A matching raw `read_arrow` CTAS constrained to the product's one native thread and `1GB` DuckDB memory took `37.07s`, while the same CTAS at 16 threads and `4GB` took `4.46s`. The extension path is sound; the product default had preserved an obsolete one-thread floor.
- 2026-07-18: The 16-thread product probe then exposed a P0 lane-accounting deadlock rather than a DuckDB regression. DuckDB declared one blocking lane with `native_internal_parallelism=16` for both concurrent staged-stream consumption and final binding. The staged consumer acquired all host CPU slots and waited for the stream while the producers required those same slots; both the harness and a retained direct run timed out at zero CPU. Split the destination declaration into a one-slot staged-ingress lane and a separately sized final-binding lane. The selected nanoarrow bulk descriptor names final binding; the appender descriptor names staged ingress. This uses the already-generic operation-specific lane boundary and removes the destination-specific deadlock without hiding native work from admission.
- 2026-07-18: After the lane repair, the exact full-CDF workload completed `41,169,720` rows in `8.896720783s` at `4,627,516` rows/s. Final DuckDB write/receipt was `5.217891790s`; package execution was `3.508245659s`, with the staged pipeline overlapping the longer segment-encode total. Peak process RSS was `3,922,153,472` bytes and the enclosing session cgroup observed `6,175,150,080` bytes with zero memory events or spill. This is approximately `3.8x` faster than the `33.956s` pre-D15 appender baseline and clears the D14 product retention gate.
- 2026-07-18: Repeated the product smoke with DuckDB memory set to the exact `3,489,660,928`-byte managed-pool authority instead of `4GiB`; it completed in `9.337770690s`, at `3,444,363,264` bytes peak process RSS and `5,822,488,576` bytes cgroup peak. This supports a default derived from the execution host's logical CPU slots and managed memory authority rather than the obsolete hard-coded one-thread/1GiB ceiling. Explicit environment knobs remain authoritative.
- 2026-07-18: A three-sample transient-cgroup attempt at both `MemoryMax=8G` and `12G` timed out after the first worker rather than producing samples; the single-run session-cgroup observations and the earlier raw 24GiB controlled median remain valid, but these failed harness cells are not promotion evidence. The failure is isolated to the multi-sample wrapper and must not be misreported as a nanoarrow product regression.
- 2026-07-18: Deep embedding research is recorded in `.10x/research/2026-07-18-duckdb-nanoarrow-default-embedding.md`. DuckDB's extension configuration already produced a custom `libduckdb.so` with nanoarrow statically linked and automatically loaded. The chosen release shape builds that exact library once per release target and packages it beside CDF; normal Cargo builds retain the official prebuilt DuckDB download. This preserves the compile-time win, removes runtime `INSTALL`/network/unsigned-policy requirements, and keeps selection destination-owned. The digest-pinned loadable path remains an explicit development override.
- 2026-07-18: Added the destination-owned static-link capability and made its evidence explicit. A release compiled with `CDF_DUCKDB_NANOARROW_STATIC_LINK=1` selects nanoarrow without a runtime artifact, does not enable DuckDB's unsigned-extension policy, verifies `nanoarrow_version() = 0.8.0`, and records `nanoarrow_linkage=statically_linked` in the receipt. The explicit loadable path now re-hashes immediately before `LOAD` and records `digest_pinned_loadable`; its SHA remains part of the receipt. Nanoarrow final binding defaults to the execution host's logical CPU slots and exact managed-memory budget, while both existing environment knobs remain authoritative. All 35 active DuckDB destination tests and strict all-target Clippy passed; the release-built static library and zero-configuration EC2 product run remain the next acceptance gate.
- 2026-07-18: Implemented the release-owned embedded runtime builder. It downloads and verifies exact source archives for DuckDB `08e34c447b`/`v1.5.4`, duckdb-nanoarrow `42e4199a67`, Apache nanoarrow `a579fbf5d1`/`0.8.0`, and LZ4 `1.9.4`; patches only the reviewed nanoarrow pin and `NANOARROW_IPC_WITH_LZ4=ON`; builds LZ4 as PIC static code; and links nanoarrow into the DuckDB shared runtime. The build refuses dynamic LZ4 linkage and emits a hash-bearing machine-readable provenance manifest. Release CI now builds that runtime once per native target, verifies DuckDB/nanoarrow versions through the C API, compiles CDF against it with `DUCKDB_LIB_DIR`, packages the runtime plus provenance, and preserves the manifest on install. Ordinary Cargo and EC2 iteration continue using the downloaded prebuilt DuckDB library.
- 2026-07-18: The controlled Linux x86-64 release build passed. The resulting `libduckdb.so` reports DuckDB `v1.5.4`; `nanoarrow_version()` resolves without `INSTALL`, `LOAD`, network access, or unsigned-extension enablement and returns `0.8.0`; `readelf` shows no dynamic `liblz4` dependency. A real fat-LTO CDF release binary compiled against that runtime with `CDF_DUCKDB_NANOARROW_STATIC_LINK=1` and ran its version probe. Packaging, reproducibility, archive-verification, installer, shell syntax, Python syntax, YAML parse, and whitespace checks passed locally. The zero-configuration full-CDF throughput run remains the promotion gate.
- 2026-07-18: The zero-configuration static-runtime promotion gate passed on the controlled EC2 host. Three full-year CDF samples completed in `9.448199817s`, `9.448418685s`, and `10.324856630s`; median was `9.448418685s`, `4,357,313` rows/s, with `3,894,951,936` bytes peak process RSS. No DuckDB performance environment variable or runtime extension path was supplied. One earlier multi-sample wrapper invocation timed out nondeterministically and is not retained as evidence; explicit-control and threads-only one-sample observations remain supporting diagnostics, not promotion authority.
- 2026-07-19: A stock-`libduckdb` public-C-API table function over a fresh ordinal-bearing canonical package measured a `5.645601216s` three-sample raw median versus nanoarrow `0.8.0`'s `4.96s` exact-package median. The approximately 13.8% raw cost is modest relative to deleting the custom DuckDB runtime, embedded extension, ABI-coupled release build, and development loadable-extension path. The user superseded the prior rigid 10% raw gate and authorized productization subject to full-CDF EC2 proof. D14 now owns replacing its nanoarrow implementation with the stock destination-local table function; the static runtime and all superseded paths are deleted only after the full product cell preserves exceptional throughput and semantics.
- 2026-07-19: Promoted the stock scanner into `cdf-dest-duckdb` as its sole bulk path. Staged ingress now retains only exact durable canonical segment paths; final binding opens one bounded stock DuckDB database, registers a private parallel C table function, decodes LZ4 Arrow IPC with arrow-rs, transfers each vector-sized slice through the Arrow C Data Interface, derives `_cdf_row_key` from the canonical package ordinal, and commits through the existing transaction/receipt gate. The high-level Arrow appender, Arrow-major bridge, nanoarrow loader/static-link modes, extension digest receipt fields, and all runtime fallback selection were deleted. The same destination type preflight remains authoritative for primitive, decimal, list, fixed-list, struct, and map bindings. Local destination tests prove append/replace/merge/idempotency/corrections/provenance plus decimal/list/struct round trips; project event and first-party catalog conformance now name the sole path. The old 256 MiB execution floor was also removed: managed memory and explicit tuning values pass through exactly, while 256 MiB remains only the no-execution inspection default. Full-CDF EC2 throughput is the next gate before deleting release-level custom-runtime machinery.
- 2026-07-19: The first stock-library full-product EC2 smoke passed against the exact 41,169,720-row full-year package in `10.105268967s` at `4,074,084` rows/s. Final DuckDB write/receipt was `6.529108999s`; package execution was `3.417824154s`; process peak RSS was `3,928,817,664` bytes and cgroup peak was `6,363,344,896` bytes with zero memory events. This is only approximately 6.9% slower than the prior custom-runtime three-sample median while preserving the approximately 3.4x gain over the old appender and deleting the release/extension architecture.
- 2026-07-19: Removed the remaining superseded product and distribution surfaces. `appender-arrow` is no longer a workspace/product feature and survives only in the benchmark crate's historical comparator. `DuckDbArrowWriter` and Arrow-specific merge/error vocabulary were replaced with commit/scanner terms. The 301-line custom DuckDB+nanoarrow builder, its 71-line verifier, build manifest, installer metadata branch, and release CI custom-runtime steps were deleted. Release CI now uses `DUCKDB_DOWNLOAD_LIB=1`, packages the exact downloaded pinned stock library, and retains the same checksummed archive/atomic installer contract. The release and installer smoke suites passed, all 29 destination tests passed, and strict destination Clippy passed.

## Blockers

None.

## Evidence

- Exact controlled-host observation: `.10x/evidence/.storage/2026-07-18-p3-d14-ec2-current-package-lz4-nanoarrow-080-observation.json`.
- Upstream capability: Apache Arrow nanoarrow PR 819 and release `apache-arrow-nanoarrow-0.8.0`, linked under References.
- Focused local lab validation: `cargo fmt --all`; `CARGO_BUILD_JOBS=12 cargo check -p cdf-benchmarks --locked -j 12`; `CARGO_BUILD_JOBS=12 cargo test -p cdf-benchmarks arrow_ipc_handoff_writer_emits_readable_files --locked -j 12 -- --nocapture`; and `git diff --check` all passed.
- Neutral staged-object validation: `CARGO_BUILD_JOBS=12 cargo check -p cdf-runtime -p cdf-package -p cdf-engine -p cdf-project --locked -j 12`; focused durable-file request validation passed; all 53 non-ignored `cdf-package` library tests passed; and the `cdf-project` library test command completed successfully. The local-file request test also proves length drift is rejected before destination consumption.
- Product-path local validation: all 32 active `cdf-dest-duckdb` library tests passed (one performance benchmark ignored), including exact extension-digest/path validation and existing append/replace/merge/duplicate/correction/provenance behavior. Strict all-target Clippy passed for `cdf-dest-duckdb` and its dependent project/conformance graph. The actual extension load and SQL path remain gated on the controlled Linux host because the pinned artifact is platform-specific.
- Full-CDF one-thread smoke: `.10x/evidence/.storage/2026-07-18-p3-d14-ec2-product-nanoarrow-full-cdf-smoke.json`; it proves correctness and isolates the obsolete native-resource floor but is not retained performance evidence.
- Full-CDF corrected-lane smoke: `.10x/evidence/.storage/2026-07-18-p3-d14-ec2-product-nanoarrow-lanes-t16-m4g-smoke.json`; it proves the product retention threshold on the controlled host. The exact-managed-memory repeat was `9.337770690s` and is journaled from the fetched diagnostic cell; it is not promoted as a separate durable artifact.
- Static-link capability validation: `cargo fmt --all`; `CARGO_BUILD_JOBS=12 cargo test -p cdf-dest-duckdb --lib --locked -j 12` (35 passed, one performance benchmark ignored); `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-dest-duckdb --all-targets --locked -j 12 -- -D warnings`; and `git diff --check` all passed.
- Release embedding validation: controlled EC2 `tools/build-duckdb-nanoarrow-runtime.sh --target x86_64-unknown-linux-gnu ...` passed; `tools/verify-duckdb-nanoarrow-runtime.py` observed DuckDB `v1.5.4` and nanoarrow `0.8.0`; `readelf -d` listed no `liblz4`; custom-runtime fat-LTO `cargo build -p cdf-cli --bin cdf --release --locked -j 16` passed. Local `tools/test-release-artifacts.sh`, `tools/test-install-cdf.sh`, Bash syntax, Python compilation, Ruby YAML parse, and `git diff --check` passed.
- Zero-configuration static-runtime promotion: `.10x/evidence/.storage/2026-07-18-p3-d14-ec2-static-default-full-year-three-sample.json`. Supporting one-sample controls: `.10x/evidence/.storage/2026-07-18-p3-d14-ec2-static-explicit-control.json` and `.10x/evidence/.storage/2026-07-18-p3-d14-ec2-static-threads-only.json`.
- Stock-path controlled evidence: `.10x/evidence/.storage/2026-07-19-p3-d16-stock-table-function-full-year-observation.json`, its `.10x/evidence/.storage/2026-07-19-p3-d16-stock-table-function-reference.json` workload, `.10x/evidence/.storage/2026-07-19-p3-d16-stock-table-function-run-cell.json`, exact-package `.10x/evidence/.storage/2026-07-19-p3-d16-nanoarrow-exact-package-comparator.json`, and `.10x/evidence/.storage/2026-07-19-p3-d16-canonical-package-manifest.json`.
- Acceptance mapping: exact pin/build configuration is recorded in the Journal; extension load/version, generated LZ4, and current-segment compatibility are proved by the controlled-host probes; the raw JSON records every full-year sample, row count, throughput, RSS, cgroup peak/events, and measurement biases. D15's canonical ordinal, the destination-owned nanoarrow product path, release-artifact static embedding, and its zero-configuration EC2 run are implemented. Closure now requires the authorized stock-library replacement to pass its full-CDF promotion cell and remove the superseded custom runtime without weakening those results.

## Review

Pending.

## Retrospective

Pending.
