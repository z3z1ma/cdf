Status: active
Created: 2026-07-11
Updated: 2026-07-16
Parent: .10x/tickets/2026-07-05-implement-cdf-system.md
Depends-On: .10x/tickets/done/2026-07-11-p0-dx1-neutral-runtime-crate.md, .10x/tickets/done/2026-07-11-p3-a2-unified-memory-ledger.md, .10x/tickets/done/2026-07-11-p3-a4-injected-execution-host.md, .10x/specs/source-extension-runtime-contract.md

# P0 SX1: source driver registry and resource-plan boundary

## Scope

Implement neutral source driver/registry/plan/config schema contracts, extend capabilities, migrate file/REST/Postgres compilation/discovery/runtime/product hooks through drivers, extract Postgres source ownership from the destination crate, and prove a mock external source. Preserve existing TOML ergonomics/artifacts through an explicit migration.

## Acceptance criteria

- Generic declarative/project/CLI/discovery code contains no file/REST/SQL/Postgres source-kind match tree.
- Standard JSON Schema remains precise for common and driver fields; runtime parser is registry-open.
- Project/generic CLI have no `cdf-source-*` dependency/import; Postgres source is independent of its destination.
- Existing file/REST/Postgres add/discover/deep/preview/plan/run/doctor behavior passes compatibility/golden conformance.
- Extended capability declarations are live-falsified and sufficient for P3 scheduler admission without source ids.
- A mock source adds only driver crate/test, composition/schema catalog, and fixture entries and inherits every applicable law.
- Cargo graph/rebuild evidence proves source dependency isolation.

## Evidence expectations

Config/schema migration goldens, dependency/static tests, mock source, first-party parity matrix, discovery/package artifact hashes, redaction/egress/retry/memory/jobs tests, build graph evidence, and adversarial extension review.

## Explicit exclusions

No new source protocol, parser optimization, dynamic ABI, or distributed execution.

## Blockers

None. Neutral runtime, memory, and execution-host dependencies are done. Remaining work is owned by this ticket.

## References

- `.10x/decisions/source-driver-registry-and-resource-plan-boundary.md`
- `.10x/research/2026-07-11-source-extension-boundary-audit.md`
- `.10x/specs/source-extension-runtime-contract.md`
- `.10x/knowledge/source-destination-extension-invariant.md`

## Progress and notes

- 2026-07-11: Added the engine-neutral `SourceDriver`, `SourceRegistry`, compiled source plan, resolution context, and scheduler-facing execution capability contracts to `cdf-runtime`. Compiled plans bind driver/version/option-schema authority and canonical redacted-option/physical-plan hashes; registry resolution rejects authority drift. A mock driver proves deterministic registration, compilation, serialization, and resolution without source-id scheduling branches. First-party source migration and dependency isolation remain open.
- 2026-07-11: Extracted Postgres table scan and catalog discovery into dependency-isolated `cdf-source-postgres`; the destination no longer owns or exports source behavior. Shared, protocol-level identifier/target validation now lives in leaf crate `cdf-postgres`, used independently by source and destination. Declarative and project discovery currently consume the source crate directly pending registry-driven composition.
- 2026-07-11: Implemented the first first-party `SourceDriver` in `cdf-source-postgres`. It strictly compiles driver-owned options, preserves only secret references in the plan, declares an installable bounded blocking lane and retry/attestation/working-set semantics, resolves secrets through injected context, and executes scans through the host lane. The neutral registry now installs source-declared lanes generically before resolution.
- 2026-07-11: Declarative Postgres table compilation now stamps the neutral driver plan onto `CompiledResource`, updates that plan when discovery/effective schema changes, and fails invalid dialect/table options at compile time. CLI plan/preview/run resolution uses the single source composition module and neutral registry whenever execution services are present; the legacy SQL wrapper remains only for compatibility/inspection tests while the remaining drivers migrate.
- 2026-07-11: Extracted the REST plan, HTTP/pagination execution engine, discovery sampler, and resource implementation into dependency-isolated `cdf-source-rest`. The source resource now owns only neutral descriptor/schema/capability/type-policy inputs rather than a declarative compiler object; `cdf-declarative` retains a thin construction/discovery compatibility bridge while driver compilation is added.
- 2026-07-11: Added `RestSourceDriver` with strict driver-owned source/resource options, canonical physical/redacted artifacts, secret-ref validation, transport-factory composition, complete source execution declarations, and managed host-lane execution. Declarative REST resources now carry the neutral plan; CLI resolves every available neutral source plan before entering compatibility dispatch. Resolution context owns a shared secret provider, and neutral plans now carry type-policy allowances so drivers cannot silently drop schema policy.
- 2026-07-11: Extracted file format/compression declarations, file/object-store transport facade, ranged readers, partition resolution, attestation, and runtime execution into dependency-isolated `cdf-source-files`. `FileResource` now consumes neutral descriptor/schema/capability/type-policy/effective-schema inputs; declarative retains only a thin compatibility constructor while the driver artifact is added.
- 2026-07-11: Added `FileSourceDriver` with strict canonical option/physical artifacts, source composition transport factory, complete scheduler capabilities, and neutral resolution for local/HTTP/S3/GCS/Azure schemes. Effective schema runtime/catalog/budget evidence is now serializable source-plan authority and updates after reconciliation. CLI composition registers all three first-party drivers and production file/REST/Postgres execution resolves uniformly before compatibility dispatch.
- 2026-07-11: Replaced the nine-argument compiled-plan constructor with a cohesive `CompiledSourcePlanInput` artifact. Adding a source now supplies one payload plus generic resource/execution capabilities, avoiding positional wiring and keeping strict Clippy clean as the extension surface grows.
- 2026-07-11: Deleted source-kind runtime construction from generic CLI execution. Every executable declarative resource must carry a neutral source plan and execution services; registry resolution is the only file/REST/Postgres runtime path. Inspection uses the neutral compiled resource surface without resolving secrets or constructing transports. Source-specific file dependencies remain only in discovery/deep/doctor compatibility hooks pending hook migration.
- 2026-07-11: Removed the direct Postgres source implementation dependency/import from `cdf-project`; catalog compatibility exports are temporarily mediated by declarative while discovery hooks move to the registry.
- 2026-07-11: Migrated the canonical live local-file conformance fixture from executable `CompiledResource` compatibility behavior to a neutral `FileSourceDriver` plan resolved through `SourceRegistry`, injected execution services, shared secret authority, and the file transport facade. The fixture now plans and runs against the resolved `QueryableResource`; the old anonymous `seg-000001` golden was promoted to the current deterministic `p00000000-s00000000` artifact rather than preserved through a shim. The DuckDB live golden passes across 100 rebuilds under strict Clippy. This is a file-fixture milestone, not SX1 closure; REST/Postgres/general conformance catalog and remaining compatibility hooks are still open. Evidence: `.10x/evidence/2026-07-11-p0-sx1-live-file-registry-golden.md`.
- 2026-07-11: Hardened the neutral transport side of source extension: both file and REST transport contracts now require shared `Send + Sync` implementations and no longer impose adapter-wide mutable mutexes. Generic project discovery, CLI commands, benchmarks, and conformance consume the shared HTTP contract without source-id branches. Concrete async provider extraction and remaining compiler/discovery hook migration remain open. Evidence: `.10x/evidence/2026-07-11-p3-g1-concurrent-rest-transport.md` and `.10x/evidence/2026-07-11-p3-g2-concurrent-transport-spool.md`.
- 2026-07-12: Extracted concrete Reqwest ownership from CLI into `cdf-transport-http`; CLI now only composes the provider. File runtime consumes its optional neutral byte source through the transport hook, and source access is joined from format capabilities rather than source/format ids. Project/CLI discovery hooks and the blocking REST compatibility surface still block SX1 closure. Evidence/review: `.10x/evidence/2026-07-12-p3-g1-async-http-byte-source.md`, `.10x/reviews/2026-07-12-p3-g1-async-http-byte-source-review.md`.
- 2026-07-12 (build-graph shaping): `.10x/research/2026-07-12-cargo-product-build-graph-audit.md` found that `cdf-cli` directly links all source/format/transform implementations and therefore compiles the complete 377-package product graph for parser/render tests. `.10x/tickets/2026-07-12-p0-cg1-lean-cli-core.md` reuses this ticket's single standard source-composition authority while extracting dependency-light CLI concerns; it does not create another source registry or duplicate SX1 migration scope. SX1 retains source behavior/composition closure and build-graph evidence authority.
- 2026-07-14: Reactivated as the dependency blocking IX1/H1/H2 and therefore SA4. Fresh audit confirms runtime resolution is registry-driven and `cdf-project` has no direct `cdf-source-*` Cargo dependency, but closure is not yet honest: declarative source/resource models and `CompiledResourcePlan` remain closed enums; project discovery still matches file/REST/SQL and imports their concrete types through declarative re-exports; generic CLI add/schema/deep/doctor paths repeat the same source-kind branches; `SourceDriver` exposes only compile/resolve, not discovery/add/doctor hooks or an actual option-schema fragment; and Python remains a CLI-side source special case. The next slice extends the neutral driver contract with falsifiable lifecycle hooks and migrates discovery without moving source types into project/runtime.
- 2026-07-14: Began the contract repair at registration rather than in the declarative parser. Every `SourceDriver` now exposes its actual deterministic source/resource JSON Schema fragment; registry admission hashes that fragment and rejects descriptor/schema mismatch before compile or source contact; the registry exposes a stable driver-id-keyed catalog for the standard schema generator. File, REST, and Postgres drivers now retain real closed option schemas instead of hashing throwaway ad-hoc field lists. This does not yet make Tier-0 parsing registry-open; it establishes the authority needed to delete the closed enum without accepting unvalidated maps.
- 2026-07-14 (option-schema milestone evidence): `CARGO_BUILD_JOBS=12 cargo check -p cdf-runtime -p cdf-source-files -p cdf-source-rest -p cdf-source-postgres --all-targets --locked` passed. `CARGO_BUILD_JOBS=12 cargo test -p cdf-runtime -p cdf-source-files -p cdf-source-rest -p cdf-source-postgres --locked` passed 39 runtime unit tests plus its build-graph test, 42 file-source tests, 11 Postgres-source tests, and 5 REST-source tests (one runtime benchmark ignored by design). `CARGO_BUILD_JOBS=12 cargo test -p cdf-cli keyless_append_rest_validate_plan_preview_run_has_no_key_nudge --locked` passed its one selected end-to-end CLI test and therefore exercised the real standard registry composition. A preceding invocation with `--exact` selected zero tests and is deliberately not closure evidence. Fresh adversarial review found and fixed a mock schema that omitted the now-required closed `properties` objects; no critical or significant finding remains in this bounded milestone. Residual risk: the closed declarative enums and source-specific discovery/CLI hooks remain, so SX1 stays active.
- 2026-07-14: Removed three framework-owned values from adapter option space before opening the declarative envelope. `SourceCompileContext` now owns the source name, project root, and optional cursor-pushdown parameter/fidelity. Registry and direct-driver entry validate that context; file and REST schemas no longer pretend those common fields are driver options; their physical plans record the common values needed by execution. This prevents future drivers from re-declaring common config and gives registry-open compilation one typed context rather than magic option insertion. A direct REST driver test proves source identity and exact cursor pushdown survive into its runtime plan; the file plan test proves source identity is retained outside file options; the mock registry rejects an empty common source name.
- 2026-07-14 (common-context evidence/review): `CARGO_BUILD_JOBS=12 cargo check -p cdf-runtime -p cdf-declarative -p cdf-source-files -p cdf-source-rest -p cdf-source-postgres --all-targets --locked` passed. `CARGO_BUILD_JOBS=12 cargo test -p cdf-runtime -p cdf-source-files -p cdf-source-rest -p cdf-source-postgres --locked` passed 39+1 runtime tests, 42 file tests, 11 Postgres tests, and 6 REST tests; one runtime benchmark remains ignored by design. The selected real CLI REST validate/plan/preview/run case passed. A broader declarative run reached 76/81 and exposed five stale pre-SX1 assertions owned by the active pre-production-vestige ticket: three collapse observed physical hash into output-schema identity, and two require payload hashes during metadata-only local inventory. They are not claimed as this milestone's evidence and are journaled with their owner. Fresh adversarial review found no common-field leakage into first-party driver schemas or loss of cursor/source identity. Residual risk remains the closed declarative compiler and lifecycle hook match trees; SX1 stays active.
- 2026-07-14: Added the first neutral lifecycle hook as a source discovery session. Drivers expose discovery kind, metadata-only candidate inventory, and bounded per-candidate schema observations through shared runtime types; registry resolution verifies the compiled driver/version/schema authority and installs declared blocking lanes before the session exists. File, REST, and Postgres implement the hook inside their own crates. File inventory supports local and remote multi-file candidates and delegates observation to the registered format/transform boundary; REST retains the sampled page in the prepared-payload handoff for same-command execution; Postgres reads catalog metadata without misreporting columns as source records. The declarative REST compatibility bridge now supplies the same explicit byte/record budget rather than retaining a second unbounded signature.
- 2026-07-14 (discovery-session review/evidence): Fresh self-review found the initial trait trusted driver-reported order, identity, and coverage. Repaired it with a registry-owned verification wrapper that canonicalizes candidate order, rejects duplicate canonical locations, validates control-free identity/schema hashes, binds observations to their candidate, and rejects byte/record budget overruns. Removed an unused candidate scheduling helper rather than freezing speculative policy. Permanent falsification tests cover duplicate/order, candidate drift, and over-budget observations. First-party executable tests cover local NDJSON inventory/observation and REST sample retention. `CARGO_BUILD_JOBS=12 cargo test -p cdf-runtime -p cdf-source-files -p cdf-source-rest -p cdf-source-postgres -j 12 --lib` passed 42+1, 44, 7, and 11 tests respectively; `cargo check -p cdf-declarative -p cdf-cli --all-targets -j 12` passed; strict `--no-deps` Clippy passed for runtime/REST/Postgres. Source-files strict Clippy remains blocked by four pre-existing runtime warnings outside this slice, so no clean source-files lint claim is made. SX1 remains active: generic project/CLI discovery does not consume this session yet, add/doctor hooks are absent, and the closed declarative enums still exist.
- 2026-07-14: Cleared the four source-files strict-lint blockers immediately after the discovery-session commit. Payload-reuse identity construction now receives one cohesive typed input instead of eleven positional arguments; the dead `TypePolicyAllowances` partition-preparation argument was deleted rather than retained under an underscore; and the two mechanical lazy/collapsible branches were simplified. This changes no key preimage or source semantics. `CARGO_BUILD_JOBS=12 cargo clippy -p cdf-source-files --all-targets --no-deps -j 12 -- -D warnings` passed, followed by all 44 source-files library tests.
- 2026-07-14: Migrated the shared project discovery compiler and the generic CLI add/schema/auto-pin/deep-discovery paths to the verified `SourceRegistry` discovery session. Drivers now supply inventory and bounded observations while project retains deterministic selection, schema aggregation/reconciliation, observation caching, snapshot/manifest identity, normalization, and runtime evidence. Source-specific snapshot labels were replaced with driver id/version/plan authority; source-specific facts remain in the observation identity. Metadata authorities may honestly observe a complete schema with zero payload bytes, and unknown candidate size is no longer serialized as zero. The external codec conformance now exercises the registry path through pin, preview, package, receipt, and checkpoint. Evidence: `.10x/evidence/2026-07-14-p0-sx1-registry-discovery.md`.
- 2026-07-14 (remaining closure cut): SX1 remains active. Delete the parallel legacy discovery adapters/APIs and their source-kind match tree; remove declared-file and file-only deep-validation branches through the fixed-schema stream-admission owner; open the declarative source envelope; move add suggestions, doctor probes, and Python through registered lifecycle hooks; then record the source-isolated Cargo graph. These are the next gates, not residual compatibility surfaces to retain.
- 2026-07-14 (registry-discovery adversarial repair): Initial fresh review found three material boundary defects before commit: discovery and execution could compile separate source plans; driver evidence could overwrite framework authority or persist credential-bearing operational locations; and malformed strong cache identity was silently discarded. Repaired all three. Commands now compile once, pass the exact plan through discovery, bind only compiler-owned schema authority without driver re-entry, verify pinned driver/version/plan-hash authority, use typed redacted evidence locations, namespace driver facts below `driver.*`, and propagate strong-identity validation errors. Permanent hostile tests cover plan preservation/mismatch, evidence overwrite attempts, URI userinfo/query redaction, and malformed generation tokens. The follow-up independent verdict is recorded below.
- 2026-07-14 (registry-discovery second adversarial repair): Follow-up review falsified the first repair in three narrower cases: `source_plan_hash` covered only the physical payload rather than complete compiled source semantics; evidence-location fields could be forged after construction and cached source identity bypassed observation validation; and unknown-size candidates returned cache bypass before validating an advertised strong token. Repaired at the neutral boundaries. `schema_binding_stable_hash` covers driver, non-schema descriptor authority, resource/execution capabilities, policy allowances, redacted options, and physical plan while excluding only fields discovery is allowed to bind. Candidate validation recomputes the typed redaction, file diagnostics use that single sanitizer, cache entries validate evidence on read, and cache-source construction validates before size-based bypass. Focused runtime/project/CLI tests and strict Clippy pass.
- 2026-07-14 (fragment repair): The narrow re-review then found the shared evidence validator rejected URI userinfo and query secrets but not fragment secrets. URI-shaped evidence now rejects fragments as well; a cache artifact containing only `#access_token=...` evidence is rejected and removed on lookup. The exact regression and strict runtime/project Clippy pass.
- 2026-07-14 (compiled scan-intent milestone): Added one required, versioned `CompiledScanIntent` to every partition and made it the sole executable projection/filter/limit/order artifact. Engine negotiation validates it against the canonical request, predicate classification, and declared capability vocabulary before source contact. File codecs receive it through the neutral prepared-session boundary; Postgres translates it to SQL at its adapter edge; REST translates it to a cursor URL at its adapter edge. The duplicate serialized Postgres scan and REST cursor metadata were deleted. Tier-A file planning now records a full scan and cannot silently perform Tier-B pushdown. Format-derived file capabilities are checked against the identity-bearing compiled source plan, and relative file roots remain portable without a hard-coded URI scheme allowlist. Pushed Parquet/Arrow IPC projections now carry projected physical evidence while retaining the complete discovery observation for generation verification. This unblocks B2's production projection work but does not close SX1's remaining registry-open declarative/add/doctor/Python lifecycle scope. Evidence: `.10x/evidence/2026-07-14-p0-sx1-compiled-scan-intent.md`.
- 2026-07-14 (compiled scan-intent adversarial repair): The first independent review failed the milestone on four cross-adapter contradictions: Tier-A Postgres/REST/declarative partitions could still compile pushdown, REST driver capabilities contradicted executable cursor negotiation, Parquet projection masks did not preserve requested field order, and URI syntax acceptance exceeded runtime dispatch. Repaired each at its shared boundary. Engine now rejects any non-full Tier-A intent; the first-party Tier-A adapters publish full scans while Tier-B owns pushdown; REST capabilities come from the executable cursor plan and execution services are construction-required; Parquet zero-copy reorders selected arrays and schema to compiled order; supported URI schemes are normalized and unknown schemes fail before contact. The conformance harness now compares Tier-A/Tier-B topology while treating scan intent as the intentional tier boundary, and its operator check uses canonical expression operators rather than substring matching.
- 2026-07-15 (registry-only discovery and stream-admission cut): Deleted the parallel project-owned file/REST/Postgres discovery adapters and their source-kind dispatch. Project discovery now accepts only a verified `SourceRegistry` plan/session; `validate --deep` uses one generic explicit fixed-schema preflight, while ordinary declared and pinned plan/run preparation performs no physical-schema pre-scan. CSV and JSON codecs now label decoded source schemas as physical observations so the engine—not the codec—owns normalization and coercion in-stream. The benchmark path and project test harness were migrated to the same compiler lifecycle. The cold unversioned-HTTP test additionally exposed and repaired a payload-handoff bug: the generic resolution context now retains the exact discovery spool, so execution does not redownload the object.
- 2026-07-15 (pinned heterogeneous baseline repair): Removing the pre-scan exposed that a pinned aggregate schema alone could not distinguish an exact physical schema that formed a financial/freeze baseline from new drift. Discovery manifests now persist the complete canonical physical schema catalog, not only full-schema hashes. Pinned preparation hydrates that catalog without source contact; the generic compiled source plan carries it; `SourceRegistry` binds it onto every resolved source; and compiled schema-admission v3 derives and serializes projection-specific baseline identities. Execution recomputes the streamed projected Arrow hash before allowing a baseline width coercion. This admits exact heterogeneous inputs under full or pushed-projection scans, keeps new schemas governed in-stream, and adds no source-specific branch or planning I/O. Fixed-schema deep diagnostics render observation, field, observed type, fixed type, exact reason, and remediation from generic quarantine evidence.
- 2026-07-15 (adversarial repair): The bounded fresh review found three significant defects: full physical hashes were not projection-stable, registry resolution trusted driver-returned executable authority, and a single-implementation discovery adapter survived the legacy deletion. All three were repaired in one pass. Baseline catalogs compile through a permanent projected heterogeneous-schema law; `SourceRegistry::resolve` now verifies descriptor, Arrow schema, capabilities, type allowances, effective runtime, and exact compiled-plan hash before returning a resource; and project discovery directly consumes `CompiledSourcePlan + SourceDiscoverySession` with the adapter trait and binary/file-centric shared names deleted. The new verifier immediately exposed REST dropping effective-schema runtime authority; REST now carries it and binds its discovery observation to the executable partition rather than receiving an exemption.
- 2026-07-15 (evidence): `CARGO_BUILD_JOBS=12 cargo test -p cdf-project -p cdf-engine -j 12` passed 195 project tests and 161 engine tests with six explicitly slow/release benchmarks ignored; `cargo test -p cdf-runtime` passed 72 with one performance benchmark ignored; first-party source libraries passed 56 file + 11 Postgres + 7 REST tests. Strict all-target Clippy with `-D warnings` passed across kernel/runtime/engine/declarative/project/CLI/benchmarks, all three sources, and the touched JSON/delimited/Parquet codecs. Focused CLI regressions passed for financial heterogeneous freeze admission, declared Arrow IPC in-stream widening evidence, REST and Postgres registered discovery, REST non-coercing defaults, REST schema diff, deep validation, SQL-query fail-closed behavior, and Postgres schema pinning. The broad CLI suite remains unsuitable as one closure signal because unrelated active tickets still own its known promotion/render/replay fixture failures; no pass is claimed for it.
- 2026-07-15 (source-frontier memory repair): Registry-open conformance exposed a generic memory-authority contradiction rather than an adapter defect. The scheduler correctly reserves each source partition's complete concurrent working set (`maximum_poll_bytes + maximum_decode_bytes`), but the source frontier then reserved that same total again for the retained output batch. With the persistent validation lease this could deadlock a valid 64 MiB plan before REST or Postgres emitted one batch. The frontier now validates the complete schedule and retains only the compiled `maximum_decode_bytes`; transport/poll memory remains exclusively schedule-owned. `CARGO_BUILD_JOBS=12 cargo test -p cdf-engine -p cdf-project --lib -j 12` passed 161 engine tests (six intentional ignores) and 191 project tests, including the REST jobs matrix and Postgres registered-discovery paths. This is a neutral scheduler-boundary repair, not a source-id exemption.
- 2026-07-15 (NDJSON physical-evidence repair): The registry-only drift scenario exposed that record-local JSON recovery changed a non-nullable physical field to a nullable materialized Arrow field but retained the decoder schema's hash and labeled the result as an unmaterialized physical observation. The batch constructor correctly failed the contradictory evidence. NDJSON now hashes the exact emitted Arrow schema and identifies recovered batches as `MaterializedOutput`, preserving typed raw residual candidates as the explanation for the nullable placeholders. The focused non-nullable drift regression and the full REST→DuckDB recovery/replay/drift MVP conformance scenario pass. No source or destination exemption was added.
- 2026-07-15 (registry-open compiler milestone): Replaced the closed declarative source/resource enums with one registry-validated open envelope: common fields stay typed, driver fields remain flattened JSON only until the selected registered driver validates and compiles them, and `CompiledResource` owns the exact `CompiledSourcePlan` used by discovery and execution. Deleted the file/REST/SQL compiler compatibility modules and their concrete source dependencies from `cdf-declarative`; schema rebinding now updates the already-compiled plan without invoking a driver a second time. Generic project, CLI, benchmarks, and conformance callers now provide the registry explicitly. The generated JSON Schema remains closed and precise by composing common properties with each registered driver's authoritative fragments. The engine is now the sole owner of file-position canonicalization; project no longer overwrites concrete manifest paths with a resource wildcard. Workspace all-target check passes with 12 build jobs; strict touched-crate Clippy and the focused declarative/source/project/CLI/MVP matrices passed before this record update. The remaining closure tail is registered Python/add lifecycle behavior, build-graph proof, and regeneration of the canonical live golden after deleting its invalid test-only post-plan metadata mutation.
- 2026-07-15 (registered reference-source lifecycle milestone): Replaced the project/CLI Python special case with registry-owned project-reference compilation, project-option validation, schema discovery, resolution, execution, and health probing. `ProjectConfig` now retains driver-id-keyed project options generically; URI references compile through the driver that owns their scheme; registry verification prevents drivers from changing resource id, trust, or freshness; and resolution receives driver options without serializing interpreter bindings into package identity. The Python driver owns its interpreter probe and fixed code-free doctor behavior, hashes its module into the compiled physical plan, and rejects code changes between planning and open. Generic inspect consumes the compiled plan without executing source code, while preview/run use the same registry resolution and S8 parity path. The Python-specific CLI error mapping and direct conformance construction were deleted. Project `alert_after` now binds the neutral descriptor freshness rather than disappearing at the reference boundary. The canonical Arrow type-string parser moved from the declarative frontend to `cdf-kernel`, removing `cdf-declarative` and DataFusion from `cdf-python`'s normal dependency tree. Evidence: workspace all-target check passed; strict no-deps all-target Clippy passed across kernel/runtime/declarative/Python/project/CLI/conformance; Python's 21 library tests, runtime's 72 tests (one performance ignore), all 11 Python CLI cases, and the file/Python/REST/SQL preview-run parity law passed. Focused project validation, SQL-secret, and Parquet discovery regressions also passed. SX1 remains active for registered `cdf add`, canonical live-golden regeneration, build-graph evidence, and ticket-wide review/closure.
- 2026-07-15 (registered add lifecycle milestone): Added the neutral `SourceAddPlanner` lifecycle hook and registry-owned selection/ambiguity verification. File, REST, and Postgres now own location parsing, format inference, egress hints, REST cursor proposals, and Postgres private DSN materialization inside their driver crates. Generic `add_command` no longer imports or matches concrete sources; it accepts repeatable `--option KEY=VALUE`, compiles the registered proposal through the ordinary declarative/source-plan path, discovers and pins through the same registry session, renders source-neutral location/selection/driver evidence, and writes driver-declared private files through one owner-only redacted path. Common cursor semantics are typed neutral proposal data rather than raw driver options. The deleted `AddRequestTarget`, file/REST/Postgres target structs, and ad-hoc Parquet shim were not retained; ad-hoc mode now consumes the same registered proposal and supports every no-secret file format recognized by the file driver while preserving stable format-derived identity and local-path redaction. Permanent tests cover registry selection/ambiguity, local Parquet/NDJSON, REST option/cursor pinning, Postgres secret redaction and mode, signed URL rejection, and local/HTTP ad-hoc security/reuse/failure/collision behavior. Verification: focused add 7/7 and ad-hoc 6/6 passed; the new NDJSON and registry hook cases passed independently; all touched crates passed all-target check and strict no-deps Clippy with 12 jobs; generated docs/completions/man pages are fresh. SX1 remains active only for its canonical live golden, source-isolated graph proof, ticket-wide adversarial review, and closure judgment.
- 2026-07-15 (portable source identity and canonical golden): The regenerated live golden exposed two operational facts contaminating package identity: absolute project roots entered the file-plan membership binding, and local inode/ctime entered serialized source generation. File membership now binds only logical resource/glob/path/count authority. Local open re-attestation still uses size, mtime, inode, and ctime internally for TOCTOU protection, while portable package evidence records the weak `local-metadata-v1:<size>:<mtime-ns>` generation and the terminal stream evidence records the canonical `sha256:` content digest. The fixture pins a deterministic source mtime and no longer mutates plan metadata after compilation. The resulting package—including compiled schema-admission and stream-admission evidence—has hash `sha256:bc3bfc9a4e89545e7a4f77f2193ad3c312ffe3a0aabaaa7324e2aae51cb472f2` across 100 independent project roots.
- 2026-07-15 (source-isolated graph proof): Added permanent Cargo-tree laws proving `cdf-runtime`, `cdf-declarative`, and `cdf-project` normal graphs exclude all concrete first-party source drivers, and each of file, REST, Postgres, and Python excludes every sibling driver plus orchestration/compiler/destination/package-implementation layers. Normal-graph unique package counts on this host are runtime 55, declarative 93, project 226, files 163, REST 111, Postgres 124, and Python 97. The full runtime suite passed 73 tests with one explicit release benchmark ignored, source-files passed 57, all four graph laws passed, and strict no-deps Clippy passed for runtime/source-files/conformance. SX1 now awaits only the ticket-wide two-slice adversarial review and closure judgment.
- 2026-07-16 (incremental first-party source repair): The ticket-wide review correctly falsified the advertised source memory bounds. Added one neutral `ExecutionServices::spawn_blocking_stream` boundary with bounded backpressure plus structural cancellation/join authority for synchronous adapters. REST now emits each bounded page before fetching the next, Postgres consumes `query_raw` through bounded row chunks rather than `Client::query`, and Python advances dict/Arrow C-stream iterators only after the previous batch is accepted while releasing the GIL across memory admission and downstream waits. All emitted batches retain shared-ledger leases and the three source sheets now truthfully declare pre-accounted, pausable production; Python is no longer mislabeled as necessarily bounded. The old whole-input vectors were deleted rather than retained as compatibility paths. A hostile Python generator test proves the downstream callback runs before generator exhaustion. Evidence: the four-crate all-target check passed; the consolidated library run passed Python 21, runtime 73 plus one intentional performance ignore, Postgres 11, and REST 7 tests; the new early-stop regression and both first-party source suites passed after the final cap centralization. This repairs the shared critical review finding but does not claim ticket closure; generation binding, egress/health, add atomicity, plan validation, mock conformance, typed position authority, and scheduler-capability consumption remain.
- 2026-07-16 (registry trust-boundary repair): Discovery observations now carry a framework-created hash binding the exact operational candidate, redacted evidence location, generation identity, size, and modification time; the registry rejects a driver observation from another inventoried generation even when its display URL is identical. The option-schema catalog is now executable authority rather than documentation: registration accepts an explicit, recursively validated Draft 2020-12 subset and rejects every unsupported keyword, while compile and add proposals are validated against the selected driver's closed source/resource schemas before driver code runs. Health results are driver-namespaced, bounded, control-free, recursively URI-redacted, and sensitive-key-redacted at the registry boundary; drivers without a bounded probe emit an explicit `Unsupported` result instead of disappearing. Hostile tests cover generation substitution, ignored unknown options, nested `oneOf`/type/range/prefix/URI constraints, unsupported schema keywords, arbitrary probe ids, secret-bearing details, and URI-bearing messages. Evidence: `CARGO_BUILD_JOBS=12 cargo test -p cdf-runtime -p cdf-source-files -p cdf-source-rest -p cdf-source-postgres -p cdf-python --all-targets` passed 75 runtime tests plus one intentional performance ignore, four build-graph laws, 57 file, 11 Postgres, 7 REST, and 22 Python tests; strict all-target no-deps Clippy passed for all five crates. Host-owned egress injection, add transactionality, complete plan validation, data-driven conformance, typed positions, scheduler consumption, application-scoped composition, and legacy deletion remain open.
- 2026-07-16 (host-owned source egress and legacy deletion): Added one engine-neutral `SourceEgressAuthorizer` authority to `SourceResolutionContext`; drivers receive only a credential-free, driver-bound `SourceEgressScope`. File HTTP metadata, every redirect hop, HTTP payload open, S3/GCS/Azure list/head/open, REST discovery/execution requests, and Postgres catalog/execution connection attempts must pass host policy before adapter policy or transport contact. URI parsing uses the standards-based URL parser and exposes only normalized scheme/host/port, never user info, path, query, or fragments. Hostile laws prove an adapter-wide allowlist cannot override a host denial and that denied files, REST, and Postgres make zero network calls. Deleted the obsolete Postgres-destination source exercise hook, its destination-owned source live tests/helpers, and the resulting `cdf-engine`, `cdf-source-postgres`, and `futures-executor` development edges rather than preserving a compatibility seam. Evidence: the four source/runtime suites passed 76 runtime tests plus one intentional performance ignore, four graph laws, 58 file tests, 12 Postgres tests, and 8 REST tests. Strict all-target no-deps Clippy passed across runtime, all touched sources and consumers, project, CLI, benchmarks, conformance, and Postgres destination. The Postgres destination's 15 non-live tests passed; its ten local-server live tests could not start `initdb` inside this sandbox (`Operation not permitted`) and are not claimed as runtime evidence.
- 2026-07-16 (`cdf add` no-mutation and failure-atomic publication): Removed observation-cache persistence from add discovery, so `--dry-run` performs bounded source I/O but leaves the complete project tree byte-identical. Replaced the command's ordered `fs::write` sequence with a generic project-file transaction in `cdf-project`: every path is relative/non-symlinked and unique, all expected prior bytes are validated under the existing project mutation guard, payloads are written and synced to same-directory temporaries, new files install without clobbering, replacements are atomic renames, parent directories are synced, and any error rolls every prior install back. Content-addressed discovery manifest/snapshot bytes are validated and joined in memory before publication; private driver state is mode `0600` from initial creation; resource/schema/private artifacts precede `cdf.toml`; and `cdf.lock` is required as the final commit point. Current lock bytes are semantically checked against the authority loaded by the command before freeze and byte-checked again at publication, closing the stale-context race. Evidence: permanent failpoint coverage proves rollback after multiple installs; success coverage proves commit-point ordering; owner-only coverage proves mode `0600`; both local-Parquet add write/dry-run CLI laws pass; and strict all-target no-deps Clippy passes for project and CLI. Crash-interrupted pre-commit files are unreferenced or fail closed behind the absent/stale final lock; this slice does not claim a cross-filesystem atomicity guarantee.
- 2026-07-16 (complete compiled-source-plan validation): Promoted descriptor and resource-capability validation to their kernel owners, then made `CompiledSourcePlan::new` pass through the same validator used for deserialized plans before it can escape a driver. The compiler binding and execution ceiling now also require canonical driver/resource ids, versions, SHA-256 identities, quota authority, and nonoverflowing working-set bounds. The boundary rejects forged empty ids/paths/scopes, invalid merge/dedup/freshness structure, duplicate or contradictory filter/partition capabilities, scalar option/physical payloads, malformed or mismatched canonical hashes, invalid effective-schema catalogs/budgets/manifest joins, and resource capability claims that exceed execution idempotency, pause, or spill authority. Hostile serialization tests mutate each authority class and prove failure before source contact; the coherent-forgery engine law now uses syntactically canonical hostile hashes and still reaches the external compiler binding. The consolidated runtime/file/REST/Postgres/Python library matrix passed (76 runtime plus one intentional performance ignore, 58 file, 12 Postgres, 8 REST, and 22 Python); the focused engine binding law passed; strict kernel/runtime all-target no-deps Clippy passed. A project-library diagnostic run additionally exposed existing independently owned sandbox-Postgres, source-frontier fixture, and stale diagnostic-expectation failures; the five auto-pin cases reach the runtime after validation, with one pre-existing retained-batch lease assertion remaining outside this slice, so no broad project-suite pass is claimed.
- 2026-07-16 (application-scoped source composition and HTTP pooling): Replaced per-command/per-phase built-in registry construction with one process-scoped immutable `SourceRegistry`. The registry captures one cloneable `ReqwestHttpTransport`; REST requests and file metadata/payload opens now share Reqwest's asynchronous connection pool and one lazily initialized blocking client across project load, discovery, validation, and execution. The clone shares only transport client state—the source/format/transform registries remain immutable capability authority and each resolved source still receives invocation-scoped secrets, execution services, egress, prepared payloads, and memory. Permanent tests prove registry pointer identity and blocking-pool identity across clones. The CLI all-target check passed, both focused pooling laws passed, and strict all-target no-deps Clippy passed for the HTTP transport and CLI with 12 jobs.
- 2026-07-16 (mandatory compiled authority and speculative-open admission): Deleted the remaining optional compiled-source-plan branches from CLI plan/run/report construction and backfill planning. Every executable backfill slice now binds the exact compiled source artifact, including planning-only CLI output; tests construct a real mock artifact rather than preserving an unbound compatibility state. The engine source frontier now consumes `speculative_safe` independently of retry policy: host concurrency cannot open later partitions for a source that has not proven speculative opens safe, while attested safe sources retain deterministic parallelism. Permanent tests prove both the parallel stalled-head case and the non-speculative serial gate. Evidence: both engine laws and all four backfill planner laws passed; the CLI/project/engine all-target check and strict all-target Clippy passed with 12 jobs.
- 2026-07-16 (typed planned-position authority): Added the required `PartitionPlan.planned_position` authority and migrated file planning to one typed single-file `FileManifest` unit. `start_position` remains prior-state resume authority; the new field is the complete planned source unit whose generation completion may enrich but never replace. File path, byte size, source generation, ETag, object version, and checksum were deleted from partition metadata; file open/reattestation, preview stratification, discovery, manifest incrementality, replay, deep validation, and benchmarks now consume the typed field. Old recorded plans without the field fail deserialization by design. Permanent hostile coverage rejects missing, multi-file, and scope-divergent planned authority. All 58 file-source tests, six HTTP project lifecycle tests, seven file-manifest incrementality/state laws, and the replay generation law passed. Strict all-target Clippy passed across kernel/runtime/files/engine/project/CLI/conformance/benchmarks with 12 jobs. The authority-bearing package migration regenerated all three live local-file goldens from verified current executions instead of retaining `seg-000001`/coercion compatibility artifacts: DuckDB and Parquet each passed 100 deterministic runs; Postgres successfully started a real local server, committed and verified the current package/receipt, and emitted the replacement golden before the expected old-golden comparison failed. A subsequent 10-repeat confirmation was terminated after an abnormal pre-test macOS link stall and is not claimed as repeat evidence.
- 2026-07-16 (data-driven external-source conformance and bounded Postgres portal): Replaced the closed `SourceArchetype` enum and duplicate preview/run source match trees with one open source identifier and fixture catalog. One prepared-source surface now feeds preview, plan, run, receipt, checkpoint, and replay laws; destination target names derive generically from the registered source id. A conformance-only external driver joins solely through its driver/schema registration and fixture entry, exposes nonempty custom option schema plus add/discovery/doctor hooks, and inherits the complete destination/disposition matrix. Append and replace fixtures now carry no merge authority, while merge alone records `merge_key`; replay assertions enforce the same disposition rule. The matrix exposed the recent synchronous Postgres `query_raw` stream failing with a closed connection. Replaced it with an 8,192-row server portal inside one read-only repeatable-read snapshot, preserving bounded memory and stable source semantics while deleting the direct `fallible-iterator` dependency. Evidence: both focused external-driver lifecycle/run laws passed; the live catalog matrix passed 40 executed cells and five sheet-owned Parquet-merge exclusions across file, Python, REST, SQL, and external sources with package verification, receipt gate, checkpoint, duplicate, and artifact replay assertions; strict all-target no-deps Clippy passed for Postgres source and conformance with 12 jobs. The three diagnostic matrix attempts before the portal repair are not pass evidence: they successively found stale key authority, a stale replay assertion, and the real Postgres stream regression.
- 2026-07-16 (scheduler authority and single-pass driver health): Deleted the optional `SourceHealthProbe` adapter and the CLI's second generic resolve/partition doctor pass. Every registered driver must now implement one bounded health operation over only its registry-validated compiled plans and the host-injected resolution context. File checks perform metadata inventory, REST checks one record under a one-MiB discovery budget using an isolated prepared-payload store, Postgres checks catalog metadata, and Python checks the configured interpreter without executing resource code. The isolated REST store was required by a focused test that initially caught doctor contaminating the same-process execution payload handoff. The registry groups plans by exact driver authority, canonicalizes order, namespaces/redacts results, and no source-id branch exists in generic doctor code. Replaced the lossy per-second capability with protocol-neutral `SourceRateLimit { operations, interval_ms }`; REST records the exact requests-per-minute budget and a normalized credential-free origin authority. Rate, quota, boundedness, retry, ordering, working-set, and executor declarations are now copied into every canonical scheduled partition and the runtime scheduler join, revalidated against compiled source authority immediately before extraction, and hostile mutation reaches the engine's rejection path. Evidence: the focused runtime scheduler law, REST rate/origin law, file and REST driver health laws, external mock registry/add/discovery/doctor law, file and Python CLI doctor laws, and engine hostile scheduler law passed. All touched runtime/source/engine/declarative/CLI/conformance targets passed strict all-target no-deps Clippy with 12 jobs. One misspelled conformance filter selected zero tests and is deliberately excluded from evidence.
- 2026-07-16 (ticket-wide two-slice closure review): The requested one-pass architecture and correctness reviews returned one consolidated repair set rather than serial nit loops. Critical findings: REST materialized unbounded, multiply copied response bodies before memory admission; compiled source/health evidence could retain credential-bearing URLs. Shared significant findings: executable source/scheduler authority remained optional; rate/quota/boundedness were descriptive; doctor lacked total cancellation/work/output budgets; REST's DOM/NDJSON path exceeded its advertised working set; discovery repeated file inventory; and the external mock did not inherit the complete extension-law matrix. Architecture additionally identified the prepared-inventory handoff; correctness additionally identified generated-artifact redaction and preview parity. No finding is waived and no closure pass is claimed until the complete set is repaired.
- 2026-07-16 (bounded REST response and truthful scratch envelope): Replaced the REST transport's full `response.bytes() -> Vec -> String` path with a mandatory response budget carrying a memory coordinator, byte ceiling, and cancellation check. Fixed-length oversize rejects before reservation/read; unknown/chunked bodies reserve the complete configured window, read only to the limit, probe one boundary byte, and retain the conservative lease with the zero-copy `Bytes` payload. `HttpResponse` no longer hides bodies as strings in pagination fields. Discovery reuses that body lease instead of duplicating it. The current DOM fallback now moves selected objects instead of cloning them and reserves an explicit conservative structural/NDJSON/allocator envelope before parsing; the scheduler records the resulting true maximum until B5 replaces the fallback with the tape decoder. Permanent transport laws cover pre-allocation `Content-Length` rejection and exact unknown-length cutoff with zero residual memory. The REST library's eight tests pass; strict all-target no-deps Clippy passes for memory/HTTP transport/REST with 12 jobs; project, conformance, and benchmark all-target checks pass after fixture migration to budget-accounted bodies.

- 2026-07-16 (credential-safe compiled evidence and health failures): Made the registry boundary reject coherently rehashed compiled artifacts that contain raw sensitive fields or absolute URIs with user information, query parameters, or fragments. File roots now reject credential-bearing/signed URLs in favor of transport secret authority. REST base URLs and paths are query-free compiled structure; credential-bearing query parameters must be `secret://` references, resolve only in the runtime request builder, and never enter plan/package evidence as values. Health failures now expose only the typed error class and resource id; raw adapter error strings are forbidden, mixed URI/text detail strings fail closed, and duplicate namespaced probe ids are rejected. Permanent hostile tests cover forged plan payloads, URL userinfo/query, raw API parameters, safe secret parameters, and multi-URI health text. Focused runtime registry tests, the file portable-root law, and the REST common-context/credential law pass. Strict all-target Clippy passes for runtime, file, REST, Postgres, and their transitive contracts with 12 jobs.

- 2026-07-16 (live scheduler contracts): Executable engine plans now fail closed unless both the compiler-bound source artifact and its canonical partition schedule are present, and bounded execution rejects a source that does not declare finite completion. Python resources declare boundedness explicitly at the SDK boundary instead of being permanently mislabeled unbounded. Replaced REST's per-resource descriptive request counter with one source-neutral, invocation-shared quota gate in `ExecutionServices`: exact integer token accounting, host-injected monotonic and wall clocks, cancellation-aware delays, conflict rejection for one authority carrying two policies, and scheduler telemetry for admissions, delay events, wait time, and server-response deferrals. REST discovery and execution use the same gate; `Retry-After`, 429, and configured quota-reset headers defer every concurrent resource sharing the normalized credential-free origin rather than only one paginator. Permanent runtime coverage proves clone-shared admission, exact timing, response deferral, telemetry, and policy conflict rejection. The focused runtime, HTTP, REST, engine, and Python tests pass. Strict all-target Clippy passes across runtime, HTTP, REST, engine, Python, CLI, and conformance with 12 jobs.

- 2026-07-16 (bounded source health authority): Replaced driver-local notions of a “bounded” doctor probe with one clone-shared `SourceHealthBudget` injected by the registry. The default command envelope is an explicit 30-second deadline, 10,000 work units/list entries, 4,096 results, eight MiB each of aggregate detail and payload allowance, and 64 KiB of subprocess output; every value is validated and callers may supply a tighter policy. File, REST, Postgres, Python, and the external mock consume the same work/list/payload counters, while the registry atomically owns aggregate result/detail admission. File transport listing stops at the caller boundary before collecting object-store results; recursive local traversal also stops at the same aggregate entry boundary rather than collecting an unbounded directory. Python doctor uses the shared `cdf-subprocess` supervisor instead of `Command::output`: stdout and redacted stderr are concurrently bounded, overflow sentinels are memory-ledger accounted before spawn, retained stdout transfers its lease into the decoder without a copy or second reservation, and deadline/cancellation kills and reaps the child. Permanent tests prove aggregate/deadline rejection, bounded remote/local inventory, output/memory rejection, interpreter oversize and stall termination, first-party health behavior, and the external-driver lifecycle law. Library suites passed for runtime (78), file (59), Python (23), and subprocess (13), with one explicit runtime performance benchmark ignored by design. Strict all-target Clippy passed across runtime, all four source implementations, subprocess, CLI, and conformance with 12 jobs.

## Review

The registry-discovery milestone received an adversarial review before commit. Its initial verdict was `concerns`; follow-up attempts found narrower full-plan, forged/cached evidence, unknown-size identity, and fragment-redaction defects. All were repaired and falsified by focused tests. Final independent verdict: `pass`. Residual risk is the explicitly listed legacy adapters and lifecycle hooks, so this is a milestone review and SX1 remains active.

The compiled scan-intent milestone's first independent verdict was `fail` with four significant findings and one legacy-surface concern. All four shared-boundary findings and the optional REST execution fallback were repaired. Fresh re-review found two further significant defects: empty Parquet projections applied the order permutation twice, and REST discovery/resolve did not revalidate compiled capability truth against the executable cursor plan. Both were repaired with non-vacuous heterogeneous-type coverage and pre-transport capability validation. Final independent verdict: `pass`, with no critical or significant residual risk. The review's one minor note about transport parser errors flowing through boolean helpers was eliminated by matching the typed transport scheme once in runtime dispatch. SX1 remains active because this is not its registry-open lifecycle closure.

The registry-only stream-admission batch received one bounded independent review with verdict `fail` and three significant findings: projection-unstable baseline identity, unverified resolved executable authority, and a surviving single-implementation adapter. The implementation pass above addresses each finding with permanent falsification coverage and the consolidated runtime/engine/project/source/strict-Clippy matrix is green. Per the program's anti-thrash review policy, the same batch was not sent through repeated review cycles. SX1 remains active for the explicit registry-open declarative/add/doctor/Python/build-graph tail; this milestone is not ticket closure.

The 2026-07-15 ticket-wide closure review deliberately split two fresh reviewers across correctness/security/identity/lifecycle and architecture/extensibility/performance/legacy. Both independently returned `fail` on the same critical defect: REST, Postgres, and Python advertised bounded source working sets while materializing complete inputs before exposing a stream. Their complementary significant findings are now one repair set: discovery-generation binding; host-owned egress and health validation; read-only/failure-atomic add; complete compiled-plan validation; data-driven mock-source conformance; typed incremental-position authority instead of file metadata strings; live scheduler consumption of declared capabilities; runtime-authoritative option schemas; one application-scoped registry/client set; complete driver-owned doctor probes; and deletion of obsolete destination-owned Postgres source hooks and optional-plan shims. No closure or pass is claimed until those shared-boundary repairs and their hostile laws land. Reviewers ran no redundant broad suites and were not asked for serial re-review.
