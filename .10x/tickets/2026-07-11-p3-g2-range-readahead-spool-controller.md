Status: active
Created: 2026-07-11
Updated: 2026-07-14
Parent: .10x/tickets/2026-07-10-p3-ws-g-remote-io-overlap.md
Depends-On: .10x/tickets/2026-07-11-p3-g1-streaming-transport-byte-sources.md, .10x/tickets/done/2026-07-11-p3-c1-scheduler-admission-contract.md, .10x/tickets/done/2026-07-11-p3-a2-unified-memory-ledger.md

# P3 G2: per-origin range, readahead, spool, and cache controller

## Scope

Implement accounted per-origin connection/range admission, low-gain BDP feedback, coalescing/readahead, exact range retries, bounded spool with early decode where safe, and opt-in cryptographic cache promotion/revalidation.

## Acceptance criteria

- High-BDP fixtures saturate without unbounded buffers; throttle/pressure reduces concurrency cleanly.
- Coalescing/prefetch never changes logical bytes/order and reports waste.
- Spool/cache disk/security/identity/cleanup laws pass; cache state cannot change packages.
- Fixed/auto/manual settings are observable and outside package identity.
- Full/growing spools admit only finite known lengths reserved in full before transfer; over-budget objects use generation-bound ranges or proven monotone prefix eviction, otherwise fail cleanly.
- Unbounded sources never enter the finite spool path; rolling replay retention is bounded and checkpoint-evicted under the stream-epoch owner.

## Evidence expectations

Controller simulations, high-BDP/throttle benchmarks, memory/disk/cancel chaos, cache/spool identity/security tests, and network profiles.

## Explicit exclusions

No format-specific range planning.

## Blockers

Depends on G1, C1, and memory ledger.

## References

- `.10x/specs/remote-local-io-overlap.md`
- `.10x/specs/schema-discovery-and-stream-admission.md`

## Progress and notes

- 2026-07-11: Removed the serialized 8 MiB range-loop spool. `FileTransport` now exposes one sequential transfer into the bounded spool; HTTP streams one GET with `If-Match` where available and weak identities reattest, while object stores stream through the injected I/O host. Full/unknown remote Parquet execution uses this path. Controller feedback, preallocation, cache, and high-BDP range work remain open. Evidence: `.10x/evidence/2026-07-11-http-parquet-sequential-spool-and-positioned-slicing.md`.
- 2026-07-11: Removed the remaining global transport mutex and moved remote partition validate/spool/decode forwarding into one injected I/O scope per partition. Engine jobs can now overlap independent HTTP/object-store opens; each remote forwarding edge is bounded to two batches, while local native files retain their direct stream with no added channel. Shared transport concurrency reaches two simultaneous callers in the permanent test, all 17 source tests pass, and the real HTTP-Parquet project fixture passes. Adaptive BDP/range/cache policy and the fully async HTTP provider remain open. Evidence/review: `.10x/evidence/2026-07-11-p3-g2-concurrent-transport-spool.md`, `.10x/reviews/2026-07-11-p3-g2-concurrent-transport-spool-review.md`.
- 2026-07-11: Follow-up review found that creating the remote scope inside the returned open future could let an immediately ready first `FuturesOrdered` entry delay polling later admissions. Remote scope creation now occurs eagerly during `ResourceStream::open`, so filling C2's bounded frontier starts every admitted remote transfer; the returned future only exposes the already-started bounded stream. Source and real HTTP-Parquet tests plus strict lint remain green.
- 2026-07-12: Added the first explicit access-policy join. Format drivers declare sequential/seekable/adaptive access; sequential HTTP/object-store formats stream directly, while adaptive full/unknown scans use one verified spool. This prevents capability availability from accidentally selecting pathological ranges. Selectivity propagation, controller feedback, retries, cache, and overlap remain open. Evidence/review: `.10x/evidence/2026-07-12-p3-g1-async-http-byte-source.md`, `.10x/reviews/2026-07-12-p3-g1-async-http-byte-source-review.md`.
- 2026-07-12: Migrated strong-generation full-scan spooling from synchronous transport download to the injected async byte source. The spool reserves the shared spill budget before transfer, streams accounted chunks with async writes, hashes/verifies once, and retains disk authority through decode. Growing-spool overlap, weak providers, transform staging, chaos, cache, and controller feedback remain. Evidence/review: `.10x/evidence/2026-07-12-p3-g2-accounted-async-full-scan-spool.md`, `.10x/reviews/2026-07-12-p3-g2-accounted-async-full-scan-spool-review.md`.
- 2026-07-12: Removed provider-input plus transformed-output double spooling for injected sources. Unknown transformed lengths now grow the shared spill reservation before disk writes; sequential transformed codecs avoid the spool entirely, while adaptive codecs retain one bounded transformed-output spool. Growing-spool decode overlap remains open. Evidence/review: `.10x/evidence/2026-07-12-p3-b1-streaming-transform-product-composition.md`, `.10x/reviews/2026-07-12-p3-b1-streaming-transform-product-composition-review.md`.
- 2026-07-13: The fixed-schema discovery/stream-admission model makes G2's cache/spool controller the payload-reuse owner, not schema discovery. A materially downloaded schema observation must continue through the same live stream or exact spool; a small unspooled bounded probe may be reread within its recorded budget. Cache keys include source generation plus codec/options/normalizer/contract identity. G2 must expose this reusable source session without embedding schema or format policy.
- 2026-07-13: The corrected lifecycle permits rereading a small unspooled bounded probe but requires same-command reuse of any fully downloaded/decompressed payload spool. G2 owns the neutral reusable spool/session handoff; schema discovery owns neither transport caching nor format-specific extraction.
- 2026-07-14: Live FineWeb execution followed Hugging Face's allowlisted CAS redirect and completed one 2,147,509,487-byte full scan without returning to range fan-out. End-to-end debug time was 92.58 seconds; extraction/package took about 90.8 seconds, DuckDB/final gate about one second, and an in-flight sample showed 2.0 GiB RSS while the package had reached 1.1 GiB. Raw sequential `curl` of the exact URL completed in 19.116 seconds at 112,341,173 bytes/s, so CDF's 4.84x wall ratio is not a network roofline. G2 still owns growing-spool decode overlap and transfer progress telemetry; B2 owns separating local decode/package cost from the now-measured download floor.
- 2026-07-14: Added the neutral strong-generation growing-spool substrate. It reserves the complete finite object length before transfer, exposes only already-written local ranges plus a bounded generation-bound tail window, joins transfer completion with partition completion, and leaves weak/unversioned sources on the verified full-spool path. The same slice removed hashing work when no source checksum can consume it. A deterministic gated-source test proves prefix/tail overlap, middle-range backpressure, one bounded upstream range, and zero retained memory/spill after completion. Live FineWeb evidence is `.10x/evidence/2026-07-14-p3-g2-fineweb-growing-spool-overlap.md`. Controller telemetry, adaptive tail/range policy, cache, large-object prefix eviction, and chaos remain open.
- 2026-07-14: Made full/growing spool admission an atomic policy choice rather than a mandatory prerequisite. If a strong, known-length, exact-range source exceeds the per-object spool ceiling or cannot reserve its complete length from the shared spill coordinator, execution retains the original generation-bound byte source and the registered format driver reads ranges directly. The same Parquet fixture now proves admitted spool, configured-over-budget fallback, and concurrent-budget-contention fallback with identical row counts and exact spill accounting. Weak and transformed inputs retain their safer paths. Evidence: `.10x/evidence/2026-07-14-p3-g2-large-object-range-fallback.md`. Coalescing, BDP feedback, readahead/waste telemetry, cache, and proven monotone eviction remain open.
- 2026-07-14: Moved exact-range batching behind the neutral `ByteSource` boundary. The default controller coalesces overlap/adjacency without extra egress, fans physical requests out to the source capability ceiling, restores codec-requested order, and reports logical bytes, physical bytes, and request count. Coalesced zero-copy slices retain one complete physical memory lease. Parquet now delegates instead of owning provider concurrency; a loopback HTTP fixture proves three logical extents become two generation-bound requests with exact output order and accounting. Evidence: `.10x/evidence/2026-07-14-p3-g2-transport-neutral-range-batching.md`. Gap-prefetch policy, adaptive BDP feedback, retry/telemetry, cache, and eviction remain open.
- 2026-07-14: Carried the range batch's previously discarded measurements through one neutral observed-byte-source seam and the partition EOF barrier. Successful file runs now record `source_read` wait time, physical/logical bytes, and physical request count outside package identity; the CLI derives and names prefetch waste. Sequential timing excludes downstream backpressure between source polls, while concurrent range wait is explicitly cumulative work. The implementation also deleted duplicated local/remote source-opening branches so hashing, transforms, spool selection, and codecs share one invocation boundary. Evidence: `.10x/evidence/2026-07-14-p3-g2-source-io-controller-telemetry.md`. Adaptive feedback, gap prefetch, retry/throttle observations, cache, eviction, rolling spool, and failed/partial-run telemetry remain open.
- 2026-07-14: Removed the object-store adapter's legacy “sequential” exact-range loop. Strong and weak S3/GCS/Azure scans now use one full-object stream: strong generations bind ETag/version conditions, weak generations retain terminal reattestation, content hashes verify in flight, provider frames are ledger-admitted and zero-copy sliced to the requested downstream shape, and empty generations no longer need a synthetic range. This also makes `source_read.requests = 1` truthful for object-store sequential scans. Evidence: `.10x/evidence/2026-07-14-p3-g2-object-store-single-stream.md`. Selective/over-budget scans retain exact generation-bound ranges.
- 2026-07-14: Added the first bounded sparse-range readahead policy. Batched exact reads are the codec capability assertion; the source controller alone chooses physical coalescing. Remote HTTP/object stores admit gaps up to 64 KiB only while the physical request stays within its byte ceiling and at most 1.25x useful coverage; local reads remain adjacency-only. Accounting now separates returned logical bytes, useful physical coverage, overlap reuse, physical bytes, and actual waste. Evidence: `.10x/evidence/2026-07-14-p3-g2-bounded-gap-readahead.md`. Adaptive per-origin feedback and controller-mode rendering remain open.
- 2026-07-14: Added a neutral cancellable delay to the injected execution host. Backoff can now run on the one host-owned I/O runtime instead of blocking an adapter lane, assuming an ambient Tokio context, or creating a transport runtime. This prerequisite deliberately ratifies no new attempt/deadline/jitter values. Evidence: `.10x/evidence/2026-07-14-p3-g2-injected-delay-authority.md`. Exact range retry policy/execution and retry telemetry remain open.
- 2026-07-14: Source-read telemetry now records and renders the selected neutral access strategy: direct stream, exact ranges, full spool, growing spool, or mixed access. Strategy-specific metrics aggregate separately and remain outside identity. The spool controller explicitly labels its selection, while ordinary stream/range activity is inferred at the `ByteSource` boundary. Evidence: `.10x/evidence/2026-07-14-p3-g2-source-read-mode-telemetry.md`. Retry/throttle/controller feedback, disk high-water, cache, eviction, rolling spool, and failed/partial telemetry remain open.
- 2026-07-14: External-project live follow-up ran all five `github.userdata` Parquet files through package, DuckDB receipt, and checkpoint in 4.18 seconds. It rendered `growing_spool`, 349 KiB physical, 339 KiB unique useful, 9.9 KiB duplicate tail/footer waste, and 15 physical requests. The live run found two defects not visible in focused tests: normal progress discarded the metric field, and cross-operation tail bytes were counted useful again when the full spool covered them. Normal progress now includes typed phase metrics, while full-coverage strategies cap useful bytes at the generation extent; selective exact-range mode remains measured rather than assumed. Evidence: `.10x/evidence/2026-07-14-p3-g2-source-read-mode-telemetry.md`.
- 2026-07-14: Made spool body waits cancellable at the neutral runtime boundary. `RunCancellation::await_or_cancel` drops a pending provider future even when that provider does not cooperatively inspect CDF's token; both growing and finalized spool loops now use it. A deterministic chaos law cancels after one prefix is durable while the provider and a middle-range decoder are both blocked, then proves both futures terminate, the full reservation remains retained only while the session owns it, and memory, disk, and the temporary file all return to zero after owner release. Evidence: `.10x/evidence/2026-07-14-p3-g2-growing-spool-cancellation-cleanup.md`. Live provider socket teardown, failed-run telemetry, eviction, and rolling spool remain open.
- 2026-07-14: Landed the codec-side prerequisite for progressive finite-object eviction. `DecodeUnitPlan.extent` is now a complete source-read-envelope contract, the neutral runtime derives monotone no-lookback frontiers only from complete canonical envelopes, and Parquet row groups publish and conformance-test those envelopes. Evidence: `.10x/evidence/2026-07-14-p3-b2-parquet-no-lookback-byte-envelopes.md`. The source-side bounded residency store and frontier-consumption lifecycle remain open; current full/growing spools continue to reserve the entire admitted object.
- 2026-07-14: Landed the lifecycle half of progressive eviction. The canonical unit frontier publishes completion exactly once in canonical order, and registered format execution forwards the corresponding codec-derived offset through neutral `ByteSource::release_before`. Evidence: `.10x/evidence/2026-07-14-p3-c2-canonical-unit-completion-frontiers.md`. The hook is intentionally a no-op until a bounded residency source can physically reclaim storage; generic orchestration contains no source or format branch.
- 2026-07-14: Completed the physical bounded-residency half. Oversized finite strong sources can now stream once through a fixed-capacity ring spool; the producer backpressures at the disk frontier, canonical codec completion alone authorizes overwrite, out-of-window reads retain generation-bound exact-range escape, and final codec EOF releases an otherwise-unused tail. The shared spill ledger reserves capacity rather than object length, while weak sources remain on complete verified spool/fail semantics. Evidence: `.10x/evidence/2026-07-14-p3-g2-bounded-evicting-spool.md`. G2 remains active for high-BDP feedback, retries/throttle observations, cache promotion, rolling replay retention, and live large-object envelope evidence.

## Review

- 2026-07-14 growing-spool slice — adversarial self-review traced generation authority, disk admission, temporary-file lifetime, completion/error propagation, cancellation, notification races, checksum behavior, range fan-out, and the absence of format/destination identity branches. The first pass found two significant internal risks before commit: a `Notify::notify_waiters` race could miss terminal publication, and a byte-count mismatch could mark readers failed while returning successful transfer completion. The repair registers each waiter before inspecting progress and makes the completion invariant fail the joined source future. Focused tests and strict Clippy pass after the repairs. Verdict: **pass for this slice**. Residual risk is explicitly open: fixed tail tuning/telemetry, cancellation chaos, very-large-object eviction/ranges, cache, and the 3.09 GB FineWeb RSS envelope.
- 2026-07-14 large-object fallback slice — adversarial self-review checked the decision at the actual atomic reservation point, concurrent spill ownership, generation enforcement on every fallback read, transformed-byte safety, weak-provider behavior, package equivalence, and orchestration layering. It found and corrected an ordering hazard: spool-size rejection originally preceded identity/capability validation and could have routed an oversized weak source toward an unsafe caller fallback. Capability validation now precedes the optional admission result, and the permanent test fixes that order. Verdict: **pass for this slice**. Residual risk remains open in the controller: uncoalesced ranges may be latency-bound, and no progressive prefix eviction or cache exists yet.
- 2026-07-14 exact-range batch slice — adversarial self-review traced out-of-order inputs, overlaps, shared allocation ownership, capability ceilings, cancellation, physical-length disagreement, duplicate/missing output slots, and codec/provider layering. No significant or critical finding remains. Single extents larger than the preferred request ceiling deliberately retain their contiguous request shape; silently splitting and reassembling them would add an unbudgeted copy and hide a codec working-set problem. Verdict: **pass for this slice**. Residual risk remains explicit: exact adjacency alone does not solve high-BDP sparse ranges, and adaptive feedback/retry/telemetry are still open.
- 2026-07-14 source-I/O telemetry slice — adversarial self-review traced full/growing spool, strong range fallback, weak hash sweeps, transforms, retained discovery payloads, coalesced overlap, concurrent units, cancellation/error paths, EOF ownership, hot-path cost, and identity separation. It corrected the first timing implementation, which included consumer backpressure across the entire sequential stream; the final counter sums only source open/body-poll wait. It also verified one terminal counter update per sequential request and one per physical range batch, avoiding wrapper/default-method double counting. Verdict: **pass for this slice**. Residual risk is explicit: failed/partial invocations do not publish EOF metrics, and retry/throttle/controller-mode telemetry remains open.
- 2026-07-14 object-store single-stream slice — adversarial self-review compared strong/weak/content-addressed generation laws, empty objects, initial and terminal attestations, provider-frame bounds, zero-copy slicing ownership, checksum timing, cancellation, exact selective fallback, and telemetry semantics. It found two correctness constraints during implementation: full GET results must prove the exact `0..size` range before publication, and sliced `Bytes` must retain the complete provider-frame lease rather than reconcile to visible slices. Both are permanent in code/tests. Verdict: **pass for this slice**. Residual risk remains live-provider frame behavior and throttle/cancellation chaos across S3/GCS/Azure.
- 2026-07-14 bounded gap-readahead slice — adversarial self-review exercised unordered, adjacent, overlapping, duplicate, sparse, oversized, amplification-rejected, and request-ceiling cases; traced zero-copy lease retention, cancellation, source policy dispatch through the observation wrapper, and package-identity exclusion. It found and corrected a measurement flaw before commit: logical slice bytes cannot measure waste when ranges overlap, so the final model carries useful coverage separately and maps phase output to useful—not returned—bytes. No significant or critical finding remains. Verdict: **pass for this slice**. Residual risk is fixed-policy calibration until BDP/throttle feedback lands.
- 2026-07-14 injected-delay slice — adversarial self-review traced runtime ownership, polling outside Tokio, cancellation before and during the wait, worker-task cleanup, test-host obligations, and dependency layering. No transport/source identity appears in the host API, the timer consumes no CPU/blocking slot, and the cancelled branch aborts and joins the timer task before returning. Verdict: **pass for this prerequisite**. Residual risk is policy authority: the existing HTTP retry defaults and REST's ignored returned delay are not silently promoted into file-range semantics.
- 2026-07-14 source-read-mode slice — adversarial self-review traced every current preparation branch: retained payload, local direct stream, local verified ranges, strong admitted growing spool, strong disk-refused range fallback, weak full spool, and transformed full/direct paths. It also checked aggregation ordering, event validation, CLI rendering, and identity separation. The review rejected provider-method inference as sole authority because every spool performs sequential reads internally; the final implementation lets the neutral spool controller explicitly override inferred activity. Conflicting explicit modes fail as an internal invariant. Verdict: **pass for this telemetry slice**. Residual risk is that failed/partial invocations still do not publish their accumulated mode/counters.
- 2026-07-14 source-read-mode live follow-up — the external smoke run falsified two claims in the first self-review: formatting a metric did not prove that normal progress selected that field, and per-operation useful coverage did not prove cross-operation uniqueness. Both are repaired and have focused plus live evidence. Full-coverage strategies now use the immutable generation length as their unique-byte ceiling; exact selective ranges are deliberately not given that shortcut. Verdict: **pass after follow-up**. Residual risk remains exact-range overlap across separate batches and failed/partial telemetry; both stay open rather than being reported as zero waste.
- 2026-07-14 spool-cancellation slice — adversarial self-review traced cancellation before open, during a non-cooperative provider wait, after a published prefix, through a blocked reader, and across completion/error notification and RAII owner release. It found the original between-chunk checks could not interrupt a provider future that never returned; the repair belongs on `RunCancellation`, not in a transport or codec. The chaos fixture deliberately leaves its provider semaphore closed and therefore proves the wrapper drops pending I/O rather than relying on provider cooperation. No significant or critical finding remains. Verdict: **pass for this slice**. Residual risk stays open for live socket teardown latency, cancellation during OS file writes, failed/partial telemetry, and bounded eviction/rolling-spool policies.
- 2026-07-14 no-lookback prerequisite — adversarial self-review required the proof to fail closed for missing envelopes, remain monotone with overlapping or physically out-of-order units, reject noncanonical ordinals, and bind driver reads rather than merely estimate locality. Parquet's asserting metadata helper was removed in favor of checked conversion. Verdict: **pass for the proof layer**. Residual risk remains source-side: no eviction is authorized until the spool consumes completed-unit frontiers and physically reclaims storage under the disk ledger.
- 2026-07-14 completion-lifecycle prerequisite — adversarial self-review rejected release on task scheduling, last observed batch, or out-of-order unit EOF. The accepted hook fires only after canonical EOF, preserves callback errors as frontier failures, and forwards only through offset-preserving byte-source decorators. Verdict: **pass for the lifecycle layer**. Residual risk is the still-unimplemented bounded physical residency store.
- 2026-07-14 bounded-eviction slice — adversarial self-review traced out-of-order unit completion, overlapping/future envelopes, wraparound reads, release races, local read/write exclusion, sequential checksum coverage, cancellation ownership, disk admission, weak generation rejection, and the possibility of producer deadlock when a requested future extent lies beyond current capacity. The final design releases only from the canonical frontier, rechecks residency while holding the read lock, routes ahead-of-window extents to the strong upstream range source, and publishes the full generation frontier at codec EOF. No significant or critical finding remains. Verdict: **pass for this slice**. Residual risk remains fixed 512 MiB tuning, live multi-gigabyte selection/performance, and G2's controller/cache/retry work.

## Retrospective

The performance failure had two independent serial barriers: repeated Parquet footer parsing on every row group and full transfer completion before any seekable decode. Fixing only the first made local decode competitive but would not have made remote execution I/O-bound. A neutral prepared codec session plus a neutral growing `ByteSource` removed both without teaching source orchestration about Parquet. The live roofline immediately exposed the correct success criterion: cumulative CPU stages may exceed wall time when overlap is working; package wall versus contemporaneous transfer is the useful measure. The remaining memory/disk work must preserve that overlap while replacing full retention with byte-aware residency, generation-bound ranges, or a proven monotone eviction frontier.
