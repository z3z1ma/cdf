Status: recorded
Created: 2026-07-09
Updated: 2026-07-09
Relates-To: .10x/tickets/done/2026-07-09-p2-ws-a7-schema-pin-show-diff-cli.md, .10x/tickets/done/2026-07-09-p2-ws-d3-file-manifest-incremental-noop.md, .10x/tickets/done/2026-07-09-p2-ws-i2-preview-run-parity-and-golden-path-matrix.md, .10x/tickets/2026-07-08-p2-data-onramp-program.md

# P2 A7/D3/I2 schema CLI, file manifest no-op, and conformance foundation

## What was observed

The A7/D3/I2 batch adds three P2 foundation slices:

- `cdf schema pin`, `cdf schema show`, and `cdf schema diff` are wired through the generic discovery dispatcher for local Parquet, declarative Postgres table resources, and REST sample discovery. Diff is no-write; pin writes snapshots and updates an existing lockfile resource entry where the current project model supports it.
- Local append file resources now compare planned file identities against the committed `FileManifest`, skip unchanged files as an explicit no-op, load only new or changed files, merge changed manifests into checkpoint state, and keep replace disposition planning every file.
- `cdf-conformance` now has a named P2 data-onramp matrix foundation for S1-S8, a preview/run parity law for currently supported local file, REST fixture, and Postgres table archetypes, and a concrete eighteen-friction registry mapping to closed tests, active tickets, or honest exclusions.

The batch does not complete P2. Public HTTPS runtime/discovery, `cdf add`, cloud transports, compression, deep validate diagnostics, final S1-S8 green conformance, and the S1+S2 terminal recording remain open under existing P2 workstreams.

## Procedure

Focused Rust and unit checks:

- `cargo check -p cdf-declarative -p cdf-project -p cdf-cli -p cdf-conformance --tests --locked`: passed.
- `cargo test -p cdf-project file_manifest --locked`: passed.
- `cargo test -p cdf-declarative file_glob --locked`: passed.
- `cargo test -p cdf-cli schema_ --locked`: passed after the schema-command duplication cleanup.
- `cargo test -p cdf-conformance p2_ --locked`: passed.
- `cargo test -p cdf-conformance run_matrix --locked`: passed.
- Combined rerun after final fixes: `cargo test -p cdf-project file_manifest --locked && cargo test -p cdf-declarative file_glob --locked && cargo test -p cdf-conformance p2_ --locked`: passed.

Formatting, static checks, duplication, and complexity:

- `cargo fmt --all`: applied.
- `cargo fmt --all -- --check`: passed.
- `cargo clippy -p cdf-cli -p cdf-project -p cdf-declarative -p cdf-conformance --all-targets --locked -- -D warnings`: passed.
- `git diff --check`: passed before record closure.
- `jscpd crates/cdf-cli/src crates/cdf-project/src crates/cdf-declarative/src crates/cdf-conformance/src --reporters json,console --output reports/ai-quality/jscpd-p2-a7-d3-i2-after --ignore "**/target/**,**/.git/**,**/reports/**"`: passed with 348 existing clones, 3,766 duplicated lines, 6.21%, `newClones=0`, `newDuplicatedLines=0`.
- `jscpd crates/cdf-cli/src/schema_command.rs --reporters json,console --output reports/ai-quality/jscpd-p2-schema-command-after --ignore "**/target/**,**/.git/**,**/reports/**"`: passed with 0 clones.
- `rust-code-analysis-cli -m -p crates/cdf-cli/src -p crates/cdf-project/src -p crates/cdf-declarative/src -p crates/cdf-conformance/src -O json -o reports/ai-quality/rust-code-analysis-p2-a7-d3-i2-after`: completed and wrote per-file JSON reports. Touched hotspots include `run_project_inner` in `cdf-project` and `from_snapshots` in `schema_command`; neither newly failed a configured gate.

Security and supply-chain checks:

- `semgrep scan --config p/rust --error crates/cdf-cli/src crates/cdf-project/src crates/cdf-declarative/src crates/cdf-conformance/src`: passed with 0 findings.
- `gitleaks dir --redact --report-format json --report-path reports/ai-quality/gitleaks-p2-a7-d3-i2-crates.json crates`: passed with no leaks.
- `gitleaks dir --redact --report-format json --report-path reports/ai-quality/gitleaks-p2-a7-d3-i2-10x.json .10x`: passed with no leaks before final record edits.
- `cargo deny check`: passed. Known duplicate Arrow 58/59 warnings remain governed by the existing duckdb residual acceptance.
- `cargo audit`: exited 0 with the already-ratified `paste` advisory warning.
- `cargo vet --locked`: passed.
- `cargo machete`: passed with no unused dependencies.
- `osv-scanner --lockfile Cargo.lock`: reported only `paste 1.0.15` / `RUSTSEC-2024-0436` with no fix available; this is already governed by the ratified scoped Parquet/DataFusion advisory exception.
- `tools/codeql-rust-quality.sh`: passed using the reusable database path `target/quality/codeql-db-rust`. The content fingerprint changed, so the script refreshed the reusable database instead of creating an ad hoc one. CodeQL reported 3 existing `rust/hard-coded-cryptographic-value` test-fixture findings in `crates/cdf-cli/src/tests.rs`, owned by `.10x/tickets/2026-07-09-p1-ws5e-codeql-backfill-test-secret-fixtures.md`.

## What this supports

This supports closing A7: the operator now has the required `schema pin/show/diff` surface for the currently supported discovery archetypes, with no-write diff behavior and tests for snapshot/lockfile/redaction paths.

This supports closing D3: local file append resources now exercise `FileManifest` as managed incremental state rather than a recorded-only artifact. The runtime proves unchanged no-op, new-file load-only-new behavior, changed-file reload, replace-disposition full planning, and manifest merge across checkpoint state.

This supports closing I2: conformance now has durable P2 scenario ids and parity/friction laws that future P2 children can extend without rewriting the harness. Unsupported cells are recorded as pending or excluded rather than hidden.

## Limits

This batch is local-file and current-discovery-archetype scoped. It does not prove public HTTP(S) Parquet ingestion, HTTP glob/template enumeration, cloud object stores, compressed NDJSON, `cdf add`, ad-hoc mode, deep validation diagnostics, or final S1/S2/S3/S4/S5/S6/S7/S8 completion.

The existing CodeQL hard-coded cryptographic value findings are outside this batch and remain separately owned. The existing `paste` advisory is ratified and remains visible in OSV/audit output until the dependency graph has an upstream fix. The broad jscpd run still reports pre-existing duplication in touched areas; targeted schema-command cleanup introduced no new clones.
