Status: recorded
Created: 2026-07-09
Updated: 2026-07-09
Relates-To: .10x/tickets/done/2026-07-08-p2-ws-i1-friction-regression-registry.md, .10x/tickets/2026-07-08-p2-ws-i-conformance-parity-friction-suite.md, .10x/tickets/2026-07-08-p2-data-onramp-program.md, .10x/specs/data-onramp-conformance.md

# P2 friction regression registry

## What was observed

The P2 directive's eighteen field-test frictions were compared against active P2 records, current conformance tests, adjacent unit/CLI tests, and source behavior. Initial capture found no row with complete P2 regression coverage. Several rows had useful primitive or negative coverage, but the required P2 behavior remained owned by open P2 tickets.

Update 2026-07-09: B1 closed the direct declarative-type-vocabulary expressibility gap for row 3. C1 closed the direct declarative compiled-schema normalization and automatic source-name metadata gaps for rows 6 and 7. F1 closed the direct append-default, keyless append, missing merge-key, and local scaffold fake-key gaps for row 17. D1 added local modest-N primitive coverage for rows 8 and 9, and D3 added local append manifest incrementality/no-op coverage for row 8 and the S2 state primitive. Later P2 slices added concrete primitive coverage for rows 1, 2, 4, 5, 8, 9, 10, 15, and 18. E2/G1/B4 then added production HTTPS single-file Parquet discovery/run coverage, source-specific diagnostics/deep validate coverage, and conformance-owned widening property coverage. H2/D4/B5 added `cdf add` single-file Parquet scaffolding, local gzip/zstd NDJSON decode foundation, and package validation-program coercion evidence. WS-I2 records the conformance-owned concrete mapping in `crates/cdf-conformance/src/run_matrix/data_onramp.rs::P2_FRICTIONS`, including actual test names for closed slices and active tickets for open rows. The broader final S1/S2/S3/S4/S5/S6/S7/S8 conformance and P2 closure obligations remain open where that module keeps rows tied to active tickets.

WS-I2 concrete mapping highlights:

- Row 4 now names `crates/cdf-contract/src/tests.rs::schema_reconciliation_records_lossless_widenings_and_physical_type`, `crates/cdf-formats/src/tests.rs::declared_parquet_int32_declared_int64_materializes_lossless_widening`, and `crates/cdf-formats/src/tests.rs::declared_parquet_float32_declared_float64_materializes_lossless_widening`, while WS-B and WS-I remain active for full conformance/property closure.
- Row 5 now names `crates/cdf-formats/src/tests.rs::declared_parquet_projection_renames_by_source_name_and_drops_extra_fields`, `crates/cdf-formats/src/tests.rs::declared_parquet_lossy_narrowing_fails_before_batches_are_emitted`, and `crates/cdf-formats/src/tests.rs::undeclared_parquet_read_preserves_physical_schema_after_declared_path_added`, while WS-B and WS-I remain active.
- Row 9 includes `crates/cdf-conformance/src/run_matrix/data_onramp.rs::p2_preview_run_parity_law_covers_supported_archetypes` for current local file, REST fixture, and Postgres table preview/run parity; active WS-E/WS-I own the missing remote and full-front-end parity.
- Row 15 names the WS-E1 facade tests `crates/cdf-declarative/src/file_transport.rs::tests::file_transport_http_metadata_and_bounded_range_use_http_client` and `crates/cdf-declarative/src/file_transport.rs::tests::file_transport_http_range_rejects_unbounded_or_ignored_range`; closed E2 adds the production runtime/discovery tests, while active WS-D/WS-E/WS-I own the missing HTTP-glob, cloud, and final conformance slices.

WS-I3 reconciliation update 2026-07-09: the executable registry now distinguishes terminal evidence from active ownership by reading each owner ticket's `Status` header. Missing paths, terminal tickets, and non-ticket records fail the registry tests. Every named test is also resolved back to a source file and test function. Closed A7/D3/D4/E2/G1/H2 tickets remain evidence, not blockers; remaining work is assigned only to active P2 workstream tickets. S8 is explicitly pending: the existing local-file, REST-fixture, and Postgres-table row/schema fingerprint law is partial evidence and does not yet prove that all required source archetypes share the complete resolution, decode, discovery, reconciliation, normalization, and compiler-front-end path. No scenario was promoted to covered by this registry maintenance.

## Registry

| # | Friction | Classification | Current coverage evidence | Required guard owner and rationale |
|---|---|---|---|---|
| 1 | Parquet schema discovery unusable for plan/run | Partially covered by A3/E2; full S1/S2 remains | Local Parquet discovery/auto-pin is covered by A3. E2 adds deterministic HTTPS single-file Parquet discovery/run coverage through `crates/cdf-project/src/tests.rs::http_parquet_schema_discovery_uses_bounded_ranges_without_artifacts` and `crates/cdf-project/src/tests.rs::http_parquet_auto_pin_plan_preview_and_run_use_file_runtime`. | The direct single-file local/HTTPS discover-run gap is partially closed. `.10x/tickets/2026-07-08-p2-ws-h-scaffolding-id-model-two-minute-path.md`, WS-D/E, and WS-I still own `cdf add`, public TLC S1/S2, remote multi-file globs, and final conformance. |
| 2 | No schema inference/export command | Covered for supported schema commands and first Parquet add surface; full P2 owner remains | `.10x/tickets/done/2026-07-09-p2-ws-a3-local-parquet-discover-autopin.md` added `cdf schema discover` for local Parquet and auto-pin, `.10x/tickets/done/2026-07-09-p2-ws-a7-schema-pin-show-diff-cli.md` added `cdf schema pin`, `show`, and `diff` for supported discovery archetypes, and `.10x/tickets/done/2026-07-09-p2-ws-h2-cdf-add-single-file-parquet.md` added the first `cdf add` schema-inference/scaffold doorway for local and deterministic HTTPS single-file Parquet. | The direct missing-command/scaffold gap is closed for supported single-file Parquet. WS-A/WS-E/WS-H still own remote multi-file discovery, REST/Postgres add, ad-hoc mode, and S1/S2 closure. |
| 3 | Type vocabulary too small (`int32` unexpressible) | Covered by B1 for declarative expressibility; broader WS-B conformance remains open | `.10x/tickets/done/2026-07-08-p2-ws-b1-declarative-arrow-type-vocabulary.md` closed with focused TOML/YAML compilation tests for `int32`, signed/unsigned widths, floats, decimal128/256, temporal units, binary/large variants, list/struct/map forms, invalid-string error naming, and JSON Schema string-form acceptance. Evidence: `.10x/evidence/2026-07-09-p2-ws-b1-declarative-arrow-type-vocabulary.md`. | B1 guards the direct vocabulary regression. `.10x/tickets/2026-07-08-p2-ws-b-schema-reconciliation-arrow-vocabulary.md` still owns observed-vs-declared reconciliation, widening/coercion, physical provenance, and conformance/property coverage before WS-B can close. |
| 4 | No lossless widening (`INT32` -> `int64` fails) | Covered for implemented Arrow width families; broader WS-B remains open | B2/B3 added reconciler and local Parquet materialization tests. B4 adds conformance-owned property tests in `crates/cdf-conformance/src/property_fuzz/contract.rs`: signed/unsigned integer widening composition, float32-to-float64 preservation, date32-to-timestamp preservation, and `FieldCoercionDecision::Widened` classification. | The direct `INT32 -> int64` and implemented no-loss widening regression is guarded. WS-B still owns validation-program serialization/in-package evidence and any future cast families before workstream closure. |
| 5 | Declared schema does not drive Parquet reads (two truths) | Covered for local declared Parquet reads; broader WS-B remains open | `crates/cdf-formats/src/tests.rs::declared_parquet_projection_renames_by_source_name_and_drops_extra_fields`, `declared_parquet_lossy_narrowing_fails_before_batches_are_emitted`, and `undeclared_parquet_read_preserves_physical_schema_after_declared_path_added` prove the declared Parquet path uses reconciliation without breaking undeclared physical reads. | Local declared Parquet no longer has two competing truths. WS-B/I still own validation-program serialization and final S1/S2/S8 conformance. |
| 6 | Destination validation before normalization (`VendorID` rejected) | Covered for live DuckDB/Postgres; Parquet policy remains open | C1 proves declarative normalization. C3 adds `crates/cdf-cli/src/tests.rs::duckdb_destination_policy_normalizes_plan_preview_package_and_commit`, `destination_normalization_collision_fails_before_writes`, `crates/cdf-project/src/runtime_tests.rs::postgres_destination_policy_truncates_package_and_committed_column_identically`, and `stale_long_name_column_program_cannot_spoof_destination_policy_before_writes`. | DuckDB/Postgres plan, preview, package, and commit now share the destination-derived policy and fail closed on stale evidence. WS-C/WS-I still own the explicitly unresolved Parquet column policy and final cross-destination parity. |
| 7 | `source_name` manual per column | Covered for declarative plus live DuckDB/Postgres paths | C1 proves automatic source-original metadata. C3's DuckDB/Postgres regressions prove the live normalizer uses `cdf:source_name` to restore or truncate destination identifiers without field-by-field mappings and preserves it in package output. | Manual source mappings are no longer required on implemented live paths. WS-C/WS-I remain open for Parquet policy and final conformance. |
| 8 | Multi-file glob rejected in run | Partially covered by D1/D3; full P2 owner remains | `.10x/tickets/done/2026-07-08-p2-ws-d1-file-glob-partition-planning.md` removed the local modest-N multi-file runtime rejection in `cdf-declarative` and added deterministic per-file partition tests. `.10x/tickets/done/2026-07-09-p2-ws-d3-file-manifest-incremental-noop.md` added local append manifest filtering, unchanged no-op reruns, and new/changed-file-only planning. | Later WS-D/WS-E/WS-I work must still add remote/public Parquet glob fixtures, compression, schema variance, large-N policy, and final S2 conformance before this row is fully covered. |
| 9 | Preview/run file-semantics divergence | Partially covered by D1/I2; full parity law remains | D1 evidence proves `cdf-declarative` preview/open consume the selected partition path through the same validation path, and the CLI preview multi-match expectation now uses the new file partition id shape. I2 adds `crates/cdf-conformance/src/run_matrix/data_onramp.rs::p2_preview_run_parity_law_covers_supported_archetypes` for current local file, REST fixture, and Postgres table archetypes. | `.10x/tickets/2026-07-08-p2-ws-e-remote-transports.md` and later WS-I children must still add remote/public file parity and final S8 conformance. |
| 10 | Resource-id derivation confusing (`[source.<name>]` vs mapping) | Covered for compiled-id visibility and zero-match mapping validation; broader authoring ergonomics remain | H1 added compiled resource id/origin/mapping rendering and zero-match mapping diagnostics through `cdf validate`/`cdf plan`. Relevant tests include `crates/cdf-cli/src/tests.rs::resource_mapping_pattern_mismatch_reports_validate_and_plan_commands` and project mapping validation tests. H2 subsequently added the supported single-file Parquet `cdf add` surface. | The direct confusing-id diagnostic gap is guarded. Active WS-H still owns REST/Postgres add, ad-hoc mode, and broader authoring ergonomics. |
| 11 | "Not compiled" error under-informative | Covered for current CLI source diagnostics foundation | G1 adds `CDF-RESOURCE-NOT-COMPILED` with compiled ids, source files/origins, mapping status, likely causes, suggestions, and remediation. Concrete guard: `crates/cdf-cli/src/tests.rs::resource_not_compiled_error_names_compiled_ids_origins_and_fix`. | The direct under-informative error is guarded for current CLI source selection. Active WS-G/WS-I own future source surfaces and final conformance. |
| 12 | `plan` emits `run`-worded errors | Partially covered by G1; final sweep remains | G1 guards the known destination-planning leak with `crates/cdf-cli/src/scan_command.rs::tests::plan_error_wording_uses_plan_command_name` and command-specific mapping failures with `crates/cdf-cli/src/tests.rs::resource_mapping_pattern_mismatch_reports_validate_and_plan_commands`. | The known plan/run wording leak is guarded. Active WS-G/WS-I own broad construction-site and conformance sweeps as new source failures land. |
| 13 | Generic remediation text | Partially covered by G1; final catalog sweep remains | G1's `crates/cdf-cli/src/tests.rs::resource_not_compiled_error_names_compiled_ids_origins_and_fix` asserts the source-specific error code, compiled context, suggestions, and concrete remediation summary. Existing generic lower-layer mapping remains for non-source/general errors. | Active WS-G/WS-I must keep replacing generic remediation for future source-experience failures as they become concrete. |
| 14 | `validate` cannot catch schema/file mismatches early | Foundation covered by G1; full deep matrix remains | G1 implements `cdf validate --deep`; `crates/cdf-cli/src/tests.rs::validate_deep_reports_source_front_end_checks_without_writes` exercises file resolution, bounded discovery, validation-program construction, identifier normalization, destination checks, and no-write guarantees. | Deep validate now exists for supported current archetypes. Active WS-G/WS-I own cloud transports, compression, Python/WASM/future source probes, richer mismatch remediation, and final conformance. |
| 15 | No direct HTTP ingestion of public datasets | Covered for deterministic single-file HTTPS Parquet; full public/cloud path remains | E1 facade tests remain guards, and E2 production integration is concretely covered by `crates/cdf-project/src/tests.rs::http_parquet_schema_discovery_uses_bounded_ranges_without_artifacts` plus `crates/cdf-project/src/tests.rs::http_parquet_auto_pin_plan_preview_and_run_use_file_runtime`. | Direct HTTP single-file Parquet ingestion is guarded. Active WS-D/WS-E/WS-I own HTTP template/glob enumeration, public TLC evidence, cloud object stores, remote compression, and S1/S2 closure. |
| 16 | Compressed datasets need manual preprocessing | Covered for local gzip/zstd NDJSON foundation; full S3 remains open | D4's concrete guards are `crates/cdf-formats/src/tests.rs::compression_ndjson_file_sources_decode_and_preserve_compressed_identity`, `crates/cdf-declarative/src/tests.rs::file_runtime_auto_compression_decodes_gzip_and_zstd_ndjson`, and `crates/cdf-declarative/src/tests.rs::file_runtime_explicit_compression_mismatch_names_file_and_signals`. | The local preprocessing gap is guarded. Active WS-D/WS-E/WS-I own remote compressed reads, S3/cloud object stores, zip policy, and final S3 conformance. |
| 17 | Append nudges toward fake keys | Covered by standalone deterministic S7 conformance | F1 guards omitted/default append and explicit merge identity. F2 guards the file/REST CLI entry paths. I4 adds `crates/cdf-conformance/src/run_matrix/data_onramp.rs::p2_s7_keyless_append_and_precontact_merge_failure_conformance`, which runs keyless append through validate/plan/preview/run and proves merge-without-key fails once with both fixes before source contact or project mutation. Local and `cdf add` scaffolds omit invented keys. | The original fake-key friction is closed and regression-owned. Exact-row dedup remains a separate excluded feature, not a condition of this friction row. |
| 18 | Parquet, though core to the vision, is the worst first-run path | Improved by `cdf add`; still open for public S1/S2 | Current coverage proves local and deterministic HTTPS single-file Parquet discovery/run, declared-schema reconciliation, schema discover/pin/show/diff for supported archetypes, `cdf add` single-file Parquet scaffolding, local multi-file partitions, local append manifest no-op reruns, and Parquet destination commits. S1/S2 are still not covered end to end: no public multi-month HTTP glob/template enumeration, no public TLC live evidence, and no recorded S1/S2 session. | `.10x/tickets/2026-07-08-p2-ws-h-scaffolding-id-model-two-minute-path.md`, WS-D/E, and WS-I must converge on public TLC S1/S2 conformance, compression/cloud follow-ons, and closure evidence. |

## Source and record inspection

Read the governing records requested by the ticket:

- `.10x/tickets/done/2026-07-08-p2-ws-i1-friction-regression-registry.md`
- `.10x/specs/data-onramp-conformance.md`
- `.10x/knowledge/runtime-conformance-throughput-rule.md`
- `.10x/tickets/2026-07-08-p2-data-onramp-program.md`
- P2 directive attachment `/Users/alexanderbut/.codex/attachments/d0eec256-72c9-4739-ba04-1fb750208a7f/goal-objective.md`, especially section 8.

Inspected P2 workstream tickets A-I and executable children B1, C1, and F1. Inspected conformance modules under `crates/cdf-conformance/src`, especially run matrix, live run goldens, resource conformance, drift quarantine, and property fuzz targets. Inspected adjacent implementation/tests in `crates/cdf-formats`, `crates/cdf-declarative`, `crates/cdf-project`, `crates/cdf-cli`, and `crates/cdf-contract` for exact assertions before marking any row partial. During final status review, unowned dirty `crates/cdf-declarative/**` edits appeared and were read only where they affected row 3.

## What this supports or challenges

This supports the P2 closure rule in `.10x/specs/data-onramp-conformance.md`: the eighteen frictions are now named one by one with current coverage limits and P2 owners. It challenges any closure claim that treats local Parquet reader tests, REST transport tests, or normalizer primitive tests as full P2 data-onramp coverage. After B1, row 3 is no longer an expressibility gap, but it is still not proof of the broader reconciliation/coercion path. After D1, rows 8 and 9 have local modest-N primitive coverage, but they are still not full S2/S8 conformance coverage.

Update 2026-07-09 after C1: Rows 6 and 7 were updated only for the direct declarative compiled-schema slice. The registry does not treat that as closure of full WS-C or destination-specific behavior.

Update 2026-07-09 after F1: Row 17 was updated only for declarative compilation and the generated local append scaffold. The registry does not treat that as closure of S7 CLI rendering or conformance.

Update 2026-07-09 after F2: Row 17 now names file and REST keyless-append CLI proofs plus command-correct pre-contact merge-key failure. S7 remains pending because registry references to unit/integration tests are not a substitute for the exact conformance scenario; exact-row dedup is also excluded from F2.

Update 2026-07-09 after I4: Row 17 now names the standalone deterministic S7 conformance scenario and no longer carries an implementation owner. The executable P2 matrix promotes only S5 and S7; exact-row dedup remains explicitly outside S7 rather than holding the fake-key regression open.

Update 2026-07-09 after C3: Rows 6 and 7 now name live DuckDB/Postgres plan-preview-package-commit regressions, collision/no-write coverage, and stale-policy rejection. Parquet remains an explicit active-policy boundary rather than being inferred from its object-key normalizer.

## Limits

No conformance code helper was added during the initial registry slice, so `cargo test -p cdf-conformance --locked` was not required for that slice. The broad WS-A, WS-D, WS-E, WS-G, and WS-H owners still need focused executable child tickets before their remaining implementation; this registry records their coverage obligation but does not create all of those children. B1 closure updates row 3 only; it does not close WS-B reconciliation/coercion or WS-I golden-path coverage. D1 closure updates rows 8 and 9 only for local modest-N primitive coverage; it does not close S2/S8 conformance.

## Verification

Recorded after registry creation:

- `git diff --check`: passed.
- `gitleaks dir --no-banner --redact` over the new/changed `.10x` records: passed for the registry evidence, WS-I1 ticket, WS-I ticket, and P2 parent.
- `jscpd .10x/evidence/2026-07-08-p2-friction-regression-registry.md --format txt --formats-exts txt:md --min-lines 10 --min-tokens 60 --no-gitignore --reporters console --output target/quality/reports/jscpd-p2-friction-registry`: passed with 0 clones.
