default:
  name: cdf-test
  workspaces:
  - workspace1
  filesystem:
    provider: local
    options:
      path: /tmp
prod:
  feature_flags:
    provider: harness
    options:
      api_key: "@format {env[HOME]}"
dev:
  feature_flags:
    provider: local
    options:
      path: flags.json

