default:
  name: cdf-test
  workspaces:
  - workspace1
  filesystem:
    provider: file
    root: _storage
prod:
  filesystem:
    provider: gcs
    root: harness_analytics_staging/cdf_test_1
dev:
  feature_flags:
    provider: file
    options:
      path: flags.json

